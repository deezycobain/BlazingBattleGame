# Development Branch

`dev` is the active integration/testing branch. `main` remains the approved stable build.

Current Senku work:
- lock the approved 2x basic bomb projectile size
- integrate the uploaded 6-frame Small Explosion VFX
- keep body animation, projectile VFX, explosion VFX, and damage timing separate
