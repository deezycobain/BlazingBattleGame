LEGENDARY ITACHI UNIT ASSET UPLOAD

This archive is laid out from repository root.

Install target:
  assets/characters/itachi/

Included:
  art/       full card art + shiny foreground cutout
  cards/     card artwork
  sprites/   high-resolution source sheets + 420x420 runtime body frames
  vfx/       separate crow and Tsukuyomi VFX
  data/      asset install manifest

Runtime body standard:
  420 x 420 PNG
  anchor (210, 392)
  individual frame files

Important:
  This archive installs assets only.
  unit.json, runtime-map.json, balance, targeting, gameplay effect behavior,
  and registry wiring are intentionally handled in the code-integration pass.
