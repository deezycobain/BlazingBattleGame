
===== basic_shape =====

--- match 1 @ 24598980 ---
"}};</script>
<div id="meteorScreenFlash" aria-hidden="true"></div>
<script id="blazing-unit-data">window.BLAZING_UNIT_DATA={"senku":{"schema_version":2,"id":"senku","display_name":"Senku","title":"The Kingdom of Science","role":"playable","archetype":"ranged_tactician","element":"Nature","rarity":"Legendary","collection":{"owned":true,"inventory_visible":true,"battle_ready":true},"stats":{"level":1,"hp":180,"attack":38,"defense":28,"speed":64},"combat":{"mark":"S","movement_range":92,"basic_shape":{"type":"rect","w":255,"h":58,"offset_x":116},"jutsu_shape":{"type":"screen_rect","scope":"all_allies"},"chakra_max":8},"render":{"scale":1.3},"abilities":{"basic":{"id":"explosive_bomb","name":"Explosive Bomb","damage_multiplier":1.0,"chakra_gain":1,"delivery":"ranged_projectile","range_presentation":"forward_lane","presentation":{"cast_duration_ms":560,"flight_duration_ms":620,"impact_duration_ms":360,"arc_height_px":86,"release_frame":3,"projectile_rotation":true,"impact_anchor":{"target":"feet","x":0.5,"y":0.96,"ground_offset_y_px":3,"layer":"ground"},"impact_vfx_mode":"single_frame_fade","impact_frame_index":4,"impact_fade_start":0.34,"impact_height_px":58,"impact_animation":"static_hold_fade","impact_asset":"vfx/bomb/static_impact/explosion.png","projectile_assets":["vfx/bomb/projectile_clean/frame_01.png","vfx/bomb/projectile_clean/frame_02.png","vfx/bomb/projectile_clean/frame_03.png","vfx/bomb/projectile_clean/frame_04.png","vfx/bomb/projectile_clean/frame_05.png"],"projectile_cleaned":true}},"jutsu":{"id":"revival_formula","name":"Revival Formula","cost":4,"damage_multiplier":0,"delivery":"bottle_party_heal","category":"jutsu","ultimate":false,"status_effect":null,"status_chance":0,"presentation":{"cast_frames":4,"projectile_frames":3,"impact_frames":3,"cast_duration_ms":680,"flight_duration_ms":420,"impact_duration_ms":620,"screen_dim_alpha":0.12},"target":"all_allies","effect":"heal_full","heal_percent":1.0}},"assets":{"art":"art/senku_full_art.jpeg","card":"cards/senku_card.jpeg","sprites":{"idle":"sprites/runtime/idle/","attack_basic":"sprites/runtime/attack/basic/","recoil":"sprites/runtime/recoil/","jutsu":"sprites/runtime/jutsu/chemical_reaction/cast/"},"vfx":{"projectile":"vfx/bomb/projectile/","explosion":"vfx/bomb/explosion/","jutsu_projectile":"vfx/jutsu/chemical_reaction/projectile/","jutsu_impact":"vfx/jutsu/chemical_reaction/impact/"}},"readiness":{"collection":true,"battle":true,"idle":true,"basic_attack":true,"jutsu":true,"recoil":true,"notes":"v0.6.22: Bomb projectile frames cleaned to remove detached source-sheet/text-like artifacts while retaining bomb, fuse spark, and intended trail. Arc/timing unchanged.","animation_map":true},"b

--- match 2 @ 24604107 ---
ctile/frame_03.png","vfx/bomb/projectile/frame_04.png","vfx/bomb/projectile/frame_05.png"],"frame_ms":90,"loop":false,"body_asset":false},"jutsu/chemical_reaction/impact":{"frames":["vfx/jutsu/chemical_reaction/impact/frame_01.png","vfx/jutsu/chemical_reaction/impact/frame_02.png","vfx/jutsu/chemical_reaction/impact/frame_03.png"],"frame_ms":90,"loop":false,"body_asset":false},"jutsu/chemical_reaction/projectile":{"frames":["vfx/jutsu/chemical_reaction/projectile/frame_01.png","vfx/jutsu/chemical_reaction/projectile/frame_02.png","vfx/jutsu/chemical_reaction/projectile/frame_03.png"],"frame_ms":90,"loop":false,"body_asset":false}}}},"anubis":{"schema_version":2,"id":"anubis","display_name":"Anubis","title":"Judge of Souls","role":"boss","archetype":"boss_tank_controller","element":"Dark","rarity":"Boss","collection":{"owned":false,"inventory_visible":false,"battle_ready":true},"stats":{"level":1,"hp":950,"attack":38,"defense":32,"speed":43},"combat":{"mark":"BOSS","movement_range":72,"basic_shape":{"type":"circle","r":105},"jutsu_shape":{"type":"circle","r":205},"chakra_max":8},"render":{"scale":1.25,"battle_size_scale":2.3713},"abilities":{"basic":{"id":"staff_strike","name":"Staff Strike","damage_multiplier":1.0,"delivery":"melee"},"jutsu":{"id":"boss_rotation","name":"Boss Rotation","cost":5,"damage_multiplier":2.05,"delivery":"boss_rotation"}},"assets":{"art":null,"card":null,"portrait":"portraits/","icon":"icons/","sprites":{"idle":"sprites/runtime/idle/","attack_basic":"sprites/runtime/attack/basic/","recoil":"sprites/runtime/recoil/","jutsu":"sprites/runtime/jutsu/"},"vfx":"vfx/"},"readiness":{"collection":false,"battle":true,"idle":true,"basic_attack":true,"jutsu":true,"recoil":false,"notes":"Boss definition. Existing combat code remains authoritative until migration.","animation_map":true},"balance":{"status":"provisional","pass":"v0.5.30","notes":"Initial archetype-based values. Tune freely during balance testing."},"animation_standard":{"version":"v0.6.16","canvas":{"width":420,"height":420,"anchor_x":210,"anchor_y":392},"animations":{"idle":{"frames":["sprites/runtime/idle/frame_01.png"],"frame_ms":180,"loop":true,"events":[]}},"vfx":{}}},"crimson":{"schema_version":2,"id":"crimson","display_name":"Crimson","title":"Blazing Vanguard","role":"playable","archetype":"assassin_melee","element":"Fire","rarity":"Legendary","collection":{"owned":true,"inventory_visible":true,"battle_ready":true},"stats":{"level":1,"hp":118,"attack":35,"defense":18,"speed":68},"combat":{"mark":"V","movement_range":96,"basic_shape":{"type":"cone","r":182,"a":1.0},"jutsu_shape":{"type":"rect","w":310,"h":86},"chakra_max":8},"render":{"scale":1.0},"abilities":{"basic":{"id":"basic_strike","name":"Basic Strike","damage_multiplier":1.0,"chakra_gain":1,"delivery":"melee"},"jutsu":{"id":"current_jutsu","name":"Crimson Jutsu","cost":4,"damage_multiplier":1.9,"delivery":"forward_area","category":"jutsu","ultimate":false}},"assets":{"art":"art/current_collection_art.jpg","card":null,"portrait":"portraits/","icon":"icons/","sprites":{"idle":"sprites/runtime/idle/","attack_basic":"sprites/runtime/attack/basic/","recoil":"sprites/runtime/recoil/","jutsu":"sprites/runtime/jutsu/"

--- match 3 @ 24605658 ---
untime/recoil/","jutsu":"sprites/runtime/jutsu/"},"vfx":"vfx/"},"readiness":{"collection":false,"battle":true,"idle":true,"basic_attack":true,"jutsu":true,"recoil":false,"notes":"Boss definition. Existing combat code remains authoritative until migration.","animation_map":true},"balance":{"status":"provisional","pass":"v0.5.30","notes":"Initial archetype-based values. Tune freely during balance testing."},"animation_standard":{"version":"v0.6.16","canvas":{"width":420,"height":420,"anchor_x":210,"anchor_y":392},"animations":{"idle":{"frames":["sprites/runtime/idle/frame_01.png"],"frame_ms":180,"loop":true,"events":[]}},"vfx":{}}},"crimson":{"schema_version":2,"id":"crimson","display_name":"Crimson","title":"Blazing Vanguard","role":"playable","archetype":"assassin_melee","element":"Fire","rarity":"Legendary","collection":{"owned":true,"inventory_visible":true,"battle_ready":true},"stats":{"level":1,"hp":118,"attack":35,"defense":18,"speed":68},"combat":{"mark":"V","movement_range":96,"basic_shape":{"type":"cone","r":182,"a":1.0},"jutsu_shape":{"type":"rect","w":310,"h":86},"chakra_max":8},"render":{"scale":1.0},"abilities":{"basic":{"id":"basic_strike","name":"Basic Strike","damage_multiplier":1.0,"chakra_gain":1,"delivery":"melee"},"jutsu":{"id":"current_jutsu","name":"Crimson Jutsu","cost":4,"damage_multiplier":1.9,"delivery":"forward_area","category":"jutsu","ultimate":false}},"assets":{"art":"art/current_collection_art.jpg","card":null,"portrait":"portraits/","icon":"icons/","sprites":{"idle":"sprites/runtime/idle/","attack_basic":"sprites/runtime/attack/basic/","recoil":"sprites/runtime/recoil/","jutsu":"sprites/runtime/jutsu/"},"vfx":"vfx/","jutsu_cast":"assets/characters/crimson/sprites/runtime/jutsu/cast.png"},"readiness":{"collection":true,"battle":true,"idle":true,"basic_attack":true,"jutsu":true,"recoil":false,"notes":"v0.5.47: Crimson special animation corrected to Crimson-only Jutsu asset. Strict per-character routing prevents Lebee/Sub-Zero body frames from resolving on Crimson.","animation_map":true},"balance":{"status":"provisional","pass":"v0.5.30","notes":"Initial archetype-based values. Tune freely during balance testing."},"animation_standard":{"version":"v0.6.16","canvas":{"width":420,"height":420,"anchor_x":210,"anchor_y":392},"animations":{"idle":{"frames":["sprites/runtime/idle/frame_01.png","sprites/runtime/idle/frame_02.png"],"frame_ms":180,"loop":true,"events":[]},"basic_attack":{"frames":["sprites/runtime/attack/basic/frame_01.png"],"frame_ms":120,"loop":false,"events":[{"frame":1,"event":"release_projectile_or_apply_melee"}]},"jutsu":{"current":{"frames":["sprites/runtime/jutsu/current/frame_01.png"],"frame_ms":150,"loop":false,"events":[{"frame":1,"event":"release_jutsu_vfx"}]}}},"vfx":{}}},"lebee":{"schema_version":2,"id":"lebee","display_name":"Lebee","title":"Cosmic Wish","role":"playable","archetype":"caster_aoe","element":"Cosmic","rarity":"Legendary","collection":{"owned":true,"inventory_visible":true,"battle_ready":true},"stats":{"level":1,"hp":105,"attack":30,"defense":16,"speed":62},"combat":{"mark":"L","movement_range":90,"basic_shape":{"type":"rect","w":255,"h":58,"offset_x":116},"jutsu_shape":{"type":"screen_rec

--- match 4 @ 24607777 ---
archetype-based values. Tune freely during balance testing."},"animation_standard":{"version":"v0.6.16","canvas":{"width":420,"height":420,"anchor_x":210,"anchor_y":392},"animations":{"idle":{"frames":["sprites/runtime/idle/frame_01.png","sprites/runtime/idle/frame_02.png"],"frame_ms":180,"loop":true,"events":[]},"basic_attack":{"frames":["sprites/runtime/attack/basic/frame_01.png"],"frame_ms":120,"loop":false,"events":[{"frame":1,"event":"release_projectile_or_apply_melee"}]},"jutsu":{"current":{"frames":["sprites/runtime/jutsu/current/frame_01.png"],"frame_ms":150,"loop":false,"events":[{"frame":1,"event":"release_jutsu_vfx"}]}}},"vfx":{}}},"lebee":{"schema_version":2,"id":"lebee","display_name":"Lebee","title":"Cosmic Wish","role":"playable","archetype":"caster_aoe","element":"Cosmic","rarity":"Legendary","collection":{"owned":true,"inventory_visible":true,"battle_ready":true},"stats":{"level":1,"hp":105,"attack":30,"defense":16,"speed":62},"combat":{"mark":"L","movement_range":90,"basic_shape":{"type":"rect","w":255,"h":58,"offset_x":116},"jutsu_shape":{"type":"screen_rect","padding":18},"chakra_max":8},"render":{"scale":1.0},"abilities":{"basic":{"id":"star_blast","name":"Star Blast","damage_multiplier":1.0,"chakra_gain":1,"delivery":"ranged_projectile","range_presentation":"forward_lane"},"jutsu":{"id":"meteor_jutsu","name":"Meteor Jutsu","cost":4,"damage_multiplier":2.25,"delivery":"battlefield_aoe","area":"battlefield_rectangle","aoe":true,"presentation":{"cast_duration_ms":900,"meteor_fall_duration_ms":700,"meteor_count":7,"screen_dim_alpha":0.34,"meteor_scale_start":2.65,"meteor_scale_end":0.9,"meteor_stagger_ms":72,"impact_scatter_x":54,"impact_scatter_y":34,"cast_frame_count":6,"cast_sequence_path":"assets/characters/lebee/sprites/runtime/jutsu/meteor/sequence_v0532/","impact_hold_ms":680,"aftermath_hold_ms":420,"meteor_start_scale":132,"meteor_end_scale":42,"post_damage_fade_ms":150,"impact_art_enabled":false,"final_flash":true,"final_flash_yellow_ms":240,"final_flash_white_ms":105,"final_flash_fade_ms":120,"final_flash_mode":"fullscreen_overlay","final_flash_white_hold_ms":220,"final_flash_white_fade_ms":780,"flash_yellow_trigger":"second_to_last_meteor","flash_white_trigger":"final_meteor","meteor_entry_side":"opposite_caster","meteor_entry_top_margin":128,"meteor_entry_horizontal_spread":92},"category":"ultimate","ultimate":true}},"assets":{"art":"art/full_art_cosmic_wish.jpeg","card":"cards/unit_details_cosmic_wish.jpeg","portrait":"portraits/","icon":"icons/","sprites":{"idle":"sprites/runtime/idle/","attack_basic":"sprites/runtime/attack/basic/","recoil":"sprites/runtime/recoil/","jutsu":"sprites/runtime/jutsu/"},"vfx":"vfx/"},"readiness":{"collection":true,"battle":true,"idle":true,"basic_attack":true,"jutsu":true,"recoil":true,"notes":"v0.6.10: Lebee normal attack uses canonical thin forward rectangular ranged hitbox. Jutsu geometry remains independently defined by jutsu_shape.","animation_map":true},"balance":{"status":"provisional","pass":"v0.5.36","notes":"Initial archetype-based values. Tune freely during balance testing."},"presentation":{"idle":{"mode":"source_aligned_four_frame","source":"assets/characters/lebee/sprites/sourc

--- match 5 @ 24612655 ---
/bridge/frame_01.png","sprites/runtime/jutsu/meteor/bridge/frame_02.png"],"frame_ms":150,"loop":false,"events":[{"frame":1,"event":"release_jutsu_vfx"}]},"meteor/sequence_v0532":{"frames":["sprites/runtime/jutsu/meteor/sequence_v0532/frame_01.png","sprites/runtime/jutsu/meteor/sequence_v0532/frame_02.png","sprites/runtime/jutsu/meteor/sequence_v0532/frame_03.png","sprites/runtime/jutsu/meteor/sequence_v0532/frame_04.png","sprites/runtime/jutsu/meteor/sequence_v0532/frame_05.png","sprites/runtime/jutsu/meteor/sequence_v0532/frame_06.png"],"frame_ms":150,"loop":false,"events":[{"frame":5,"event":"release_jutsu_vfx"}]}}},"vfx":{}}},"subzero":{"schema_version":2,"id":"subzero","display_name":"Sub-Zero","title":"Absolute Zero","role":"playable","archetype":"ranged_controller","element":"Ice","rarity":"Legendary","collection":{"owned":true,"inventory_visible":true,"battle_ready":true},"stats":{"level":1,"hp":126,"attack":31,"defense":24,"speed":60},"combat":{"mark":"SZ","movement_range":88,"basic_shape":{"type":"circle","r":92},"jutsu_shape":{"type":"cone","r":205,"a":1.05},"chakra_max":8},"render":{"scale":0.88},"abilities":{"basic":{"id":"basic_attack","name":"Basic Attack","damage_multiplier":1.0,"chakra_gain":1,"delivery":"melee"},"jutsu":{"id":"freeze_blast","name":"Freeze Blast","cost":4,"damage_multiplier":2.1,"delivery":"projectile","status_effect":"freeze","status_chance":1.0,"category":"jutsu","ultimate":false}},"assets":{"art":"art/full_art_absolute_zero_v2.jpeg","card":"cards/unit_details_absolute_zero_v2.jpeg","portrait":"portraits/","icon":"icons/","sprites":{"idle":"sprites/runtime/idle/","attack_basic":"sprites/runtime/attack/basic/","recoil":"sprites/runtime/recoil/","jutsu":"sprites/runtime/jutsu/"},"vfx":"vfx/"},"readiness":{"collection":true,"battle":true,"idle":true,"basic_attack":true,"jutsu":true,"recoil":true,"notes":"v0.5.46: battlefield render scale reduced to 0.88 so Sub-Zero visually matches the current Crimson/Lebee team footprint. Applies consistently to all body animations.","animation_map":true},"balance":{"status":"provisional","pass":"v0.5.30","notes":"Initial archetype-based values. Tune freely during balance testing."},"animation_standard":{"version":"v0.6.16","canvas":{"width":420,"height":420,"anchor_x":210,"anchor_y":392},"animations":{"idle":{"frames":["sprites/runtime/idle/frame_01.png"],"frame_ms":180,"loop":true,"events":[]},"basic_attack":{"frames":["sprites/runtime/attack/basic/frame_01.png","sprites/runtime/attack/basic/frame_01_alt.png"],"frame_ms":120,"loop":false,"events":[{"frame":1,"event":"release_projectile_or_apply_melee"}]}},"vfx":{"jutsu/freeze_blast/projectile":{"frames":["vfx/jutsu/freeze_blast/projectile/frame_01.png","vfx/jutsu/freeze_blast/projectile/frame_02.png","vfx/jutsu/freeze_blast/projectile/frame_03.png"],"frame_ms":90,"loop":false,"body_asset":false}}}},"gotoku":{"schema_version":2,"id":"gotoku","display_name":"Gotoku","title":null,"role":"enemy","archetype":"ghost_striker","element":"Spirit","rarity":null,"collection":{"owned":false,"inventory_visible":false,"battle_ready":true},"stats":{"level":1,"hp":100,"attack":15,"defense":9,"speed":31},"combat":{"mark":"G","movement_range":70,"basic_

--- match 6 @ 24614860 ---
d":{"version":"v0.6.16","canvas":{"width":420,"height":420,"anchor_x":210,"anchor_y":392},"animations":{"idle":{"frames":["sprites/runtime/idle/frame_01.png"],"frame_ms":180,"loop":true,"events":[]},"basic_attack":{"frames":["sprites/runtime/attack/basic/frame_01.png","sprites/runtime/attack/basic/frame_01_alt.png"],"frame_ms":120,"loop":false,"events":[{"frame":1,"event":"release_projectile_or_apply_melee"}]}},"vfx":{"jutsu/freeze_blast/projectile":{"frames":["vfx/jutsu/freeze_blast/projectile/frame_01.png","vfx/jutsu/freeze_blast/projectile/frame_02.png","vfx/jutsu/freeze_blast/projectile/frame_03.png"],"frame_ms":90,"loop":false,"body_asset":false}}}},"gotoku":{"schema_version":2,"id":"gotoku","display_name":"Gotoku","title":null,"role":"enemy","archetype":"ghost_striker","element":"Spirit","rarity":null,"collection":{"owned":false,"inventory_visible":false,"battle_ready":true},"stats":{"level":1,"hp":100,"attack":15,"defense":9,"speed":31},"combat":{"mark":"G","movement_range":70,"basic_shape":{"type":"rect","w":180,"h":55},"jutsu_shape":null,"chakra_max":0},"render":{"scale":1.0},"abilities":{"basic":{"id":"ghost_strike","name":"Ghost Strike","damage_multiplier":1.0,"delivery":"melee"},"jutsu":null},"assets":{"art":null,"card":null,"portrait":"portraits/","icon":"icons/","sprites":{"idle":"sprites/runtime/idle/","attack_basic":"sprites/runtime/attack/","recoil":"sprites/runtime/recoil/","jutsu":"sprites/runtime/jutsu/"},"vfx":"vfx/"},"readiness":{"collection":false,"battle":true,"idle":true,"basic_attack":false,"jutsu":false,"recoil":false,"notes":"Existing ghost enemy; stats remain in current runtime during migration."},"balance":{"status":"provisional","pass":"v0.5.30","notes":"Initial archetype-based values. Tune freely during balance testing."}},"yurei":{"schema_version":2,"id":"yurei","display_name":"Yurei","title":null,"role":"enemy","archetype":"ghost_skirmisher","element":"Spirit","rarity":null,"collection":{"owned":false,"inventory_visible":false,"battle_ready":true},"stats":{"level":1,"hp":110,"attack":15,"defense":8,"speed":36},"combat":{"mark":"G","movement_range":76,"basic_shape":{"type":"circle","r":76},"jutsu_shape":null,"chakra_max":0},"render":{"scale":1.0},"abilities":{"basic":{"id":"spirit_claw","name":"Spirit Claw","damage_multiplier":1.0,"delivery":"melee"},"jutsu":null},"assets":{"art":null,"card":null,"portrait":"portraits/","icon":"icons/","sprites":{"idle":"sprites/runtime/idle/","attack_basic":"sprites/runtime/attack/","recoil":"sprites/runtime/recoil/","jutsu":"sprites/runtime/jutsu/"},"vfx":"vfx/"},"readiness":{"collection":false,"battle":true,"idle":true,"basic_attack":false,"jutsu":false,"recoil":false,"notes":"Existing ghost enemy; stats remain in current runtime during migration."},"balance":{"status":"provisional","pass":"v0.5.30","notes":"Initial archetype-based values. Tune freely during balance testing."}},"onre":{"schema_version":2,"id":"onre","display_name":"Onre","title":null,"role":"enemy","archetype":"ghost_skirmisher","element":"Spirit","rarity":null,"collection":{"owned":false,"inventory_visible":false,"battle_ready":true},"stats":{"level":1,"hp":100,"attack":14,"defense":8,"speed":34},"combat":{"mark":"G"

--- match 7 @ 24615982 ---
:{"id":"ghost_strike","name":"Ghost Strike","damage_multiplier":1.0,"delivery":"melee"},"jutsu":null},"assets":{"art":null,"card":null,"portrait":"portraits/","icon":"icons/","sprites":{"idle":"sprites/runtime/idle/","attack_basic":"sprites/runtime/attack/","recoil":"sprites/runtime/recoil/","jutsu":"sprites/runtime/jutsu/"},"vfx":"vfx/"},"readiness":{"collection":false,"battle":true,"idle":true,"basic_attack":false,"jutsu":false,"recoil":false,"notes":"Existing ghost enemy; stats remain in current runtime during migration."},"balance":{"status":"provisional","pass":"v0.5.30","notes":"Initial archetype-based values. Tune freely during balance testing."}},"yurei":{"schema_version":2,"id":"yurei","display_name":"Yurei","title":null,"role":"enemy","archetype":"ghost_skirmisher","element":"Spirit","rarity":null,"collection":{"owned":false,"inventory_visible":false,"battle_ready":true},"stats":{"level":1,"hp":110,"attack":15,"defense":8,"speed":36},"combat":{"mark":"G","movement_range":76,"basic_shape":{"type":"circle","r":76},"jutsu_shape":null,"chakra_max":0},"render":{"scale":1.0},"abilities":{"basic":{"id":"spirit_claw","name":"Spirit Claw","damage_multiplier":1.0,"delivery":"melee"},"jutsu":null},"assets":{"art":null,"card":null,"portrait":"portraits/","icon":"icons/","sprites":{"idle":"sprites/runtime/idle/","attack_basic":"sprites/runtime/attack/","recoil":"sprites/runtime/recoil/","jutsu":"sprites/runtime/jutsu/"},"vfx":"vfx/"},"readiness":{"collection":false,"battle":true,"idle":true,"basic_attack":false,"jutsu":false,"recoil":false,"notes":"Existing ghost enemy; stats remain in current runtime during migration."},"balance":{"status":"provisional","pass":"v0.5.30","notes":"Initial archetype-based values. Tune freely during balance testing."}},"onre":{"schema_version":2,"id":"onre","display_name":"Onre","title":null,"role":"enemy","archetype":"ghost_skirmisher","element":"Spirit","rarity":null,"collection":{"owned":false,"inventory_visible":false,"battle_ready":true},"stats":{"level":1,"hp":100,"attack":14,"defense":8,"speed":34},"combat":{"mark":"G","movement_range":72,"basic_shape":{"type":"circle","r":82},"jutsu_shape":null,"chakra_max":0},"render":{"scale":1.0},"abilities":{"basic":{"id":"ghost_swipe","name":"Ghost Swipe","damage_multiplier":1.0,"delivery":"melee"},"jutsu":null},"assets":{"art":null,"card":null,"portrait":"portraits/","icon":"icons/","sprites":{"idle":"sprites/runtime/idle/","attack_basic":"sprites/runtime/attack/","recoil":"sprites/runtime/recoil/","jutsu":"sprites/runtime/jutsu/"},"vfx":"vfx/"},"readiness":{"collection":false,"battle":true,"idle":true,"basic_attack":false,"jutsu":false,"recoil":false,"notes":"Existing ghost enemy; stats remain in current runtime during migration."},"balance":{"status":"provisional","pass":"v0.5.30","notes":"Initial archetype-based values. Tune freely during balance testing."}}};</script>
<script>
const cvs=document.getElementById('game');
const LOGICAL_W=480,LOGICAL_H=720;
const RENDER_DPR=Math.min(window.devicePixelRatio||1,2);
cvs.width=Math.round(LOGICAL_W*RENDER_DPR);
cvs.height=Math.round(LOGICAL_H*RENDER_DPR);
const ctx=cvs.getContext('2d');
ctx.setTransform(RENDER_DPR,0,0,RENDER_DPR,0,0)

--- match 8 @ 24617093 ---
asic":{"id":"spirit_claw","name":"Spirit Claw","damage_multiplier":1.0,"delivery":"melee"},"jutsu":null},"assets":{"art":null,"card":null,"portrait":"portraits/","icon":"icons/","sprites":{"idle":"sprites/runtime/idle/","attack_basic":"sprites/runtime/attack/","recoil":"sprites/runtime/recoil/","jutsu":"sprites/runtime/jutsu/"},"vfx":"vfx/"},"readiness":{"collection":false,"battle":true,"idle":true,"basic_attack":false,"jutsu":false,"recoil":false,"notes":"Existing ghost enemy; stats remain in current runtime during migration."},"balance":{"status":"provisional","pass":"v0.5.30","notes":"Initial archetype-based values. Tune freely during balance testing."}},"onre":{"schema_version":2,"id":"onre","display_name":"Onre","title":null,"role":"enemy","archetype":"ghost_skirmisher","element":"Spirit","rarity":null,"collection":{"owned":false,"inventory_visible":false,"battle_ready":true},"stats":{"level":1,"hp":100,"attack":14,"defense":8,"speed":34},"combat":{"mark":"G","movement_range":72,"basic_shape":{"type":"circle","r":82},"jutsu_shape":null,"chakra_max":0},"render":{"scale":1.0},"abilities":{"basic":{"id":"ghost_swipe","name":"Ghost Swipe","damage_multiplier":1.0,"delivery":"melee"},"jutsu":null},"assets":{"art":null,"card":null,"portrait":"portraits/","icon":"icons/","sprites":{"idle":"sprites/runtime/idle/","attack_basic":"sprites/runtime/attack/","recoil":"sprites/runtime/recoil/","jutsu":"sprites/runtime/jutsu/"},"vfx":"vfx/"},"readiness":{"collection":false,"battle":true,"idle":true,"basic_attack":false,"jutsu":false,"recoil":false,"notes":"Existing ghost enemy; stats remain in current runtime during migration."},"balance":{"status":"provisional","pass":"v0.5.30","notes":"Initial archetype-based values. Tune freely during balance testing."}}};</script>
<script>
const cvs=document.getElementById('game');
const LOGICAL_W=480,LOGICAL_H=720;
const RENDER_DPR=Math.min(window.devicePixelRatio||1,2);
cvs.width=Math.round(LOGICAL_W*RENDER_DPR);
cvs.height=Math.round(LOGICAL_H*RENDER_DPR);
const ctx=cvs.getContext('2d');
ctx.setTransform(RENDER_DPR,0,0,RENDER_DPR,0,0);
const W=LOGICAL_W,H=LOGICAL_H;
const phaseEl=document.getElementById('phase'),logEl=document.getElementById('log'),statusEl=document.getElementById('status'),meter=document.getElementById('meter');
const normalBtn=document.getElementById('normal'),jutsuBtn=document.getElementById('jutsu'),swapBtn=document.getElementById('swap');
const bossHpHud=document.getElementById('bossHpHud'),bossHpFill=document.getElementById('bossHpFill');
const meteorScreenFlash=document.getElementById('meteorScreenFlash');
function getMeteorScreenFlash(){return meteorScreenFlash||document.getElementById('meteorScreenFlash');}
const menuScreen=document.getElementById('menuScreen'),battleScreen=document.getElementById('battleScreen'),level1Btn=document.getElementById('level1Btn'),boss1Btn=document.getElementById('boss1Btn'),tacticalTicker=document.getElementById('tacticalTicker');
const summonScreen=document.getElementById('summonScreen'),summonsBtn=document.getElementById('summonsBtn'),summonBackBtn=document.getElementById('summonBackBtn');
const singleSummonBtn=document.getElementById('singleSummonBtn'),multiSummonBt

--- match 9 @ 43515179 ---
mark,speed,attack,defense,archetype,shape,jutsuShape,jutsuCost,jutsuDamage,linkBuff,hp:maxHp,maxHp,chakra:2,maxChakra,rotation:0}
}

// v0.6.7 DATA AUTHORITY
// Canonical stats/roles/shapes/ability costs live in each unit's data/unit.json.
// The standalone embeds a generated copy in window.BLAZING_UNIT_DATA so mobile local-file play needs no fetch().
const UNIT_DATA=window.BLAZING_UNIT_DATA||{};
const UNIT_NAME_TO_ID=Object.freeze(Object.fromEntries(Object.entries(UNIT_DATA).map(([id,d])=>[d.display_name,id])));
function canonicalUnit(nameOrId){
 const id=UNIT_DATA[nameOrId]?nameOrId:UNIT_NAME_TO_ID[nameOrId];
 const d=id?UNIT_DATA[id]:null;
 if(!d)throw new Error(`Unknown canonical unit: ${nameOrId}`);
 return d;
}
function runtimeDefinition(nameOrId){
 const d=canonicalUnit(nameOrId),basic=d.abilities.basic||{},jutsu=d.abilities.jutsu||{};
 return{
  mark:d.combat.mark,speed:d.stats.speed,attack:d.stats.attack,defense:d.stats.defense,
  hp:d.stats.hp,maxHp:d.stats.hp,shape:d.combat.basic_shape,jutsuShape:d.combat.jutsu_shape||{type:'circle',r:0},
  maxChakra:d.combat.chakra_max||0,jutsuCost:jutsu.cost??99,
  jutsuDamage:jutsu?Math.round(d.stats.attack*(jutsu.damage_multiplier||1)):0,
  jutsuName:jutsu?.name||'Jutsu',archetype:d.archetype,render:d.render||{},
  linkBuff:d.combat.link_buff||null
 };
}
const BATTLE_ROSTER=Object.freeze(Object.fromEntries(
 ['crimson','subzero','lebee','senku','anubis']
   .filter(id=>UNIT_DATA[id])
   .map(id=>[UNIT_DATA[id].display_name,runtimeDefinition(id)])
));
function makeRosterUnit(name,opts={}){
 const d=BATTLE_ROSTER[name];
 if(!d)throw new Error(`Unknown battle unit: ${name}`);
 const u=U(name,d.mark,d.speed,d.attack,d.shape,d.jutsuShape,d.jutsuCost,d.jutsuDamage,d.linkBuff||null,d.maxHp,d.maxChakra,d.defense,d.archetype);
 u.jutsuName=d.jutsuName||'Jutsu';
 u.renderScale=d.render?.scale||1;
 const start=opts.startingChakra??2;
 u.chakra=start==='full'?u.maxChakra:Math.max(0,Math.min(u.maxChakra,start));
 return u;
}
function makeEnemyFromData(id,name,x,y,mark,overrides={}){
 const d=canonicalUnit(id),base=d.stats,combat=d.combat;
 const hp=overrides.hp??base.hp;
 return{name:name||d.display_name,spriteKey:d.display_name,mark:mark||combat.mark,x,y,r:overrides.r??19,
  hp,maxHp:hp,speed:overrides.speed??base.speed,attack:overrides.attack??base.attack,defense:overrides.defense??base.defense,
  gauge:0,shape:overrides.shape??combat.basic_shape,archetype:d.archetype};
}

// v0.5.19 core-roster whitelist. Legacy units stay in BATTLE_ROSTER for rollback only.
const ACTIVE_PLAYABLE_UNITS=Object.freeze(['Crimson','Sub-Zero','Lebee','Senku']);
const DEFAULT_ACTIVE_TEAM=Object.freeze(['Crimson','Lebee','Sub-Zero']);

function validatePlayableRosterRegistration(){
 const missing=ACTIVE_PLAYABLE_UNITS.filter(name=>!BATTLE_ROSTER[name]);
 if(missing.length){
   console.error('Playable roster registration missing:',missing);
   return false;
 }
 return true;
}
const PLAYABLE_ROSTER_VALID=validatePlayableRosterRegistration();
const TEAM_STORAGE_KEY='blazingBattle.activeTeam.v1';

function validActiveTeam(team){
 return Array.isArray(team)
   && team.length===3
   && new Set(team).size===3
   && team.every(name=>ACTIVE_PLAYAB

--- match 10 @ 43516594 ---
,'anubis']
   .filter(id=>UNIT_DATA[id])
   .map(id=>[UNIT_DATA[id].display_name,runtimeDefinition(id)])
));
function makeRosterUnit(name,opts={}){
 const d=BATTLE_ROSTER[name];
 if(!d)throw new Error(`Unknown battle unit: ${name}`);
 const u=U(name,d.mark,d.speed,d.attack,d.shape,d.jutsuShape,d.jutsuCost,d.jutsuDamage,d.linkBuff||null,d.maxHp,d.maxChakra,d.defense,d.archetype);
 u.jutsuName=d.jutsuName||'Jutsu';
 u.renderScale=d.render?.scale||1;
 const start=opts.startingChakra??2;
 u.chakra=start==='full'?u.maxChakra:Math.max(0,Math.min(u.maxChakra,start));
 return u;
}
function makeEnemyFromData(id,name,x,y,mark,overrides={}){
 const d=canonicalUnit(id),base=d.stats,combat=d.combat;
 const hp=overrides.hp??base.hp;
 return{name:name||d.display_name,spriteKey:d.display_name,mark:mark||combat.mark,x,y,r:overrides.r??19,
  hp,maxHp:hp,speed:overrides.speed??base.speed,attack:overrides.attack??base.attack,defense:overrides.defense??base.defense,
  gauge:0,shape:overrides.shape??combat.basic_shape,archetype:d.archetype};
}

// v0.5.19 core-roster whitelist. Legacy units stay in BATTLE_ROSTER for rollback only.
const ACTIVE_PLAYABLE_UNITS=Object.freeze(['Crimson','Sub-Zero','Lebee','Senku']);
const DEFAULT_ACTIVE_TEAM=Object.freeze(['Crimson','Lebee','Sub-Zero']);

function validatePlayableRosterRegistration(){
 const missing=ACTIVE_PLAYABLE_UNITS.filter(name=>!BATTLE_ROSTER[name]);
 if(missing.length){
   console.error('Playable roster registration missing:',missing);
   return false;
 }
 return true;
}
const PLAYABLE_ROSTER_VALID=validatePlayableRosterRegistration();
const TEAM_STORAGE_KEY='blazingBattle.activeTeam.v1';

function validActiveTeam(team){
 return Array.isArray(team)
   && team.length===3
   && new Set(team).size===3
   && team.every(name=>ACTIVE_PLAYABLE_UNITS.includes(name));
}
function getActiveTeam(){
 if(validActiveTeam(window.BLAZING_ACTIVE_TEAM))return [...window.BLAZING_ACTIVE_TEAM];
 try{
   const saved=JSON.parse(localStorage.getItem(TEAM_STORAGE_KEY)||'null');
   if(validActiveTeam(saved)){
     window.BLAZING_ACTIVE_TEAM=[...saved];
     return [...saved];
   }
 }catch(_){}
 window.BLAZING_ACTIVE_TEAM=[...DEFAULT_ACTIVE_TEAM];
 return [...DEFAULT_ACTIVE_TEAM];
}
function setActiveTeam(team){
 if(!validActiveTeam(team))throw new Error('Invalid active team');
 window.BLAZING_ACTIVE_TEAM=[...team];
 try{localStorage.setItem(TEAM_STORAGE_KEY,JSON.stringify(team));}catch(_){}
 return [...team];
}
function teamSpawnOptions(name){
 // Current development test configuration: Lebee and Sub-Zero remain Jutsu-ready.
 return (name==='Lebee'||name==='Sub-Zero')?{startingChakra:'full'}:{};
}
function buildPlayerPairs(y){
 const xs=[95,240,385];
 const team=getActiveTeam();

 return team.map((name,i)=>{
   if(!BATTLE_ROSTER[name]){
     console.error(`Team unit "${name}" is not registered in BATTLE_ROSTER.`);
     const fallback=DEFAULT_ACTIVE_TEAM.find(n=>BATTLE_ROSTER[n]&&!team.includes(n))
       || DEFAULT_ACTIVE_TEAM.find(n=>BATTLE_ROSTER[n])
       || Object.keys(BATTLE_ROSTER)[0];
     name=fallback;
   }

   return {
     x:xs[i],y,active:0,gauge:0,units:[
       makeRosterUnit(name,teamSpawnOptions(name)),
       emptyReserve()
     ]

--- match 11 @ 43521047 ---
 makeEnemyFromData('onre','Rogue 1',120,175,'1'),
   makeEnemyFromData('gotoku','Rogue 2',355,225,'2'),
   makeEnemyFromData('yurei','Rogue 3',175,335,'3'),
   makeEnemyFromData('onre','Rogue 4',350,385,'4',{hp:105,speed:30,attack:16,shape:{type:'cross',l:160,t:48}})
  ]
 }}

function freshBoss(){
 return {
  time:0,phase:'charge',action:'normal',ready:null,readyStarted:0,_chargeSince:performance.now(),drag:false,dragOrigin:null,dragVisual:null,anim:null,floaters:[],victoryFX:null,jutsuDim:null,
  log:'BOSS BATTLE — Anubis, Judge of Souls.',
  bossBattle:true,battleMode:'boss',
  pairs:buildPlayerPairs(585),
  enemies:[
   {
    name:'Anubis',spriteKey:'Anubis',mark:canonicalUnit('anubis').combat.mark,
    x:240,y:195,r:29,
    hp:canonicalUnit('anubis').stats.hp,maxHp:canonicalUnit('anubis').stats.hp,
    speed:canonicalUnit('anubis').stats.speed,attack:canonicalUnit('anubis').stats.attack,defense:canonicalUnit('anubis').stats.defense,gauge:0,
    shape:canonicalUnit('anubis').combat.basic_shape,
    boss:true,sizeScale:canonicalUnit('anubis').render.battle_size_scale||2.0625
   }
  ]
 };
}

let S=fresh();

const front=p=>p.units[p.active], back=p=>p.units[1-p.active];
const pairAlive=p=>p.units.some(u=>u.hp>0);
const alivePairs=()=>S.pairs.filter(pairAlive);
const aliveEnemies=()=>S.enemies.filter(e=>e.hp>0);
const NORMAL_ATTACK_SHAPE={type:'circle',r:43};
const normalShape=()=>NORMAL_ATTACK_SHAPE;
const attackShape=u=>{
 // Strict separation:
 // NORMAL -> unit's canonical combat.basic_shape
 // JUTSU  -> unit's canonical combat.jutsu_shape
 if(S.action==='jutsu')return u.jutsuShape;
 return u.shape||normalShape();
};
function attackProxy(u){return {...u,shape:attackShape(u)}}

// Ability-tier lookup must live at battle scope because both the ground-field
// pass and the later enemy target-feedback pass use it.
function isUltimateAction(unit){
 if(!unit)return false;
 try{
   // canonicalUnit already resolves both IDs and display names through UNIT_NAME_TO_ID.
   const ability=canonicalUnit(unit.name)?.abilities?.jutsu;
   return !!(ability?.ultimate===true || ability?.category==='ultimate' || ability?.tier==='ultimate');
 }catch(_){
   return false;
 }
}

function isAllySupportJutsu(unit){
 if(!unit)return false;
 try{
   const ability=canonicalUnit(unit.name)?.abilities?.jutsu;
   return ability?.target==='all_allies' || ability?.effect==='heal_full';
 }catch(_){
   return false;
 }
}


function ensureFront(p){
 if(front(p).hp>0)return true;
 if(front(p).hp<=0 && koAnimating(front(p).name))return true;
 if(back(p).hp>0){p.active=1-p.active;return true}
 return false
}
function fxMap(){
 if(!S.anim)S.anim={positions:{}};
 if(!S.anim.unitFX)S.anim.unitFX={};
 return S.anim.unitFX;
}
function unitFX(name){
 return S.anim?.unitFX?.[name]||null;
}
function koAnimating(name){
 let fx=unitFX(name);
 return !!(fx&&fx.kind==='ko'&&(performance.now()-fx.start)<fx.duration);
}
function renderWhileKO(name){
 let fx=unitFX(name);
 return !!(fx&&fx.kind==='ko'&&(performance.now()-fx.start)<fx.duration+90);
}
function startHitReaction(name,dir=1,strong=false){
 fxMap()[name]={kind:'hit',start:performance.now(),duration:strong?230:180,dir:dir||1,strong:!!strong};


--- match 12 @ 43521555 ---
BOSS BATTLE — Anubis, Judge of Souls.',
  bossBattle:true,battleMode:'boss',
  pairs:buildPlayerPairs(585),
  enemies:[
   {
    name:'Anubis',spriteKey:'Anubis',mark:canonicalUnit('anubis').combat.mark,
    x:240,y:195,r:29,
    hp:canonicalUnit('anubis').stats.hp,maxHp:canonicalUnit('anubis').stats.hp,
    speed:canonicalUnit('anubis').stats.speed,attack:canonicalUnit('anubis').stats.attack,defense:canonicalUnit('anubis').stats.defense,gauge:0,
    shape:canonicalUnit('anubis').combat.basic_shape,
    boss:true,sizeScale:canonicalUnit('anubis').render.battle_size_scale||2.0625
   }
  ]
 };
}

let S=fresh();

const front=p=>p.units[p.active], back=p=>p.units[1-p.active];
const pairAlive=p=>p.units.some(u=>u.hp>0);
const alivePairs=()=>S.pairs.filter(pairAlive);
const aliveEnemies=()=>S.enemies.filter(e=>e.hp>0);
const NORMAL_ATTACK_SHAPE={type:'circle',r:43};
const normalShape=()=>NORMAL_ATTACK_SHAPE;
const attackShape=u=>{
 // Strict separation:
 // NORMAL -> unit's canonical combat.basic_shape
 // JUTSU  -> unit's canonical combat.jutsu_shape
 if(S.action==='jutsu')return u.jutsuShape;
 return u.shape||normalShape();
};
function attackProxy(u){return {...u,shape:attackShape(u)}}

// Ability-tier lookup must live at battle scope because both the ground-field
// pass and the later enemy target-feedback pass use it.
function isUltimateAction(unit){
 if(!unit)return false;
 try{
   // canonicalUnit already resolves both IDs and display names through UNIT_NAME_TO_ID.
   const ability=canonicalUnit(unit.name)?.abilities?.jutsu;
   return !!(ability?.ultimate===true || ability?.category==='ultimate' || ability?.tier==='ultimate');
 }catch(_){
   return false;
 }
}

function isAllySupportJutsu(unit){
 if(!unit)return false;
 try{
   const ability=canonicalUnit(unit.name)?.abilities?.jutsu;
   return ability?.target==='all_allies' || ability?.effect==='heal_full';
 }catch(_){
   return false;
 }
}


function ensureFront(p){
 if(front(p).hp>0)return true;
 if(front(p).hp<=0 && koAnimating(front(p).name))return true;
 if(back(p).hp>0){p.active=1-p.active;return true}
 return false
}
function fxMap(){
 if(!S.anim)S.anim={positions:{}};
 if(!S.anim.unitFX)S.anim.unitFX={};
 return S.anim.unitFX;
}
function unitFX(name){
 return S.anim?.unitFX?.[name]||null;
}
function koAnimating(name){
 let fx=unitFX(name);
 return !!(fx&&fx.kind==='ko'&&(performance.now()-fx.start)<fx.duration);
}
function renderWhileKO(name){
 let fx=unitFX(name);
 return !!(fx&&fx.kind==='ko'&&(performance.now()-fx.start)<fx.duration+90);
}
function startHitReaction(name,dir=1,strong=false){
 fxMap()[name]={kind:'hit',start:performance.now(),duration:strong?230:180,dir:dir||1,strong:!!strong};
}
function startKOReaction(name,dir=1){
 fxMap()[name]={kind:'ko',start:performance.now(),duration:520,dir:dir||1};
}
function pairRenderable(p){
 return pairAlive(p)||renderWhileKO(front(p).name)||renderWhileKO(back(p).name);
}
function handleKO(p,defeatedName,dir=1){
 addFloat(p.x,p.y-58,'KO','#ff7272');
 startKOReaction(defeatedName,dir);
 p.gauge=0;
 if(back(p).hp>0){
   S.log=`${defeatedName} was knocked out — ${back(p).name} is preparing to move to the front.`;
   setTimeout(()=>{
     if(front(p)

--- match 13 @ 50971392 ---
/2Q==",
     hp:'—',atk:'—',spd:'—',range:'—',cost:'—',collectionOnly:true,
     skills:[
       ['✦','BATTLE KIT PENDING','Lebee is owned and collection-ready. Her battle abilities will be defined when her sprite/Jutsu package is approved.','—']
     ]
   }
 };
 // v0.6.7 — Inventory reads combat numbers from the same canonical unit data as battle.
 const canonicalByDisplay=Object.fromEntries(Object.values(window.BLAZING_UNIT_DATA||{}).map(d=>[d.display_name,d]));
 function rangeLabel(shape){
   if(!shape)return '—';
   if(shape.type==='cone')return `CONE ${shape.r}`;
   if(shape.type==='circle')return `RADIUS ${shape.r}`;
   if(shape.type==='rect')return `LINE ${shape.w}`;
   if(shape.type==='cross')return `CROSS ${shape.l}`;
   return String(shape.type||'—').toUpperCase();
 }
 Object.entries(owned).forEach(([name,u])=>{
   const d=canonicalByDisplay[name]; if(!d)return;
   u.hp=d.stats.hp;u.atk=d.stats.attack;u.def=d.stats.defense;u.spd=d.stats.speed;
   u.range=rangeLabel(d.combat.basic_shape);u.cost=d.abilities.jutsu?.cost??'—';u.archetype=d.archetype;
 });

 document.getElementById('inventoryCrimsonThumb').src="data:image/png;base64,

===== jutsu_shape =====

--- match 1 @ 24599040 ---
"}};</script>
<div id="meteorScreenFlash" aria-hidden="true"></div>
<script id="blazing-unit-data">window.BLAZING_UNIT_DATA={"senku":{"schema_version":2,"id":"senku","display_name":"Senku","title":"The Kingdom of Science","role":"playable","archetype":"ranged_tactician","element":"Nature","rarity":"Legendary","collection":{"owned":true,"inventory_visible":true,"battle_ready":true},"stats":{"level":1,"hp":180,"attack":38,"defense":28,"speed":64},"combat":{"mark":"S","movement_range":92,"basic_shape":{"type":"rect","w":255,"h":58,"offset_x":116},"jutsu_shape":{"type":"screen_rect","scope":"all_allies"},"chakra_max":8},"render":{"scale":1.3},"abilities":{"basic":{"id":"explosive_bomb","name":"Explosive Bomb","damage_multiplier":1.0,"chakra_gain":1,"delivery":"ranged_projectile","range_presentation":"forward_lane","presentation":{"cast_duration_ms":560,"flight_duration_ms":620,"impact_duration_ms":360,"arc_height_px":86,"release_frame":3,"projectile_rotation":true,"impact_anchor":{"target":"feet","x":0.5,"y":0.96,"ground_offset_y_px":3,"layer":"ground"},"impact_vfx_mode":"single_frame_fade","impact_frame_index":4,"impact_fade_start":0.34,"impact_height_px":58,"impact_animation":"static_hold_fade","impact_asset":"vfx/bomb/static_impact/explosion.png","projectile_assets":["vfx/bomb/projectile_clean/frame_01.png","vfx/bomb/projectile_clean/frame_02.png","vfx/bomb/projectile_clean/frame_03.png","vfx/bomb/projectile_clean/frame_04.png","vfx/bomb/projectile_clean/frame_05.png"],"projectile_cleaned":true}},"jutsu":{"id":"revival_formula","name":"Revival Formula","cost":4,"damage_multiplier":0,"delivery":"bottle_party_heal","category":"jutsu","ultimate":false,"status_effect":null,"status_chance":0,"presentation":{"cast_frames":4,"projectile_frames":3,"impact_frames":3,"cast_duration_ms":680,"flight_duration_ms":420,"impact_duration_ms":620,"screen_dim_alpha":0.12},"target":"all_allies","effect":"heal_full","heal_percent":1.0}},"assets":{"art":"art/senku_full_art.jpeg","card":"cards/senku_card.jpeg","sprites":{"idle":"sprites/runtime/idle/","attack_basic":"sprites/runtime/attack/basic/","recoil":"sprites/runtime/recoil/","jutsu":"sprites/runtime/jutsu/chemical_reaction/cast/"},"vfx":{"projectile":"vfx/bomb/projectile/","explosion":"vfx/bomb/explosion/","jutsu_projectile":"vfx/jutsu/chemical_reaction/projectile/","jutsu_impact":"vfx/jutsu/chemical_reaction/impact/"}},"readiness":{"collection":true,"battle":true,"idle":true,"basic_attack":true,"jutsu":true,"recoil":true,"notes":"v0.6.22: Bomb projectile frames cleaned to remove detached source-sheet/text-like artifacts while retaining bomb, fuse spark, and intended trail. Arc/timing unchanged.","animation_map":true},"balance":{"status":"provisional","pass":"v0.6.8"},"animation_

--- match 2 @ 24604147 ---
/frame_04.png","vfx/bomb/projectile/frame_05.png"],"frame_ms":90,"loop":false,"body_asset":false},"jutsu/chemical_reaction/impact":{"frames":["vfx/jutsu/chemical_reaction/impact/frame_01.png","vfx/jutsu/chemical_reaction/impact/frame_02.png","vfx/jutsu/chemical_reaction/impact/frame_03.png"],"frame_ms":90,"loop":false,"body_asset":false},"jutsu/chemical_reaction/projectile":{"frames":["vfx/jutsu/chemical_reaction/projectile/frame_01.png","vfx/jutsu/chemical_reaction/projectile/frame_02.png","vfx/jutsu/chemical_reaction/projectile/frame_03.png"],"frame_ms":90,"loop":false,"body_asset":false}}}},"anubis":{"schema_version":2,"id":"anubis","display_name":"Anubis","title":"Judge of Souls","role":"boss","archetype":"boss_tank_controller","element":"Dark","rarity":"Boss","collection":{"owned":false,"inventory_visible":false,"battle_ready":true},"stats":{"level":1,"hp":950,"attack":38,"defense":32,"speed":43},"combat":{"mark":"BOSS","movement_range":72,"basic_shape":{"type":"circle","r":105},"jutsu_shape":{"type":"circle","r":205},"chakra_max":8},"render":{"scale":1.25,"battle_size_scale":2.3713},"abilities":{"basic":{"id":"staff_strike","name":"Staff Strike","damage_multiplier":1.0,"delivery":"melee"},"jutsu":{"id":"boss_rotation","name":"Boss Rotation","cost":5,"damage_multiplier":2.05,"delivery":"boss_rotation"}},"assets":{"art":null,"card":null,"portrait":"portraits/","icon":"icons/","sprites":{"idle":"sprites/runtime/idle/","attack_basic":"sprites/runtime/attack/basic/","recoil":"sprites/runtime/recoil/","jutsu":"sprites/runtime/jutsu/"},"vfx":"vfx/"},"readiness":{"collection":false,"battle":true,"idle":true,"basic_attack":true,"jutsu":true,"recoil":false,"notes":"Boss definition. Existing combat code remains authoritative until migration.","animation_map":true},"balance":{"status":"provisional","pass":"v0.5.30","notes":"Initial archetype-based values. Tune freely during balance testing."},"animation_standard":{"version":"v0.6.16","canvas":{"width":420,"height":420,"anchor_x":210,"anchor_y":392},"animations":{"idle":{"frames":["sprites/runtime/idle/frame_01.png"],"frame_ms":180,"loop":true,"events":[]}},"vfx":{}}},"crimson":{"schema_version":2,"id":"crimson","display_name":"Crimson","title":"Blazing Vanguard","role":"playable","archetype":"assassin_melee","element":"Fire","rarity":"Legendary","collection":{"owned":true,"inventory_visible":true,"battle_ready":true},"stats":{"level":1,"hp":118,"attack":35,"defense":18,"speed":68},"combat":{"mark":"V","movement_range":96,"basic_shape":{"type":"cone","r":182,"a":1.0},"jutsu_shape":{"type":"rect","w":310,"h":86},"chakra_max":8},"render":{"scale":1.0},"abilities":{"basic":{"id":"basic_strike","name":"Basic Strike","damage_multiplier":1.0,"chakra_gain":1,"delivery":"melee"},"jutsu":{"id":"current_jutsu","name":"Crimson Jutsu","cost":4,"damage_multiplier":1.9,"delivery":"forward_area","category":"jutsu","ultimate":false}},"assets":{"art":"art/current_collection_art.jpg","card":null,"portrait":"portraits/","icon":"icons/","sprites":{"idle":"sprites/runtime/idle/","attack_basic":"sprites/runtime/attack/basic/","recoil":"sprites/runtime/recoil/","jutsu":"sprites/runtime/jutsu/"},"vfx":"vfx/","jutsu_cast":"assets/char

--- match 3 @ 24605704 ---
/"},"vfx":"vfx/"},"readiness":{"collection":false,"battle":true,"idle":true,"basic_attack":true,"jutsu":true,"recoil":false,"notes":"Boss definition. Existing combat code remains authoritative until migration.","animation_map":true},"balance":{"status":"provisional","pass":"v0.5.30","notes":"Initial archetype-based values. Tune freely during balance testing."},"animation_standard":{"version":"v0.6.16","canvas":{"width":420,"height":420,"anchor_x":210,"anchor_y":392},"animations":{"idle":{"frames":["sprites/runtime/idle/frame_01.png"],"frame_ms":180,"loop":true,"events":[]}},"vfx":{}}},"crimson":{"schema_version":2,"id":"crimson","display_name":"Crimson","title":"Blazing Vanguard","role":"playable","archetype":"assassin_melee","element":"Fire","rarity":"Legendary","collection":{"owned":true,"inventory_visible":true,"battle_ready":true},"stats":{"level":1,"hp":118,"attack":35,"defense":18,"speed":68},"combat":{"mark":"V","movement_range":96,"basic_shape":{"type":"cone","r":182,"a":1.0},"jutsu_shape":{"type":"rect","w":310,"h":86},"chakra_max":8},"render":{"scale":1.0},"abilities":{"basic":{"id":"basic_strike","name":"Basic Strike","damage_multiplier":1.0,"chakra_gain":1,"delivery":"melee"},"jutsu":{"id":"current_jutsu","name":"Crimson Jutsu","cost":4,"damage_multiplier":1.9,"delivery":"forward_area","category":"jutsu","ultimate":false}},"assets":{"art":"art/current_collection_art.jpg","card":null,"portrait":"portraits/","icon":"icons/","sprites":{"idle":"sprites/runtime/idle/","attack_basic":"sprites/runtime/attack/basic/","recoil":"sprites/runtime/recoil/","jutsu":"sprites/runtime/jutsu/"},"vfx":"vfx/","jutsu_cast":"assets/characters/crimson/sprites/runtime/jutsu/cast.png"},"readiness":{"collection":true,"battle":true,"idle":true,"basic_attack":true,"jutsu":true,"recoil":false,"notes":"v0.5.47: Crimson special animation corrected to Crimson-only Jutsu asset. Strict per-character routing prevents Lebee/Sub-Zero body frames from resolving on Crimson.","animation_map":true},"balance":{"status":"provisional","pass":"v0.5.30","notes":"Initial archetype-based values. Tune freely during balance testing."},"animation_standard":{"version":"v0.6.16","canvas":{"width":420,"height":420,"anchor_x":210,"anchor_y":392},"animations":{"idle":{"frames":["sprites/runtime/idle/frame_01.png","sprites/runtime/idle/frame_02.png"],"frame_ms":180,"loop":true,"events":[]},"basic_attack":{"frames":["sprites/runtime/attack/basic/frame_01.png"],"frame_ms":120,"loop":false,"events":[{"frame":1,"event":"release_projectile_or_apply_melee"}]},"jutsu":{"current":{"frames":["sprites/runtime/jutsu/current/frame_01.png"],"frame_ms":150,"loop":false,"events":[{"frame":1,"event":"release_jutsu_vfx"}]}}},"vfx":{}}},"lebee":{"schema_version":2,"id":"lebee","display_name":"Lebee","title":"Cosmic Wish","role":"playable","archetype":"caster_aoe","element":"Cosmic","rarity":"Legendary","collection":{"owned":true,"inventory_visible":true,"battle_ready":true},"stats":{"level":1,"hp":105,"attack":30,"defense":16,"speed":62},"combat":{"mark":"L","movement_range":90,"basic_shape":{"type":"rect","w":255,"h":58,"offset_x":116},"jutsu_shape":{"type":"screen_rect","padding":18},"chakra_max":8},"render":{"sc

--- match 4 @ 24607837 ---
},"animation_standard":{"version":"v0.6.16","canvas":{"width":420,"height":420,"anchor_x":210,"anchor_y":392},"animations":{"idle":{"frames":["sprites/runtime/idle/frame_01.png","sprites/runtime/idle/frame_02.png"],"frame_ms":180,"loop":true,"events":[]},"basic_attack":{"frames":["sprites/runtime/attack/basic/frame_01.png"],"frame_ms":120,"loop":false,"events":[{"frame":1,"event":"release_projectile_or_apply_melee"}]},"jutsu":{"current":{"frames":["sprites/runtime/jutsu/current/frame_01.png"],"frame_ms":150,"loop":false,"events":[{"frame":1,"event":"release_jutsu_vfx"}]}}},"vfx":{}}},"lebee":{"schema_version":2,"id":"lebee","display_name":"Lebee","title":"Cosmic Wish","role":"playable","archetype":"caster_aoe","element":"Cosmic","rarity":"Legendary","collection":{"owned":true,"inventory_visible":true,"battle_ready":true},"stats":{"level":1,"hp":105,"attack":30,"defense":16,"speed":62},"combat":{"mark":"L","movement_range":90,"basic_shape":{"type":"rect","w":255,"h":58,"offset_x":116},"jutsu_shape":{"type":"screen_rect","padding":18},"chakra_max":8},"render":{"scale":1.0},"abilities":{"basic":{"id":"star_blast","name":"Star Blast","damage_multiplier":1.0,"chakra_gain":1,"delivery":"ranged_projectile","range_presentation":"forward_lane"},"jutsu":{"id":"meteor_jutsu","name":"Meteor Jutsu","cost":4,"damage_multiplier":2.25,"delivery":"battlefield_aoe","area":"battlefield_rectangle","aoe":true,"presentation":{"cast_duration_ms":900,"meteor_fall_duration_ms":700,"meteor_count":7,"screen_dim_alpha":0.34,"meteor_scale_start":2.65,"meteor_scale_end":0.9,"meteor_stagger_ms":72,"impact_scatter_x":54,"impact_scatter_y":34,"cast_frame_count":6,"cast_sequence_path":"assets/characters/lebee/sprites/runtime/jutsu/meteor/sequence_v0532/","impact_hold_ms":680,"aftermath_hold_ms":420,"meteor_start_scale":132,"meteor_end_scale":42,"post_damage_fade_ms":150,"impact_art_enabled":false,"final_flash":true,"final_flash_yellow_ms":240,"final_flash_white_ms":105,"final_flash_fade_ms":120,"final_flash_mode":"fullscreen_overlay","final_flash_white_hold_ms":220,"final_flash_white_fade_ms":780,"flash_yellow_trigger":"second_to_last_meteor","flash_white_trigger":"final_meteor","meteor_entry_side":"opposite_caster","meteor_entry_top_margin":128,"meteor_entry_horizontal_spread":92},"category":"ultimate","ultimate":true}},"assets":{"art":"art/full_art_cosmic_wish.jpeg","card":"cards/unit_details_cosmic_wish.jpeg","portrait":"portraits/","icon":"icons/","sprites":{"idle":"sprites/runtime/idle/","attack_basic":"sprites/runtime/attack/basic/","recoil":"sprites/runtime/recoil/","jutsu":"sprites/runtime/jutsu/"},"vfx":"vfx/"},"readiness":{"collection":true,"battle":true,"idle":true,"basic_attack":true,"jutsu":true,"recoil":true,"notes":"v0.6.10: Lebee normal attack uses canonical thin forward rectangular ranged hitbox. Jutsu geometry remains independently defined by jutsu_shape.","animation_map":true},"balance":{"status":"provisional","pass":"v0.5.36","notes":"Initial archetype-based values. Tune freely during balance testing."},"presentation":{"idle":{"mode":"source_aligned_four_frame","source":"assets/characters/lebee/sprites/source/lebee_idle_sheet_v0538.jpeg","runtime_path":"assets/charac

--- match 5 @ 24609716 ---
t_enabled":false,"final_flash":true,"final_flash_yellow_ms":240,"final_flash_white_ms":105,"final_flash_fade_ms":120,"final_flash_mode":"fullscreen_overlay","final_flash_white_hold_ms":220,"final_flash_white_fade_ms":780,"flash_yellow_trigger":"second_to_last_meteor","flash_white_trigger":"final_meteor","meteor_entry_side":"opposite_caster","meteor_entry_top_margin":128,"meteor_entry_horizontal_spread":92},"category":"ultimate","ultimate":true}},"assets":{"art":"art/full_art_cosmic_wish.jpeg","card":"cards/unit_details_cosmic_wish.jpeg","portrait":"portraits/","icon":"icons/","sprites":{"idle":"sprites/runtime/idle/","attack_basic":"sprites/runtime/attack/basic/","recoil":"sprites/runtime/recoil/","jutsu":"sprites/runtime/jutsu/"},"vfx":"vfx/"},"readiness":{"collection":true,"battle":true,"idle":true,"basic_attack":true,"jutsu":true,"recoil":true,"notes":"v0.6.10: Lebee normal attack uses canonical thin forward rectangular ranged hitbox. Jutsu geometry remains independently defined by jutsu_shape.","animation_map":true},"balance":{"status":"provisional","pass":"v0.5.36","notes":"Initial archetype-based values. Tune freely during balance testing."},"presentation":{"idle":{"mode":"source_aligned_four_frame","source":"assets/characters/lebee/sprites/source/lebee_idle_sheet_v0538.jpeg","runtime_path":"assets/characters/lebee/sprites/runtime/idle/","frame_duration_ms":520,"sequence":["frame_01.png","frame_02.png","frame_03.png","frame_04.png","frame_03.png","frame_02.png"],"anchor":{"type":"fixed_lower_body","canvas":[420,420],"x":210,"baseline_y":396},"procedural_bob":false,"procedural_rotation":false}},"animation_standard":{"version":"v0.6.16","canvas":{"width":420,"height":420,"anchor_x":210,"anchor_y":392},"animations":{"idle":{"frames":["sprites/runtime/idle/frame_01.png","sprites/runtime/idle/frame_02.png","sprites/runtime/idle/frame_03.png","sprites/runtime/idle/frame_04.png"],"frame_ms":180,"loop":true,"events":[]},"basic_attack":{"frames":["sprites/runtime/attack/basic/frame_01.png","sprites/runtime/attack/basic/frame_02.png","sprites/runtime/attack/basic/frame_03.png","sprites/runtime/attack/basic/frame_04.png","sprites/runtime/attack/basic/frame_05.png","sprites/runtime/attack/basic/frame_06.png","sprites/runtime/attack/basic/frame_07.png"],"frame_ms":120,"loop":false,"events":[{"frame":6,"event":"release_projectile_or_apply_melee"}]},"recoil":{"frames":["sprites/runtime/recoil/frame_01.png","sprites/runtime/recoil/frame_02.png","sprites/runtime/recoil/frame_03.png","sprites/runtime/recoil/frame_04.png"],"frame_ms":100,"loop":false,"events":[]},"jutsu":{"meteor":{"frames":["sprites/runtime/jutsu/meteor/frame_01.png","sprites/runtime/jutsu/meteor/frame_02.png","sprites/runtime/jutsu/meteor/frame_03.png","sprites/runtime/jutsu/meteor/frame_04.png"],"frame_ms":150,"loop":false,"events":[{"frame":3,"event":"release_jutsu_vfx"}]},"meteor/bridge":{"frames":["sprites/runtime/jutsu/meteor/bridge/frame_01.png","sprites/runtime/jutsu/meteor/bridge/frame_02.png"],"frame_ms":150,"loop":false,"events":[{"frame":1,"event":"release_jutsu_vfx"}]},"meteor/sequence_v0532":{"frames":["sprites/runtime/jutsu/meteor/sequence_v0532/frame_01.png","sprites/runtime/jutsu/m

--- match 6 @ 24612694 ---
jutsu/meteor/bridge/frame_02.png"],"frame_ms":150,"loop":false,"events":[{"frame":1,"event":"release_jutsu_vfx"}]},"meteor/sequence_v0532":{"frames":["sprites/runtime/jutsu/meteor/sequence_v0532/frame_01.png","sprites/runtime/jutsu/meteor/sequence_v0532/frame_02.png","sprites/runtime/jutsu/meteor/sequence_v0532/frame_03.png","sprites/runtime/jutsu/meteor/sequence_v0532/frame_04.png","sprites/runtime/jutsu/meteor/sequence_v0532/frame_05.png","sprites/runtime/jutsu/meteor/sequence_v0532/frame_06.png"],"frame_ms":150,"loop":false,"events":[{"frame":5,"event":"release_jutsu_vfx"}]}}},"vfx":{}}},"subzero":{"schema_version":2,"id":"subzero","display_name":"Sub-Zero","title":"Absolute Zero","role":"playable","archetype":"ranged_controller","element":"Ice","rarity":"Legendary","collection":{"owned":true,"inventory_visible":true,"battle_ready":true},"stats":{"level":1,"hp":126,"attack":31,"defense":24,"speed":60},"combat":{"mark":"SZ","movement_range":88,"basic_shape":{"type":"circle","r":92},"jutsu_shape":{"type":"cone","r":205,"a":1.05},"chakra_max":8},"render":{"scale":0.88},"abilities":{"basic":{"id":"basic_attack","name":"Basic Attack","damage_multiplier":1.0,"chakra_gain":1,"delivery":"melee"},"jutsu":{"id":"freeze_blast","name":"Freeze Blast","cost":4,"damage_multiplier":2.1,"delivery":"projectile","status_effect":"freeze","status_chance":1.0,"category":"jutsu","ultimate":false}},"assets":{"art":"art/full_art_absolute_zero_v2.jpeg","card":"cards/unit_details_absolute_zero_v2.jpeg","portrait":"portraits/","icon":"icons/","sprites":{"idle":"sprites/runtime/idle/","attack_basic":"sprites/runtime/attack/basic/","recoil":"sprites/runtime/recoil/","jutsu":"sprites/runtime/jutsu/"},"vfx":"vfx/"},"readiness":{"collection":true,"battle":true,"idle":true,"basic_attack":true,"jutsu":true,"recoil":true,"notes":"v0.5.46: battlefield render scale reduced to 0.88 so Sub-Zero visually matches the current Crimson/Lebee team footprint. Applies consistently to all body animations.","animation_map":true},"balance":{"status":"provisional","pass":"v0.5.30","notes":"Initial archetype-based values. Tune freely during balance testing."},"animation_standard":{"version":"v0.6.16","canvas":{"width":420,"height":420,"anchor_x":210,"anchor_y":392},"animations":{"idle":{"frames":["sprites/runtime/idle/frame_01.png"],"frame_ms":180,"loop":true,"events":[]},"basic_attack":{"frames":["sprites/runtime/attack/basic/frame_01.png","sprites/runtime/attack/basic/frame_01_alt.png"],"frame_ms":120,"loop":false,"events":[{"frame":1,"event":"release_projectile_or_apply_melee"}]}},"vfx":{"jutsu/freeze_blast/projectile":{"frames":["vfx/jutsu/freeze_blast/projectile/frame_01.png","vfx/jutsu/freeze_blast/projectile/frame_02.png","vfx/jutsu/freeze_blast/projectile/frame_03.png"],"frame_ms":90,"loop":false,"body_asset":false}}}},"gotoku":{"schema_version":2,"id":"gotoku","display_name":"Gotoku","title":null,"role":"enemy","archetype":"ghost_striker","element":"Spirit","rarity":null,"collection":{"owned":false,"inventory_visible":false,"battle_ready":true},"stats":{"level":1,"hp":100,"attack":15,"defense":9,"speed":31},"combat":{"mark":"G","movement_range":70,"basic_shape":{"type":"rect","w":180,"h":55},"

--- match 7 @ 24614905 ---
,"height":420,"anchor_x":210,"anchor_y":392},"animations":{"idle":{"frames":["sprites/runtime/idle/frame_01.png"],"frame_ms":180,"loop":true,"events":[]},"basic_attack":{"frames":["sprites/runtime/attack/basic/frame_01.png","sprites/runtime/attack/basic/frame_01_alt.png"],"frame_ms":120,"loop":false,"events":[{"frame":1,"event":"release_projectile_or_apply_melee"}]}},"vfx":{"jutsu/freeze_blast/projectile":{"frames":["vfx/jutsu/freeze_blast/projectile/frame_01.png","vfx/jutsu/freeze_blast/projectile/frame_02.png","vfx/jutsu/freeze_blast/projectile/frame_03.png"],"frame_ms":90,"loop":false,"body_asset":false}}}},"gotoku":{"schema_version":2,"id":"gotoku","display_name":"Gotoku","title":null,"role":"enemy","archetype":"ghost_striker","element":"Spirit","rarity":null,"collection":{"owned":false,"inventory_visible":false,"battle_ready":true},"stats":{"level":1,"hp":100,"attack":15,"defense":9,"speed":31},"combat":{"mark":"G","movement_range":70,"basic_shape":{"type":"rect","w":180,"h":55},"jutsu_shape":null,"chakra_max":0},"render":{"scale":1.0},"abilities":{"basic":{"id":"ghost_strike","name":"Ghost Strike","damage_multiplier":1.0,"delivery":"melee"},"jutsu":null},"assets":{"art":null,"card":null,"portrait":"portraits/","icon":"icons/","sprites":{"idle":"sprites/runtime/idle/","attack_basic":"sprites/runtime/attack/","recoil":"sprites/runtime/recoil/","jutsu":"sprites/runtime/jutsu/"},"vfx":"vfx/"},"readiness":{"collection":false,"battle":true,"idle":true,"basic_attack":false,"jutsu":false,"recoil":false,"notes":"Existing ghost enemy; stats remain in current runtime during migration."},"balance":{"status":"provisional","pass":"v0.5.30","notes":"Initial archetype-based values. Tune freely during balance testing."}},"yurei":{"schema_version":2,"id":"yurei","display_name":"Yurei","title":null,"role":"enemy","archetype":"ghost_skirmisher","element":"Spirit","rarity":null,"collection":{"owned":false,"inventory_visible":false,"battle_ready":true},"stats":{"level":1,"hp":110,"attack":15,"defense":8,"speed":36},"combat":{"mark":"G","movement_range":76,"basic_shape":{"type":"circle","r":76},"jutsu_shape":null,"chakra_max":0},"render":{"scale":1.0},"abilities":{"basic":{"id":"spirit_claw","name":"Spirit Claw","damage_multiplier":1.0,"delivery":"melee"},"jutsu":null},"assets":{"art":null,"card":null,"portrait":"portraits/","icon":"icons/","sprites":{"idle":"sprites/runtime/idle/","attack_basic":"sprites/runtime/attack/","recoil":"sprites/runtime/recoil/","jutsu":"sprites/runtime/jutsu/"},"vfx":"vfx/"},"readiness":{"collection":false,"battle":true,"idle":true,"basic_attack":false,"jutsu":false,"recoil":false,"notes":"Existing ghost enemy; stats remain in current runtime during migration."},"balance":{"status":"provisional","pass":"v0.5.30","notes":"Initial archetype-based values. Tune freely during balance testing."}},"onre":{"schema_version":2,"id":"onre","display_name":"Onre","title":null,"role":"enemy","archetype":"ghost_skirmisher","element":"Spirit","rarity":null,"collection":{"owned":false,"inventory_visible":false,"battle_ready":true},"stats":{"level":1,"hp":100,"attack":14,"defense":8,"speed":34},"combat":{"mark":"G","movement_range":72,"basic_shape":{"type":"c

--- match 8 @ 24616021 ---
ike","damage_multiplier":1.0,"delivery":"melee"},"jutsu":null},"assets":{"art":null,"card":null,"portrait":"portraits/","icon":"icons/","sprites":{"idle":"sprites/runtime/idle/","attack_basic":"sprites/runtime/attack/","recoil":"sprites/runtime/recoil/","jutsu":"sprites/runtime/jutsu/"},"vfx":"vfx/"},"readiness":{"collection":false,"battle":true,"idle":true,"basic_attack":false,"jutsu":false,"recoil":false,"notes":"Existing ghost enemy; stats remain in current runtime during migration."},"balance":{"status":"provisional","pass":"v0.5.30","notes":"Initial archetype-based values. Tune freely during balance testing."}},"yurei":{"schema_version":2,"id":"yurei","display_name":"Yurei","title":null,"role":"enemy","archetype":"ghost_skirmisher","element":"Spirit","rarity":null,"collection":{"owned":false,"inventory_visible":false,"battle_ready":true},"stats":{"level":1,"hp":110,"attack":15,"defense":8,"speed":36},"combat":{"mark":"G","movement_range":76,"basic_shape":{"type":"circle","r":76},"jutsu_shape":null,"chakra_max":0},"render":{"scale":1.0},"abilities":{"basic":{"id":"spirit_claw","name":"Spirit Claw","damage_multiplier":1.0,"delivery":"melee"},"jutsu":null},"assets":{"art":null,"card":null,"portrait":"portraits/","icon":"icons/","sprites":{"idle":"sprites/runtime/idle/","attack_basic":"sprites/runtime/attack/","recoil":"sprites/runtime/recoil/","jutsu":"sprites/runtime/jutsu/"},"vfx":"vfx/"},"readiness":{"collection":false,"battle":true,"idle":true,"basic_attack":false,"jutsu":false,"recoil":false,"notes":"Existing ghost enemy; stats remain in current runtime during migration."},"balance":{"status":"provisional","pass":"v0.5.30","notes":"Initial archetype-based values. Tune freely during balance testing."}},"onre":{"schema_version":2,"id":"onre","display_name":"Onre","title":null,"role":"enemy","archetype":"ghost_skirmisher","element":"Spirit","rarity":null,"collection":{"owned":false,"inventory_visible":false,"battle_ready":true},"stats":{"level":1,"hp":100,"attack":14,"defense":8,"speed":34},"combat":{"mark":"G","movement_range":72,"basic_shape":{"type":"circle","r":82},"jutsu_shape":null,"chakra_max":0},"render":{"scale":1.0},"abilities":{"basic":{"id":"ghost_swipe","name":"Ghost Swipe","damage_multiplier":1.0,"delivery":"melee"},"jutsu":null},"assets":{"art":null,"card":null,"portrait":"portraits/","icon":"icons/","sprites":{"idle":"sprites/runtime/idle/","attack_basic":"sprites/runtime/attack/","recoil":"sprites/runtime/recoil/","jutsu":"sprites/runtime/jutsu/"},"vfx":"vfx/"},"readiness":{"collection":false,"battle":true,"idle":true,"basic_attack":false,"jutsu":false,"recoil":false,"notes":"Existing ghost enemy; stats remain in current runtime during migration."},"balance":{"status":"provisional","pass":"v0.5.30","notes":"Initial archetype-based values. Tune freely during balance testing."}}};</script>
<script>
const cvs=document.getElementById('game');
const LOGICAL_W=480,LOGICAL_H=720;
const RENDER_DPR=Math.min(window.devicePixelRatio||1,2);
cvs.width=Math.round(LOGICAL_W*RENDER_DPR);
cvs.height=Math.round(LOGICAL_H*RENDER_DPR);
const ctx=cvs.getContext('2d');
ctx.setTransform(RENDER_DPR,0,0,RENDER_DPR,0,0);
const W=LOGICAL_W,H=LOGICAL_H;
const 

--- match 9 @ 24617132 ---
t Claw","damage_multiplier":1.0,"delivery":"melee"},"jutsu":null},"assets":{"art":null,"card":null,"portrait":"portraits/","icon":"icons/","sprites":{"idle":"sprites/runtime/idle/","attack_basic":"sprites/runtime/attack/","recoil":"sprites/runtime/recoil/","jutsu":"sprites/runtime/jutsu/"},"vfx":"vfx/"},"readiness":{"collection":false,"battle":true,"idle":true,"basic_attack":false,"jutsu":false,"recoil":false,"notes":"Existing ghost enemy; stats remain in current runtime during migration."},"balance":{"status":"provisional","pass":"v0.5.30","notes":"Initial archetype-based values. Tune freely during balance testing."}},"onre":{"schema_version":2,"id":"onre","display_name":"Onre","title":null,"role":"enemy","archetype":"ghost_skirmisher","element":"Spirit","rarity":null,"collection":{"owned":false,"inventory_visible":false,"battle_ready":true},"stats":{"level":1,"hp":100,"attack":14,"defense":8,"speed":34},"combat":{"mark":"G","movement_range":72,"basic_shape":{"type":"circle","r":82},"jutsu_shape":null,"chakra_max":0},"render":{"scale":1.0},"abilities":{"basic":{"id":"ghost_swipe","name":"Ghost Swipe","damage_multiplier":1.0,"delivery":"melee"},"jutsu":null},"assets":{"art":null,"card":null,"portrait":"portraits/","icon":"icons/","sprites":{"idle":"sprites/runtime/idle/","attack_basic":"sprites/runtime/attack/","recoil":"sprites/runtime/recoil/","jutsu":"sprites/runtime/jutsu/"},"vfx":"vfx/"},"readiness":{"collection":false,"battle":true,"idle":true,"basic_attack":false,"jutsu":false,"recoil":false,"notes":"Existing ghost enemy; stats remain in current runtime during migration."},"balance":{"status":"provisional","pass":"v0.5.30","notes":"Initial archetype-based values. Tune freely during balance testing."}}};</script>
<script>
const cvs=document.getElementById('game');
const LOGICAL_W=480,LOGICAL_H=720;
const RENDER_DPR=Math.min(window.devicePixelRatio||1,2);
cvs.width=Math.round(LOGICAL_W*RENDER_DPR);
cvs.height=Math.round(LOGICAL_H*RENDER_DPR);
const ctx=cvs.getContext('2d');
ctx.setTransform(RENDER_DPR,0,0,RENDER_DPR,0,0);
const W=LOGICAL_W,H=LOGICAL_H;
const phaseEl=document.getElementById('phase'),logEl=document.getElementById('log'),statusEl=document.getElementById('status'),meter=document.getElementById('meter');
const normalBtn=document.getElementById('normal'),jutsuBtn=document.getElementById('jutsu'),swapBtn=document.getElementById('swap');
const bossHpHud=document.getElementById('bossHpHud'),bossHpFill=document.getElementById('bossHpFill');
const meteorScreenFlash=document.getElementById('meteorScreenFlash');
function getMeteorScreenFlash(){return meteorScreenFlash||document.getElementById('meteorScreenFlash');}
const menuScreen=document.getElementById('menuScreen'),battleScreen=document.getElementById('battleScreen'),level1Btn=document.getElementById('level1Btn'),boss1Btn=document.getElementById('boss1Btn'),tacticalTicker=document.getElementById('tacticalTicker');
const summonScreen=document.getElementById('summonScreen'),summonsBtn=document.getElementById('summonsBtn'),summonBackBtn=document.getElementById('summonBackBtn');
const singleSummonBtn=document.getElementById('singleSummonBtn'),multiSummonBtn=document.getElementById('multiSummonB

--- match 10 @ 43515211 ---
ype,shape,jutsuShape,jutsuCost,jutsuDamage,linkBuff,hp:maxHp,maxHp,chakra:2,maxChakra,rotation:0}
}

// v0.6.7 DATA AUTHORITY
// Canonical stats/roles/shapes/ability costs live in each unit's data/unit.json.
// The standalone embeds a generated copy in window.BLAZING_UNIT_DATA so mobile local-file play needs no fetch().
const UNIT_DATA=window.BLAZING_UNIT_DATA||{};
const UNIT_NAME_TO_ID=Object.freeze(Object.fromEntries(Object.entries(UNIT_DATA).map(([id,d])=>[d.display_name,id])));
function canonicalUnit(nameOrId){
 const id=UNIT_DATA[nameOrId]?nameOrId:UNIT_NAME_TO_ID[nameOrId];
 const d=id?UNIT_DATA[id]:null;
 if(!d)throw new Error(`Unknown canonical unit: ${nameOrId}`);
 return d;
}
function runtimeDefinition(nameOrId){
 const d=canonicalUnit(nameOrId),basic=d.abilities.basic||{},jutsu=d.abilities.jutsu||{};
 return{
  mark:d.combat.mark,speed:d.stats.speed,attack:d.stats.attack,defense:d.stats.defense,
  hp:d.stats.hp,maxHp:d.stats.hp,shape:d.combat.basic_shape,jutsuShape:d.combat.jutsu_shape||{type:'circle',r:0},
  maxChakra:d.combat.chakra_max||0,jutsuCost:jutsu.cost??99,
  jutsuDamage:jutsu?Math.round(d.stats.attack*(jutsu.damage_multiplier||1)):0,
  jutsuName:jutsu?.name||'Jutsu',archetype:d.archetype,render:d.render||{},
  linkBuff:d.combat.link_buff||null
 };
}
const BATTLE_ROSTER=Object.freeze(Object.fromEntries(
 ['crimson','subzero','lebee','senku','anubis']
   .filter(id=>UNIT_DATA[id])
   .map(id=>[UNIT_DATA[id].display_name,runtimeDefinition(id)])
));
function makeRosterUnit(name,opts={}){
 const d=BATTLE_ROSTER[name];
 if(!d)throw new Error(`Unknown battle unit: ${name}`);
 const u=U(name,d.mark,d.speed,d.attack,d.shape,d.jutsuShape,d.jutsuCost,d.jutsuDamage,d.linkBuff||null,d.maxHp,d.maxChakra,d.defense,d.archetype);
 u.jutsuName=d.jutsuName||'Jutsu';
 u.renderScale=d.render?.scale||1;
 const start=opts.startingChakra??2;
 u.chakra=start==='full'?u.maxChakra:Math.max(0,Math.min(u.maxChakra,start));
 return u;
}
function makeEnemyFromData(id,name,x,y,mark,overrides={}){
 const d=canonicalUnit(id),base=d.stats,combat=d.combat;
 const hp=overrides.hp??base.hp;
 return{name:name||d.display_name,spriteKey:d.display_name,mark:mark||combat.mark,x,y,r:overrides.r??19,
  hp,maxHp:hp,speed:overrides.speed??base.speed,attack:overrides.attack??base.attack,defense:overrides.defense??base.defense,
  gauge:0,shape:overrides.shape??combat.basic_shape,archetype:d.archetype};
}

// v0.5.19 core-roster whitelist. Legacy units stay in BATTLE_ROSTER for rollback only.
const ACTIVE_PLAYABLE_UNITS=Object.freeze(['Crimson','Sub-Zero','Lebee','Senku']);
const DEFAULT_ACTIVE_TEAM=Object.freeze(['Crimson','Lebee','Sub-Zero']);

function validatePlayableRosterRegistration(){
 const missing=ACTIVE_PLAYABLE_UNITS.filter(name=>!BATTLE_ROSTER[name]);
 if(missing.length){
   console.error('Playable roster registration missing:',missing);
   return false;
 }
 return true;
}
const PLAYABLE_ROSTER_VALID=validatePlayableRosterRegistration();
const TEAM_STORAGE_KEY='blazingBattle.activeTeam.v1';

function validActiveTeam(team){
 return Array.isArray(team)
   && team.length===3
   && new Set(team).size===3
   && team.every(name=>ACTIVE_PLAYABLE_UNITS.includes(name));
}
func

--- match 11 @ 43521605 ---
le:true,battleMode:'boss',
  pairs:buildPlayerPairs(585),
  enemies:[
   {
    name:'Anubis',spriteKey:'Anubis',mark:canonicalUnit('anubis').combat.mark,
    x:240,y:195,r:29,
    hp:canonicalUnit('anubis').stats.hp,maxHp:canonicalUnit('anubis').stats.hp,
    speed:canonicalUnit('anubis').stats.speed,attack:canonicalUnit('anubis').stats.attack,defense:canonicalUnit('anubis').stats.defense,gauge:0,
    shape:canonicalUnit('anubis').combat.basic_shape,
    boss:true,sizeScale:canonicalUnit('anubis').render.battle_size_scale||2.0625
   }
  ]
 };
}

let S=fresh();

const front=p=>p.units[p.active], back=p=>p.units[1-p.active];
const pairAlive=p=>p.units.some(u=>u.hp>0);
const alivePairs=()=>S.pairs.filter(pairAlive);
const aliveEnemies=()=>S.enemies.filter(e=>e.hp>0);
const NORMAL_ATTACK_SHAPE={type:'circle',r:43};
const normalShape=()=>NORMAL_ATTACK_SHAPE;
const attackShape=u=>{
 // Strict separation:
 // NORMAL -> unit's canonical combat.basic_shape
 // JUTSU  -> unit's canonical combat.jutsu_shape
 if(S.action==='jutsu')return u.jutsuShape;
 return u.shape||normalShape();
};
function attackProxy(u){return {...u,shape:attackShape(u)}}

// Ability-tier lookup must live at battle scope because both the ground-field
// pass and the later enemy target-feedback pass use it.
function isUltimateAction(unit){
 if(!unit)return false;
 try{
   // canonicalUnit already resolves both IDs and display names through UNIT_NAME_TO_ID.
   const ability=canonicalUnit(unit.name)?.abilities?.jutsu;
   return !!(ability?.ultimate===true || ability?.category==='ultimate' || ability?.tier==='ultimate');
 }catch(_){
   return false;
 }
}

function isAllySupportJutsu(unit){
 if(!unit)return false;
 try{
   const ability=canonicalUnit(unit.name)?.abilities?.jutsu;
   return ability?.target==='all_allies' || ability?.effect==='heal_full';
 }catch(_){
   return false;
 }
}


function ensureFront(p){
 if(front(p).hp>0)return true;
 if(front(p).hp<=0 && koAnimating(front(p).name))return true;
 if(back(p).hp>0){p.active=1-p.active;return true}
 return false
}
function fxMap(){
 if(!S.anim)S.anim={positions:{}};
 if(!S.anim.unitFX)S.anim.unitFX={};
 return S.anim.unitFX;
}
function unitFX(name){
 return S.anim?.unitFX?.[name]||null;
}
function koAnimating(name){
 let fx=unitFX(name);
 return !!(fx&&fx.kind==='ko'&&(performance.now()-fx.start)<fx.duration);
}
function renderWhileKO(name){
 let fx=unitFX(name);
 return !!(fx&&fx.kind==='ko'&&(performance.now()-fx.start)<fx.duration+90);
}
function startHitReaction(name,dir=1,strong=false){
 fxMap()[name]={kind:'hit',start:performance.now(),duration:strong?230:180,dir:dir||1,strong:!!strong};
}
function startKOReaction(name,dir=1){
 fxMap()[name]={kind:'ko',start:performance.now(),duration:520,dir:dir||1};
}
function pairRenderable(p){
 return pairAlive(p)||renderWhileKO(front(p).name)||renderWhileKO(back(p).name);
}
function handleKO(p,defeatedName,dir=1){
 addFloat(p.x,p.y-58,'KO','#ff7272');
 startKOReaction(defeatedName,dir);
 p.gauge=0;
 if(back(p).hp>0){
   S.log=`${defeatedName} was knocked out — ${back(p).name} is preparing to move to the front.`;
   setTimeout(()=>{
     if(front(p).name===defeatedName && back(p).hp>0){
       p.ac

===== Math.atan2 =====

--- match 1 @ 43525092 ---
6,'KO','#ff7272');
 startKOReaction(e.name,dir);
 e.gauge=0;
 S.log=`${e.name} was defeated.`;
}

function local(p,u,e){let a=-(u.rotation||0),dx=e.x-p.x,dy=e.y-p.y;return{x:dx*Math.cos(a)-dy*Math.sin(a),y:dx*Math.sin(a)+dy*Math.cos(a)}}
function circleRect(q,r,hw,hh){let x=clamp(q.x,-hw,hw),y=clamp(q.y,-hh,hh);return Math.hypot(q.x-x,q.y-y)<=r}
function hits(p,u,e){let s=u.shape,q=local(p,u,e),er=e.r||20;
 if(s.type==='screen_rect')return e.x>=BATTLE_BOUNDS.left&&e.x<=BATTLE_BOUNDS.right&&e.y>=BATTLE_BOUNDS.top&&e.y<=BATTLE_BOUNDS.bottom;
 if(s.type==='circle')return d(p,e)<=s.r+er;
 if(s.type==='rect'){
   const rq={x:q.x-(s.offset_x||0),y:q.y-(s.offset_y||0)};
   return circleRect(rq,er,s.w/2,s.h/2);
 }
 if(s.type==='square')return circleRect(q,er,s.s/2,s.s/2);
 if(s.type==='cross')return circleRect(q,er,s.l/2,s.t/2)||circleRect(q,er,s.t/2,s.l/2);
 if(s.type==='donut'){let z=Math.hypot(q.x,q.y);return z+er>=s.i&&z-er<=s.o}
 if(s.type==='cone'){let z=Math.hypot(q.x,q.y),ang=Math.abs(Math.atan2(q.y,q.x)),pad=Math.asin(Math.min(1,er/Math.max(z,.01)));return q.x+er>=0&&z-er<=s.r&&ang<=s.a/2+pad}
 return false}
function drawField(){
  const img=S.bossBattle?ANUBIS_PORTRAIT_MAP:LEVEL1_PORTRAIT_MAP;
  if(img.complete && img.naturalWidth>0){
    ctx.imageSmoothingEnabled=true;
    // Portrait maps are authored for the phone battlefield: crop only if aspect ratios differ slightly.
    const ir=img.naturalWidth/img.naturalHeight, cr=W/H;
    let sx=0,sy=0,sw=img.naturalWidth,sh=img.naturalHeight;
    if(ir>cr){sw=img.naturalHeight*cr;sx=(img.naturalWidth-sw)/2;}
    else if(ir<cr){sh=img.naturalWidth/cr;sy=(img.naturalHeight-sh)/2;}
    // Camera zoom: retain the same battlefield coordinates, but crop the art
    // slightly tighter so combat reads closer on a phone.
    const baseSw=sw,baseSh=sh;
    sw=baseSw/MAP_ZOOM; sh=baseSh/MAP_ZOOM;
    sx+=(baseSw-sw)/2; sy+=(baseSh-sh)/2;
    ctx.drawImage(img,sx,sy,sw,sh,0,0,W,H);
    ctx.fillStyle='rgba(7,10,12,.025)';ctx.fillRect(0,0,W,H);
    return;
  }
  // Keep canvas transparent until the stage image is decoded; CSS background is the fallback.
  ctx.clearRect(0,0,W,H);
}
function drawShape(p,u,color,alpha=.22,glow=false,visualOffsetY=0){
 let s=u.shape;
 let pulse=1+Math.sin(performance.now()/180)*0.014;
 let breathe=.5+.5*Math.sin(performance.now()/230);

 // Battlefield AOE: fixed screen-space rectangle, not centered on the caster.
 if(s.type==='screen_rect'){
   const pad=s.padding??18;
   const x=BATTLE_BOUNDS.left+pad,y=BATTLE_BOUNDS.top+pad;
   const w=(BATTLE_BOUNDS.right-BATTLE_BOUNDS.left)-pad*2;
   const h=(BATTLE_BOUNDS.bottom-BATTLE_BOUNDS.top)-pad*2;
   ctx.save();
   const ultimateRed=color==='#ff3548'||color==='#ff3348';
   ctx.shadowColor=color;ctx.shadowBlur=glow?18:6;
   ctx.fillStyle=glow
     ? (ultimateRed?'rgba(255,53,72,.10)':'rgba(69,215,255,.10)')
     : 'rgba(255,255,255,.045)';
   ctx.strokeStyle=glow
     ? (ultimateRed?`rgba(255,110,126,${.82+.10*breathe})`:`rgba(132,235,255,${.80+.12*breathe})`)
     : 'rgba(255,255,255,.78)';
   ctx.lineWidth=glow?2.2:1.25;
   ctx.beginPath();ctx.roundRect(x,y,w,h,13);ctx.fill();ctx.stroke();
   // inner line makes the full-field AOE readable w

--- match 2 @ 43530485 ---
ing/range logic unchanged.
 if(name==='Crimson'||name==='Anubis')dir*=-1;
 return dir;
}

function nearestEnemyPoint(x,y){
 let alive=S.enemies.filter(e=>e.hp>0);
 if(!alive.length)return null;
 return alive.reduce((best,e)=>!best||d({x,y},e)<d({x,y},best)?e:best,null);
}


// v0.5.10: sprites are normalized during the asset-production step.
// Never scan/crop millions of pixels on the phone gameplay thread.
function getRenderableSprite(asset,spriteName=null){ return asset; }

function spriteDims(asset){
 return{
   w:asset?(asset.naturalWidth||asset.width||0):0,
   h:asset?(asset.naturalHeight||asset.height||0):0
 };
}

function nearestPlayerPoint(x,y){
 let alive=S.pairs.filter(p=>pairAlive(p));
 if(!alive.length)return null;
 return alive.reduce((best,p)=>!best||d({x,y},p)<d({x,y},best)?p:best,null);
}

function updateFacing(){
 S.pairs.forEach(pair=>{
   pair.units.forEach(u=>{
     let pos=actorPos(pair,u);
     let target=nearestEnemyPoint(pos.x,pos.y);
     if(target)u.rotation=Math.atan2(target.y-pos.y,target.x-pos.x);
   });
 });

 S.enemies.forEach(e=>{
   let target=nearestPlayerPoint(e.x,e.y);
   if(target)e.rotation=Math.atan2(target.y-e.y,target.x-e.x);
 });
}

function battleSpriteFor(name){
 return (name && SPRITE_DATA[name]) ? SPRITE_DATA[name] : null;
}
function drawUnit(x,y,label,color,active=false,visual=null,name=null,isEnemy=false,flipX=1,sizeScale=1){
 // Body sprites must always begin from a clean canvas state.
 ctx.globalCompositeOperation='source-over';
 if(!Number.isFinite(ctx.globalAlpha) || ctx.globalAlpha<=0)ctx.globalAlpha=1;
 let scale=visual?.scale||1,rot=visual?.rot||0,lift=visual?.lift||0;
 let activePulse=1;
 let fx=name?unitFX(name):null;

 // Deterministic per-character idle timing offset.
 // This MUST be initialized before any transform code references it.
 let idlePhase=0;
 if(name){
   for(let i=0;i<name.length;i++)idlePhase+=name.charCodeAt(i)*(i+3);
 }

 if(active && !visual){
   activePulse=1+Math.sin(performance.now()/170)*.018;
 }

 // PLAYER GROUND SHADOW — v0.5.18
 // The sprite artwork is anchored with its feet at local y≈12. Keep the shadow
 // directly on that contact point instead of floating well below the character.
 // Width follows the same render scale used by the body so larger units receive
 // a proportionally larger footprint without turning into a detached blob.
 if(!isEnemy){
   let liftRatio=clamp(lift/17,0,1);
   let bodyScale=(sizeScale||1)*UNIT_RENDER_SCALE*1.07*PLAYER_RENDER_BONUS*(name==='Crimson'?CRIMSON_RENDER_BONUS:1);
   let shadowW=clamp(10.5*bodyScale,10,18.5);
   let shadowH=clamp(2.55*bodyScale,2.5,4.25);
   let shadowAlpha=.24-.08*liftRatio;

   ctx.save();
   ctx.translate(x,y+11.5);
   ctx.fillStyle=`rgba(0,0,0,${shadowAlpha})`;
   ctx.shadowColor=`rgba(0,0,0,${.14+.04*(1-liftRatio)})`;
   ctx.shadowBlur=1.5+4.5*liftRatio;
   ctx.beginPath();
   ctx.ellipse(0,0,shadowW,shadowH,0,0,Math.PI*2);
   ctx.fill();
   ctx.restore();
 }

 ctx.save();
 ctx.translate(x,y-lift);

 if(!visual){
   let t=(performance.now()+idlePhase*5);
   // Lebee's supplied idle sheet already contains floating motion.
   if(name!=='Lebee'){
     ctx.translate(0,Math.sin(t/420)*.55);
     ctx.rotate(Ma

--- match 3 @ 43530632 ---
ter(e=>e.hp>0);
 if(!alive.length)return null;
 return alive.reduce((best,e)=>!best||d({x,y},e)<d({x,y},best)?e:best,null);
}


// v0.5.10: sprites are normalized during the asset-production step.
// Never scan/crop millions of pixels on the phone gameplay thread.
function getRenderableSprite(asset,spriteName=null){ return asset; }

function spriteDims(asset){
 return{
   w:asset?(asset.naturalWidth||asset.width||0):0,
   h:asset?(asset.naturalHeight||asset.height||0):0
 };
}

function nearestPlayerPoint(x,y){
 let alive=S.pairs.filter(p=>pairAlive(p));
 if(!alive.length)return null;
 return alive.reduce((best,p)=>!best||d({x,y},p)<d({x,y},best)?p:best,null);
}

function updateFacing(){
 S.pairs.forEach(pair=>{
   pair.units.forEach(u=>{
     let pos=actorPos(pair,u);
     let target=nearestEnemyPoint(pos.x,pos.y);
     if(target)u.rotation=Math.atan2(target.y-pos.y,target.x-pos.x);
   });
 });

 S.enemies.forEach(e=>{
   let target=nearestPlayerPoint(e.x,e.y);
   if(target)e.rotation=Math.atan2(target.y-e.y,target.x-e.x);
 });
}

function battleSpriteFor(name){
 return (name && SPRITE_DATA[name]) ? SPRITE_DATA[name] : null;
}
function drawUnit(x,y,label,color,active=false,visual=null,name=null,isEnemy=false,flipX=1,sizeScale=1){
 // Body sprites must always begin from a clean canvas state.
 ctx.globalCompositeOperation='source-over';
 if(!Number.isFinite(ctx.globalAlpha) || ctx.globalAlpha<=0)ctx.globalAlpha=1;
 let scale=visual?.scale||1,rot=visual?.rot||0,lift=visual?.lift||0;
 let activePulse=1;
 let fx=name?unitFX(name):null;

 // Deterministic per-character idle timing offset.
 // This MUST be initialized before any transform code references it.
 let idlePhase=0;
 if(name){
   for(let i=0;i<name.length;i++)idlePhase+=name.charCodeAt(i)*(i+3);
 }

 if(active && !visual){
   activePulse=1+Math.sin(performance.now()/170)*.018;
 }

 // PLAYER GROUND SHADOW — v0.5.18
 // The sprite artwork is anchored with its feet at local y≈12. Keep the shadow
 // directly on that contact point instead of floating well below the character.
 // Width follows the same render scale used by the body so larger units receive
 // a proportionally larger footprint without turning into a detached blob.
 if(!isEnemy){
   let liftRatio=clamp(lift/17,0,1);
   let bodyScale=(sizeScale||1)*UNIT_RENDER_SCALE*1.07*PLAYER_RENDER_BONUS*(name==='Crimson'?CRIMSON_RENDER_BONUS:1);
   let shadowW=clamp(10.5*bodyScale,10,18.5);
   let shadowH=clamp(2.55*bodyScale,2.5,4.25);
   let shadowAlpha=.24-.08*liftRatio;

   ctx.save();
   ctx.translate(x,y+11.5);
   ctx.fillStyle=`rgba(0,0,0,${shadowAlpha})`;
   ctx.shadowColor=`rgba(0,0,0,${.14+.04*(1-liftRatio)})`;
   ctx.shadowBlur=1.5+4.5*liftRatio;
   ctx.beginPath();
   ctx.ellipse(0,0,shadowW,shadowH,0,0,Math.PI*2);
   ctx.fill();
   ctx.restore();
 }

 ctx.save();
 ctx.translate(x,y-lift);

 if(!visual){
   let t=(performance.now()+idlePhase*5);
   // Lebee's supplied idle sheet already contains floating motion.
   if(name!=='Lebee'){
     ctx.translate(0,Math.sin(t/420)*.55);
     ctx.rotate(Math.sin(t/610)*.004);
   }
 }

 if(fx){
   let elapsed=performance.now()-fx.start;
   if(elapsed<fx.duration){
     if(fx.kind==='hit'){
       let 

--- match 4 @ 43555289 ---
turn;
   if(renderWhileKO(e.name))return;
   const pos=(S.anim&&S.anim.positions&&S.anim.positions[e.name])||e;

   ctx.save();
   ctx.globalAlpha=S.phase==='player'?.92:1;
   ctx.fillStyle='#222';
   ctx.fillRect(pos.x-27,pos.y+29,54,6);
   ctx.fillStyle='#ffbd4a';
   ctx.fillRect(pos.x-27,pos.y+29,54*(e.hp/e.maxHp),6);
   ctx.restore();
 });

 S.floaters.forEach(f=>{
   ctx.save();
   ctx.globalAlpha=f.life??1;
   if(f.kind==='screenFlash'){
     const elapsed=performance.now()-f.start;
     const t=clamp(elapsed/f.duration,0,1);
     ctx.setTransform(1,0,0,1,0,0);
     ctx.globalCompositeOperation='source-over';
     ctx.fillStyle=f.color||'#fff';
     ctx.globalAlpha=f.fade?Math.max(0,1-t):1;
     ctx.fillRect(0,0,W,H);
   }else if(f.kind==='lebeeStarProjectile'){
     const t=clamp((performance.now()-f.start)/f.duration,0,1);
     const e=1-Math.pow(1-t,2.4);
     const x=f.from.x+(f.to.x-f.from.x)*e;
     const y=f.from.y+(f.to.y-f.from.y)*e-5*Math.sin(t*Math.PI);
     const ang=Math.atan2(f.to.y-f.from.y,f.to.x-f.from.x);
     ctx.translate(x,y);ctx.rotate(ang);
     if(LEBEE_STAR_PROJECTILE.complete&&LEBEE_STAR_PROJECTILE.naturalWidth>0){
       const h=24+8*t,ratio=LEBEE_STAR_PROJECTILE.naturalWidth/LEBEE_STAR_PROJECTILE.naturalHeight,w=h*ratio;
       ctx.globalCompositeOperation='screen';
       ctx.shadowColor='#ff78df';ctx.shadowBlur=10;
       ctx.drawImage(LEBEE_STAR_PROJECTILE,-w*.6,-h*.5,w,h);
       ctx.globalCompositeOperation='source-over';
     }
   }else if(f.kind==='senkuChemProjectile'){
     const t=clamp((performance.now()-f.start)/f.duration,0,1);
     const e=1-Math.pow(1-t,2.15);
     const x=f.from.x+(f.to.x-f.from.x)*e;
     const y=f.from.y+(f.to.y-f.from.y)*e-7*Math.sin(t*Math.PI);
     const idx=Math.min(SENKU_CHEM_PROJECTILE_FRAMES.length-1,Math.floor(t*SENKU_CHEM_PROJECTILE_FRAMES.length));
     const img=SENKU_CHEM_PROJECTILE_FRAMES[idx];
     if(img?.complete&&img.naturalWidth>0){
       const h=30+5*Math.sin(t*Math.PI),ratio=img.naturalWidth/img.naturalHeight,w=h*ratio;
       ctx.translate(x,y);
       ctx.shadowColor='#62ff63';ctx.shadowBlur=10;
       ctx.drawImage(img,-w/2,-h/2,w,h);
     }
   }else if(f.kind==='senkuChemImpact'){
     const t=clamp((performance.now()-f.start)/f.duration,0,1);
     const idx=Math.min(SENKU_CHEM_IMPACT_FRAMES.length-1,Math.floor(t*SENKU_CHEM_IMPACT_FRAMES.length));
     const img=SENKU_CHEM_IMPACT_FRAMES[idx];
     if(img?.complete&&img.naturalWidth>0){
       const h=72+45*Math.sin(Math.min(1,t)*Math.PI*.72);
       const ratio=img.naturalWidth/img.naturalHeight,w=h*ratio;
       ctx.translate(f.x,f.y);
       ctx.globalAlpha=Math.max(0,1-Math.max(0,t-.75)/.25);
       ctx.shadowColor='#5dff6e';ctx.shadowBlur=12;
       ctx.drawImage(img,-w/2,-h*.70,w,h);
     }
   }else if(f.kind==='senkuBombProjectile'){
     const t=clamp((performance.now()-f.start)/f.duration,0,1);
     const e=t<.5 ? 2*t*t : 1-Math.pow(-2*t+2,2)/2;
     const arc=f.arcHeight||86;
     const x=f.from.x+(f.to.x-f.from.x)*e;
     const baseY=f.from.y+(f.to.y-f.from.y)*e;
     const y=baseY-4*arc*t*(1-t);

     // Tangent direction of the parabolic trajectory.
     const dx=f.to.x-f.from.x;
     const dy=(f.t

--- match 5 @ 43557545 ---
t)/f.duration,0,1);
     const idx=Math.min(SENKU_CHEM_IMPACT_FRAMES.length-1,Math.floor(t*SENKU_CHEM_IMPACT_FRAMES.length));
     const img=SENKU_CHEM_IMPACT_FRAMES[idx];
     if(img?.complete&&img.naturalWidth>0){
       const h=72+45*Math.sin(Math.min(1,t)*Math.PI*.72);
       const ratio=img.naturalWidth/img.naturalHeight,w=h*ratio;
       ctx.translate(f.x,f.y);
       ctx.globalAlpha=Math.max(0,1-Math.max(0,t-.75)/.25);
       ctx.shadowColor='#5dff6e';ctx.shadowBlur=12;
       ctx.drawImage(img,-w/2,-h*.70,w,h);
     }
   }else if(f.kind==='senkuBombProjectile'){
     const t=clamp((performance.now()-f.start)/f.duration,0,1);
     const e=t<.5 ? 2*t*t : 1-Math.pow(-2*t+2,2)/2;
     const arc=f.arcHeight||86;
     const x=f.from.x+(f.to.x-f.from.x)*e;
     const baseY=f.from.y+(f.to.y-f.from.y)*e;
     const y=baseY-4*arc*t*(1-t);

     // Tangent direction of the parabolic trajectory.
     const dx=f.to.x-f.from.x;
     const dy=(f.to.y-f.from.y)-4*arc*(1-2*t);
     const angle=Math.atan2(dy,dx);

     const idx=Math.min(
       SENKU_BOMB_FRAMES.length-1,
       Math.floor(t*SENKU_BOMB_FRAMES.length)
     );
     const img=SENKU_BOMB_FRAMES[idx];
     if(img?.complete&&img.naturalWidth>0){
       const h=30,ratio=img.naturalWidth/img.naturalHeight,w=h*ratio;
       ctx.translate(x,y);
       ctx.rotate(angle);
       ctx.shadowColor='#ff9a32';
       ctx.shadowBlur=8;
       ctx.drawImage(img,-w/2,-h/2,w,h);
     }
   }else if(f.kind==='senkuExplosion'){
     const t=clamp((performance.now()-f.start)/f.duration,0,1);
     const img=SENKU_BASIC_STATIC_EXPLOSION;

     if(img?.complete&&img.naturalWidth>0){
       const hold=.30;
       const fade=t<=hold ? 1 : Math.max(0,1-(t-hold)/(1-hold));
       const pop=Math.min(1,t/.12);
       const scale=.90+.10*(1-Math.pow(1-pop,3));

       // Compact basic-attack impact.
       const baseH=58;
       const h=baseH*scale;
       const w=h*(img.naturalWidth/img.naturalHeight);

       // Exact target ground-point placement.
       const anchorX=.50;
       const anchorY=.96;
       const yNudge=3;

       ctx.globalAlpha*=fade;
       ctx.translate(f.x,f.y+yNudge);
       ctx.shadowColor='#ff9a32';
       ctx.shadowBlur=5*fade;
       ctx.drawImage(img,-w*anchorX,-h*anchorY,w,h);
     }
   }else if(f.kind==='lebeeMeteor'){
     const raw=(performance.now()-f.start)/f.duration;
     const t=clamp(raw,0,1);
     const e=t*t*(3-2*t);
     const x=f.from.x+(f.to.x-f.from.x)*e;
     const y=f.from.y+(f.to.y-f.from.y)*e;
     const img=LEBEE_METEOR_FRAMES[f.frameIndex%LEBEE_METEOR_FRAMES.length];
     ctx.translate(x,y);
     ctx.rotate((f.spin||.08)*(1-t)+(f.rot||0));
     if(img?.complete&&img.naturalWidth>0){
       const startH=f.startH||110,endH=f.endH||44;
       const h=startH+(endH-startH)*t,ratio=img.naturalWidth/img.naturalHeight,w=h*ratio;
       const tailFade=t<.84?1:Math.max(0,(1-t)/.16);
       ctx.globalAlpha=raw<0?0:tailFade;
       ctx.globalCompositeOperation='screen';
       ctx.shadowColor=f.frameIndex%3===0?'#ff61d8':f.frameIndex%3===1?'#58a8ff':'#ffc94e';
       ctx.shadowBlur=11+5*(1-t);
       ctx.drawImage(img,-w/2,-h/2,w,h);
       ctx.globalCompositeOperation='source-over';
     }

--- match 6 @ 43561609 ---
=Math.sin(Math.min(1,t)*Math.PI),w=220+72*pulse;
       const ratio=LEBEE_METEOR_IMPACT_COMBO.naturalHeight/LEBEE_METEOR_IMPACT_COMBO.naturalWidth,h=w*ratio;
       ctx.globalAlpha=t<.70?1:(1-t)/.30;ctx.shadowColor='#ffffff';ctx.shadowBlur=18;
       ctx.drawImage(LEBEE_METEOR_IMPACT_COMBO,-w/2,-h*.76,w,h);
     }
   }else if(f.kind==='lebeeMeteorAftermath'){
     const t=clamp((performance.now()-f.start)/f.duration,0,1);
     ctx.translate(f.x,f.y);
     if(LEBEE_METEOR_AFTERMATH.complete&&LEBEE_METEOR_AFTERMATH.naturalWidth>0){
       const w=210,ratio=LEBEE_METEOR_AFTERMATH.naturalHeight/LEBEE_METEOR_AFTERMATH.naturalWidth,h=w*ratio;
       ctx.globalAlpha=.68*(1-t);ctx.drawImage(LEBEE_METEOR_AFTERMATH,-w/2,-h*.70,w,h);
     }
   }else if(f.kind==='iceProjectile'){
     const t=clamp((performance.now()-f.start)/f.duration,0,1);
     const e=1-Math.pow(1-t,2.2);
     const x=f.from.x+(f.to.x-f.from.x)*e;
     const y=f.from.y+(f.to.y-f.from.y)*e-8*Math.sin(t*Math.PI);
     const ang=Math.atan2(f.to.y-f.from.y,f.to.x-f.from.x);
     ctx.translate(x,y);
     ctx.rotate(ang);
     // v0.5.23: use the supplied Freeze Blast projectile artwork rather than
     // the procedural placeholder orb. It grows through the three approved
     // projectile frames as it leaves Sub-Zero and closes on the target.
     const idx=t<.24?0:(t<.58?1:2);
     const img=FREEZE_PROJECTILE_FRAMES[idx];
     if(img&&img.complete&&img.naturalWidth>0){
       const targetH=18+15*t;
       const ratio=img.naturalWidth/img.naturalHeight;
       const targetW=targetH*ratio;
       ctx.globalCompositeOperation='screen';
       ctx.shadowColor='#72eaff';ctx.shadowBlur=11;
       ctx.drawImage(img,-targetW*.62,-targetH*.5,targetW,targetH);
       ctx.globalCompositeOperation='source-over';
     }
   }else if(f.kind==='frozenTarget'){
     const pulse=.72+.18*Math.sin(performance.now()/75);
     ctx.translate(f.x,f.y-17);
     ctx.globalCompositeOperation='screen';
     ctx.strokeStyle=`rgba(133,236,255,${pulse})`;ctx.fillStyle='rgba(82,185,255,.16)';ctx.lineWidth=2.5;
     ctx.shadowColor='#7feaff';ctx.shadowBlur=10;
     ctx.beginPath();
     ctx.moveTo(-19,18);ctx.lineTo(-14,-20);ctx.lineTo(-4,-31);ctx.lineTo(2,-22);ctx.lineTo(12,-34);ctx.lineTo(18,-13);ctx.lineTo(20,18);ctx.closePath();
     ctx.fill();ctx.stroke();
     ctx.globalCompositeOperation='source-over';
   }else{
     ctx.fillStyle=f.color||'#fff';
     ctx.font='bold 19px system-ui';
     ctx.fillText(f.text,f.x,f.y);
   }
   ctx.restore();
 });

 drawVictoryOverlay();

 updateUI();
 }catch(err){
   console.error('Blazing Battle render error:',err);
   phaseEl.textContent='Recovering renderer…';
 }
 requestAnimationFrame(render);
}

function updateMeter(){
 [...meter.querySelectorAll('.icon')].forEach(n=>n.remove());
 let entries=[];
 S.pairs.forEach((p,i)=>{if(pairAlive(p)){ensureFront(p);entries.push({label:front(p).mark,name:front(p).name,speed:front(p).speed,g:p.gauge,color:COLORS[i]})}});
 S.enemies.forEach(e=>{if(e.hp>0)entries.push({label:e.boss?'BOSS':'E'+e.mark,name:e.name,speed:e.speed,g:e.gauge,color:'#d84b54'})});
 entries.forEach(e=>{
 let n=document.createElement('div');
 n.className='icon';
 n.style.backg

--- match 7 @ 43603509 ---
nt(p);
   let dist=d(e,p);
   let hpRatio=u.hp/u.maxHp;
   let cluster=nearbyCount(p);
   let jitter=Math.random()*12;

   if(mode==='nearest')
     return dist*1.35 + hpRatio*30 - cluster*8 + jitter;
   if(mode==='weakest')
     return hpRatio*150 + dist*.35 - cluster*6 + jitter;
   if(mode==='cluster')
     return dist*.45 + hpRatio*35 - cluster*55 + jitter;

   // balanced
   return dist*.7 + hpRatio*75 - cluster*24 + jitter;
 }

 let target=[...candidates].sort((a,b)=>targetScore(a)-targetScore(b))[0];

 // Evaluate several approach angles instead of always taking the same straight-line slot.
 let angles=[];
 for(let i=0;i<10;i++)angles.push((Math.PI*2/10)*i);

 let best=null;
 angles.forEach(a=>{
   let radius=58+Math.random()*35;
   let pos=clampToBattlefield({
     x:target.x+Math.cos(a)*radius,
     y:target.y+Math.sin(a)*radius
   });

   // Score position by direct hits + mutual proximity links with enemy allies.
   let testEnemy={...e,x:pos.x,y:pos.y};
   testEnemy.rotation=Math.atan2(target.y-pos.y,target.x-pos.x);
   let hitCount=0;
   let comboSupport=0;

   candidates.forEach(p=>{
     let body={x:p.x,y:p.y,r:18};
     let fakeUnit={shape:normalShape(),rotation:testEnemy.rotation||0};
     if(hits(testEnemy,fakeUnit,body))hitCount++;
   });

   S.enemies.forEach(ally=>{
     if(ally===e||ally.hp<=0)return;

     let bodySelf={x:testEnemy.x,y:testEnemy.y,r:testEnemy.r||16};
     let bodyAlly={x:ally.x,y:ally.y,r:ally.r||16};

     let selfCoversAlly=hits(testEnemy,{shape:normalShape(),rotation:testEnemy.rotation||0},bodyAlly);
     let allyCoversSelf=hits(ally,{shape:normalShape(),rotation:ally.rotation||0},bodySelf);

     if(selfCoversAlly&&allyCoversSelf)comboSupport++;
   });

   let movement=d(e,pos);
   let targetDistance=d(pos,target);
   let score=
     hitCount*92 +
     comboSupport*38 -
     movement*.12 -
     targetDistance*.16 +
     Math.random()*10;

   if(!best || score>best.score)best={pos,score,hitCount,comboSupport};
 });

 let desired=best?.pos || clampToBattlefield({x:target.x-55,y:target.y});
 let from={x:e.x,y:e.y},start=performance.now(),dur=520;
 S.anim={positions:{}};

 S.log=`${e.name} is evaluating a ${mode} attack.`;

 function move(n){
   let t=Math.min(1,(n-start)/dur),q=t*t*(3-2*t);
   S.anim.positions[e.name]={
     x:from.x+(desired.x-from.x)*q,
     y:from.y+(desired.y-from.y)*q
   };

   if(t<1)requestAnimationFrame(move);
   else{
     let legal=clampToBattlefield(desired);
     e.x=legal.x;e.y=legal.y;
     e.rotation=Math.atan2(target.y-e.y,target.x-e.x);
     resolveEnemyOverlap(e);
     delete S.anim.positions[e.name];
     S.anim.showEnemyRange=e;
     setTimeout(()=>cpuAttack(e,target),360);
   }
 }
 requestAnimationFrame(move);
}
function cpuAttack(e,p){
 S.anim.showEnemyRange=null;

 let validTargets=S.pairs.filter(pair=>{
   if(!pairAlive(pair))return false;
   ensureFront(pair);
   let body={x:pair.x,y:pair.y,r:18};
   let fakeUnit={shape:normalShape(),rotation:e.rotation||0};
   return hits(e,fakeUnit,body)
 });

 // Safety fallback if movement somehow ends just outside its chosen target.
 if(!validTargets.length)validTargets=[p];

 let targetIndex=0;
 let anyCombo=false;

 function runTarg

--- match 8 @ 43605024 ---
lShape(),rotation:testEnemy.rotation||0},bodyAlly);
     let allyCoversSelf=hits(ally,{shape:normalShape(),rotation:ally.rotation||0},bodySelf);

     if(selfCoversAlly&&allyCoversSelf)comboSupport++;
   });

   let movement=d(e,pos);
   let targetDistance=d(pos,target);
   let score=
     hitCount*92 +
     comboSupport*38 -
     movement*.12 -
     targetDistance*.16 +
     Math.random()*10;

   if(!best || score>best.score)best={pos,score,hitCount,comboSupport};
 });

 let desired=best?.pos || clampToBattlefield({x:target.x-55,y:target.y});
 let from={x:e.x,y:e.y},start=performance.now(),dur=520;
 S.anim={positions:{}};

 S.log=`${e.name} is evaluating a ${mode} attack.`;

 function move(n){
   let t=Math.min(1,(n-start)/dur),q=t*t*(3-2*t);
   S.anim.positions[e.name]={
     x:from.x+(desired.x-from.x)*q,
     y:from.y+(desired.y-from.y)*q
   };

   if(t<1)requestAnimationFrame(move);
   else{
     let legal=clampToBattlefield(desired);
     e.x=legal.x;e.y=legal.y;
     e.rotation=Math.atan2(target.y-e.y,target.x-e.x);
     resolveEnemyOverlap(e);
     delete S.anim.positions[e.name];
     S.anim.showEnemyRange=e;
     setTimeout(()=>cpuAttack(e,target),360);
   }
 }
 requestAnimationFrame(move);
}
function cpuAttack(e,p){
 S.anim.showEnemyRange=null;

 let validTargets=S.pairs.filter(pair=>{
   if(!pairAlive(pair))return false;
   ensureFront(pair);
   let body={x:pair.x,y:pair.y,r:18};
   let fakeUnit={shape:normalShape(),rotation:e.rotation||0};
   return hits(e,fakeUnit,body)
 });

 // Safety fallback if movement somehow ends just outside its chosen target.
 if(!validTargets.length)validTargets=[p];

 let targetIndex=0;
 let anyCombo=false;

 function runTarget(){
   if(targetIndex>=validTargets.length){
     e.gauge=0;
     S.log=anyCombo
       ? `${e.name}'s side completed a combo attack across ${validTargets.length} target${validTargets.length===1?'':'s'}.`
       : `${e.name} hit ${validTargets.length} target${validTargets.length===1?'':'s'}.`;
     return finishAction()
   }

   let targetPair=validTargets[targetIndex++];
   if(!pairAlive(targetPair))return runTarget();
   ensureFront(targetPair);

   // Initiator first, then every other enemy whose current normal range
   // covers this same player target.
   let helpers=enemyComboMembers(targetPair,e).filter(enemy=>enemy!==e);
   if(helpers.length)anyCombo=true;
   let attackers=[e,...helpers];
   let attackerIndex=0;

   function runAttacker(){
     if(!pairAlive(targetPair) || attackerIndex>=attackers.length)
       return setTimeout(runTarget,100);

     ensureFront(targetPair);
     if(front(targetPair).hp<=0)return setTimeout(runTarget,100);
     let victim=front(targetPair);
     let attacker=attackers[attackerIndex++];

     let from={x:attacker.x,y:attacker.y},
         dx=targetPair.x-attacker.x,
         dy=targetPair.y-attacker.y,
         len=Math.max(1,Math.hypot(dx,dy)),
         to={
           x:targetPair.x-dx/len*42,
           y:targetPair.y-dy/len*42
         };

     animateLunge(
       attacker.name,from,to,
       ()=>{
         victim.hp=Math.max(0,victim.hp-attacker.attack);
         addImpactFlash(targetPair.x,targetPair.y-8,'#ff8b8b');
         addFloat(
    

===== atan2( =====

--- match 1 @ 43525097 ---
','#ff7272');
 startKOReaction(e.name,dir);
 e.gauge=0;
 S.log=`${e.name} was defeated.`;
}

function local(p,u,e){let a=-(u.rotation||0),dx=e.x-p.x,dy=e.y-p.y;return{x:dx*Math.cos(a)-dy*Math.sin(a),y:dx*Math.sin(a)+dy*Math.cos(a)}}
function circleRect(q,r,hw,hh){let x=clamp(q.x,-hw,hw),y=clamp(q.y,-hh,hh);return Math.hypot(q.x-x,q.y-y)<=r}
function hits(p,u,e){let s=u.shape,q=local(p,u,e),er=e.r||20;
 if(s.type==='screen_rect')return e.x>=BATTLE_BOUNDS.left&&e.x<=BATTLE_BOUNDS.right&&e.y>=BATTLE_BOUNDS.top&&e.y<=BATTLE_BOUNDS.bottom;
 if(s.type==='circle')return d(p,e)<=s.r+er;
 if(s.type==='rect'){
   const rq={x:q.x-(s.offset_x||0),y:q.y-(s.offset_y||0)};
   return circleRect(rq,er,s.w/2,s.h/2);
 }
 if(s.type==='square')return circleRect(q,er,s.s/2,s.s/2);
 if(s.type==='cross')return circleRect(q,er,s.l/2,s.t/2)||circleRect(q,er,s.t/2,s.l/2);
 if(s.type==='donut'){let z=Math.hypot(q.x,q.y);return z+er>=s.i&&z-er<=s.o}
 if(s.type==='cone'){let z=Math.hypot(q.x,q.y),ang=Math.abs(Math.atan2(q.y,q.x)),pad=Math.asin(Math.min(1,er/Math.max(z,.01)));return q.x+er>=0&&z-er<=s.r&&ang<=s.a/2+pad}
 return false}
function drawField(){
  const img=S.bossBattle?ANUBIS_PORTRAIT_MAP:LEVEL1_PORTRAIT_MAP;
  if(img.complete && img.naturalWidth>0){
    ctx.imageSmoothingEnabled=true;
    // Portrait maps are authored for the phone battlefield: crop only if aspect ratios differ slightly.
    const ir=img.naturalWidth/img.naturalHeight, cr=W/H;
    let sx=0,sy=0,sw=img.naturalWidth,sh=img.naturalHeight;
    if(ir>cr){sw=img.naturalHeight*cr;sx=(img.naturalWidth-sw)/2;}
    else if(ir<cr){sh=img.naturalWidth/cr;sy=(img.naturalHeight-sh)/2;}
    // Camera zoom: retain the same battlefield coordinates, but crop the art
    // slightly tighter so combat reads closer on a phone.
    const baseSw=sw,baseSh=sh;
    sw=baseSw/MAP_ZOOM; sh=baseSh/MAP_ZOOM;
    sx+=(baseSw-sw)/2; sy+=(baseSh-sh)/2;
    ctx.drawImage(img,sx,sy,sw,sh,0,0,W,H);
    ctx.fillStyle='rgba(7,10,12,.025)';ctx.fillRect(0,0,W,H);
    return;
  }
  // Keep canvas transparent until the stage image is decoded; CSS background is the fallback.
  ctx.clearRect(0,0,W,H);
}
function drawShape(p,u,color,alpha=.22,glow=false,visualOffsetY=0){
 let s=u.shape;
 let pulse=1+Math.sin(performance.now()/180)*0.014;
 let breathe=.5+.5*Math.sin(performance.now()/230);

 // Battlefield AOE: fixed screen-space rectangle, not centered on the caster.
 if(s.type==='screen_rect'){
   const pad=s.padding??18;
   const x=BATTLE_BOUNDS.left+pad,y=BATTLE_BOUNDS.top+pad;
   const w=(BATTLE_BOUNDS.right-BATTLE_BOUNDS.left)-pad*2;
   const h=(BATTLE_BOUNDS.bottom-BATTLE_BOUNDS.top)-pad*2;
   ctx.save();
   const ultimateRed=color==='#ff3548'||color==='#ff3348';
   ctx.shadowColor=color;ctx.shadowBlur=glow?18:6;
   ctx.fillStyle=glow
     ? (ultimateRed?'rgba(255,53,72,.10)':'rgba(69,215,255,.10)')
     : 'rgba(255,255,255,.045)';
   ctx.strokeStyle=glow
     ? (ultimateRed?`rgba(255,110,126,${.82+.10*breathe})`:`rgba(132,235,255,${.80+.12*breathe})`)
     : 'rgba(255,255,255,.78)';
   ctx.lineWidth=glow?2.2:1.25;
   ctx.beginPath();ctx.roundRect(x,y,w,h,13);ctx.fill();ctx.stroke();
   // inner line makes the full-field AOE readable wi

--- match 2 @ 43530490 ---
ange logic unchanged.
 if(name==='Crimson'||name==='Anubis')dir*=-1;
 return dir;
}

function nearestEnemyPoint(x,y){
 let alive=S.enemies.filter(e=>e.hp>0);
 if(!alive.length)return null;
 return alive.reduce((best,e)=>!best||d({x,y},e)<d({x,y},best)?e:best,null);
}


// v0.5.10: sprites are normalized during the asset-production step.
// Never scan/crop millions of pixels on the phone gameplay thread.
function getRenderableSprite(asset,spriteName=null){ return asset; }

function spriteDims(asset){
 return{
   w:asset?(asset.naturalWidth||asset.width||0):0,
   h:asset?(asset.naturalHeight||asset.height||0):0
 };
}

function nearestPlayerPoint(x,y){
 let alive=S.pairs.filter(p=>pairAlive(p));
 if(!alive.length)return null;
 return alive.reduce((best,p)=>!best||d({x,y},p)<d({x,y},best)?p:best,null);
}

function updateFacing(){
 S.pairs.forEach(pair=>{
   pair.units.forEach(u=>{
     let pos=actorPos(pair,u);
     let target=nearestEnemyPoint(pos.x,pos.y);
     if(target)u.rotation=Math.atan2(target.y-pos.y,target.x-pos.x);
   });
 });

 S.enemies.forEach(e=>{
   let target=nearestPlayerPoint(e.x,e.y);
   if(target)e.rotation=Math.atan2(target.y-e.y,target.x-e.x);
 });
}

function battleSpriteFor(name){
 return (name && SPRITE_DATA[name]) ? SPRITE_DATA[name] : null;
}
function drawUnit(x,y,label,color,active=false,visual=null,name=null,isEnemy=false,flipX=1,sizeScale=1){
 // Body sprites must always begin from a clean canvas state.
 ctx.globalCompositeOperation='source-over';
 if(!Number.isFinite(ctx.globalAlpha) || ctx.globalAlpha<=0)ctx.globalAlpha=1;
 let scale=visual?.scale||1,rot=visual?.rot||0,lift=visual?.lift||0;
 let activePulse=1;
 let fx=name?unitFX(name):null;

 // Deterministic per-character idle timing offset.
 // This MUST be initialized before any transform code references it.
 let idlePhase=0;
 if(name){
   for(let i=0;i<name.length;i++)idlePhase+=name.charCodeAt(i)*(i+3);
 }

 if(active && !visual){
   activePulse=1+Math.sin(performance.now()/170)*.018;
 }

 // PLAYER GROUND SHADOW — v0.5.18
 // The sprite artwork is anchored with its feet at local y≈12. Keep the shadow
 // directly on that contact point instead of floating well below the character.
 // Width follows the same render scale used by the body so larger units receive
 // a proportionally larger footprint without turning into a detached blob.
 if(!isEnemy){
   let liftRatio=clamp(lift/17,0,1);
   let bodyScale=(sizeScale||1)*UNIT_RENDER_SCALE*1.07*PLAYER_RENDER_BONUS*(name==='Crimson'?CRIMSON_RENDER_BONUS:1);
   let shadowW=clamp(10.5*bodyScale,10,18.5);
   let shadowH=clamp(2.55*bodyScale,2.5,4.25);
   let shadowAlpha=.24-.08*liftRatio;

   ctx.save();
   ctx.translate(x,y+11.5);
   ctx.fillStyle=`rgba(0,0,0,${shadowAlpha})`;
   ctx.shadowColor=`rgba(0,0,0,${.14+.04*(1-liftRatio)})`;
   ctx.shadowBlur=1.5+4.5*liftRatio;
   ctx.beginPath();
   ctx.ellipse(0,0,shadowW,shadowH,0,0,Math.PI*2);
   ctx.fill();
   ctx.restore();
 }

 ctx.save();
 ctx.translate(x,y-lift);

 if(!visual){
   let t=(performance.now()+idlePhase*5);
   // Lebee's supplied idle sheet already contains floating motion.
   if(name!=='Lebee'){
     ctx.translate(0,Math.sin(t/420)*.55);
     ctx.rotate(Mat

--- match 3 @ 43530637 ---
=>e.hp>0);
 if(!alive.length)return null;
 return alive.reduce((best,e)=>!best||d({x,y},e)<d({x,y},best)?e:best,null);
}


// v0.5.10: sprites are normalized during the asset-production step.
// Never scan/crop millions of pixels on the phone gameplay thread.
function getRenderableSprite(asset,spriteName=null){ return asset; }

function spriteDims(asset){
 return{
   w:asset?(asset.naturalWidth||asset.width||0):0,
   h:asset?(asset.naturalHeight||asset.height||0):0
 };
}

function nearestPlayerPoint(x,y){
 let alive=S.pairs.filter(p=>pairAlive(p));
 if(!alive.length)return null;
 return alive.reduce((best,p)=>!best||d({x,y},p)<d({x,y},best)?p:best,null);
}

function updateFacing(){
 S.pairs.forEach(pair=>{
   pair.units.forEach(u=>{
     let pos=actorPos(pair,u);
     let target=nearestEnemyPoint(pos.x,pos.y);
     if(target)u.rotation=Math.atan2(target.y-pos.y,target.x-pos.x);
   });
 });

 S.enemies.forEach(e=>{
   let target=nearestPlayerPoint(e.x,e.y);
   if(target)e.rotation=Math.atan2(target.y-e.y,target.x-e.x);
 });
}

function battleSpriteFor(name){
 return (name && SPRITE_DATA[name]) ? SPRITE_DATA[name] : null;
}
function drawUnit(x,y,label,color,active=false,visual=null,name=null,isEnemy=false,flipX=1,sizeScale=1){
 // Body sprites must always begin from a clean canvas state.
 ctx.globalCompositeOperation='source-over';
 if(!Number.isFinite(ctx.globalAlpha) || ctx.globalAlpha<=0)ctx.globalAlpha=1;
 let scale=visual?.scale||1,rot=visual?.rot||0,lift=visual?.lift||0;
 let activePulse=1;
 let fx=name?unitFX(name):null;

 // Deterministic per-character idle timing offset.
 // This MUST be initialized before any transform code references it.
 let idlePhase=0;
 if(name){
   for(let i=0;i<name.length;i++)idlePhase+=name.charCodeAt(i)*(i+3);
 }

 if(active && !visual){
   activePulse=1+Math.sin(performance.now()/170)*.018;
 }

 // PLAYER GROUND SHADOW — v0.5.18
 // The sprite artwork is anchored with its feet at local y≈12. Keep the shadow
 // directly on that contact point instead of floating well below the character.
 // Width follows the same render scale used by the body so larger units receive
 // a proportionally larger footprint without turning into a detached blob.
 if(!isEnemy){
   let liftRatio=clamp(lift/17,0,1);
   let bodyScale=(sizeScale||1)*UNIT_RENDER_SCALE*1.07*PLAYER_RENDER_BONUS*(name==='Crimson'?CRIMSON_RENDER_BONUS:1);
   let shadowW=clamp(10.5*bodyScale,10,18.5);
   let shadowH=clamp(2.55*bodyScale,2.5,4.25);
   let shadowAlpha=.24-.08*liftRatio;

   ctx.save();
   ctx.translate(x,y+11.5);
   ctx.fillStyle=`rgba(0,0,0,${shadowAlpha})`;
   ctx.shadowColor=`rgba(0,0,0,${.14+.04*(1-liftRatio)})`;
   ctx.shadowBlur=1.5+4.5*liftRatio;
   ctx.beginPath();
   ctx.ellipse(0,0,shadowW,shadowH,0,0,Math.PI*2);
   ctx.fill();
   ctx.restore();
 }

 ctx.save();
 ctx.translate(x,y-lift);

 if(!visual){
   let t=(performance.now()+idlePhase*5);
   // Lebee's supplied idle sheet already contains floating motion.
   if(name!=='Lebee'){
     ctx.translate(0,Math.sin(t/420)*.55);
     ctx.rotate(Math.sin(t/610)*.004);
   }
 }

 if(fx){
   let elapsed=performance.now()-fx.start;
   if(elapsed<fx.duration){
     if(fx.kind==='hit'){
       let t

--- match 4 @ 43555294 ---

   if(renderWhileKO(e.name))return;
   const pos=(S.anim&&S.anim.positions&&S.anim.positions[e.name])||e;

   ctx.save();
   ctx.globalAlpha=S.phase==='player'?.92:1;
   ctx.fillStyle='#222';
   ctx.fillRect(pos.x-27,pos.y+29,54,6);
   ctx.fillStyle='#ffbd4a';
   ctx.fillRect(pos.x-27,pos.y+29,54*(e.hp/e.maxHp),6);
   ctx.restore();
 });

 S.floaters.forEach(f=>{
   ctx.save();
   ctx.globalAlpha=f.life??1;
   if(f.kind==='screenFlash'){
     const elapsed=performance.now()-f.start;
     const t=clamp(elapsed/f.duration,0,1);
     ctx.setTransform(1,0,0,1,0,0);
     ctx.globalCompositeOperation='source-over';
     ctx.fillStyle=f.color||'#fff';
     ctx.globalAlpha=f.fade?Math.max(0,1-t):1;
     ctx.fillRect(0,0,W,H);
   }else if(f.kind==='lebeeStarProjectile'){
     const t=clamp((performance.now()-f.start)/f.duration,0,1);
     const e=1-Math.pow(1-t,2.4);
     const x=f.from.x+(f.to.x-f.from.x)*e;
     const y=f.from.y+(f.to.y-f.from.y)*e-5*Math.sin(t*Math.PI);
     const ang=Math.atan2(f.to.y-f.from.y,f.to.x-f.from.x);
     ctx.translate(x,y);ctx.rotate(ang);
     if(LEBEE_STAR_PROJECTILE.complete&&LEBEE_STAR_PROJECTILE.naturalWidth>0){
       const h=24+8*t,ratio=LEBEE_STAR_PROJECTILE.naturalWidth/LEBEE_STAR_PROJECTILE.naturalHeight,w=h*ratio;
       ctx.globalCompositeOperation='screen';
       ctx.shadowColor='#ff78df';ctx.shadowBlur=10;
       ctx.drawImage(LEBEE_STAR_PROJECTILE,-w*.6,-h*.5,w,h);
       ctx.globalCompositeOperation='source-over';
     }
   }else if(f.kind==='senkuChemProjectile'){
     const t=clamp((performance.now()-f.start)/f.duration,0,1);
     const e=1-Math.pow(1-t,2.15);
     const x=f.from.x+(f.to.x-f.from.x)*e;
     const y=f.from.y+(f.to.y-f.from.y)*e-7*Math.sin(t*Math.PI);
     const idx=Math.min(SENKU_CHEM_PROJECTILE_FRAMES.length-1,Math.floor(t*SENKU_CHEM_PROJECTILE_FRAMES.length));
     const img=SENKU_CHEM_PROJECTILE_FRAMES[idx];
     if(img?.complete&&img.naturalWidth>0){
       const h=30+5*Math.sin(t*Math.PI),ratio=img.naturalWidth/img.naturalHeight,w=h*ratio;
       ctx.translate(x,y);
       ctx.shadowColor='#62ff63';ctx.shadowBlur=10;
       ctx.drawImage(img,-w/2,-h/2,w,h);
     }
   }else if(f.kind==='senkuChemImpact'){
     const t=clamp((performance.now()-f.start)/f.duration,0,1);
     const idx=Math.min(SENKU_CHEM_IMPACT_FRAMES.length-1,Math.floor(t*SENKU_CHEM_IMPACT_FRAMES.length));
     const img=SENKU_CHEM_IMPACT_FRAMES[idx];
     if(img?.complete&&img.naturalWidth>0){
       const h=72+45*Math.sin(Math.min(1,t)*Math.PI*.72);
       const ratio=img.naturalWidth/img.naturalHeight,w=h*ratio;
       ctx.translate(f.x,f.y);
       ctx.globalAlpha=Math.max(0,1-Math.max(0,t-.75)/.25);
       ctx.shadowColor='#5dff6e';ctx.shadowBlur=12;
       ctx.drawImage(img,-w/2,-h*.70,w,h);
     }
   }else if(f.kind==='senkuBombProjectile'){
     const t=clamp((performance.now()-f.start)/f.duration,0,1);
     const e=t<.5 ? 2*t*t : 1-Math.pow(-2*t+2,2)/2;
     const arc=f.arcHeight||86;
     const x=f.from.x+(f.to.x-f.from.x)*e;
     const baseY=f.from.y+(f.to.y-f.from.y)*e;
     const y=baseY-4*arc*t*(1-t);

     // Tangent direction of the parabolic trajectory.
     const dx=f.to.x-f.from.x;
     const dy=(f.to

--- match 5 @ 43557550 ---
duration,0,1);
     const idx=Math.min(SENKU_CHEM_IMPACT_FRAMES.length-1,Math.floor(t*SENKU_CHEM_IMPACT_FRAMES.length));
     const img=SENKU_CHEM_IMPACT_FRAMES[idx];
     if(img?.complete&&img.naturalWidth>0){
       const h=72+45*Math.sin(Math.min(1,t)*Math.PI*.72);
       const ratio=img.naturalWidth/img.naturalHeight,w=h*ratio;
       ctx.translate(f.x,f.y);
       ctx.globalAlpha=Math.max(0,1-Math.max(0,t-.75)/.25);
       ctx.shadowColor='#5dff6e';ctx.shadowBlur=12;
       ctx.drawImage(img,-w/2,-h*.70,w,h);
     }
   }else if(f.kind==='senkuBombProjectile'){
     const t=clamp((performance.now()-f.start)/f.duration,0,1);
     const e=t<.5 ? 2*t*t : 1-Math.pow(-2*t+2,2)/2;
     const arc=f.arcHeight||86;
     const x=f.from.x+(f.to.x-f.from.x)*e;
     const baseY=f.from.y+(f.to.y-f.from.y)*e;
     const y=baseY-4*arc*t*(1-t);

     // Tangent direction of the parabolic trajectory.
     const dx=f.to.x-f.from.x;
     const dy=(f.to.y-f.from.y)-4*arc*(1-2*t);
     const angle=Math.atan2(dy,dx);

     const idx=Math.min(
       SENKU_BOMB_FRAMES.length-1,
       Math.floor(t*SENKU_BOMB_FRAMES.length)
     );
     const img=SENKU_BOMB_FRAMES[idx];
     if(img?.complete&&img.naturalWidth>0){
       const h=30,ratio=img.naturalWidth/img.naturalHeight,w=h*ratio;
       ctx.translate(x,y);
       ctx.rotate(angle);
       ctx.shadowColor='#ff9a32';
       ctx.shadowBlur=8;
       ctx.drawImage(img,-w/2,-h/2,w,h);
     }
   }else if(f.kind==='senkuExplosion'){
     const t=clamp((performance.now()-f.start)/f.duration,0,1);
     const img=SENKU_BASIC_STATIC_EXPLOSION;

     if(img?.complete&&img.naturalWidth>0){
       const hold=.30;
       const fade=t<=hold ? 1 : Math.max(0,1-(t-hold)/(1-hold));
       const pop=Math.min(1,t/.12);
       const scale=.90+.10*(1-Math.pow(1-pop,3));

       // Compact basic-attack impact.
       const baseH=58;
       const h=baseH*scale;
       const w=h*(img.naturalWidth/img.naturalHeight);

       // Exact target ground-point placement.
       const anchorX=.50;
       const anchorY=.96;
       const yNudge=3;

       ctx.globalAlpha*=fade;
       ctx.translate(f.x,f.y+yNudge);
       ctx.shadowColor='#ff9a32';
       ctx.shadowBlur=5*fade;
       ctx.drawImage(img,-w*anchorX,-h*anchorY,w,h);
     }
   }else if(f.kind==='lebeeMeteor'){
     const raw=(performance.now()-f.start)/f.duration;
     const t=clamp(raw,0,1);
     const e=t*t*(3-2*t);
     const x=f.from.x+(f.to.x-f.from.x)*e;
     const y=f.from.y+(f.to.y-f.from.y)*e;
     const img=LEBEE_METEOR_FRAMES[f.frameIndex%LEBEE_METEOR_FRAMES.length];
     ctx.translate(x,y);
     ctx.rotate((f.spin||.08)*(1-t)+(f.rot||0));
     if(img?.complete&&img.naturalWidth>0){
       const startH=f.startH||110,endH=f.endH||44;
       const h=startH+(endH-startH)*t,ratio=img.naturalWidth/img.naturalHeight,w=h*ratio;
       const tailFade=t<.84?1:Math.max(0,(1-t)/.16);
       ctx.globalAlpha=raw<0?0:tailFade;
       ctx.globalCompositeOperation='screen';
       ctx.shadowColor=f.frameIndex%3===0?'#ff61d8':f.frameIndex%3===1?'#58a8ff':'#ffc94e';
       ctx.shadowBlur=11+5*(1-t);
       ctx.drawImage(img,-w/2,-h/2,w,h);
       ctx.globalCompositeOperation='source-over';
     }


--- match 6 @ 43561614 ---
.sin(Math.min(1,t)*Math.PI),w=220+72*pulse;
       const ratio=LEBEE_METEOR_IMPACT_COMBO.naturalHeight/LEBEE_METEOR_IMPACT_COMBO.naturalWidth,h=w*ratio;
       ctx.globalAlpha=t<.70?1:(1-t)/.30;ctx.shadowColor='#ffffff';ctx.shadowBlur=18;
       ctx.drawImage(LEBEE_METEOR_IMPACT_COMBO,-w/2,-h*.76,w,h);
     }
   }else if(f.kind==='lebeeMeteorAftermath'){
     const t=clamp((performance.now()-f.start)/f.duration,0,1);
     ctx.translate(f.x,f.y);
     if(LEBEE_METEOR_AFTERMATH.complete&&LEBEE_METEOR_AFTERMATH.naturalWidth>0){
       const w=210,ratio=LEBEE_METEOR_AFTERMATH.naturalHeight/LEBEE_METEOR_AFTERMATH.naturalWidth,h=w*ratio;
       ctx.globalAlpha=.68*(1-t);ctx.drawImage(LEBEE_METEOR_AFTERMATH,-w/2,-h*.70,w,h);
     }
   }else if(f.kind==='iceProjectile'){
     const t=clamp((performance.now()-f.start)/f.duration,0,1);
     const e=1-Math.pow(1-t,2.2);
     const x=f.from.x+(f.to.x-f.from.x)*e;
     const y=f.from.y+(f.to.y-f.from.y)*e-8*Math.sin(t*Math.PI);
     const ang=Math.atan2(f.to.y-f.from.y,f.to.x-f.from.x);
     ctx.translate(x,y);
     ctx.rotate(ang);
     // v0.5.23: use the supplied Freeze Blast projectile artwork rather than
     // the procedural placeholder orb. It grows through the three approved
     // projectile frames as it leaves Sub-Zero and closes on the target.
     const idx=t<.24?0:(t<.58?1:2);
     const img=FREEZE_PROJECTILE_FRAMES[idx];
     if(img&&img.complete&&img.naturalWidth>0){
       const targetH=18+15*t;
       const ratio=img.naturalWidth/img.naturalHeight;
       const targetW=targetH*ratio;
       ctx.globalCompositeOperation='screen';
       ctx.shadowColor='#72eaff';ctx.shadowBlur=11;
       ctx.drawImage(img,-targetW*.62,-targetH*.5,targetW,targetH);
       ctx.globalCompositeOperation='source-over';
     }
   }else if(f.kind==='frozenTarget'){
     const pulse=.72+.18*Math.sin(performance.now()/75);
     ctx.translate(f.x,f.y-17);
     ctx.globalCompositeOperation='screen';
     ctx.strokeStyle=`rgba(133,236,255,${pulse})`;ctx.fillStyle='rgba(82,185,255,.16)';ctx.lineWidth=2.5;
     ctx.shadowColor='#7feaff';ctx.shadowBlur=10;
     ctx.beginPath();
     ctx.moveTo(-19,18);ctx.lineTo(-14,-20);ctx.lineTo(-4,-31);ctx.lineTo(2,-22);ctx.lineTo(12,-34);ctx.lineTo(18,-13);ctx.lineTo(20,18);ctx.closePath();
     ctx.fill();ctx.stroke();
     ctx.globalCompositeOperation='source-over';
   }else{
     ctx.fillStyle=f.color||'#fff';
     ctx.font='bold 19px system-ui';
     ctx.fillText(f.text,f.x,f.y);
   }
   ctx.restore();
 });

 drawVictoryOverlay();

 updateUI();
 }catch(err){
   console.error('Blazing Battle render error:',err);
   phaseEl.textContent='Recovering renderer…';
 }
 requestAnimationFrame(render);
}

function updateMeter(){
 [...meter.querySelectorAll('.icon')].forEach(n=>n.remove());
 let entries=[];
 S.pairs.forEach((p,i)=>{if(pairAlive(p)){ensureFront(p);entries.push({label:front(p).mark,name:front(p).name,speed:front(p).speed,g:p.gauge,color:COLORS[i]})}});
 S.enemies.forEach(e=>{if(e.hp>0)entries.push({label:e.boss?'BOSS':'E'+e.mark,name:e.name,speed:e.speed,g:e.gauge,color:'#d84b54'})});
 entries.forEach(e=>{
 let n=document.createElement('div');
 n.className='icon';
 n.style.backgr

--- match 7 @ 43603514 ---
;
   let dist=d(e,p);
   let hpRatio=u.hp/u.maxHp;
   let cluster=nearbyCount(p);
   let jitter=Math.random()*12;

   if(mode==='nearest')
     return dist*1.35 + hpRatio*30 - cluster*8 + jitter;
   if(mode==='weakest')
     return hpRatio*150 + dist*.35 - cluster*6 + jitter;
   if(mode==='cluster')
     return dist*.45 + hpRatio*35 - cluster*55 + jitter;

   // balanced
   return dist*.7 + hpRatio*75 - cluster*24 + jitter;
 }

 let target=[...candidates].sort((a,b)=>targetScore(a)-targetScore(b))[0];

 // Evaluate several approach angles instead of always taking the same straight-line slot.
 let angles=[];
 for(let i=0;i<10;i++)angles.push((Math.PI*2/10)*i);

 let best=null;
 angles.forEach(a=>{
   let radius=58+Math.random()*35;
   let pos=clampToBattlefield({
     x:target.x+Math.cos(a)*radius,
     y:target.y+Math.sin(a)*radius
   });

   // Score position by direct hits + mutual proximity links with enemy allies.
   let testEnemy={...e,x:pos.x,y:pos.y};
   testEnemy.rotation=Math.atan2(target.y-pos.y,target.x-pos.x);
   let hitCount=0;
   let comboSupport=0;

   candidates.forEach(p=>{
     let body={x:p.x,y:p.y,r:18};
     let fakeUnit={shape:normalShape(),rotation:testEnemy.rotation||0};
     if(hits(testEnemy,fakeUnit,body))hitCount++;
   });

   S.enemies.forEach(ally=>{
     if(ally===e||ally.hp<=0)return;

     let bodySelf={x:testEnemy.x,y:testEnemy.y,r:testEnemy.r||16};
     let bodyAlly={x:ally.x,y:ally.y,r:ally.r||16};

     let selfCoversAlly=hits(testEnemy,{shape:normalShape(),rotation:testEnemy.rotation||0},bodyAlly);
     let allyCoversSelf=hits(ally,{shape:normalShape(),rotation:ally.rotation||0},bodySelf);

     if(selfCoversAlly&&allyCoversSelf)comboSupport++;
   });

   let movement=d(e,pos);
   let targetDistance=d(pos,target);
   let score=
     hitCount*92 +
     comboSupport*38 -
     movement*.12 -
     targetDistance*.16 +
     Math.random()*10;

   if(!best || score>best.score)best={pos,score,hitCount,comboSupport};
 });

 let desired=best?.pos || clampToBattlefield({x:target.x-55,y:target.y});
 let from={x:e.x,y:e.y},start=performance.now(),dur=520;
 S.anim={positions:{}};

 S.log=`${e.name} is evaluating a ${mode} attack.`;

 function move(n){
   let t=Math.min(1,(n-start)/dur),q=t*t*(3-2*t);
   S.anim.positions[e.name]={
     x:from.x+(desired.x-from.x)*q,
     y:from.y+(desired.y-from.y)*q
   };

   if(t<1)requestAnimationFrame(move);
   else{
     let legal=clampToBattlefield(desired);
     e.x=legal.x;e.y=legal.y;
     e.rotation=Math.atan2(target.y-e.y,target.x-e.x);
     resolveEnemyOverlap(e);
     delete S.anim.positions[e.name];
     S.anim.showEnemyRange=e;
     setTimeout(()=>cpuAttack(e,target),360);
   }
 }
 requestAnimationFrame(move);
}
function cpuAttack(e,p){
 S.anim.showEnemyRange=null;

 let validTargets=S.pairs.filter(pair=>{
   if(!pairAlive(pair))return false;
   ensureFront(pair);
   let body={x:pair.x,y:pair.y,r:18};
   let fakeUnit={shape:normalShape(),rotation:e.rotation||0};
   return hits(e,fakeUnit,body)
 });

 // Safety fallback if movement somehow ends just outside its chosen target.
 if(!validTargets.length)validTargets=[p];

 let targetIndex=0;
 let anyCombo=false;

 function runTarge

--- match 8 @ 43605029 ---
e(),rotation:testEnemy.rotation||0},bodyAlly);
     let allyCoversSelf=hits(ally,{shape:normalShape(),rotation:ally.rotation||0},bodySelf);

     if(selfCoversAlly&&allyCoversSelf)comboSupport++;
   });

   let movement=d(e,pos);
   let targetDistance=d(pos,target);
   let score=
     hitCount*92 +
     comboSupport*38 -
     movement*.12 -
     targetDistance*.16 +
     Math.random()*10;

   if(!best || score>best.score)best={pos,score,hitCount,comboSupport};
 });

 let desired=best?.pos || clampToBattlefield({x:target.x-55,y:target.y});
 let from={x:e.x,y:e.y},start=performance.now(),dur=520;
 S.anim={positions:{}};

 S.log=`${e.name} is evaluating a ${mode} attack.`;

 function move(n){
   let t=Math.min(1,(n-start)/dur),q=t*t*(3-2*t);
   S.anim.positions[e.name]={
     x:from.x+(desired.x-from.x)*q,
     y:from.y+(desired.y-from.y)*q
   };

   if(t<1)requestAnimationFrame(move);
   else{
     let legal=clampToBattlefield(desired);
     e.x=legal.x;e.y=legal.y;
     e.rotation=Math.atan2(target.y-e.y,target.x-e.x);
     resolveEnemyOverlap(e);
     delete S.anim.positions[e.name];
     S.anim.showEnemyRange=e;
     setTimeout(()=>cpuAttack(e,target),360);
   }
 }
 requestAnimationFrame(move);
}
function cpuAttack(e,p){
 S.anim.showEnemyRange=null;

 let validTargets=S.pairs.filter(pair=>{
   if(!pairAlive(pair))return false;
   ensureFront(pair);
   let body={x:pair.x,y:pair.y,r:18};
   let fakeUnit={shape:normalShape(),rotation:e.rotation||0};
   return hits(e,fakeUnit,body)
 });

 // Safety fallback if movement somehow ends just outside its chosen target.
 if(!validTargets.length)validTargets=[p];

 let targetIndex=0;
 let anyCombo=false;

 function runTarget(){
   if(targetIndex>=validTargets.length){
     e.gauge=0;
     S.log=anyCombo
       ? `${e.name}'s side completed a combo attack across ${validTargets.length} target${validTargets.length===1?'':'s'}.`
       : `${e.name} hit ${validTargets.length} target${validTargets.length===1?'':'s'}.`;
     return finishAction()
   }

   let targetPair=validTargets[targetIndex++];
   if(!pairAlive(targetPair))return runTarget();
   ensureFront(targetPair);

   // Initiator first, then every other enemy whose current normal range
   // covers this same player target.
   let helpers=enemyComboMembers(targetPair,e).filter(enemy=>enemy!==e);
   if(helpers.length)anyCombo=true;
   let attackers=[e,...helpers];
   let attackerIndex=0;

   function runAttacker(){
     if(!pairAlive(targetPair) || attackerIndex>=attackers.length)
       return setTimeout(runTarget,100);

     ensureFront(targetPair);
     if(front(targetPair).hp<=0)return setTimeout(runTarget,100);
     let victim=front(targetPair);
     let attacker=attackers[attackerIndex++];

     let from={x:attacker.x,y:attacker.y},
         dx=targetPair.x-attacker.x,
         dy=targetPair.y-attacker.y,
         len=Math.max(1,Math.hypot(dx,dy)),
         to={
           x:targetPair.x-dx/len*42,
           y:targetPair.y-dy/len*42
         };

     animateLunge(
       attacker.name,from,to,
       ()=>{
         victim.hp=Math.max(0,victim.hp-attacker.attack);
         addImpactFlash(targetPair.x,targetPair.y-8,'#ff8b8b');
         addFloat(
     

===== nearest =====

--- match 1 @ 43529584 ---
ct(-h,-t,s.l,s.t);
     ctx.rect(-t,-h,s.t,s.l)
   }
   else if(s.type==='donut'){
     ctx.arc(0,0,s.o,0,Math.PI*2);
     ctx.arc(0,0,s.i,0,Math.PI*2,true)
   }
   else if(s.type==='cone'){
     ctx.moveTo(0,0);
     ctx.arc(0,0,s.r,-s.a/2,s.a/2);
     ctx.closePath()
   }
 }

 path();
 ctx.fill('evenodd');
 ctx.stroke();

 if(glow){
   // Crisp inner edge over the diffused aura.
   ctx.shadowBlur=5;
   ctx.globalAlpha=.92;
   ctx.strokeStyle=(color==='#ff3548'||color==='#ff3348')?'rgba(255,162,172,.94)':'rgba(174,242,255,.92)';
   ctx.lineWidth=1.05;
   path();
   ctx.stroke();
 }

 ctx.restore()
}
function actorPos(p,u){if(S.anim&&S.anim.positions&&S.anim.positions[u.name])return S.anim.positions[u.name];return p}

function facingFlip(rot,name=null){
 let dir=Math.cos(rot||0)>=0?1:-1;
 // Some source art faces screen-left by default. Invert those assets while
 // keeping mechanical facing/range logic unchanged.
 if(name==='Crimson'||name==='Anubis')dir*=-1;
 return dir;
}

function nearestEnemyPoint(x,y){
 let alive=S.enemies.filter(e=>e.hp>0);
 if(!alive.length)return null;
 return alive.reduce((best,e)=>!best||d({x,y},e)<d({x,y},best)?e:best,null);
}


// v0.5.10: sprites are normalized during the asset-production step.
// Never scan/crop millions of pixels on the phone gameplay thread.
function getRenderableSprite(asset,spriteName=null){ return asset; }

function spriteDims(asset){
 return{
   w:asset?(asset.naturalWidth||asset.width||0):0,
   h:asset?(asset.naturalHeight||asset.height||0):0
 };
}

function nearestPlayerPoint(x,y){
 let alive=S.pairs.filter(p=>pairAlive(p));
 if(!alive.length)return null;
 return alive.reduce((best,p)=>!best||d({x,y},p)<d({x,y},best)?p:best,null);
}

function updateFacing(){
 S.pairs.forEach(pair=>{
   pair.units.forEach(u=>{
     let pos=actorPos(pair,u);
     let target=nearestEnemyPoint(pos.x,pos.y);
     if(target)u.rotation=Math.atan2(target.y-pos.y,target.x-pos.x);
   });
 });

 S.enemies.forEach(e=>{
   let target=nearestPlayerPoint(e.x,e.y);
   if(target)e.rotation=Math.atan2(target.y-e.y,target.x-e.x);
 });
}

function battleSpriteFor(name){
 return (name && SPRITE_DATA[name]) ? SPRITE_DATA[name] : null;
}
function drawUnit(x,y,label,color,active=false,visual=null,name=null,isEnemy=false,flipX=1,sizeScale=1){
 // Body sprites must always begin from a clean canvas state.
 ctx.globalCompositeOperation='source-over';
 if(!Number.isFinite(ctx.globalAlpha) || ctx.globalAlpha<=0)ctx.globalAlpha=1;
 let scale=visual?.scale||1,rot=visual?.rot||0,lift=visual?.lift||0;
 let activePulse=1;
 let fx=name?unitFX(name):null;

 // Deterministic per-character idle timing offset.
 // This MUST be initialized before any transform code references it.
 let idlePhase=0;
 if(name){
   for(let i=0;i<name.length;i++)idlePhase+=name.charCodeAt(i)*(i+3);
 }

 if(active && !visual){
   activePulse=1+Math.sin(performance.now()/170)*.018;
 }

 // PLAYER GROUND SHADOW — v0.5.18
 // The sprite artwork is anchored with its feet at local y≈12. Keep the shadow
 // directly on that contact point instead of floating well below the character.
 // Width follows the same render scale used by the body so larger units receive
 // a proportion

--- match 2 @ 43530123 ---
ctx.lineWidth=1.05;
   path();
   ctx.stroke();
 }

 ctx.restore()
}
function actorPos(p,u){if(S.anim&&S.anim.positions&&S.anim.positions[u.name])return S.anim.positions[u.name];return p}

function facingFlip(rot,name=null){
 let dir=Math.cos(rot||0)>=0?1:-1;
 // Some source art faces screen-left by default. Invert those assets while
 // keeping mechanical facing/range logic unchanged.
 if(name==='Crimson'||name==='Anubis')dir*=-1;
 return dir;
}

function nearestEnemyPoint(x,y){
 let alive=S.enemies.filter(e=>e.hp>0);
 if(!alive.length)return null;
 return alive.reduce((best,e)=>!best||d({x,y},e)<d({x,y},best)?e:best,null);
}


// v0.5.10: sprites are normalized during the asset-production step.
// Never scan/crop millions of pixels on the phone gameplay thread.
function getRenderableSprite(asset,spriteName=null){ return asset; }

function spriteDims(asset){
 return{
   w:asset?(asset.naturalWidth||asset.width||0):0,
   h:asset?(asset.naturalHeight||asset.height||0):0
 };
}

function nearestPlayerPoint(x,y){
 let alive=S.pairs.filter(p=>pairAlive(p));
 if(!alive.length)return null;
 return alive.reduce((best,p)=>!best||d({x,y},p)<d({x,y},best)?p:best,null);
}

function updateFacing(){
 S.pairs.forEach(pair=>{
   pair.units.forEach(u=>{
     let pos=actorPos(pair,u);
     let target=nearestEnemyPoint(pos.x,pos.y);
     if(target)u.rotation=Math.atan2(target.y-pos.y,target.x-pos.x);
   });
 });

 S.enemies.forEach(e=>{
   let target=nearestPlayerPoint(e.x,e.y);
   if(target)e.rotation=Math.atan2(target.y-e.y,target.x-e.x);
 });
}

function battleSpriteFor(name){
 return (name && SPRITE_DATA[name]) ? SPRITE_DATA[name] : null;
}
function drawUnit(x,y,label,color,active=false,visual=null,name=null,isEnemy=false,flipX=1,sizeScale=1){
 // Body sprites must always begin from a clean canvas state.
 ctx.globalCompositeOperation='source-over';
 if(!Number.isFinite(ctx.globalAlpha) || ctx.globalAlpha<=0)ctx.globalAlpha=1;
 let scale=visual?.scale||1,rot=visual?.rot||0,lift=visual?.lift||0;
 let activePulse=1;
 let fx=name?unitFX(name):null;

 // Deterministic per-character idle timing offset.
 // This MUST be initialized before any transform code references it.
 let idlePhase=0;
 if(name){
   for(let i=0;i<name.length;i++)idlePhase+=name.charCodeAt(i)*(i+3);
 }

 if(active && !visual){
   activePulse=1+Math.sin(performance.now()/170)*.018;
 }

 // PLAYER GROUND SHADOW — v0.5.18
 // The sprite artwork is anchored with its feet at local y≈12. Keep the shadow
 // directly on that contact point instead of floating well below the character.
 // Width follows the same render scale used by the body so larger units receive
 // a proportionally larger footprint without turning into a detached blob.
 if(!isEnemy){
   let liftRatio=clamp(lift/17,0,1);
   let bodyScale=(sizeScale||1)*UNIT_RENDER_SCALE*1.07*PLAYER_RENDER_BONUS*(name==='Crimson'?CRIMSON_RENDER_BONUS:1);
   let shadowW=clamp(10.5*bodyScale,10,18.5);
   let shadowH=clamp(2.55*bodyScale,2.5,4.25);
   let shadowAlpha=.24-.08*liftRatio;

   ctx.save();
   ctx.translate(x,y+11.5);
   ctx.fillStyle=`rgba(0,0,0,${shadowAlpha})`;
   ctx.shadowColor=`rgba(0,0,0,${.14+.04*(1-liftRatio)})`;
   ctx.shadowBlur=1.5+4.5*li

--- match 3 @ 43530427 ---
ault. Invert those assets while
 // keeping mechanical facing/range logic unchanged.
 if(name==='Crimson'||name==='Anubis')dir*=-1;
 return dir;
}

function nearestEnemyPoint(x,y){
 let alive=S.enemies.filter(e=>e.hp>0);
 if(!alive.length)return null;
 return alive.reduce((best,e)=>!best||d({x,y},e)<d({x,y},best)?e:best,null);
}


// v0.5.10: sprites are normalized during the asset-production step.
// Never scan/crop millions of pixels on the phone gameplay thread.
function getRenderableSprite(asset,spriteName=null){ return asset; }

function spriteDims(asset){
 return{
   w:asset?(asset.naturalWidth||asset.width||0):0,
   h:asset?(asset.naturalHeight||asset.height||0):0
 };
}

function nearestPlayerPoint(x,y){
 let alive=S.pairs.filter(p=>pairAlive(p));
 if(!alive.length)return null;
 return alive.reduce((best,p)=>!best||d({x,y},p)<d({x,y},best)?p:best,null);
}

function updateFacing(){
 S.pairs.forEach(pair=>{
   pair.units.forEach(u=>{
     let pos=actorPos(pair,u);
     let target=nearestEnemyPoint(pos.x,pos.y);
     if(target)u.rotation=Math.atan2(target.y-pos.y,target.x-pos.x);
   });
 });

 S.enemies.forEach(e=>{
   let target=nearestPlayerPoint(e.x,e.y);
   if(target)e.rotation=Math.atan2(target.y-e.y,target.x-e.x);
 });
}

function battleSpriteFor(name){
 return (name && SPRITE_DATA[name]) ? SPRITE_DATA[name] : null;
}
function drawUnit(x,y,label,color,active=false,visual=null,name=null,isEnemy=false,flipX=1,sizeScale=1){
 // Body sprites must always begin from a clean canvas state.
 ctx.globalCompositeOperation='source-over';
 if(!Number.isFinite(ctx.globalAlpha) || ctx.globalAlpha<=0)ctx.globalAlpha=1;
 let scale=visual?.scale||1,rot=visual?.rot||0,lift=visual?.lift||0;
 let activePulse=1;
 let fx=name?unitFX(name):null;

 // Deterministic per-character idle timing offset.
 // This MUST be initialized before any transform code references it.
 let idlePhase=0;
 if(name){
   for(let i=0;i<name.length;i++)idlePhase+=name.charCodeAt(i)*(i+3);
 }

 if(active && !visual){
   activePulse=1+Math.sin(performance.now()/170)*.018;
 }

 // PLAYER GROUND SHADOW — v0.5.18
 // The sprite artwork is anchored with its feet at local y≈12. Keep the shadow
 // directly on that contact point instead of floating well below the character.
 // Width follows the same render scale used by the body so larger units receive
 // a proportionally larger footprint without turning into a detached blob.
 if(!isEnemy){
   let liftRatio=clamp(lift/17,0,1);
   let bodyScale=(sizeScale||1)*UNIT_RENDER_SCALE*1.07*PLAYER_RENDER_BONUS*(name==='Crimson'?CRIMSON_RENDER_BONUS:1);
   let shadowW=clamp(10.5*bodyScale,10,18.5);
   let shadowH=clamp(2.55*bodyScale,2.5,4.25);
   let shadowAlpha=.24-.08*liftRatio;

   ctx.save();
   ctx.translate(x,y+11.5);
   ctx.fillStyle=`rgba(0,0,0,${shadowAlpha})`;
   ctx.shadowColor=`rgba(0,0,0,${.14+.04*(1-liftRatio)})`;
   ctx.shadowBlur=1.5+4.5*liftRatio;
   ctx.beginPath();
   ctx.ellipse(0,0,shadowW,shadowH,0,0,Math.PI*2);
   ctx.fill();
   ctx.restore();
 }

 ctx.save();
 ctx.translate(x,y-lift);

 if(!visual){
   let t=(performance.now()+idlePhase*5);
   // Lebee's supplied idle sheet already contains floating motion.
   if(name!=='Lebee'){


--- match 4 @ 43530579 ---
tion nearestEnemyPoint(x,y){
 let alive=S.enemies.filter(e=>e.hp>0);
 if(!alive.length)return null;
 return alive.reduce((best,e)=>!best||d({x,y},e)<d({x,y},best)?e:best,null);
}


// v0.5.10: sprites are normalized during the asset-production step.
// Never scan/crop millions of pixels on the phone gameplay thread.
function getRenderableSprite(asset,spriteName=null){ return asset; }

function spriteDims(asset){
 return{
   w:asset?(asset.naturalWidth||asset.width||0):0,
   h:asset?(asset.naturalHeight||asset.height||0):0
 };
}

function nearestPlayerPoint(x,y){
 let alive=S.pairs.filter(p=>pairAlive(p));
 if(!alive.length)return null;
 return alive.reduce((best,p)=>!best||d({x,y},p)<d({x,y},best)?p:best,null);
}

function updateFacing(){
 S.pairs.forEach(pair=>{
   pair.units.forEach(u=>{
     let pos=actorPos(pair,u);
     let target=nearestEnemyPoint(pos.x,pos.y);
     if(target)u.rotation=Math.atan2(target.y-pos.y,target.x-pos.x);
   });
 });

 S.enemies.forEach(e=>{
   let target=nearestPlayerPoint(e.x,e.y);
   if(target)e.rotation=Math.atan2(target.y-e.y,target.x-e.x);
 });
}

function battleSpriteFor(name){
 return (name && SPRITE_DATA[name]) ? SPRITE_DATA[name] : null;
}
function drawUnit(x,y,label,color,active=false,visual=null,name=null,isEnemy=false,flipX=1,sizeScale=1){
 // Body sprites must always begin from a clean canvas state.
 ctx.globalCompositeOperation='source-over';
 if(!Number.isFinite(ctx.globalAlpha) || ctx.globalAlpha<=0)ctx.globalAlpha=1;
 let scale=visual?.scale||1,rot=visual?.rot||0,lift=visual?.lift||0;
 let activePulse=1;
 let fx=name?unitFX(name):null;

 // Deterministic per-character idle timing offset.
 // This MUST be initialized before any transform code references it.
 let idlePhase=0;
 if(name){
   for(let i=0;i<name.length;i++)idlePhase+=name.charCodeAt(i)*(i+3);
 }

 if(active && !visual){
   activePulse=1+Math.sin(performance.now()/170)*.018;
 }

 // PLAYER GROUND SHADOW — v0.5.18
 // The sprite artwork is anchored with its feet at local y≈12. Keep the shadow
 // directly on that contact point instead of floating well below the character.
 // Width follows the same render scale used by the body so larger units receive
 // a proportionally larger footprint without turning into a detached blob.
 if(!isEnemy){
   let liftRatio=clamp(lift/17,0,1);
   let bodyScale=(sizeScale||1)*UNIT_RENDER_SCALE*1.07*PLAYER_RENDER_BONUS*(name==='Crimson'?CRIMSON_RENDER_BONUS:1);
   let shadowW=clamp(10.5*bodyScale,10,18.5);
   let shadowH=clamp(2.55*bodyScale,2.5,4.25);
   let shadowAlpha=.24-.08*liftRatio;

   ctx.save();
   ctx.translate(x,y+11.5);
   ctx.fillStyle=`rgba(0,0,0,${shadowAlpha})`;
   ctx.shadowColor=`rgba(0,0,0,${.14+.04*(1-liftRatio)})`;
   ctx.shadowBlur=1.5+4.5*liftRatio;
   ctx.beginPath();
   ctx.ellipse(0,0,shadowW,shadowH,0,0,Math.PI*2);
   ctx.fill();
   ctx.restore();
 }

 ctx.save();
 ctx.translate(x,y-lift);

 if(!visual){
   let t=(performance.now()+idlePhase*5);
   // Lebee's supplied idle sheet already contains floating motion.
   if(name!=='Lebee'){
     ctx.translate(0,Math.sin(t/420)*.55);
     ctx.rotate(Math.sin(t/610)*.004);
   }
 }

 if(fx){
   let elapsed=performance.now()-fx.start;
   if(ela

--- match 5 @ 43602112 ---
t<minDist && dist<blockerDist){
       blocker=other;
       blockerDist=dist;
     }
   });

   if(!blocker)break;

   let dx=enemy.x-blocker.x;
   let dy=enemy.y-blocker.y;
   let len=Math.hypot(dx,dy);

   // Exact same-center fallback gets a deterministic escape direction.
   if(len<.01){
     let angle=(pass+1)*1.913;
     dx=Math.cos(angle);
     dy=Math.sin(angle);
     len=1;
   }

   let push=(minDist-len)+.75;
   enemy.x+=(dx/len)*push;
   enemy.y+=(dy/len)*push;

   let legal=clampToBattlefield(enemy);
   enemy.x=legal.x;
   enemy.y=legal.y;
 }
}

function cpuTurn(){
 if(S.phase!=='cpu')return;
 const cpuActionToken=beginActionToken();
 armActionWatchdog('Anubis / enemy turn',cpuActionToken);
 let e=S.ready.ref;
 if(e.hp<=0){e.gauge=0;return finishAction()}

 let candidates=S.pairs.filter(pairAlive);candidates.forEach(ensureFront);
 if(!candidates.length)return;

 // Enemy personality changes each turn so the CPU does not dogpile one unit.
 // Weighted considerations:
 // - nearest target
 // - lowest-health target
 // - clustered target opportunity
 // - mild randomness so equal situations do not look scripted
 const modes=['nearest','weakest','cluster','balanced'];
 const mode=modes[Math.floor(Math.random()*modes.length)];

 function nearbyCount(p){
   return candidates.filter(other=>other!==p && d(p,other)<145).length;
 }

 function targetScore(p){
   let u=front(p);
   let dist=d(e,p);
   let hpRatio=u.hp/u.maxHp;
   let cluster=nearbyCount(p);
   let jitter=Math.random()*12;

   if(mode==='nearest')
     return dist*1.35 + hpRatio*30 - cluster*8 + jitter;
   if(mode==='weakest')
     return hpRatio*150 + dist*.35 - cluster*6 + jitter;
   if(mode==='cluster')
     return dist*.45 + hpRatio*35 - cluster*55 + jitter;

   // balanced
   return dist*.7 + hpRatio*75 - cluster*24 + jitter;
 }

 let target=[...candidates].sort((a,b)=>targetScore(a)-targetScore(b))[0];

 // Evaluate several approach angles instead of always taking the same straight-line slot.
 let angles=[];
 for(let i=0;i<10;i++)angles.push((Math.PI*2/10)*i);

 let best=null;
 angles.forEach(a=>{
   let radius=58+Math.random()*35;
   let pos=clampToBattlefield({
     x:target.x+Math.cos(a)*radius,
     y:target.y+Math.sin(a)*radius
   });

   // Score position by direct hits + mutual proximity links with enemy allies.
   let testEnemy={...e,x:pos.x,y:pos.y};
   testEnemy.rotation=Math.atan2(target.y-pos.y,target.x-pos.x);
   let hitCount=0;
   let comboSupport=0;

   candidates.forEach(p=>{
     let body={x:p.x,y:p.y,r:18};
     let fakeUnit={shape:normalShape(),rotation:testEnemy.rotation||0};
     if(hits(testEnemy,fakeUnit,body))hitCount++;
   });

   S.enemies.forEach(ally=>{
     if(ally===e||ally.hp<=0)return;

     let bodySelf={x:testEnemy.x,y:testEnemy.y,r:testEnemy.r||16};
     let bodyAlly={x:ally.x,y:ally.y,r:ally.r||16};

     let selfCoversAlly=hits(testEnemy,{shape:normalShape(),rotation:testEnemy.rotation||0},bodyAlly);
     let allyCoversSelf=hits(ally,{shape:normalShape(),rotation:ally.rotation||0},bodySelf);

     if(selfCoversAlly&&allyCoversSelf)comboSupport++;
   });

   let movement=d(e,pos);
   let targetDistance=d(pos,target);
   let score=
     hitC

--- match 6 @ 43602267 ---
=enemy.y-blocker.y;
   let len=Math.hypot(dx,dy);

   // Exact same-center fallback gets a deterministic escape direction.
   if(len<.01){
     let angle=(pass+1)*1.913;
     dx=Math.cos(angle);
     dy=Math.sin(angle);
     len=1;
   }

   let push=(minDist-len)+.75;
   enemy.x+=(dx/len)*push;
   enemy.y+=(dy/len)*push;

   let legal=clampToBattlefield(enemy);
   enemy.x=legal.x;
   enemy.y=legal.y;
 }
}

function cpuTurn(){
 if(S.phase!=='cpu')return;
 const cpuActionToken=beginActionToken();
 armActionWatchdog('Anubis / enemy turn',cpuActionToken);
 let e=S.ready.ref;
 if(e.hp<=0){e.gauge=0;return finishAction()}

 let candidates=S.pairs.filter(pairAlive);candidates.forEach(ensureFront);
 if(!candidates.length)return;

 // Enemy personality changes each turn so the CPU does not dogpile one unit.
 // Weighted considerations:
 // - nearest target
 // - lowest-health target
 // - clustered target opportunity
 // - mild randomness so equal situations do not look scripted
 const modes=['nearest','weakest','cluster','balanced'];
 const mode=modes[Math.floor(Math.random()*modes.length)];

 function nearbyCount(p){
   return candidates.filter(other=>other!==p && d(p,other)<145).length;
 }

 function targetScore(p){
   let u=front(p);
   let dist=d(e,p);
   let hpRatio=u.hp/u.maxHp;
   let cluster=nearbyCount(p);
   let jitter=Math.random()*12;

   if(mode==='nearest')
     return dist*1.35 + hpRatio*30 - cluster*8 + jitter;
   if(mode==='weakest')
     return hpRatio*150 + dist*.35 - cluster*6 + jitter;
   if(mode==='cluster')
     return dist*.45 + hpRatio*35 - cluster*55 + jitter;

   // balanced
   return dist*.7 + hpRatio*75 - cluster*24 + jitter;
 }

 let target=[...candidates].sort((a,b)=>targetScore(a)-targetScore(b))[0];

 // Evaluate several approach angles instead of always taking the same straight-line slot.
 let angles=[];
 for(let i=0;i<10;i++)angles.push((Math.PI*2/10)*i);

 let best=null;
 angles.forEach(a=>{
   let radius=58+Math.random()*35;
   let pos=clampToBattlefield({
     x:target.x+Math.cos(a)*radius,
     y:target.y+Math.sin(a)*radius
   });

   // Score position by direct hits + mutual proximity links with enemy allies.
   let testEnemy={...e,x:pos.x,y:pos.y};
   testEnemy.rotation=Math.atan2(target.y-pos.y,target.x-pos.x);
   let hitCount=0;
   let comboSupport=0;

   candidates.forEach(p=>{
     let body={x:p.x,y:p.y,r:18};
     let fakeUnit={shape:normalShape(),rotation:testEnemy.rotation||0};
     if(hits(testEnemy,fakeUnit,body))hitCount++;
   });

   S.enemies.forEach(ally=>{
     if(ally===e||ally.hp<=0)return;

     let bodySelf={x:testEnemy.x,y:testEnemy.y,r:testEnemy.r||16};
     let bodyAlly={x:ally.x,y:ally.y,r:ally.r||16};

     let selfCoversAlly=hits(testEnemy,{shape:normalShape(),rotation:testEnemy.rotation||0},bodyAlly);
     let allyCoversSelf=hits(ally,{shape:normalShape(),rotation:ally.rotation||0},bodySelf);

     if(selfCoversAlly&&allyCoversSelf)comboSupport++;
   });

   let movement=d(e,pos);
   let targetDistance=d(pos,target);
   let score=
     hitCount*92 +
     comboSupport*38 -
     movement*.12 -
     targetDistance*.16 +
     Math.random()*10;

   if(!best || score>best.score)best={pos,score,hitC

--- match 7 @ 43602643 ---
egal.x;
   enemy.y=legal.y;
 }
}

function cpuTurn(){
 if(S.phase!=='cpu')return;
 const cpuActionToken=beginActionToken();
 armActionWatchdog('Anubis / enemy turn',cpuActionToken);
 let e=S.ready.ref;
 if(e.hp<=0){e.gauge=0;return finishAction()}

 let candidates=S.pairs.filter(pairAlive);candidates.forEach(ensureFront);
 if(!candidates.length)return;

 // Enemy personality changes each turn so the CPU does not dogpile one unit.
 // Weighted considerations:
 // - nearest target
 // - lowest-health target
 // - clustered target opportunity
 // - mild randomness so equal situations do not look scripted
 const modes=['nearest','weakest','cluster','balanced'];
 const mode=modes[Math.floor(Math.random()*modes.length)];

 function nearbyCount(p){
   return candidates.filter(other=>other!==p && d(p,other)<145).length;
 }

 function targetScore(p){
   let u=front(p);
   let dist=d(e,p);
   let hpRatio=u.hp/u.maxHp;
   let cluster=nearbyCount(p);
   let jitter=Math.random()*12;

   if(mode==='nearest')
     return dist*1.35 + hpRatio*30 - cluster*8 + jitter;
   if(mode==='weakest')
     return hpRatio*150 + dist*.35 - cluster*6 + jitter;
   if(mode==='cluster')
     return dist*.45 + hpRatio*35 - cluster*55 + jitter;

   // balanced
   return dist*.7 + hpRatio*75 - cluster*24 + jitter;
 }

 let target=[...candidates].sort((a,b)=>targetScore(a)-targetScore(b))[0];

 // Evaluate several approach angles instead of always taking the same straight-line slot.
 let angles=[];
 for(let i=0;i<10;i++)angles.push((Math.PI*2/10)*i);

 let best=null;
 angles.forEach(a=>{
   let radius=58+Math.random()*35;
   let pos=clampToBattlefield({
     x:target.x+Math.cos(a)*radius,
     y:target.y+Math.sin(a)*radius
   });

   // Score position by direct hits + mutual proximity links with enemy allies.
   let testEnemy={...e,x:pos.x,y:pos.y};
   testEnemy.rotation=Math.atan2(target.y-pos.y,target.x-pos.x);
   let hitCount=0;
   let comboSupport=0;

   candidates.forEach(p=>{
     let body={x:p.x,y:p.y,r:18};
     let fakeUnit={shape:normalShape(),rotation:testEnemy.rotation||0};
     if(hits(testEnemy,fakeUnit,body))hitCount++;
   });

   S.enemies.forEach(ally=>{
     if(ally===e||ally.hp<=0)return;

     let bodySelf={x:testEnemy.x,y:testEnemy.y,r:testEnemy.r||16};
     let bodyAlly={x:ally.x,y:ally.y,r:ally.r||16};

     let selfCoversAlly=hits(testEnemy,{shape:normalShape(),rotation:testEnemy.rotation||0},bodyAlly);
     let allyCoversSelf=hits(ally,{shape:normalShape(),rotation:ally.rotation||0},bodySelf);

     if(selfCoversAlly&&allyCoversSelf)comboSupport++;
   });

   let movement=d(e,pos);
   let targetDistance=d(pos,target);
   let score=
     hitCount*92 +
     comboSupport*38 -
     movement*.12 -
     targetDistance*.16 +
     Math.random()*10;

   if(!best || score>best.score)best={pos,score,hitCount,comboSupport};
 });

 let desired=best?.pos || clampToBattlefield({x:target.x-55,y:target.y});
 let from={x:e.x,y:e.y},start=performance.now(),dur=520;
 S.anim={positions:{}};

 S.log=`${e.name} is evaluating a ${mode} attack.`;

 function move(n){
   let t=Math.min(1,(n-start)/dur),q=t*t*(3-2*t);
   S.anim.positions[e.name]={
     x:from.x+(desired.x-from.x)*q,
     y

===== targets= =====

--- match 1 @ 43592308 ---
eMeteorImpactFlash(()=>{
       if(!actionTokenAlive(token))return;
       try{
         onDone&&onDone();
       }catch(err){
         recoverAction('Lebee Meteor completion');
       }
     });
   },finalImpact);

 },castDuration);
}

function recoil(enemy,done,dir=1,strong=false){
 const token=ACTIVE_ACTION_TOKEN;
 startHitReaction(enemy.name,dir,strong);
 let ox=enemy.x,oy=enemy.y,dx=18*(dir||1),start=performance.now();
 function f(n){
  if(!actionTokenAlive(token))return;
  let state=ensureAnimState(),t=Math.min(1,(n-start)/130),off=Math.sin(t*Math.PI)*dx;
  state.positions[enemy.name]={x:ox+off,y:oy};
  if(t<1)return requestAnimationFrame(f);
  delete state.positions[enemy.name];
  try{done&&done()}catch(err){console.error('Recoil callback failed:',err);recoverAction('recoil callback')}
 }
 requestAnimationFrame(f)
}
function resolvePlayer(){

 let p=S.ready.ref,u=front(p),useJutsu=S.action==='jutsu',proxy=attackProxy(u);
 const supportJutsu=useJutsu&&isAllySupportJutsu(u);
 let targets=supportJutsu?[]:aliveEnemies().filter(e=>hits(p,proxy,e));
 p.gauge=0;S.phase='resolve';S.anim={positions:{}};const playerActionToken=beginActionToken();armActionWatchdog('player attack',playerActionToken);

 if(useJutsu&&u.chakra<u.jutsuCost){
  S.log=`${u.name} does not have enough chakra.`;S.action='normal';return finishAction()
 }

 if(supportJutsu){
  u.chakra-=u.jutsuCost;

  const allies=alivePairs().filter(pair=>front(pair).hp>0);
  if(!allies.length){
   S.log=`${u.name} activated ${u.jutsuName||'Jutsu'}, but no allies can be healed.`;
   return finishAction();
  }

  const teamCenter={
   x:allies.reduce((sum,pair)=>sum+pair.x,0)/allies.length,
   y:allies.reduce((sum,pair)=>sum+pair.y,0)/allies.length-18
  };

  const from={x:p.x,y:p.y};
  S.log=`${u.name} activated ${u.jutsuName||'Revival Formula'} — restoring all active allies to full HP.`;

  return animateSenkuChemicalReaction(
   u.name,from,teamCenter,
   ()=>{
    allies.forEach(pair=>{
     const ally=front(pair);
     const healed=Math.max(0,ally.maxHp-ally.hp);
     ally.hp=ally.maxHp;

     addImpactFlash(pair.x,pair.y-18,'#64ff7d');
     if(healed>0){
      addFloat(pair.x,pair.y-40,`+${healed}`,'#74ff91');
     }else{
      addFloat(pair.x,pair.y-40,'FULL','#74ff91');
     }
    });
   },
   ()=>finishAction()
  );
 }

 if(!targets.length){
  S.log=`${u.name} committed the move but caught no target.`;return finishAction()
 }

 if(useJutsu){
  u.chakra-=u.jutsuCost;

  if(u.name==='Lebee'){
   S.log=`Lebee activated Meteor Jutsu — ${targets.length} target${targets.length===1?'':'s'} caught in the cosmic impact zone.`;
   const from={x:p.x,y:p.y};
   return animateLebeeMeteorJutsu(
    u.name,from,targets,
    ()=>{
     targets.forEach(enemy=>{
      if(enemy.hp<=0)return;
      enemy.hp=Math.max(0,enemy.hp-u.jutsuDamage);
      addImpactFlash(enemy.x,enemy.y-8,'#ff7ae6');
      addFloat(enemy.x,enemy.y-34,'-'+u.jutsuDamage,'#ff8be8');
      const dir=Math.sign(enemy.x-p.x)||1;
      if(enemy.hp<=0)handleEnemyKO(enemy,dir); else recoil(enemy,()=>{},dir,true);
     });
     checkVictoryKillshot();
    },
    ()=>finishAction()
   );
  }

  // Every Jutsu target gets:
  //   active fighter's

===== targets = =====

===== attackPreview =====

--- match 1 @ 43564275 ---
nderer…';
 }
 requestAnimationFrame(render);
}

function updateMeter(){
 [...meter.querySelectorAll('.icon')].forEach(n=>n.remove());
 let entries=[];
 S.pairs.forEach((p,i)=>{if(pairAlive(p)){ensureFront(p);entries.push({label:front(p).mark,name:front(p).name,speed:front(p).speed,g:p.gauge,color:COLORS[i]})}});
 S.enemies.forEach(e=>{if(e.hp>0)entries.push({label:e.boss?'BOSS':'E'+e.mark,name:e.name,speed:e.speed,g:e.gauge,color:'#d84b54'})});
 entries.forEach(e=>{
 let n=document.createElement('div');
 n.className='icon';
 n.style.background=e.color;
 const startX=26;
 const readyInset=13; // must match .ready { right:13px }
 const readyX=Math.max(startX,meter.clientWidth-readyInset);
 const progress=Math.min(100,Math.max(0,e.g))/100;
 const centerX=startX+(readyX-startX)*progress;
 n.style.left=`${centerX-21}px`;
 n.textContent=e.label;
 meter.appendChild(n)
})
}

function tacticalTickerItems(){
 if(!gameStarted || S.phase!=='player' || S.ready?.kind!=='pair')
   return [];

 const attackPreview=S.action==='normal'||S.action==='jutsu'||S.drag;
 if(!attackPreview)return [];

 const activePair=S.ready.ref;
 if(!pairAlive(activePair))return [];

 ensureFront(activePair);

 const members=comboMembers(null,activePair);
 const items=[];

 // Nothing shows until the formation actually enters a valid link area.
 if(members.length>1){
   const normalLinkDamage=members.reduce((sum,p)=>sum+buffedNormalDamage(p).damage,0);
   items.push({
     text:`LINK ATTACK ×${members.length} · ${normalLinkDamage} DMG`,
     tone:'link'
   });
 }

 // Buff currently RECEIVED by the active fighter.
 const incoming=totalLinkAttackBuff(activePair);
 if(incoming.bonus>0){
   items.push({
     text:`LINK ATK +${Math.round(incoming.bonus*100)}%`,
     tone:'buff'
   });
 }

 // Buff currently PROVIDED by the active fighter to linked allies.
 let outgoing=[];
 S.pairs.forEach(other=>{
   if(other===activePair||!pairAlive(other))return;
   const effect=linkBuffFromProvider(activePair,other);
   if(effect.bonus>0)outgoing.push(effect.bonus);
 });
 if(outgoing.length){
   items.push({
     text:`ALLY ATK +${Math.round(Math.max(...outgoing)*100)}%`,
     tone:'buff'
   });
 }

 // The ticker is intentionally generic so Field Skills / Buddy Skills /
 // debuffs can append their own segments later without adding map clutter.
 return items;
}

function updateTacticalTicker(){
 if(!tacticalTicker)return;

 const items=tacticalTickerItems();
 if(!items.length){
   tacticalTicker.classList.remove('show','linkActive');
   tacticalTicker.innerHTML='';
   return;
 }

 tacticalTicker.innerHTML=items
   .map(item=>`<span class="tickerItem ${item.tone||'info'}">${item.text}</span>`)
   .join('');

 tacticalTicker.classList.add('show');
 tacticalTicker.classList.toggle('linkActive',items.some(item=>item.tone==='link'));
}

function updateUI(){
 updateMeter();
 updateTacticalTicker();

 // Boss-only health bar, driven by live Anubis HP.
 if(bossHpHud&&bossHpFill){
   const boss=S.bossBattle?S.enemies.find(e=>e.boss||e.name==='Anubis'):null;
   if(boss){
     bossHpHud.classList.add('active');
     const ratio=clamp(boss.hp/boss.maxHp,0,1);
     bossHpFill.style.width=`${ratio*100}%`;
     bossHpHud.se

--- match 2 @ 43564343 ---
(){
 [...meter.querySelectorAll('.icon')].forEach(n=>n.remove());
 let entries=[];
 S.pairs.forEach((p,i)=>{if(pairAlive(p)){ensureFront(p);entries.push({label:front(p).mark,name:front(p).name,speed:front(p).speed,g:p.gauge,color:COLORS[i]})}});
 S.enemies.forEach(e=>{if(e.hp>0)entries.push({label:e.boss?'BOSS':'E'+e.mark,name:e.name,speed:e.speed,g:e.gauge,color:'#d84b54'})});
 entries.forEach(e=>{
 let n=document.createElement('div');
 n.className='icon';
 n.style.background=e.color;
 const startX=26;
 const readyInset=13; // must match .ready { right:13px }
 const readyX=Math.max(startX,meter.clientWidth-readyInset);
 const progress=Math.min(100,Math.max(0,e.g))/100;
 const centerX=startX+(readyX-startX)*progress;
 n.style.left=`${centerX-21}px`;
 n.textContent=e.label;
 meter.appendChild(n)
})
}

function tacticalTickerItems(){
 if(!gameStarted || S.phase!=='player' || S.ready?.kind!=='pair')
   return [];

 const attackPreview=S.action==='normal'||S.action==='jutsu'||S.drag;
 if(!attackPreview)return [];

 const activePair=S.ready.ref;
 if(!pairAlive(activePair))return [];

 ensureFront(activePair);

 const members=comboMembers(null,activePair);
 const items=[];

 // Nothing shows until the formation actually enters a valid link area.
 if(members.length>1){
   const normalLinkDamage=members.reduce((sum,p)=>sum+buffedNormalDamage(p).damage,0);
   items.push({
     text:`LINK ATTACK ×${members.length} · ${normalLinkDamage} DMG`,
     tone:'link'
   });
 }

 // Buff currently RECEIVED by the active fighter.
 const incoming=totalLinkAttackBuff(activePair);
 if(incoming.bonus>0){
   items.push({
     text:`LINK ATK +${Math.round(incoming.bonus*100)}%`,
     tone:'buff'
   });
 }

 // Buff currently PROVIDED by the active fighter to linked allies.
 let outgoing=[];
 S.pairs.forEach(other=>{
   if(other===activePair||!pairAlive(other))return;
   const effect=linkBuffFromProvider(activePair,other);
   if(effect.bonus>0)outgoing.push(effect.bonus);
 });
 if(outgoing.length){
   items.push({
     text:`ALLY ATK +${Math.round(Math.max(...outgoing)*100)}%`,
     tone:'buff'
   });
 }

 // The ticker is intentionally generic so Field Skills / Buddy Skills /
 // debuffs can append their own segments later without adding map clutter.
 return items;
}

function updateTacticalTicker(){
 if(!tacticalTicker)return;

 const items=tacticalTickerItems();
 if(!items.length){
   tacticalTicker.classList.remove('show','linkActive');
   tacticalTicker.innerHTML='';
   return;
 }

 tacticalTicker.innerHTML=items
   .map(item=>`<span class="tickerItem ${item.tone||'info'}">${item.text}</span>`)
   .join('');

 tacticalTicker.classList.add('show');
 tacticalTicker.classList.toggle('linkActive',items.some(item=>item.tone==='link'));
}

function updateUI(){
 updateMeter();
 updateTacticalTicker();

 // Boss-only health bar, driven by live Anubis HP.
 if(bossHpHud&&bossHpFill){
   const boss=S.bossBattle?S.enemies.find(e=>e.boss||e.name==='Anubis'):null;
   if(boss){
     bossHpHud.classList.add('active');
     const ratio=clamp(boss.hp/boss.maxHp,0,1);
     bossHpFill.style.width=`${ratio*100}%`;
     bossHpHud.setAttribute('aria-label',`Anubis health ${boss.hp} of ${boss.maxHp}`)

===== hitbox =====

--- match 1 @ 24609660 ---
eteor_end_scale":42,"post_damage_fade_ms":150,"impact_art_enabled":false,"final_flash":true,"final_flash_yellow_ms":240,"final_flash_white_ms":105,"final_flash_fade_ms":120,"final_flash_mode":"fullscreen_overlay","final_flash_white_hold_ms":220,"final_flash_white_fade_ms":780,"flash_yellow_trigger":"second_to_last_meteor","flash_white_trigger":"final_meteor","meteor_entry_side":"opposite_caster","meteor_entry_top_margin":128,"meteor_entry_horizontal_spread":92},"category":"ultimate","ultimate":true}},"assets":{"art":"art/full_art_cosmic_wish.jpeg","card":"cards/unit_details_cosmic_wish.jpeg","portrait":"portraits/","icon":"icons/","sprites":{"idle":"sprites/runtime/idle/","attack_basic":"sprites/runtime/attack/basic/","recoil":"sprites/runtime/recoil/","jutsu":"sprites/runtime/jutsu/"},"vfx":"vfx/"},"readiness":{"collection":true,"battle":true,"idle":true,"basic_attack":true,"jutsu":true,"recoil":true,"notes":"v0.6.10: Lebee normal attack uses canonical thin forward rectangular ranged hitbox. Jutsu geometry remains independently defined by jutsu_shape.","animation_map":true},"balance":{"status":"provisional","pass":"v0.5.36","notes":"Initial archetype-based values. Tune freely during balance testing."},"presentation":{"idle":{"mode":"source_aligned_four_frame","source":"assets/characters/lebee/sprites/source/lebee_idle_sheet_v0538.jpeg","runtime_path":"assets/characters/lebee/sprites/runtime/idle/","frame_duration_ms":520,"sequence":["frame_01.png","frame_02.png","frame_03.png","frame_04.png","frame_03.png","frame_02.png"],"anchor":{"type":"fixed_lower_body","canvas":[420,420],"x":210,"baseline_y":396},"procedural_bob":false,"procedural_rotation":false}},"animation_standard":{"version":"v0.6.16","canvas":{"width":420,"height":420,"anchor_x":210,"anchor_y":392},"animations":{"idle":{"frames":["sprites/runtime/idle/frame_01.png","sprites/runtime/idle/frame_02.png","sprites/runtime/idle/frame_03.png","sprites/runtime/idle/frame_04.png"],"frame_ms":180,"loop":true,"events":[]},"basic_attack":{"frames":["sprites/runtime/attack/basic/frame_01.png","sprites/runtime/attack/basic/frame_02.png","sprites/runtime/attack/basic/frame_03.png","sprites/runtime/attack/basic/frame_04.png","sprites/runtime/attack/basic/frame_05.png","sprites/runtime/attack/basic/frame_06.png","sprites/runtime/attack/basic/frame_07.png"],"frame_ms":120,"loop":false,"events":[{"frame":6,"event":"release_projectile_or_apply_melee"}]},"recoil":{"frames":["sprites/runtime/recoil/frame_01.png","sprites/runtime/recoil/frame_02.png","sprites/runtime/recoil/frame_03.png","sprites/runtime/recoil/frame_04.png"],"frame_ms":100,"loop":false,"events":[]},"jutsu":{"meteor":{"frames":["sprites/runtime/jutsu/meteor/frame_01.png","sprites/runtime/jutsu/meteor/frame_02.png","sprites/runtime/jutsu/meteor/frame_03.png","sprites/runtime/jutsu/meteor/frame_04.png"],"frame_ms":150,"loop":false,"events":[{"frame":3,"event":"release_jutsu_vfx"}]},"meteor/bridge":{"frames":["sprites/runtime/jutsu/meteor/bridge/frame_01.png","sprites/runtime/jutsu/meteor/bridge/frame_02.png"],"frame_ms":150,"loop":false,"events":[{"frame":1,"event":"release_jutsu_vfx"}]},"meteor/sequence_v0532":{"frames":["sprites/runtime/jutsu

--- match 2 @ 43573421 ---
.34-(.34*t)+bounce,rot:(1-t)*v.rot,lift:17*(1-t)};
     if(t<1){requestAnimationFrame(settle);return}

     S.dragVisual=null;S.anim=null;
     if(cancelMove){
       p.x=origin.x;p.y=origin.y;S.dragOrigin=null;
       S.log=`Move preview canceled — ${front(p).name} returned to the turn-start position.`;
       updateUI();return;
     }
     S.dragOrigin=null;
     settleCollision(p);
     resolvePlayer();
   }
   requestAnimationFrame(settle);
 }else{
   S.dragOrigin=null;
   if(cancelMove){p.x=origin.x;p.y=origin.y;updateUI()}
   else{settleCollision(p);resolvePlayer()}
 }
});

cvs.addEventListener('pointercancel',()=>{
 if(!S.drag)return;
 let p=S.ready?.ref;
 if(p&&S.dragOrigin){p.x=S.dragOrigin.x;p.y=S.dragOrigin.y}
 S.drag=false;S.dragVisual=null;S.dragOrigin=null;
 updateUI();
});

normalBtn.onclick=()=>{
 if(S.phase!=='player'||S.drag||S.anim?.settle||S.ready?.kind!=='pair')return;
 S.action='normal';
 let p=S.ready.ref,u=front(p);
 S.log=`${u.name} selected Basic attack — red hitbox active. Drag to reposition, then release to commit.`;
 updateUI();
};

jutsuBtn.onclick=()=>{
 if(S.phase!=='player'||S.drag||S.anim?.settle||S.ready?.kind!=='pair')return;
 let p=S.ready.ref,u=front(p);
 if(u.chakra<u.jutsuCost){S.log=`${u.name} needs ${u.jutsuCost} chakra for Jutsu.`;updateUI();return;}
 S.action='jutsu';
 S.log=`${u.name} selected ${u.jutsuName||'Jutsu'} — blue hitbox active. Drag to reposition, then release to cast.`;
 updateUI();
};

swapBtn.onclick=()=>{
 if(S.phase!=='player'||S.drag||S.anim?.settle)return;
 let p=S.ready.ref;if(back(p).hp<=0)return;
 p.active=1-p.active;
 if(S.action==='jutsu'&&front(p).chakra<front(p).jutsuCost)S.action='normal';
 {
   let links=S.pairs.filter(other=>other!==p&&pairAlive(other)&&unitsMutuallyLinked(p,other)).length;
   S.log=links
     ? `${front(p).name} moved to the front — currently linked with ${links} allied pair${links===1?'':'s'}.`
     : `${front(p).name} moved to the front — no active combo link.`;
 }
 updateUI();
};

document.getElementById('reset').onclick=()=>{
 invalidateActions();
 const boss=!!S.bossBattle;
 S=boss?freshBoss():fresh();
 S._chargeSince=performance.now();
 setBattleMap(boss?'boss':'level');
 gameStarted=true;
 updateUI();
};
function unitsMutuallyLinked(pairA,pairB){
 if(pairA===pairB || !pairAlive(pairA) || !pairAlive(pairB))return false;
 ensureFront(pairA);ensureFront(pairB);

 let unitA=front(pairA),unitB=front(pairB);
 let bodyA={x:pairA.x,y:pairA.y,r:16};
 let bodyB={x:pairB.x,y:pairB.y,r:16};

 // Always use NORMAL attack geometry for combo-link eligibility.
 let aCoversB=hits(pairA,{...unitA,shape:normalShape()},bodyB);
 let bCoversA=hits(pairB,{...unitB,shape:normalShape()},bodyA);

 return aCoversB && bCoversA
}


function linkBuffFromProvider(providerPair,receiverPair){
 if(!pairAlive(providerPair)||!pairAlive(receiverPair)||providerPair===receiverPair)
   return {bonus:0,ratio:0,provider:null};

 ensureFront(providerPair);ensureFront(receiverPair);
 let provider=front(providerPair);
 if(!provider.linkBuff)return {bonus:0,ratio:0,provider:null};

 // Buff only matters when the two units are currently combo-linked.
 if(!unitsMutuallyLinked(providerPair,receiverPa

--- match 3 @ 43573812 ---
  resolvePlayer();
   }
   requestAnimationFrame(settle);
 }else{
   S.dragOrigin=null;
   if(cancelMove){p.x=origin.x;p.y=origin.y;updateUI()}
   else{settleCollision(p);resolvePlayer()}
 }
});

cvs.addEventListener('pointercancel',()=>{
 if(!S.drag)return;
 let p=S.ready?.ref;
 if(p&&S.dragOrigin){p.x=S.dragOrigin.x;p.y=S.dragOrigin.y}
 S.drag=false;S.dragVisual=null;S.dragOrigin=null;
 updateUI();
});

normalBtn.onclick=()=>{
 if(S.phase!=='player'||S.drag||S.anim?.settle||S.ready?.kind!=='pair')return;
 S.action='normal';
 let p=S.ready.ref,u=front(p);
 S.log=`${u.name} selected Basic attack — red hitbox active. Drag to reposition, then release to commit.`;
 updateUI();
};

jutsuBtn.onclick=()=>{
 if(S.phase!=='player'||S.drag||S.anim?.settle||S.ready?.kind!=='pair')return;
 let p=S.ready.ref,u=front(p);
 if(u.chakra<u.jutsuCost){S.log=`${u.name} needs ${u.jutsuCost} chakra for Jutsu.`;updateUI();return;}
 S.action='jutsu';
 S.log=`${u.name} selected ${u.jutsuName||'Jutsu'} — blue hitbox active. Drag to reposition, then release to cast.`;
 updateUI();
};

swapBtn.onclick=()=>{
 if(S.phase!=='player'||S.drag||S.anim?.settle)return;
 let p=S.ready.ref;if(back(p).hp<=0)return;
 p.active=1-p.active;
 if(S.action==='jutsu'&&front(p).chakra<front(p).jutsuCost)S.action='normal';
 {
   let links=S.pairs.filter(other=>other!==p&&pairAlive(other)&&unitsMutuallyLinked(p,other)).length;
   S.log=links
     ? `${front(p).name} moved to the front — currently linked with ${links} allied pair${links===1?'':'s'}.`
     : `${front(p).name} moved to the front — no active combo link.`;
 }
 updateUI();
};

document.getElementById('reset').onclick=()=>{
 invalidateActions();
 const boss=!!S.bossBattle;
 S=boss?freshBoss():fresh();
 S._chargeSince=performance.now();
 setBattleMap(boss?'boss':'level');
 gameStarted=true;
 updateUI();
};
function unitsMutuallyLinked(pairA,pairB){
 if(pairA===pairB || !pairAlive(pairA) || !pairAlive(pairB))return false;
 ensureFront(pairA);ensureFront(pairB);

 let unitA=front(pairA),unitB=front(pairB);
 let bodyA={x:pairA.x,y:pairA.y,r:16};
 let bodyB={x:pairB.x,y:pairB.y,r:16};

 // Always use NORMAL attack geometry for combo-link eligibility.
 let aCoversB=hits(pairA,{...unitA,shape:normalShape()},bodyB);
 let bCoversA=hits(pairB,{...unitB,shape:normalShape()},bodyA);

 return aCoversB && bCoversA
}


function linkBuffFromProvider(providerPair,receiverPair){
 if(!pairAlive(providerPair)||!pairAlive(receiverPair)||providerPair===receiverPair)
   return {bonus:0,ratio:0,provider:null};

 ensureFront(providerPair);ensureFront(receiverPair);
 let provider=front(providerPair);
 if(!provider.linkBuff)return {bonus:0,ratio:0,provider:null};

 // Buff only matters when the two units are currently combo-linked.
 if(!unitsMutuallyLinked(providerPair,receiverPair))
   return {bonus:0,ratio:0,provider:null};

 let dist=d(providerPair,receiverPair);
 let range=provider.linkBuff.range;
 if(dist>range)return {bonus:0,ratio:0,provider:null};

 // A small "full power" distance keeps the curve from exploding at zero.
 let inner=34;
 let t=1-clamp((dist-inner)/Math.max(1,range-inner),0,1);
 let bonus=provider.linkBuff.minBonus+
           (provider.lin

===== rangeShape =====

===== pointIn =====

===== inShape =====

===== shapeFor =====

===== targetAngle =====

===== aliveEnemies =====

--- match 1 @ 43521334 ---
reshBoss(){
 return {
  time:0,phase:'charge',action:'normal',ready:null,readyStarted:0,_chargeSince:performance.now(),drag:false,dragOrigin:null,dragVisual:null,anim:null,floaters:[],victoryFX:null,jutsuDim:null,
  log:'BOSS BATTLE — Anubis, Judge of Souls.',
  bossBattle:true,battleMode:'boss',
  pairs:buildPlayerPairs(585),
  enemies:[
   {
    name:'Anubis',spriteKey:'Anubis',mark:canonicalUnit('anubis').combat.mark,
    x:240,y:195,r:29,
    hp:canonicalUnit('anubis').stats.hp,maxHp:canonicalUnit('anubis').stats.hp,
    speed:canonicalUnit('anubis').stats.speed,attack:canonicalUnit('anubis').stats.attack,defense:canonicalUnit('anubis').stats.defense,gauge:0,
    shape:canonicalUnit('anubis').combat.basic_shape,
    boss:true,sizeScale:canonicalUnit('anubis').render.battle_size_scale||2.0625
   }
  ]
 };
}

let S=fresh();

const front=p=>p.units[p.active], back=p=>p.units[1-p.active];
const pairAlive=p=>p.units.some(u=>u.hp>0);
const alivePairs=()=>S.pairs.filter(pairAlive);
const aliveEnemies=()=>S.enemies.filter(e=>e.hp>0);
const NORMAL_ATTACK_SHAPE={type:'circle',r:43};
const normalShape=()=>NORMAL_ATTACK_SHAPE;
const attackShape=u=>{
 // Strict separation:
 // NORMAL -> unit's canonical combat.basic_shape
 // JUTSU  -> unit's canonical combat.jutsu_shape
 if(S.action==='jutsu')return u.jutsuShape;
 return u.shape||normalShape();
};
function attackProxy(u){return {...u,shape:attackShape(u)}}

// Ability-tier lookup must live at battle scope because both the ground-field
// pass and the later enemy target-feedback pass use it.
function isUltimateAction(unit){
 if(!unit)return false;
 try{
   // canonicalUnit already resolves both IDs and display names through UNIT_NAME_TO_ID.
   const ability=canonicalUnit(unit.name)?.abilities?.jutsu;
   return !!(ability?.ultimate===true || ability?.category==='ultimate' || ability?.tier==='ultimate');
 }catch(_){
   return false;
 }
}

function isAllySupportJutsu(unit){
 if(!unit)return false;
 try{
   const ability=canonicalUnit(unit.name)?.abilities?.jutsu;
   return ability?.target==='all_allies' || ability?.effect==='heal_full';
 }catch(_){
   return false;
 }
}


function ensureFront(p){
 if(front(p).hp>0)return true;
 if(front(p).hp<=0 && koAnimating(front(p).name))return true;
 if(back(p).hp>0){p.active=1-p.active;return true}
 return false
}
function fxMap(){
 if(!S.anim)S.anim={positions:{}};
 if(!S.anim.unitFX)S.anim.unitFX={};
 return S.anim.unitFX;
}
function unitFX(name){
 return S.anim?.unitFX?.[name]||null;
}
function koAnimating(name){
 let fx=unitFX(name);
 return !!(fx&&fx.kind==='ko'&&(performance.now()-fx.start)<fx.duration);
}
function renderWhileKO(name){
 let fx=unitFX(name);
 return !!(fx&&fx.kind==='ko'&&(performance.now()-fx.start)<fx.duration+90);
}
function startHitReaction(name,dir=1,strong=false){
 fxMap()[name]={kind:'hit',start:performance.now(),duration:strong?230:180,dir:dir||1,strong:!!strong};
}
function startKOReaction(name,dir=1){
 fxMap()[name]={kind:'ko',start:performance.now(),duration:520,dir:dir||1};
}
function pairRenderable(p){
 return pairAlive(p)||renderWhileKO(front(p).name)||renderWhileKO(back(p).name);
}
function handleKO(p,defeatedName,dir=1){
 addFloat(p.x,p.y-

--- match 2 @ 43546473 ---
ancelReady?10.5:hint?9:7.5,
   cancelReady?4.1:hint?3.4:2.7,
   0,0,Math.PI*2
 );
 ctx.fill();

 // The cancel symbol appears ONLY inside the true snap/cancel core.
 if(cancelReady){
   ctx.save();
   ctx.translate(0,-10);
   ctx.rotate(-.55);

   ctx.shadowBlur=0;
   ctx.strokeStyle='rgba(255,255,255,.94)';
   ctx.lineWidth=1.45;
   ctx.beginPath();
   ctx.arc(0,0,4.8,.35,5.55);
   ctx.stroke();

   ctx.beginPath();
   ctx.moveTo(3.9,-3.9);
   ctx.lineTo(7.5,-4.3);
   ctx.lineTo(5.8,-.9);
   ctx.closePath();
   ctx.fillStyle='rgba(255,255,255,.96)';
   ctx.fill();

   ctx.beginPath();
   ctx.arc(0,0,1.05,0,Math.PI*2);
   ctx.fillStyle='rgba(255,255,255,.90)';
   ctx.fill();

   ctx.restore();
 }

 ctx.restore();
}


function startVictorySequence(){
 if(S.victoryFX)return;

 S.victoryFX={
   start:performance.now(),
   textFade:700,
   dimFade:2400,
   maxDim:.55
 };

 S.phase='resolve';
 S.log='Victory — all enemies defeated.';
}

function checkVictoryKillshot(){
 if(!S.victoryFX && !aliveEnemies().length){
   startVictorySequence();
   return true;
 }
 return false;
}

function drawVictoryOverlay(){
 if(!S.victoryFX)return;

 const now=performance.now();
 const elapsed=now-S.victoryFX.start;

 // Battlefield darkening is intentionally slower than the victory text.
 const dimT=clamp(elapsed/S.victoryFX.dimFade,0,1);
 const dimEase=1-Math.pow(1-dimT,3);
 const dimAlpha=S.victoryFX.maxDim*dimEase;

 ctx.save();
 ctx.fillStyle=`rgba(2,5,8,${dimAlpha})`;
 ctx.fillRect(0,0,W,H);

 // VICTORY begins fading immediately on the killshot.
 const textT=clamp(elapsed/S.victoryFX.textFade,0,1);
 const textEase=1-Math.pow(1-textT,3);

 const scale=.92+.08*textEase;

 if(VICTORY_IMAGE.complete && VICTORY_IMAGE.naturalWidth>0){
   const maxW=W*.62;
   const maxH=H*.30;
   const ratio=VICTORY_IMAGE.naturalWidth/VICTORY_IMAGE.naturalHeight;

   let w=maxW;
   let h=w/ratio;
   if(h>maxH){
     h=maxH;
     w=h*ratio;
   }

   ctx.save();
   ctx.globalAlpha=textEase;
   ctx.translate(W/2,H/2-8);
   ctx.scale(scale,scale);
   ctx.shadowColor=`rgba(225,0,0,${.20*textEase})`;
   ctx.shadowBlur=13*textEase;
   ctx.drawImage(VICTORY_IMAGE,-w/2,-h/2,w,h);
   ctx.restore();
 }else{
   // Edge/iOS fallback: victory is never invisible because an image decode lagged.
   ctx.save();
   ctx.globalAlpha=textEase;
   ctx.translate(W/2,H/2-8);
   ctx.scale(scale,scale);
   ctx.textAlign='center';
   ctx.textBaseline='middle';
   ctx.font='64px BambooBrush, Impact, system-ui';
   ctx.fillStyle='#e31820';
   ctx.shadowColor='rgba(0,0,0,.88)';
   ctx.shadowBlur=9;
   ctx.fillText('VICTORY',0,0);
   ctx.restore();
 }

 ctx.restore();
}

function render(){
 try{
 // Start every frame from a known-good canvas state.
 ctx.globalAlpha=1;
 ctx.globalCompositeOperation='source-over';
 ctx.shadowBlur=0;
 updateFacing();
 drawField();
 if(S.jutsuDim){
   const now=performance.now();
   const elapsed=now-S.jutsuDim.start;
   const fadeIn=clamp(elapsed/220,0,1);
   const fadeOut=S.jutsuDim.end?clamp((now-S.jutsuDim.end)/260,0,1):0;
   const a=(S.jutsuDim.alpha||.42)*fadeIn*(1-fadeOut);
   if(a>0){ctx.save();ctx.fillStyle=`rgba(3,2,10,${a})`;ctx.fillRect(0,0,W,H);ctx.restore()}
   if(S.jutsuDim.end&&f

--- match 3 @ 43569953 ---
turn;ensureFront(p);p.gauge+=front(p).speed*.018;if(p.gauge>=100&&(!max||p.gauge>max.g))max={kind:'pair',ref:p,g:p.gauge}});
 S.enemies.forEach(e=>{if(e.hp<=0)return;e.gauge+=e.speed*.018;if(e.gauge>=100&&(!max||e.gauge>max.g))max={kind:'enemy',ref:e,g:e.gauge}});
 // v0.5.11 hard rescue: a charge phase may never strand the battlefield.
 if(!S._chargeSince)S._chargeSince=performance.now();
 if(!max && performance.now()-S._chargeSince>3800){
   const rescuePair=alivePairs().sort((a,b)=>b.gauge-a.gauge)[0];
   if(rescuePair){rescuePair.gauge=100;max={kind:'pair',ref:rescuePair,g:100};S.log='Turn system recovered — player control restored.';}
 }
 if(max){
   S.ready=max;
   S._chargeSince=0;
   S.readyStarted=performance.now();
   S.dragOrigin=null;
   S.phase=max.kind==='pair'?'player':'cpu';
   if(max.kind==='enemy')setTimeout(cpuTurn,350)
 }
}
setInterval(tick,30);
function settleCollision(p){
 const minDist=47;
 const blockers=[...S.pairs.filter(other=>other!==p&&pairAlive(other)),...aliveEnemies()];
 for(let pass=0;pass<3;pass++){
  blockers.forEach(b=>{
   let dx=p.x-b.x,dy=p.y-b.y,len=Math.hypot(dx,dy);
   if(len<minDist){
    if(len<.01){dx=1;dy=0;len=1}
    let push=minDist-len;
    p.x=clamp(p.x+(dx/len)*push,BATTLE_BOUNDS.left,BATTLE_BOUNDS.right);
    p.y=clamp(p.y+(dy/len)*push,BATTLE_BOUNDS.top,BATTLE_BOUNDS.bottom)
   }
  })
 }
}
function inputPoint(ev){
 let r=cvs.getBoundingClientRect();
 return {x:(ev.clientX-r.left)*W/r.width,y:(ev.clientY-r.top)*H/r.height};
}

// ===== v0.5.15 MOBILE INPUT =====
// Battlefield interaction is touch-first: press the active unit, drag to position,
// and release to commit the selected Normal/Jutsu action.
cvs.addEventListener('pointerdown',ev=>{
 if(S.phase!=='player'||S.anim?.settle)return;
 let p=S.ready?.ref,pt=inputPoint(ev);
 if(!p||!pairAlive(p)||d(pt,p)>UNIT_TOUCH_RADIUS)return;
 ev.preventDefault();
 S.drag=true;
 S.dragOrigin={x:p.x,y:p.y};
 S.dragVisual={scale:1.0,rot:0,lift:0,lastX:pt.x,lastY:pt.y,targetScale:1.34,targetLift:17};
 try{cvs.setPointerCapture(ev.pointerId)}catch(_e){}
});

cvs.addEventListener('pointermove',ev=>{
 if(!S.drag)return;
 ev.preventDefault();
 let p=S.ready.ref,pt=inputPoint(ev),v=S.dragVisual;
 let dx=pt.x-v.lastX,dy=pt.y-v.lastY;
 let targetRot=clamp(dx*.022,-.34,.34);
 v.rot=clamp(v.rot*.58 + targetRot*.72,-.36,.36);
 let flutter=clamp(Math.abs(dy)*.08,0,2.2);
 v.scale+=(v.targetScale-v.scale)*.32;
 v.lift+=(v.targetLift+flutter-v.lift)*.30;
 v.lastX=pt.x;v.lastY=pt.y;
 let legal=clampToBattlefield(pt);
 p.x=legal.x;p.y=legal.y;
});

cvs.addEventListener('pointerup',ev=>{
 if(!S.drag)return;
 ev.preventDefault();
 let p=S.ready.ref;
 let v=S.dragVisual;
 let origin=S.dragOrigin?{...S.dragOrigin}:{x:p.x,y:p.y};
 let cancelMove=d(p,origin)<=MOVE_CANCEL_RADIUS;
 S.drag=false;
 try{if(cvs.hasPointerCapture?.(ev.pointerId))cvs.releasePointerCapture(ev.pointerId)}catch(_e){}

 if(v){
   const releasePos={x:p.x,y:p.y};
   const settleDur=cancelMove?150:180;
   S.anim={positions:{},settle:{pair:p,start:performance.now(),dur:settleDur}};

   function settle(n){
     if(!S.anim?.settle){S.dragVisual=null;return}
     let t=Math.min(1,(n-S.anim.settle.start)/S.anim.settle.dur);

--- match 4 @ 43577859 ---
itself is determined only by ally-to-ally range linking.
 if(!pairAlive(initiatorPair))return [];
 ensureFront(initiatorPair);

 return S.pairs.filter(pair=>{
   if(!pairAlive(pair))return false;
   ensureFront(pair);
   return pair===initiatorPair || unitsMutuallyLinked(initiatorPair,pair)
 })
}
let ACTION_WATCHDOG=null;
let ACTION_SEQUENCE=0;
let ACTIVE_ACTION_TOKEN=0;
function clearActionWatchdog(){
 if(ACTION_WATCHDOG){clearTimeout(ACTION_WATCHDOG);ACTION_WATCHDOG=null}
}
function beginActionToken(){
 clearActionWatchdog();
 ACTIVE_ACTION_TOKEN=++ACTION_SEQUENCE;
 return ACTIVE_ACTION_TOKEN;
}
function invalidateActions(){
 clearActionWatchdog();
 ACTIVE_ACTION_TOKEN=++ACTION_SEQUENCE;
}
function actionTokenAlive(token){return token===ACTIVE_ACTION_TOKEN}
function recoverAction(reason='animation timeout'){
 console.warn('Blazing Battle recovered action:',reason);
 invalidateActions();
 S.anim=null;S.drag=false;S.dragVisual=null;S.dragOrigin=null;S.action='normal';S.ready=null;
 if(aliveEnemies().length&&alivePairs().length){S.phase='charge';S._chargeSince=performance.now();S.log='Action recovered — turn meter resumed.'}
}
function armActionWatchdog(reason='combat action',token=ACTIVE_ACTION_TOKEN){
 clearActionWatchdog();
 ACTION_WATCHDOG=setTimeout(()=>{
   if(actionTokenAlive(token)&&gameStarted&&(S.phase==='resolve'||S.phase==='cpu'))recoverAction(reason);
 },2600);
}
function ensureAnimState(){
 if(!S.anim)S.anim={positions:{}};
 if(!S.anim.positions)S.anim.positions={};
 return S.anim;
}
function animateLunge(unitName,from,to,onImpact,onDone,attackKind='punch'){
 const token=ACTIVE_ACTION_TOKEN;
 let start=performance.now(),dur=175,backDur=145;
 let anim=ensureAnimState();
 if(!anim.attackPose)anim.attackPose={};
 anim.attackPose[unitName]={kind:attackKind,start,duration:dur+150};
 function go(now){
  if(!actionTokenAlive(token))return;
  let state=ensureAnimState();
  let t=Math.min(1,(now-start)/dur),ease=1-Math.pow(1-t,3);
  state.positions[unitName]={x:from.x+(to.x-from.x)*ease,y:from.y+(to.y-from.y)*ease};
  if(t<1)return requestAnimationFrame(go);
  try{onImpact&&onImpact()}catch(err){console.error('Attack impact callback failed:',err);return recoverAction('impact callback')}
  setTimeout(()=>{
   if(!actionTokenAlive(token))return;
   let bs=performance.now();
   function ret(n){
    if(!actionTokenAlive(token))return;
    let st=ensureAnimState();
    let q=Math.min(1,(n-bs)/backDur),e=1-Math.pow(1-q,3);
    st.positions[unitName]={x:to.x+(from.x-to.x)*e,y:to.y+(from.y-to.y)*e};
    if(q<1)return requestAnimationFrame(ret);
    delete st.positions[unitName];
    if(st.attackPose)delete st.attackPose[unitName];
    try{onDone&&onDone()}catch(err){console.error('Attack completion callback failed:',err);recoverAction('completion callback')}
   }
   requestAnimationFrame(ret)
  },65)
 }
 requestAnimationFrame(go)
}
function addImpactFlash(x,y,color='#ffffff'){
 let f={x,y,text:'✦',color,life:1,impact:true};
 S.floaters.push(f);
 let id=setInterval(()=>{
   f.y-=.25;
   f.life-=.07;
   if(f.life<=0){
     clearInterval(id);
     S.floaters=S.floaters.filter(item=>item!==f);
   }
 },16);
}
function animateFreezeBlast(unitName,from,enemy,onImpa

--- match 5 @ 43592332 ---

       if(!actionTokenAlive(token))return;
       try{
         onDone&&onDone();
       }catch(err){
         recoverAction('Lebee Meteor completion');
       }
     });
   },finalImpact);

 },castDuration);
}

function recoil(enemy,done,dir=1,strong=false){
 const token=ACTIVE_ACTION_TOKEN;
 startHitReaction(enemy.name,dir,strong);
 let ox=enemy.x,oy=enemy.y,dx=18*(dir||1),start=performance.now();
 function f(n){
  if(!actionTokenAlive(token))return;
  let state=ensureAnimState(),t=Math.min(1,(n-start)/130),off=Math.sin(t*Math.PI)*dx;
  state.positions[enemy.name]={x:ox+off,y:oy};
  if(t<1)return requestAnimationFrame(f);
  delete state.positions[enemy.name];
  try{done&&done()}catch(err){console.error('Recoil callback failed:',err);recoverAction('recoil callback')}
 }
 requestAnimationFrame(f)
}
function resolvePlayer(){

 let p=S.ready.ref,u=front(p),useJutsu=S.action==='jutsu',proxy=attackProxy(u);
 const supportJutsu=useJutsu&&isAllySupportJutsu(u);
 let targets=supportJutsu?[]:aliveEnemies().filter(e=>hits(p,proxy,e));
 p.gauge=0;S.phase='resolve';S.anim={positions:{}};const playerActionToken=beginActionToken();armActionWatchdog('player attack',playerActionToken);

 if(useJutsu&&u.chakra<u.jutsuCost){
  S.log=`${u.name} does not have enough chakra.`;S.action='normal';return finishAction()
 }

 if(supportJutsu){
  u.chakra-=u.jutsuCost;

  const allies=alivePairs().filter(pair=>front(pair).hp>0);
  if(!allies.length){
   S.log=`${u.name} activated ${u.jutsuName||'Jutsu'}, but no allies can be healed.`;
   return finishAction();
  }

  const teamCenter={
   x:allies.reduce((sum,pair)=>sum+pair.x,0)/allies.length,
   y:allies.reduce((sum,pair)=>sum+pair.y,0)/allies.length-18
  };

  const from={x:p.x,y:p.y};
  S.log=`${u.name} activated ${u.jutsuName||'Revival Formula'} — restoring all active allies to full HP.`;

  return animateSenkuChemicalReaction(
   u.name,from,teamCenter,
   ()=>{
    allies.forEach(pair=>{
     const ally=front(pair);
     const healed=Math.max(0,ally.maxHp-ally.hp);
     ally.hp=ally.maxHp;

     addImpactFlash(pair.x,pair.y-18,'#64ff7d');
     if(healed>0){
      addFloat(pair.x,pair.y-40,`+${healed}`,'#74ff91');
     }else{
      addFloat(pair.x,pair.y-40,'FULL','#74ff91');
     }
    });
   },
   ()=>finishAction()
  );
 }

 if(!targets.length){
  S.log=`${u.name} committed the move but caught no target.`;return finishAction()
 }

 if(useJutsu){
  u.chakra-=u.jutsuCost;

  if(u.name==='Lebee'){
   S.log=`Lebee activated Meteor Jutsu — ${targets.length} target${targets.length===1?'':'s'} caught in the cosmic impact zone.`;
   const from={x:p.x,y:p.y};
   return animateLebeeMeteorJutsu(
    u.name,from,targets,
    ()=>{
     targets.forEach(enemy=>{
      if(enemy.hp<=0)return;
      enemy.hp=Math.max(0,enemy.hp-u.jutsuDamage);
      addImpactFlash(enemy.x,enemy.y-8,'#ff7ae6');
      addFloat(enemy.x,enemy.y-34,'-'+u.jutsuDamage,'#ff8be8');
      const dir=Math.sign(enemy.x-p.x)||1;
      if(enemy.hp<=0)handleEnemyKO(enemy,dir); else recoil(enemy,()=>{},dir,true);
     });
     checkVictoryKillshot();
    },
    ()=>finishAction()
   );
  }

  // Every Jutsu target gets:
  //   active fighter's Jutsu hit
  //   + normal a

--- match 6 @ 43599606 ---
ge(ap),dmg=result.damage;
    enemy.hp=Math.max(0,enemy.hp-dmg);
    addImpactFlash(enemy.x,enemy.y-8,au.name==='Crimson'?'#ff405c':attackIndex>1?'#ffbd4a':'#ffffff');
    addFloat(enemy.x,enemy.y-34,'-'+dmg,result.buff.bonus>0?'#65ff9e':attackIndex>1?'#ffbd4a':'#fff');
    let nDir=Math.sign(enemy.x-ap.x)||1;
    if(enemy.hp<=0)handleEnemyKO(enemy,nDir); else recoil(enemy,()=>{},nDir,attackIndex>1);
    checkVictoryKillshot();
   },()=>setTimeout(runAttacker,85),attackIndex%2===1?'punch':'kick')
  }
  runAttacker()
 }
 runTarget()
}

function finishAction(){
 const token=ACTIVE_ACTION_TOKEN;
 clearActionWatchdog();
 setTimeout(()=>{
  if(!actionTokenAlive(token))return;
  // v0.5.10: retire the completed action token BEFORE clearing S.anim.
  // This prevents late recoil/requestAnimationFrame callbacks from recreating
  // S.anim after the turn has returned to charge, which caused the hard freeze.
  invalidateActions();
  S.anim=null;S.ready=null;S.action='normal';
  if(S.victoryFX||!aliveEnemies().length){
   if(!S.victoryFX)startVictorySequence();
   S.phase='resolve';
   return;
  }
  if(!alivePairs().length){
   S.phase='resolve';
   S.log='Defeat — all three fighter pairs have been knocked out.';
   return;
  }
  S.phase='charge';S._chargeSince=performance.now();
 },220)
}

function enemiesMutuallyLinked(a,b){
 if(a===b || a.hp<=0 || b.hp<=0)return false;

 let bodyA={x:a.x,y:a.y,r:a.r||16};
 let bodyB={x:b.x,y:b.y,r:b.r||16};

 let aCoversB=hits(a,{shape:normalShape(),rotation:0},bodyB);
 let bCoversA=hits(b,{shape:normalShape(),rotation:0},bodyA);

 return aCoversB && bCoversA
}

function enemyComboMembers(targetPair,initiator){
 // Target is no longer part of combo eligibility.
 // Once linked, the helper may join the initiator's valid attack target.
 return S.enemies.filter(enemy=>{
   if(enemy.hp<=0)return false;
   return enemy===initiator || enemiesMutuallyLinked(initiator,enemy)
 })
}

function resolveEnemyOverlap(enemy){
 const minDist=47;

 // Resolve only the enemy that just moved. This intentionally does not
 // mutate player positions or other enemy positions.
 for(let pass=0;pass<8;pass++){
   let blocker=null;
   let blockerDist=Infinity;

   S.pairs.forEach(pair=>{
     if(!pairAlive(pair))return;
     let dist=d(enemy,pair);
     if(dist<minDist && dist<blockerDist){
       blocker=pair;
       blockerDist=dist;
     }
   });

   S.enemies.forEach(other=>{
     if(other===enemy||other.hp<=0)return;
     let dist=d(enemy,other);
     if(dist<minDist && dist<blockerDist){
       blocker=other;
       blockerDist=dist;
     }
   });

   if(!blocker)break;

   let dx=enemy.x-blocker.x;
   let dy=enemy.y-blocker.y;
   let len=Math.hypot(dx,dy);

   // Exact same-center fallback gets a deterministic escape direction.
   if(len<.01){
     let angle=(pass+1)*1.913;
     dx=Math.cos(angle);
     dy=Math.sin(angle);
     len=1;
   }

   let push=(minDist-len)+.75;
   enemy.x+=(dx/len)*push;
   enemy.y+=(dy/len)*push;

   let legal=clampToBattlefield(enemy);
   enemy.x=legal.x;
   enemy.y=legal.y;
 }
}

function cpuTurn(){
 if(S.phase!=='cpu')return;
 const cpuActionToken=beginActionToken();
 armActionWatchdog('Anubis / enemy turn',cpuActionT

===== alivePairs =====

--- match 1 @ 43521286 ---
:{type:'cross',l:160,t:48}})
  ]
 }}

function freshBoss(){
 return {
  time:0,phase:'charge',action:'normal',ready:null,readyStarted:0,_chargeSince:performance.now(),drag:false,dragOrigin:null,dragVisual:null,anim:null,floaters:[],victoryFX:null,jutsuDim:null,
  log:'BOSS BATTLE — Anubis, Judge of Souls.',
  bossBattle:true,battleMode:'boss',
  pairs:buildPlayerPairs(585),
  enemies:[
   {
    name:'Anubis',spriteKey:'Anubis',mark:canonicalUnit('anubis').combat.mark,
    x:240,y:195,r:29,
    hp:canonicalUnit('anubis').stats.hp,maxHp:canonicalUnit('anubis').stats.hp,
    speed:canonicalUnit('anubis').stats.speed,attack:canonicalUnit('anubis').stats.attack,defense:canonicalUnit('anubis').stats.defense,gauge:0,
    shape:canonicalUnit('anubis').combat.basic_shape,
    boss:true,sizeScale:canonicalUnit('anubis').render.battle_size_scale||2.0625
   }
  ]
 };
}

let S=fresh();

const front=p=>p.units[p.active], back=p=>p.units[1-p.active];
const pairAlive=p=>p.units.some(u=>u.hp>0);
const alivePairs=()=>S.pairs.filter(pairAlive);
const aliveEnemies=()=>S.enemies.filter(e=>e.hp>0);
const NORMAL_ATTACK_SHAPE={type:'circle',r:43};
const normalShape=()=>NORMAL_ATTACK_SHAPE;
const attackShape=u=>{
 // Strict separation:
 // NORMAL -> unit's canonical combat.basic_shape
 // JUTSU  -> unit's canonical combat.jutsu_shape
 if(S.action==='jutsu')return u.jutsuShape;
 return u.shape||normalShape();
};
function attackProxy(u){return {...u,shape:attackShape(u)}}

// Ability-tier lookup must live at battle scope because both the ground-field
// pass and the later enemy target-feedback pass use it.
function isUltimateAction(unit){
 if(!unit)return false;
 try{
   // canonicalUnit already resolves both IDs and display names through UNIT_NAME_TO_ID.
   const ability=canonicalUnit(unit.name)?.abilities?.jutsu;
   return !!(ability?.ultimate===true || ability?.category==='ultimate' || ability?.tier==='ultimate');
 }catch(_){
   return false;
 }
}

function isAllySupportJutsu(unit){
 if(!unit)return false;
 try{
   const ability=canonicalUnit(unit.name)?.abilities?.jutsu;
   return ability?.target==='all_allies' || ability?.effect==='heal_full';
 }catch(_){
   return false;
 }
}


function ensureFront(p){
 if(front(p).hp>0)return true;
 if(front(p).hp<=0 && koAnimating(front(p).name))return true;
 if(back(p).hp>0){p.active=1-p.active;return true}
 return false
}
function fxMap(){
 if(!S.anim)S.anim={positions:{}};
 if(!S.anim.unitFX)S.anim.unitFX={};
 return S.anim.unitFX;
}
function unitFX(name){
 return S.anim?.unitFX?.[name]||null;
}
function koAnimating(name){
 let fx=unitFX(name);
 return !!(fx&&fx.kind==='ko'&&(performance.now()-fx.start)<fx.duration);
}
function renderWhileKO(name){
 let fx=unitFX(name);
 return !!(fx&&fx.kind==='ko'&&(performance.now()-fx.start)<fx.duration+90);
}
function startHitReaction(name,dir=1,strong=false){
 fxMap()[name]={kind:'hit',start:performance.now(),duration:strong?230:180,dir:dir||1,strong:!!strong};
}
function startKOReaction(name,dir=1){
 fxMap()[name]={kind:'ko',start:performance.now(),duration:520,dir:dir||1};
}
function pairRenderable(p){
 return pairAlive(p)||renderWhileKO(front(p).name)||renderWhileKO(back(p).name);
}
function 

--- match 2 @ 43569418 ---
).join('')
}

function addFloat(x,y,text,color){S.floaters.push({x,y,text,color,life:1});let f=S.floaters.at(-1);let id=setInterval(()=>{f.y-=1;f.life-=.025;if(f.life<=0){clearInterval(id);S.floaters=S.floaters.filter(x=>x!==f)}},16)}
function tick(){
 if(!gameStarted||S.phase!=='charge')return;
 // v0.5.10 stability: charge is a non-animation state. A stale render animation
 // must never stop the turn meter. Clear it defensively instead of freezing.
 if(S.anim)S.anim=null;
 let max=null;
 S.pairs.forEach(p=>{if(!pairAlive(p))return;ensureFront(p);p.gauge+=front(p).speed*.018;if(p.gauge>=100&&(!max||p.gauge>max.g))max={kind:'pair',ref:p,g:p.gauge}});
 S.enemies.forEach(e=>{if(e.hp<=0)return;e.gauge+=e.speed*.018;if(e.gauge>=100&&(!max||e.gauge>max.g))max={kind:'enemy',ref:e,g:e.gauge}});
 // v0.5.11 hard rescue: a charge phase may never strand the battlefield.
 if(!S._chargeSince)S._chargeSince=performance.now();
 if(!max && performance.now()-S._chargeSince>3800){
   const rescuePair=alivePairs().sort((a,b)=>b.gauge-a.gauge)[0];
   if(rescuePair){rescuePair.gauge=100;max={kind:'pair',ref:rescuePair,g:100};S.log='Turn system recovered — player control restored.';}
 }
 if(max){
   S.ready=max;
   S._chargeSince=0;
   S.readyStarted=performance.now();
   S.dragOrigin=null;
   S.phase=max.kind==='pair'?'player':'cpu';
   if(max.kind==='enemy')setTimeout(cpuTurn,350)
 }
}
setInterval(tick,30);
function settleCollision(p){
 const minDist=47;
 const blockers=[...S.pairs.filter(other=>other!==p&&pairAlive(other)),...aliveEnemies()];
 for(let pass=0;pass<3;pass++){
  blockers.forEach(b=>{
   let dx=p.x-b.x,dy=p.y-b.y,len=Math.hypot(dx,dy);
   if(len<minDist){
    if(len<.01){dx=1;dy=0;len=1}
    let push=minDist-len;
    p.x=clamp(p.x+(dx/len)*push,BATTLE_BOUNDS.left,BATTLE_BOUNDS.right);
    p.y=clamp(p.y+(dy/len)*push,BATTLE_BOUNDS.top,BATTLE_BOUNDS.bottom)
   }
  })
 }
}
function inputPoint(ev){
 let r=cvs.getBoundingClientRect();
 return {x:(ev.clientX-r.left)*W/r.width,y:(ev.clientY-r.top)*H/r.height};
}

// ===== v0.5.15 MOBILE INPUT =====
// Battlefield interaction is touch-first: press the active unit, drag to position,
// and release to commit the selected Normal/Jutsu action.
cvs.addEventListener('pointerdown',ev=>{
 if(S.phase!=='player'||S.anim?.settle)return;
 let p=S.ready?.ref,pt=inputPoint(ev);
 if(!p||!pairAlive(p)||d(pt,p)>UNIT_TOUCH_RADIUS)return;
 ev.preventDefault();
 S.drag=true;
 S.dragOrigin={x:p.x,y:p.y};
 S.dragVisual={scale:1.0,rot:0,lift:0,lastX:pt.x,lastY:pt.y,targetScale:1.34,targetLift:17};
 try{cvs.setPointerCapture(ev.pointerId)}catch(_e){}
});

cvs.addEventListener('pointermove',ev=>{
 if(!S.drag)return;
 ev.preventDefault();
 let p=S.ready.ref,pt=inputPoint(ev),v=S.dragVisual;
 let dx=pt.x-v.lastX,dy=pt.y-v.lastY;
 let targetRot=clamp(dx*.022,-.34,.34);
 v.rot=clamp(v.rot*.58 + targetRot*.72,-.36,.36);
 let flutter=clamp(Math.abs(dy)*.08,0,2.2);
 v.scale+=(v.targetScale-v.scale)*.32;
 v.lift+=(v.targetLift+flutter-v.lift)*.30;
 v.lastX=pt.x;v.lastY=pt.y;
 let legal=clampToBattlefield(pt);
 p.x=legal.x;p.y=legal.y;
});

cvs.addEventListener('pointerup',ev=>{
 if(!S.drag)return;
 ev.preventDefault();
 let p=S.ready.ref;
 let v=

--- match 3 @ 43577882 ---
ly by ally-to-ally range linking.
 if(!pairAlive(initiatorPair))return [];
 ensureFront(initiatorPair);

 return S.pairs.filter(pair=>{
   if(!pairAlive(pair))return false;
   ensureFront(pair);
   return pair===initiatorPair || unitsMutuallyLinked(initiatorPair,pair)
 })
}
let ACTION_WATCHDOG=null;
let ACTION_SEQUENCE=0;
let ACTIVE_ACTION_TOKEN=0;
function clearActionWatchdog(){
 if(ACTION_WATCHDOG){clearTimeout(ACTION_WATCHDOG);ACTION_WATCHDOG=null}
}
function beginActionToken(){
 clearActionWatchdog();
 ACTIVE_ACTION_TOKEN=++ACTION_SEQUENCE;
 return ACTIVE_ACTION_TOKEN;
}
function invalidateActions(){
 clearActionWatchdog();
 ACTIVE_ACTION_TOKEN=++ACTION_SEQUENCE;
}
function actionTokenAlive(token){return token===ACTIVE_ACTION_TOKEN}
function recoverAction(reason='animation timeout'){
 console.warn('Blazing Battle recovered action:',reason);
 invalidateActions();
 S.anim=null;S.drag=false;S.dragVisual=null;S.dragOrigin=null;S.action='normal';S.ready=null;
 if(aliveEnemies().length&&alivePairs().length){S.phase='charge';S._chargeSince=performance.now();S.log='Action recovered — turn meter resumed.'}
}
function armActionWatchdog(reason='combat action',token=ACTIVE_ACTION_TOKEN){
 clearActionWatchdog();
 ACTION_WATCHDOG=setTimeout(()=>{
   if(actionTokenAlive(token)&&gameStarted&&(S.phase==='resolve'||S.phase==='cpu'))recoverAction(reason);
 },2600);
}
function ensureAnimState(){
 if(!S.anim)S.anim={positions:{}};
 if(!S.anim.positions)S.anim.positions={};
 return S.anim;
}
function animateLunge(unitName,from,to,onImpact,onDone,attackKind='punch'){
 const token=ACTIVE_ACTION_TOKEN;
 let start=performance.now(),dur=175,backDur=145;
 let anim=ensureAnimState();
 if(!anim.attackPose)anim.attackPose={};
 anim.attackPose[unitName]={kind:attackKind,start,duration:dur+150};
 function go(now){
  if(!actionTokenAlive(token))return;
  let state=ensureAnimState();
  let t=Math.min(1,(now-start)/dur),ease=1-Math.pow(1-t,3);
  state.positions[unitName]={x:from.x+(to.x-from.x)*ease,y:from.y+(to.y-from.y)*ease};
  if(t<1)return requestAnimationFrame(go);
  try{onImpact&&onImpact()}catch(err){console.error('Attack impact callback failed:',err);return recoverAction('impact callback')}
  setTimeout(()=>{
   if(!actionTokenAlive(token))return;
   let bs=performance.now();
   function ret(n){
    if(!actionTokenAlive(token))return;
    let st=ensureAnimState();
    let q=Math.min(1,(n-bs)/backDur),e=1-Math.pow(1-q,3);
    st.positions[unitName]={x:to.x+(from.x-to.x)*e,y:to.y+(from.y-to.y)*e};
    if(q<1)return requestAnimationFrame(ret);
    delete st.positions[unitName];
    if(st.attackPose)delete st.attackPose[unitName];
    try{onDone&&onDone()}catch(err){console.error('Attack completion callback failed:',err);recoverAction('completion callback')}
   }
   requestAnimationFrame(ret)
  },65)
 }
 requestAnimationFrame(go)
}
function addImpactFlash(x,y,color='#ffffff'){
 let f={x,y,text:'✦',color,life:1,impact:true};
 S.floaters.push(f);
 let id=setInterval(()=>{
   f.y-=.25;
   f.life-=.07;
   if(f.life<=0){
     clearInterval(id);
     S.floaters=S.floaters.filter(item=>item!==f);
   }
 },16);
}
function animateFreezeBlast(unitName,from,enemy,onImpact,onDone){
 const to

--- match 4 @ 43592714 ---
rt=performance.now();
 function f(n){
  if(!actionTokenAlive(token))return;
  let state=ensureAnimState(),t=Math.min(1,(n-start)/130),off=Math.sin(t*Math.PI)*dx;
  state.positions[enemy.name]={x:ox+off,y:oy};
  if(t<1)return requestAnimationFrame(f);
  delete state.positions[enemy.name];
  try{done&&done()}catch(err){console.error('Recoil callback failed:',err);recoverAction('recoil callback')}
 }
 requestAnimationFrame(f)
}
function resolvePlayer(){

 let p=S.ready.ref,u=front(p),useJutsu=S.action==='jutsu',proxy=attackProxy(u);
 const supportJutsu=useJutsu&&isAllySupportJutsu(u);
 let targets=supportJutsu?[]:aliveEnemies().filter(e=>hits(p,proxy,e));
 p.gauge=0;S.phase='resolve';S.anim={positions:{}};const playerActionToken=beginActionToken();armActionWatchdog('player attack',playerActionToken);

 if(useJutsu&&u.chakra<u.jutsuCost){
  S.log=`${u.name} does not have enough chakra.`;S.action='normal';return finishAction()
 }

 if(supportJutsu){
  u.chakra-=u.jutsuCost;

  const allies=alivePairs().filter(pair=>front(pair).hp>0);
  if(!allies.length){
   S.log=`${u.name} activated ${u.jutsuName||'Jutsu'}, but no allies can be healed.`;
   return finishAction();
  }

  const teamCenter={
   x:allies.reduce((sum,pair)=>sum+pair.x,0)/allies.length,
   y:allies.reduce((sum,pair)=>sum+pair.y,0)/allies.length-18
  };

  const from={x:p.x,y:p.y};
  S.log=`${u.name} activated ${u.jutsuName||'Revival Formula'} — restoring all active allies to full HP.`;

  return animateSenkuChemicalReaction(
   u.name,from,teamCenter,
   ()=>{
    allies.forEach(pair=>{
     const ally=front(pair);
     const healed=Math.max(0,ally.maxHp-ally.hp);
     ally.hp=ally.maxHp;

     addImpactFlash(pair.x,pair.y-18,'#64ff7d');
     if(healed>0){
      addFloat(pair.x,pair.y-40,`+${healed}`,'#74ff91');
     }else{
      addFloat(pair.x,pair.y-40,'FULL','#74ff91');
     }
    });
   },
   ()=>finishAction()
  );
 }

 if(!targets.length){
  S.log=`${u.name} committed the move but caught no target.`;return finishAction()
 }

 if(useJutsu){
  u.chakra-=u.jutsuCost;

  if(u.name==='Lebee'){
   S.log=`Lebee activated Meteor Jutsu — ${targets.length} target${targets.length===1?'':'s'} caught in the cosmic impact zone.`;
   const from={x:p.x,y:p.y};
   return animateLebeeMeteorJutsu(
    u.name,from,targets,
    ()=>{
     targets.forEach(enemy=>{
      if(enemy.hp<=0)return;
      enemy.hp=Math.max(0,enemy.hp-u.jutsuDamage);
      addImpactFlash(enemy.x,enemy.y-8,'#ff7ae6');
      addFloat(enemy.x,enemy.y-34,'-'+u.jutsuDamage,'#ff8be8');
      const dir=Math.sign(enemy.x-p.x)||1;
      if(enemy.hp<=0)handleEnemyKO(enemy,dir); else recoil(enemy,()=>{},dir,true);
     });
     checkVictoryKillshot();
    },
    ()=>finishAction()
   );
  }

  // Every Jutsu target gets:
  //   active fighter's Jutsu hit
  //   + normal attacks from OTHER allies whose normal range covers that target.
  let jutsuQueue=targets.map(enemy=>{
    let helpers=comboMembers(enemy,p).filter(helper=>helper!==p);
    return {enemy,helpers};
  });

  let anyHelpers=jutsuQueue.some(item=>item.helpers.length>0);
  S.log=anyHelpers
    ? `${u.name} activated ${u.jutsuName||'Jutsu'} — normal combo partners are joining where th

--- match 5 @ 43599716 ---
=='Crimson'?'#ff405c':attackIndex>1?'#ffbd4a':'#ffffff');
    addFloat(enemy.x,enemy.y-34,'-'+dmg,result.buff.bonus>0?'#65ff9e':attackIndex>1?'#ffbd4a':'#fff');
    let nDir=Math.sign(enemy.x-ap.x)||1;
    if(enemy.hp<=0)handleEnemyKO(enemy,nDir); else recoil(enemy,()=>{},nDir,attackIndex>1);
    checkVictoryKillshot();
   },()=>setTimeout(runAttacker,85),attackIndex%2===1?'punch':'kick')
  }
  runAttacker()
 }
 runTarget()
}

function finishAction(){
 const token=ACTIVE_ACTION_TOKEN;
 clearActionWatchdog();
 setTimeout(()=>{
  if(!actionTokenAlive(token))return;
  // v0.5.10: retire the completed action token BEFORE clearing S.anim.
  // This prevents late recoil/requestAnimationFrame callbacks from recreating
  // S.anim after the turn has returned to charge, which caused the hard freeze.
  invalidateActions();
  S.anim=null;S.ready=null;S.action='normal';
  if(S.victoryFX||!aliveEnemies().length){
   if(!S.victoryFX)startVictorySequence();
   S.phase='resolve';
   return;
  }
  if(!alivePairs().length){
   S.phase='resolve';
   S.log='Defeat — all three fighter pairs have been knocked out.';
   return;
  }
  S.phase='charge';S._chargeSince=performance.now();
 },220)
}

function enemiesMutuallyLinked(a,b){
 if(a===b || a.hp<=0 || b.hp<=0)return false;

 let bodyA={x:a.x,y:a.y,r:a.r||16};
 let bodyB={x:b.x,y:b.y,r:b.r||16};

 let aCoversB=hits(a,{shape:normalShape(),rotation:0},bodyB);
 let bCoversA=hits(b,{shape:normalShape(),rotation:0},bodyA);

 return aCoversB && bCoversA
}

function enemyComboMembers(targetPair,initiator){
 // Target is no longer part of combo eligibility.
 // Once linked, the helper may join the initiator's valid attack target.
 return S.enemies.filter(enemy=>{
   if(enemy.hp<=0)return false;
   return enemy===initiator || enemiesMutuallyLinked(initiator,enemy)
 })
}

function resolveEnemyOverlap(enemy){
 const minDist=47;

 // Resolve only the enemy that just moved. This intentionally does not
 // mutate player positions or other enemy positions.
 for(let pass=0;pass<8;pass++){
   let blocker=null;
   let blockerDist=Infinity;

   S.pairs.forEach(pair=>{
     if(!pairAlive(pair))return;
     let dist=d(enemy,pair);
     if(dist<minDist && dist<blockerDist){
       blocker=pair;
       blockerDist=dist;
     }
   });

   S.enemies.forEach(other=>{
     if(other===enemy||other.hp<=0)return;
     let dist=d(enemy,other);
     if(dist<minDist && dist<blockerDist){
       blocker=other;
       blockerDist=dist;
     }
   });

   if(!blocker)break;

   let dx=enemy.x-blocker.x;
   let dy=enemy.y-blocker.y;
   let len=Math.hypot(dx,dy);

   // Exact same-center fallback gets a deterministic escape direction.
   if(len<.01){
     let angle=(pass+1)*1.913;
     dx=Math.cos(angle);
     dy=Math.sin(angle);
     len=1;
   }

   let push=(minDist-len)+.75;
   enemy.x+=(dx/len)*push;
   enemy.y+=(dy/len)*push;

   let legal=clampToBattlefield(enemy);
   enemy.x=legal.x;
   enemy.y=legal.y;
 }
}

function cpuTurn(){
 if(S.phase!=='cpu')return;
 const cpuActionToken=beginActionToken();
 armActionWatchdog('Anubis / enemy turn',cpuActionToken);
 let e=S.ready.ref;
 if(e.hp<=0){e.gauge=0;return finishAction()}

 let candidates=S.pairs.filter(pai

--- match 6 @ 43634600 ---
ize every battle from its canonical state. This intentionally
   // ignores stale gameStarted values left by another screen/mode.
   invalidateActions();
   S=boss?freshBoss():fresh();
   S._chargeSince=performance.now();
   setBattleMap(boss?'boss':'level');
   gameStarted=true;

   battleScreen.classList.remove('visible');
   battleScreen.classList.add('active');
   menuScreen.style.display='none';

   requestAnimationFrame(()=>requestAnimationFrame(()=>{
     battleScreen.classList.add('visible');
     menuTransitioning=false;
     updateUI();
   }));
 },300);
}
level1Btn.addEventListener('click',()=>startBattle('level'));
boss1Btn.addEventListener('click',()=>startBattle('boss'));

// v0.5.15 global state watchdog. This is deliberately independent of sprite
// animation so a visual callback can never permanently own the combat state.
setInterval(()=>{
 if(!gameStarted)return;
 if(S.phase==='player'){
   if(!S.ready || S.ready.kind!=='pair' || !pairAlive(S.ready.ref)){
     const p=alivePairs().sort((a,b)=>b.gauge-a.gauge)[0];
     if(p){S.ready={kind:'pair',ref:p,g:p.gauge};S.readyStarted=performance.now();S.anim=null;S.drag=false;S.phase='player';S.log='Player turn recovered.';}
   }
 }else if(S.phase==='charge'){
   if(!S._chargeSince)S._chargeSince=performance.now();
   if(performance.now()-S._chargeSince>5000){
     const p=alivePairs().sort((a,b)=>b.gauge-a.gauge)[0];
     if(p){p.gauge=100;S.ready={kind:'pair',ref:p,g:100};S.readyStarted=performance.now();S._chargeSince=0;S.anim=null;S.phase='player';S.log='Turn meter fallback restored player control.';updateUI();}
   }
 }
},500);

render();
</script>
<script>
/* v0.5.25 inventory/navigation module. Does not touch battle state. */
(()=>{
 const inventoryScreen=document.getElementById('inventoryScreen');
 const teamScreen=document.getElementById('teamScreen');
 const unitCardScreen=document.getElementById('unitCardScreen');
 const artViewScreen=document.getElementById('artViewScreen');
 const mainMenu=document.getElementById('menuScreen');
 const invBtn=document.getElementById('inventoryBtn');
 const owned={
   'Crimson':{
     name:'CRIMSON',title:'BLAZING VANGUARD',element:'🔥 FIRE',stars:'★★★★★★',rarity:'LEGENDARY',
     card:"data:image/png;base64,

--- match 7 @ 43634954 ---
dd('active');
   menuScreen.style.display='none';

   requestAnimationFrame(()=>requestAnimationFrame(()=>{
     battleScreen.classList.add('visible');
     menuTransitioning=false;
     updateUI();
   }));
 },300);
}
level1Btn.addEventListener('click',()=>startBattle('level'));
boss1Btn.addEventListener('click',()=>startBattle('boss'));

// v0.5.15 global state watchdog. This is deliberately independent of sprite
// animation so a visual callback can never permanently own the combat state.
setInterval(()=>{
 if(!gameStarted)return;
 if(S.phase==='player'){
   if(!S.ready || S.ready.kind!=='pair' || !pairAlive(S.ready.ref)){
     const p=alivePairs().sort((a,b)=>b.gauge-a.gauge)[0];
     if(p){S.ready={kind:'pair',ref:p,g:p.gauge};S.readyStarted=performance.now();S.anim=null;S.drag=false;S.phase='player';S.log='Player turn recovered.';}
   }
 }else if(S.phase==='charge'){
   if(!S._chargeSince)S._chargeSince=performance.now();
   if(performance.now()-S._chargeSince>5000){
     const p=alivePairs().sort((a,b)=>b.gauge-a.gauge)[0];
     if(p){p.gauge=100;S.ready={kind:'pair',ref:p,g:100};S.readyStarted=performance.now();S._chargeSince=0;S.anim=null;S.phase='player';S.log='Turn meter fallback restored player control.';updateUI();}
   }
 }
},500);

render();
</script>
<script>
/* v0.5.25 inventory/navigation module. Does not touch battle state. */
(()=>{
 const inventoryScreen=document.getElementById('inventoryScreen');
 const teamScreen=document.getElementById('teamScreen');
 const unitCardScreen=document.getElementById('unitCardScreen');
 const artViewScreen=document.getElementById('artViewScreen');
 const mainMenu=document.getElementById('menuScreen');
 const invBtn=document.getElementById('inventoryBtn');
 const owned={
   'Crimson':{
     name:'CRIMSON',title:'BLAZING VANGUARD',element:'🔥 FIRE',stars:'★★★★★★',rarity:'LEGENDARY',
     card:"data:image/png;base64,

===== enemy.x =====

--- match 1 @ 43580782 ---
){
 let f={x,y,text:'✦',color,life:1,impact:true};
 S.floaters.push(f);
 let id=setInterval(()=>{
   f.y-=.25;
   f.life-=.07;
   if(f.life<=0){
     clearInterval(id);
     S.floaters=S.floaters.filter(item=>item!==f);
   }
 },16);
}
function animateFreezeBlast(unitName,from,enemy,onImpact,onDone){
 const token=ACTIVE_ACTION_TOKEN;
 const castDuration=340;
 const flightDuration=420;
 const freezeHold=520;
 const totalDuration=castDuration+flightDuration+freezeHold;
 const castStart=performance.now();
 const state=ensureAnimState();
 if(!state.attackPose)state.attackPose={};
 // The four user-supplied cast frames are played by drawUnit via
 // ATTACK_SPRITES['Sub-Zero'].freeze while he remains planted in place.
 state.attackPose[unitName]={kind:'freeze',start:castStart,duration:castDuration+flightDuration};

 setTimeout(()=>{
   if(!actionTokenAlive(token))return;
   const projectileStart=performance.now();
   const projectile={kind:'iceProjectile',from:{x:from.x+10,y:from.y-22},to:{x:enemy.x,y:enemy.y-18},start:projectileStart,duration:flightDuration,life:1};
   S.floaters.push(projectile);
   function fly(now){
     if(!actionTokenAlive(token))return;
     const t=Math.min(1,(now-projectileStart)/flightDuration);
     if(t<1)return requestAnimationFrame(fly);
     S.floaters=S.floaters.filter(x=>x!==projectile);
     try{onImpact&&onImpact()}catch(err){console.error('Freeze Blast impact failed:',err);return recoverAction('freeze blast impact')}
     const ice={kind:'frozenTarget',x:enemy.x,y:enemy.y,start:performance.now(),life:1};
     S.floaters.push(ice);
     setTimeout(()=>{
       if(!actionTokenAlive(token))return;
       S.floaters=S.floaters.filter(x=>x!==ice);
       const st=ensureAnimState();
       if(st.attackPose)delete st.attackPose[unitName];
       try{onDone&&onDone()}catch(err){console.error('Freeze Blast completion failed:',err);recoverAction('freeze blast completion')}
     },freezeHold);
   }
   requestAnimationFrame(fly);
 },castDuration);
}


function animateLebeeStarBlast(unitName,from,enemy,onImpact,onDone){
 const token=ACTIVE_ACTION_TOKEN;
 const castDuration=320,flightDuration=300;
 const state=ensureAnimState();
 if(!state.attackPose)state.attackPose={};
 state.attackPose[unitName]={kind:'punch',start:performance.now(),duration:castDuration+flightDuration};
 setTimeout(()=>{
   if(!actionTokenAlive(token))return;
   const projectile={kind:'lebeeStarProjectile',from:{x:from.x,y:from.y-20},to:{x:enemy.x,y:enemy.y-12},start:performance.now(),duration:flightDuration,life:1};
   S.floaters.push(projectile);
   setTimeout(()=>{
     if(!actionTokenAlive(token))return;
     S.floaters=S.floaters.filter(x=>x!==projectile);
     try{onImpact&&onImpact()}catch(err){console.error('Lebee star blast impact failed:',err);return recoverAction('Lebee basic impact')}
     const st=ensureAnimState();if(st.attackPose)delete st.attackPose[unitName];
     setTimeout(()=>{if(actionTokenAlive(token)){try{onDone&&onDone()}catch(err){recoverAction('Lebee basic completion')}}},90);
   },flightDuration);
 },castDuration*.48);
}


function animateSenkuChemicalReaction(unitName,from,target,onImpact,onDone){
 const token=ACTIVE_ACTION_TOKEN;
 con

--- match 2 @ 43581291 ---
onst state=ensureAnimState();
 if(!state.attackPose)state.attackPose={};
 // The four user-supplied cast frames are played by drawUnit via
 // ATTACK_SPRITES['Sub-Zero'].freeze while he remains planted in place.
 state.attackPose[unitName]={kind:'freeze',start:castStart,duration:castDuration+flightDuration};

 setTimeout(()=>{
   if(!actionTokenAlive(token))return;
   const projectileStart=performance.now();
   const projectile={kind:'iceProjectile',from:{x:from.x+10,y:from.y-22},to:{x:enemy.x,y:enemy.y-18},start:projectileStart,duration:flightDuration,life:1};
   S.floaters.push(projectile);
   function fly(now){
     if(!actionTokenAlive(token))return;
     const t=Math.min(1,(now-projectileStart)/flightDuration);
     if(t<1)return requestAnimationFrame(fly);
     S.floaters=S.floaters.filter(x=>x!==projectile);
     try{onImpact&&onImpact()}catch(err){console.error('Freeze Blast impact failed:',err);return recoverAction('freeze blast impact')}
     const ice={kind:'frozenTarget',x:enemy.x,y:enemy.y,start:performance.now(),life:1};
     S.floaters.push(ice);
     setTimeout(()=>{
       if(!actionTokenAlive(token))return;
       S.floaters=S.floaters.filter(x=>x!==ice);
       const st=ensureAnimState();
       if(st.attackPose)delete st.attackPose[unitName];
       try{onDone&&onDone()}catch(err){console.error('Freeze Blast completion failed:',err);recoverAction('freeze blast completion')}
     },freezeHold);
   }
   requestAnimationFrame(fly);
 },castDuration);
}


function animateLebeeStarBlast(unitName,from,enemy,onImpact,onDone){
 const token=ACTIVE_ACTION_TOKEN;
 const castDuration=320,flightDuration=300;
 const state=ensureAnimState();
 if(!state.attackPose)state.attackPose={};
 state.attackPose[unitName]={kind:'punch',start:performance.now(),duration:castDuration+flightDuration};
 setTimeout(()=>{
   if(!actionTokenAlive(token))return;
   const projectile={kind:'lebeeStarProjectile',from:{x:from.x,y:from.y-20},to:{x:enemy.x,y:enemy.y-12},start:performance.now(),duration:flightDuration,life:1};
   S.floaters.push(projectile);
   setTimeout(()=>{
     if(!actionTokenAlive(token))return;
     S.floaters=S.floaters.filter(x=>x!==projectile);
     try{onImpact&&onImpact()}catch(err){console.error('Lebee star blast impact failed:',err);return recoverAction('Lebee basic impact')}
     const st=ensureAnimState();if(st.attackPose)delete st.attackPose[unitName];
     setTimeout(()=>{if(actionTokenAlive(token)){try{onDone&&onDone()}catch(err){recoverAction('Lebee basic completion')}}},90);
   },flightDuration);
 },castDuration*.48);
}


function animateSenkuChemicalReaction(unitName,from,target,onImpact,onDone){
 const token=ACTIVE_ACTION_TOKEN;
 const meta=canonicalUnit('senku').abilities.jutsu.presentation||{};
 const castDuration=meta.cast_duration_ms||680;
 const flightDuration=meta.flight_duration_ms||420;
 const impactDuration=meta.impact_duration_ms||620;

 const state=ensureAnimState();
 if(!state.attackPose)state.attackPose={};
 state.attackPose[unitName]={kind:'special',start:performance.now(),duration:castDuration};

 if(meta.screen_dim_alpha){
   S.jutsuDim={start:performance.now(),alpha:meta.screen_dim_alpha,end:null};
 }

 setTimeout((

--- match 3 @ 43582252 ---

     const ice={kind:'frozenTarget',x:enemy.x,y:enemy.y,start:performance.now(),life:1};
     S.floaters.push(ice);
     setTimeout(()=>{
       if(!actionTokenAlive(token))return;
       S.floaters=S.floaters.filter(x=>x!==ice);
       const st=ensureAnimState();
       if(st.attackPose)delete st.attackPose[unitName];
       try{onDone&&onDone()}catch(err){console.error('Freeze Blast completion failed:',err);recoverAction('freeze blast completion')}
     },freezeHold);
   }
   requestAnimationFrame(fly);
 },castDuration);
}


function animateLebeeStarBlast(unitName,from,enemy,onImpact,onDone){
 const token=ACTIVE_ACTION_TOKEN;
 const castDuration=320,flightDuration=300;
 const state=ensureAnimState();
 if(!state.attackPose)state.attackPose={};
 state.attackPose[unitName]={kind:'punch',start:performance.now(),duration:castDuration+flightDuration};
 setTimeout(()=>{
   if(!actionTokenAlive(token))return;
   const projectile={kind:'lebeeStarProjectile',from:{x:from.x,y:from.y-20},to:{x:enemy.x,y:enemy.y-12},start:performance.now(),duration:flightDuration,life:1};
   S.floaters.push(projectile);
   setTimeout(()=>{
     if(!actionTokenAlive(token))return;
     S.floaters=S.floaters.filter(x=>x!==projectile);
     try{onImpact&&onImpact()}catch(err){console.error('Lebee star blast impact failed:',err);return recoverAction('Lebee basic impact')}
     const st=ensureAnimState();if(st.attackPose)delete st.attackPose[unitName];
     setTimeout(()=>{if(actionTokenAlive(token)){try{onDone&&onDone()}catch(err){recoverAction('Lebee basic completion')}}},90);
   },flightDuration);
 },castDuration*.48);
}


function animateSenkuChemicalReaction(unitName,from,target,onImpact,onDone){
 const token=ACTIVE_ACTION_TOKEN;
 const meta=canonicalUnit('senku').abilities.jutsu.presentation||{};
 const castDuration=meta.cast_duration_ms||680;
 const flightDuration=meta.flight_duration_ms||420;
 const impactDuration=meta.impact_duration_ms||620;

 const state=ensureAnimState();
 if(!state.attackPose)state.attackPose={};
 state.attackPose[unitName]={kind:'special',start:performance.now(),duration:castDuration};

 if(meta.screen_dim_alpha){
   S.jutsuDim={start:performance.now(),alpha:meta.screen_dim_alpha,end:null};
 }

 setTimeout(()=>{
   if(!actionTokenAlive(token))return;

   const projectile={
     kind:'senkuChemProjectile',
     from:{x:from.x,y:from.y-30},
     to:{x:target.x,y:target.y-10},
     start:performance.now(),
     duration:flightDuration,
     life:1
   };
   S.floaters.push(projectile);

   setTimeout(()=>{
     if(!actionTokenAlive(token))return;

     S.floaters=S.floaters.filter(x=>x!==projectile);
     const impact={
       kind:'senkuChemImpact',
       x:target.x,
       y:target.y,
       start:performance.now(),
       duration:impactDuration,
       life:1
     };
     S.floaters.push(impact);

     try{onImpact&&onImpact()}
     catch(err){
       console.error('Senku Chemical Reaction impact failed:',err);
       return recoverAction('Senku Chemical Reaction impact');
     }

     setTimeout(()=>{
       S.floaters=S.floaters.filter(x=>x!==impact);
       if(S.jutsuDim)S.jutsuDim.end=performance.now();
       const st=ensureAnimState();
       

--- match 4 @ 43585531 ---
live(token)){
         try{onDone&&onDone()}
         catch(err){recoverAction('Senku Chemical Reaction completion')}
       }
     },impactDuration);
   },flightDuration);
 },castDuration*.70);
}

function animateSenkuBomb(unitName,from,enemy,onImpact,onDone,attackKind='punch'){
 const token=ACTIVE_ACTION_TOKEN;
 const meta=canonicalUnit('senku')?.abilities?.basic?.presentation||{};
 const castDuration=meta.cast_duration_ms||560;
 const flightDuration=meta.flight_duration_ms||620;
 const blastDuration=meta.impact_duration_ms||430;
 const arcHeight=meta.arc_height_px||86;

 const state=ensureAnimState();
 if(!state.attackPose)state.attackPose={};
 state.attackPose[unitName]={
   kind:attackKind,
   start:performance.now(),
   duration:castDuration
 };

 // Release near the final throwing pose rather than halfway through the cast.
 setTimeout(()=>{
   if(!actionTokenAlive(token))return;

   const projectile={
     kind:'senkuBombProjectile',
     from:{x:from.x,y:from.y-31},
     to:{x:enemy.x,y:enemy.y-12},
     start:performance.now(),
     duration:flightDuration,
     arcHeight,
     life:1
   };
   S.floaters.push(projectile);

   setTimeout(()=>{
     if(!actionTokenAlive(token))return;

     S.floaters=S.floaters.filter(x=>x!==projectile);
     const targetGroundY=(enemy.feetY??enemy.groundY??enemy.y);
     const blast={
       kind:'senkuExplosion',
       x:enemy.x,
       y:targetGroundY,
       layer:'ground',
       start:performance.now(),
       duration:360,
       life:1
     };
     S.floaters.push(blast);

     // Damage and target recoil happen only when the bomb lands.
     try{
       onImpact&&onImpact();
     }catch(err){
       console.error('Senku bomb impact failed:',err);
       return recoverAction('Senku bomb impact');
     }

     setTimeout(()=>{
       S.floaters=S.floaters.filter(x=>x!==blast);
       const st=ensureAnimState();
       if(st.attackPose)delete st.attackPose[unitName];

       if(actionTokenAlive(token)){
         try{
           onDone&&onDone();
         }catch(err){
           recoverAction('Senku bomb completion');
         }
       }
     },blastDuration);
   },flightDuration);
 },castDuration*.76);
}

function setMeteorFullscreenFlash(color,opacity=1,transition='none'){
 const el=getMeteorScreenFlash();
 if(!el)return;
 el.classList.add('active');
 el.style.transition=transition;
 el.style.background=color;
 el.style.opacity=String(opacity);
}

function clearMeteorFullscreenFlash(){
 const el=getMeteorScreenFlash();
 if(!el)return;
 el.style.transition='none';
 el.style.opacity='0';
 el.style.background='transparent';
 el.classList.remove('active');
}

function playLebeeMeteorImpactFlash(done){
 const meta=canonicalUnit('lebee').abilities.jutsu.presentation||{};
 const whiteHold=meta.final_flash_white_hold_ms||170;
 const whiteFade=meta.final_flash_white_fade_ms||620;

 // Final meteor: snap to full white.
 setMeteorFullscreenFlash('#ffffff',1,'none');

 setTimeout(()=>{
   const el=getMeteorScreenFlash();
   if(!el){
     done&&done();
     return;
   }
   el.style.transition=`opacity ${whiteFade}ms ease-out`;
   el.style.opacity='0';

   setTimeout(()=>{
     clearMeteorFullscreenFlash();
     d

--- match 5 @ 43585919 ---
const castDuration=meta.cast_duration_ms||560;
 const flightDuration=meta.flight_duration_ms||620;
 const blastDuration=meta.impact_duration_ms||430;
 const arcHeight=meta.arc_height_px||86;

 const state=ensureAnimState();
 if(!state.attackPose)state.attackPose={};
 state.attackPose[unitName]={
   kind:attackKind,
   start:performance.now(),
   duration:castDuration
 };

 // Release near the final throwing pose rather than halfway through the cast.
 setTimeout(()=>{
   if(!actionTokenAlive(token))return;

   const projectile={
     kind:'senkuBombProjectile',
     from:{x:from.x,y:from.y-31},
     to:{x:enemy.x,y:enemy.y-12},
     start:performance.now(),
     duration:flightDuration,
     arcHeight,
     life:1
   };
   S.floaters.push(projectile);

   setTimeout(()=>{
     if(!actionTokenAlive(token))return;

     S.floaters=S.floaters.filter(x=>x!==projectile);
     const targetGroundY=(enemy.feetY??enemy.groundY??enemy.y);
     const blast={
       kind:'senkuExplosion',
       x:enemy.x,
       y:targetGroundY,
       layer:'ground',
       start:performance.now(),
       duration:360,
       life:1
     };
     S.floaters.push(blast);

     // Damage and target recoil happen only when the bomb lands.
     try{
       onImpact&&onImpact();
     }catch(err){
       console.error('Senku bomb impact failed:',err);
       return recoverAction('Senku bomb impact');
     }

     setTimeout(()=>{
       S.floaters=S.floaters.filter(x=>x!==blast);
       const st=ensureAnimState();
       if(st.attackPose)delete st.attackPose[unitName];

       if(actionTokenAlive(token)){
         try{
           onDone&&onDone();
         }catch(err){
           recoverAction('Senku bomb completion');
         }
       }
     },blastDuration);
   },flightDuration);
 },castDuration*.76);
}

function setMeteorFullscreenFlash(color,opacity=1,transition='none'){
 const el=getMeteorScreenFlash();
 if(!el)return;
 el.classList.add('active');
 el.style.transition=transition;
 el.style.background=color;
 el.style.opacity=String(opacity);
}

function clearMeteorFullscreenFlash(){
 const el=getMeteorScreenFlash();
 if(!el)return;
 el.style.transition='none';
 el.style.opacity='0';
 el.style.background='transparent';
 el.classList.remove('active');
}

function playLebeeMeteorImpactFlash(done){
 const meta=canonicalUnit('lebee').abilities.jutsu.presentation||{};
 const whiteHold=meta.final_flash_white_hold_ms||170;
 const whiteFade=meta.final_flash_white_fade_ms||620;

 // Final meteor: snap to full white.
 setMeteorFullscreenFlash('#ffffff',1,'none');

 setTimeout(()=>{
   const el=getMeteorScreenFlash();
   if(!el){
     done&&done();
     return;
   }
   el.style.transition=`opacity ${whiteFade}ms ease-out`;
   el.style.opacity='0';

   setTimeout(()=>{
     clearMeteorFullscreenFlash();
     done&&done();
   },whiteFade+20);
 },whiteHold);
}

function animateLebeeMeteorJutsu(unitName,from,targets,onImpact,onDone){
 const token=ACTIVE_ACTION_TOKEN;
 const meta=canonicalUnit('lebee').abilities.jutsu.presentation||{};
 const castDuration=meta.cast_duration_ms||900;
 const fallDuration=meta.meteor_fall_duration_ms||700;
 const meteorCount=meta.meteor_count||7;
 const stagger=me

--- match 6 @ 43591677 ---
nects and yellow snaps into white.
   setTimeout(()=>{
     if(!actionTokenAlive(token))return;

     try{
       onImpact&&onImpact();
     }catch(err){
       console.error('Lebee Meteor Jutsu impact failed:',err);
       S.floaters=S.floaters.filter(x=>!meteors.includes(x));
       if(S.jutsuDim)S.jutsuDim.end=performance.now();
       clearMeteorFullscreenFlash();
       return recoverAction('Lebee Meteor impact');
     }

     // Meteors immediately begin disappearing once the final hit lands.
     S.floaters=S.floaters.filter(x=>!meteors.includes(x));
     if(S.jutsuDim)S.jutsuDim.end=performance.now();

     playLebeeMeteorImpactFlash(()=>{
       if(!actionTokenAlive(token))return;
       try{
         onDone&&onDone();
       }catch(err){
         recoverAction('Lebee Meteor completion');
       }
     });
   },finalImpact);

 },castDuration);
}

function recoil(enemy,done,dir=1,strong=false){
 const token=ACTIVE_ACTION_TOKEN;
 startHitReaction(enemy.name,dir,strong);
 let ox=enemy.x,oy=enemy.y,dx=18*(dir||1),start=performance.now();
 function f(n){
  if(!actionTokenAlive(token))return;
  let state=ensureAnimState(),t=Math.min(1,(n-start)/130),off=Math.sin(t*Math.PI)*dx;
  state.positions[enemy.name]={x:ox+off,y:oy};
  if(t<1)return requestAnimationFrame(f);
  delete state.positions[enemy.name];
  try{done&&done()}catch(err){console.error('Recoil callback failed:',err);recoverAction('recoil callback')}
 }
 requestAnimationFrame(f)
}
function resolvePlayer(){

 let p=S.ready.ref,u=front(p),useJutsu=S.action==='jutsu',proxy=attackProxy(u);
 const supportJutsu=useJutsu&&isAllySupportJutsu(u);
 let targets=supportJutsu?[]:aliveEnemies().filter(e=>hits(p,proxy,e));
 p.gauge=0;S.phase='resolve';S.anim={positions:{}};const playerActionToken=beginActionToken();armActionWatchdog('player attack',playerActionToken);

 if(useJutsu&&u.chakra<u.jutsuCost){
  S.log=`${u.name} does not have enough chakra.`;S.action='normal';return finishAction()
 }

 if(supportJutsu){
  u.chakra-=u.jutsuCost;

  const allies=alivePairs().filter(pair=>front(pair).hp>0);
  if(!allies.length){
   S.log=`${u.name} activated ${u.jutsuName||'Jutsu'}, but no allies can be healed.`;
   return finishAction();
  }

  const teamCenter={
   x:allies.reduce((sum,pair)=>sum+pair.x,0)/allies.length,
   y:allies.reduce((sum,pair)=>sum+pair.y,0)/allies.length-18
  };

  const from={x:p.x,y:p.y};
  S.log=`${u.name} activated ${u.jutsuName||'Revival Formula'} — restoring all active allies to full HP.`;

  return animateSenkuChemicalReaction(
   u.name,from,teamCenter,
   ()=>{
    allies.forEach(pair=>{
     const ally=front(pair);
     const healed=Math.max(0,ally.maxHp-ally.hp);
     ally.hp=ally.maxHp;

     addImpactFlash(pair.x,pair.y-18,'#64ff7d');
     if(healed>0){
      addFloat(pair.x,pair.y-40,`+${healed}`,'#74ff91');
     }else{
      addFloat(pair.x,pair.y-40,'FULL','#74ff91');
     }
    });
   },
   ()=>finishAction()
  );
 }

 if(!targets.length){
  S.log=`${u.name} committed the move but caught no target.`;return finishAction()
 }

 if(useJutsu){
  u.chakra-=u.jutsuCost;

  if(u.name==='Lebee'){
   S.log=`Lebee activated Meteor Jutsu — ${targets.length} target${targets.lengt

--- match 7 @ 43594164 ---
ies to full HP.`;

  return animateSenkuChemicalReaction(
   u.name,from,teamCenter,
   ()=>{
    allies.forEach(pair=>{
     const ally=front(pair);
     const healed=Math.max(0,ally.maxHp-ally.hp);
     ally.hp=ally.maxHp;

     addImpactFlash(pair.x,pair.y-18,'#64ff7d');
     if(healed>0){
      addFloat(pair.x,pair.y-40,`+${healed}`,'#74ff91');
     }else{
      addFloat(pair.x,pair.y-40,'FULL','#74ff91');
     }
    });
   },
   ()=>finishAction()
  );
 }

 if(!targets.length){
  S.log=`${u.name} committed the move but caught no target.`;return finishAction()
 }

 if(useJutsu){
  u.chakra-=u.jutsuCost;

  if(u.name==='Lebee'){
   S.log=`Lebee activated Meteor Jutsu — ${targets.length} target${targets.length===1?'':'s'} caught in the cosmic impact zone.`;
   const from={x:p.x,y:p.y};
   return animateLebeeMeteorJutsu(
    u.name,from,targets,
    ()=>{
     targets.forEach(enemy=>{
      if(enemy.hp<=0)return;
      enemy.hp=Math.max(0,enemy.hp-u.jutsuDamage);
      addImpactFlash(enemy.x,enemy.y-8,'#ff7ae6');
      addFloat(enemy.x,enemy.y-34,'-'+u.jutsuDamage,'#ff8be8');
      const dir=Math.sign(enemy.x-p.x)||1;
      if(enemy.hp<=0)handleEnemyKO(enemy,dir); else recoil(enemy,()=>{},dir,true);
     });
     checkVictoryKillshot();
    },
    ()=>finishAction()
   );
  }

  // Every Jutsu target gets:
  //   active fighter's Jutsu hit
  //   + normal attacks from OTHER allies whose normal range covers that target.
  let jutsuQueue=targets.map(enemy=>{
    let helpers=comboMembers(enemy,p).filter(helper=>helper!==p);
    return {enemy,helpers};
  });

  let anyHelpers=jutsuQueue.some(item=>item.helpers.length>0);
  S.log=anyHelpers
    ? `${u.name} activated ${u.jutsuName||'Jutsu'} — normal combo partners are joining where their ranges overlap.`
    : `${u.name} activated ${u.jutsuName||'Jutsu'} — ${targets.length} target${targets.length===1?'':'s'} caught in the field.`;

  let targetIndex=0;

  function runJutsuTarget(){
   if(targetIndex>=jutsuQueue.length)return finishAction();

   let item=jutsuQueue[targetIndex++];
   let enemy=item.enemy;
   let helpers=item.helpers;

   // First: the active fighter performs the Jutsu hit.
   let dx=enemy.x-p.x,dy=enemy.y-p.y,len=Math.max(1,Math.hypot(dx,dy));
   let to={x:enemy.x-dx/len*((enemy.r||19)+27),y:enemy.y-dy/len*((enemy.r||19)+27)};
   let from={x:p.x,y:p.y};

   const runActiveJutsu=(u.name==='Sub-Zero')?animateFreezeBlast:animateLunge;
   const activeFrom=(u.name==='Sub-Zero')?from:from;
   const activeTarget=(u.name==='Sub-Zero')?enemy:to;
   runActiveJutsu(
    u.name,activeFrom,activeTarget,
    ()=>{
     enemy.hp=Math.max(0,enemy.hp-u.jutsuDamage);
     addImpactFlash(enemy.x,enemy.y-8,u.name==='Crimson'?'#ff405c':'#8cecff');
     addFloat(enemy.x,enemy.y-34,'-'+u.jutsuDamage,'#57e7ff');
     let jDir=Math.sign(enemy.x-p.x)||1;
     if(enemy.hp<=0)handleEnemyKO(enemy,jDir); else if(u.name!=='Sub-Zero')recoil(enemy,()=>{},jDir,true);
     if(u.name==='Sub-Zero')enemy.gauge=Math.max(0,(enemy.gauge||0)-35);
     checkVictoryKillshot();
    },
    ()=>{
     let helperIndex=0;

     function runHelper(){
      if(enemy.hp<=0 || helperIndex>=helpers.length)
       return setTimeout(runJutsuT

--- match 8 @ 43594209 ---
calReaction(
   u.name,from,teamCenter,
   ()=>{
    allies.forEach(pair=>{
     const ally=front(pair);
     const healed=Math.max(0,ally.maxHp-ally.hp);
     ally.hp=ally.maxHp;

     addImpactFlash(pair.x,pair.y-18,'#64ff7d');
     if(healed>0){
      addFloat(pair.x,pair.y-40,`+${healed}`,'#74ff91');
     }else{
      addFloat(pair.x,pair.y-40,'FULL','#74ff91');
     }
    });
   },
   ()=>finishAction()
  );
 }

 if(!targets.length){
  S.log=`${u.name} committed the move but caught no target.`;return finishAction()
 }

 if(useJutsu){
  u.chakra-=u.jutsuCost;

  if(u.name==='Lebee'){
   S.log=`Lebee activated Meteor Jutsu — ${targets.length} target${targets.length===1?'':'s'} caught in the cosmic impact zone.`;
   const from={x:p.x,y:p.y};
   return animateLebeeMeteorJutsu(
    u.name,from,targets,
    ()=>{
     targets.forEach(enemy=>{
      if(enemy.hp<=0)return;
      enemy.hp=Math.max(0,enemy.hp-u.jutsuDamage);
      addImpactFlash(enemy.x,enemy.y-8,'#ff7ae6');
      addFloat(enemy.x,enemy.y-34,'-'+u.jutsuDamage,'#ff8be8');
      const dir=Math.sign(enemy.x-p.x)||1;
      if(enemy.hp<=0)handleEnemyKO(enemy,dir); else recoil(enemy,()=>{},dir,true);
     });
     checkVictoryKillshot();
    },
    ()=>finishAction()
   );
  }

  // Every Jutsu target gets:
  //   active fighter's Jutsu hit
  //   + normal attacks from OTHER allies whose normal range covers that target.
  let jutsuQueue=targets.map(enemy=>{
    let helpers=comboMembers(enemy,p).filter(helper=>helper!==p);
    return {enemy,helpers};
  });

  let anyHelpers=jutsuQueue.some(item=>item.helpers.length>0);
  S.log=anyHelpers
    ? `${u.name} activated ${u.jutsuName||'Jutsu'} — normal combo partners are joining where their ranges overlap.`
    : `${u.name} activated ${u.jutsuName||'Jutsu'} — ${targets.length} target${targets.length===1?'':'s'} caught in the field.`;

  let targetIndex=0;

  function runJutsuTarget(){
   if(targetIndex>=jutsuQueue.length)return finishAction();

   let item=jutsuQueue[targetIndex++];
   let enemy=item.enemy;
   let helpers=item.helpers;

   // First: the active fighter performs the Jutsu hit.
   let dx=enemy.x-p.x,dy=enemy.y-p.y,len=Math.max(1,Math.hypot(dx,dy));
   let to={x:enemy.x-dx/len*((enemy.r||19)+27),y:enemy.y-dy/len*((enemy.r||19)+27)};
   let from={x:p.x,y:p.y};

   const runActiveJutsu=(u.name==='Sub-Zero')?animateFreezeBlast:animateLunge;
   const activeFrom=(u.name==='Sub-Zero')?from:from;
   const activeTarget=(u.name==='Sub-Zero')?enemy:to;
   runActiveJutsu(
    u.name,activeFrom,activeTarget,
    ()=>{
     enemy.hp=Math.max(0,enemy.hp-u.jutsuDamage);
     addImpactFlash(enemy.x,enemy.y-8,u.name==='Crimson'?'#ff405c':'#8cecff');
     addFloat(enemy.x,enemy.y-34,'-'+u.jutsuDamage,'#57e7ff');
     let jDir=Math.sign(enemy.x-p.x)||1;
     if(enemy.hp<=0)handleEnemyKO(enemy,jDir); else if(u.name!=='Sub-Zero')recoil(enemy,()=>{},jDir,true);
     if(u.name==='Sub-Zero')enemy.gauge=Math.max(0,(enemy.gauge||0)-35);
     checkVictoryKillshot();
    },
    ()=>{
     let helperIndex=0;

     function runHelper(){
      if(enemy.hp<=0 || helperIndex>=helpers.length)
       return setTimeout(runJutsuTarget,110);

      let hp=helpers[helperIndex

--- match 9 @ 43594284 ---

     const ally=front(pair);
     const healed=Math.max(0,ally.maxHp-ally.hp);
     ally.hp=ally.maxHp;

     addImpactFlash(pair.x,pair.y-18,'#64ff7d');
     if(healed>0){
      addFloat(pair.x,pair.y-40,`+${healed}`,'#74ff91');
     }else{
      addFloat(pair.x,pair.y-40,'FULL','#74ff91');
     }
    });
   },
   ()=>finishAction()
  );
 }

 if(!targets.length){
  S.log=`${u.name} committed the move but caught no target.`;return finishAction()
 }

 if(useJutsu){
  u.chakra-=u.jutsuCost;

  if(u.name==='Lebee'){
   S.log=`Lebee activated Meteor Jutsu — ${targets.length} target${targets.length===1?'':'s'} caught in the cosmic impact zone.`;
   const from={x:p.x,y:p.y};
   return animateLebeeMeteorJutsu(
    u.name,from,targets,
    ()=>{
     targets.forEach(enemy=>{
      if(enemy.hp<=0)return;
      enemy.hp=Math.max(0,enemy.hp-u.jutsuDamage);
      addImpactFlash(enemy.x,enemy.y-8,'#ff7ae6');
      addFloat(enemy.x,enemy.y-34,'-'+u.jutsuDamage,'#ff8be8');
      const dir=Math.sign(enemy.x-p.x)||1;
      if(enemy.hp<=0)handleEnemyKO(enemy,dir); else recoil(enemy,()=>{},dir,true);
     });
     checkVictoryKillshot();
    },
    ()=>finishAction()
   );
  }

  // Every Jutsu target gets:
  //   active fighter's Jutsu hit
  //   + normal attacks from OTHER allies whose normal range covers that target.
  let jutsuQueue=targets.map(enemy=>{
    let helpers=comboMembers(enemy,p).filter(helper=>helper!==p);
    return {enemy,helpers};
  });

  let anyHelpers=jutsuQueue.some(item=>item.helpers.length>0);
  S.log=anyHelpers
    ? `${u.name} activated ${u.jutsuName||'Jutsu'} — normal combo partners are joining where their ranges overlap.`
    : `${u.name} activated ${u.jutsuName||'Jutsu'} — ${targets.length} target${targets.length===1?'':'s'} caught in the field.`;

  let targetIndex=0;

  function runJutsuTarget(){
   if(targetIndex>=jutsuQueue.length)return finishAction();

   let item=jutsuQueue[targetIndex++];
   let enemy=item.enemy;
   let helpers=item.helpers;

   // First: the active fighter performs the Jutsu hit.
   let dx=enemy.x-p.x,dy=enemy.y-p.y,len=Math.max(1,Math.hypot(dx,dy));
   let to={x:enemy.x-dx/len*((enemy.r||19)+27),y:enemy.y-dy/len*((enemy.r||19)+27)};
   let from={x:p.x,y:p.y};

   const runActiveJutsu=(u.name==='Sub-Zero')?animateFreezeBlast:animateLunge;
   const activeFrom=(u.name==='Sub-Zero')?from:from;
   const activeTarget=(u.name==='Sub-Zero')?enemy:to;
   runActiveJutsu(
    u.name,activeFrom,activeTarget,
    ()=>{
     enemy.hp=Math.max(0,enemy.hp-u.jutsuDamage);
     addImpactFlash(enemy.x,enemy.y-8,u.name==='Crimson'?'#ff405c':'#8cecff');
     addFloat(enemy.x,enemy.y-34,'-'+u.jutsuDamage,'#57e7ff');
     let jDir=Math.sign(enemy.x-p.x)||1;
     if(enemy.hp<=0)handleEnemyKO(enemy,jDir); else if(u.name!=='Sub-Zero')recoil(enemy,()=>{},jDir,true);
     if(u.name==='Sub-Zero')enemy.gauge=Math.max(0,(enemy.gauge||0)-35);
     checkVictoryKillshot();
    },
    ()=>{
     let helperIndex=0;

     function runHelper(){
      if(enemy.hp<=0 || helperIndex>=helpers.length)
       return setTimeout(runJutsuTarget,110);

      let hp=helpers[helperIndex++];
      let hu=front(hp);
      let hdx=enemy.x-hp.x,hdy=enemy.y-hp.y,hl

--- match 10 @ 43595347 ---
 else recoil(enemy,()=>{},dir,true);
     });
     checkVictoryKillshot();
    },
    ()=>finishAction()
   );
  }

  // Every Jutsu target gets:
  //   active fighter's Jutsu hit
  //   + normal attacks from OTHER allies whose normal range covers that target.
  let jutsuQueue=targets.map(enemy=>{
    let helpers=comboMembers(enemy,p).filter(helper=>helper!==p);
    return {enemy,helpers};
  });

  let anyHelpers=jutsuQueue.some(item=>item.helpers.length>0);
  S.log=anyHelpers
    ? `${u.name} activated ${u.jutsuName||'Jutsu'} — normal combo partners are joining where their ranges overlap.`
    : `${u.name} activated ${u.jutsuName||'Jutsu'} — ${targets.length} target${targets.length===1?'':'s'} caught in the field.`;

  let targetIndex=0;

  function runJutsuTarget(){
   if(targetIndex>=jutsuQueue.length)return finishAction();

   let item=jutsuQueue[targetIndex++];
   let enemy=item.enemy;
   let helpers=item.helpers;

   // First: the active fighter performs the Jutsu hit.
   let dx=enemy.x-p.x,dy=enemy.y-p.y,len=Math.max(1,Math.hypot(dx,dy));
   let to={x:enemy.x-dx/len*((enemy.r||19)+27),y:enemy.y-dy/len*((enemy.r||19)+27)};
   let from={x:p.x,y:p.y};

   const runActiveJutsu=(u.name==='Sub-Zero')?animateFreezeBlast:animateLunge;
   const activeFrom=(u.name==='Sub-Zero')?from:from;
   const activeTarget=(u.name==='Sub-Zero')?enemy:to;
   runActiveJutsu(
    u.name,activeFrom,activeTarget,
    ()=>{
     enemy.hp=Math.max(0,enemy.hp-u.jutsuDamage);
     addImpactFlash(enemy.x,enemy.y-8,u.name==='Crimson'?'#ff405c':'#8cecff');
     addFloat(enemy.x,enemy.y-34,'-'+u.jutsuDamage,'#57e7ff');
     let jDir=Math.sign(enemy.x-p.x)||1;
     if(enemy.hp<=0)handleEnemyKO(enemy,jDir); else if(u.name!=='Sub-Zero')recoil(enemy,()=>{},jDir,true);
     if(u.name==='Sub-Zero')enemy.gauge=Math.max(0,(enemy.gauge||0)-35);
     checkVictoryKillshot();
    },
    ()=>{
     let helperIndex=0;

     function runHelper(){
      if(enemy.hp<=0 || helperIndex>=helpers.length)
       return setTimeout(runJutsuTarget,110);

      let hp=helpers[helperIndex++];
      let hu=front(hp);
      let hdx=enemy.x-hp.x,hdy=enemy.y-hp.y,hlen=Math.max(1,Math.hypot(hdx,hdy));
      let hto={
       x:enemy.x-hdx/hlen*((enemy.r||19)+27),
       y:enemy.y-hdy/hlen*((enemy.r||19)+27)
      };
      let hfrom={x:hp.x,y:hp.y};

      animateLunge(
       hu.name,hfrom,hto,
       ()=>{
        let hResult=buffedNormalDamage(hp),hDamage=hResult.damage;
        enemy.hp=Math.max(0,enemy.hp-hDamage);
        addImpactFlash(enemy.x,enemy.y-8,hu.name==='Crimson'?'#ff405c':'#ffffff');
        addFloat(enemy.x,enemy.y-34,'-'+hDamage,hResult.buff.bonus>0?'#65ff9e':'#ffbd4a');
        let hDir=Math.sign(enemy.x-hp.x)||1;
        if(enemy.hp<=0)handleEnemyKO(enemy,hDir); else recoil(enemy,()=>{},hDir,false);
        checkVictoryKillshot();
       },
       ()=>setTimeout(runHelper,85),
       helperIndex%2===1?'punch':'kick'
      )
     }

     runHelper()
    },
    'special'
   )
  }

  return runJutsuTarget()
 }

 let queue=[],anyCombo=false;
 targets.forEach(enemy=>{
  let members=comboMembers(enemy,p),attackers=[p,...members.filter(x=>x!==p)];
  if(attackers.length>1)anyCombo=true;
  queue.pu

--- match 11 @ 43595422 ---
    },
    ()=>finishAction()
   );
  }

  // Every Jutsu target gets:
  //   active fighter's Jutsu hit
  //   + normal attacks from OTHER allies whose normal range covers that target.
  let jutsuQueue=targets.map(enemy=>{
    let helpers=comboMembers(enemy,p).filter(helper=>helper!==p);
    return {enemy,helpers};
  });

  let anyHelpers=jutsuQueue.some(item=>item.helpers.length>0);
  S.log=anyHelpers
    ? `${u.name} activated ${u.jutsuName||'Jutsu'} — normal combo partners are joining where their ranges overlap.`
    : `${u.name} activated ${u.jutsuName||'Jutsu'} — ${targets.length} target${targets.length===1?'':'s'} caught in the field.`;

  let targetIndex=0;

  function runJutsuTarget(){
   if(targetIndex>=jutsuQueue.length)return finishAction();

   let item=jutsuQueue[targetIndex++];
   let enemy=item.enemy;
   let helpers=item.helpers;

   // First: the active fighter performs the Jutsu hit.
   let dx=enemy.x-p.x,dy=enemy.y-p.y,len=Math.max(1,Math.hypot(dx,dy));
   let to={x:enemy.x-dx/len*((enemy.r||19)+27),y:enemy.y-dy/len*((enemy.r||19)+27)};
   let from={x:p.x,y:p.y};

   const runActiveJutsu=(u.name==='Sub-Zero')?animateFreezeBlast:animateLunge;
   const activeFrom=(u.name==='Sub-Zero')?from:from;
   const activeTarget=(u.name==='Sub-Zero')?enemy:to;
   runActiveJutsu(
    u.name,activeFrom,activeTarget,
    ()=>{
     enemy.hp=Math.max(0,enemy.hp-u.jutsuDamage);
     addImpactFlash(enemy.x,enemy.y-8,u.name==='Crimson'?'#ff405c':'#8cecff');
     addFloat(enemy.x,enemy.y-34,'-'+u.jutsuDamage,'#57e7ff');
     let jDir=Math.sign(enemy.x-p.x)||1;
     if(enemy.hp<=0)handleEnemyKO(enemy,jDir); else if(u.name!=='Sub-Zero')recoil(enemy,()=>{},jDir,true);
     if(u.name==='Sub-Zero')enemy.gauge=Math.max(0,(enemy.gauge||0)-35);
     checkVictoryKillshot();
    },
    ()=>{
     let helperIndex=0;

     function runHelper(){
      if(enemy.hp<=0 || helperIndex>=helpers.length)
       return setTimeout(runJutsuTarget,110);

      let hp=helpers[helperIndex++];
      let hu=front(hp);
      let hdx=enemy.x-hp.x,hdy=enemy.y-hp.y,hlen=Math.max(1,Math.hypot(hdx,hdy));
      let hto={
       x:enemy.x-hdx/hlen*((enemy.r||19)+27),
       y:enemy.y-hdy/hlen*((enemy.r||19)+27)
      };
      let hfrom={x:hp.x,y:hp.y};

      animateLunge(
       hu.name,hfrom,hto,
       ()=>{
        let hResult=buffedNormalDamage(hp),hDamage=hResult.damage;
        enemy.hp=Math.max(0,enemy.hp-hDamage);
        addImpactFlash(enemy.x,enemy.y-8,hu.name==='Crimson'?'#ff405c':'#ffffff');
        addFloat(enemy.x,enemy.y-34,'-'+hDamage,hResult.buff.bonus>0?'#65ff9e':'#ffbd4a');
        let hDir=Math.sign(enemy.x-hp.x)||1;
        if(enemy.hp<=0)handleEnemyKO(enemy,hDir); else recoil(enemy,()=>{},hDir,false);
        checkVictoryKillshot();
       },
       ()=>setTimeout(runHelper,85),
       helperIndex%2===1?'punch':'kick'
      )
     }

     runHelper()
    },
    'special'
   )
  }

  return runJutsuTarget()
 }

 let queue=[],anyCombo=false;
 targets.forEach(enemy=>{
  let members=comboMembers(enemy,p),attackers=[p,...members.filter(x=>x!==p)];
  if(attackers.length>1)anyCombo=true;
  queue.push({enemy,attackers})
 });
 let chakraGain=1+(anyCombo?1:0);
 u.chakra=Math

--- match 12 @ 43595843 ---
e} activated ${u.jutsuName||'Jutsu'} — normal combo partners are joining where their ranges overlap.`
    : `${u.name} activated ${u.jutsuName||'Jutsu'} — ${targets.length} target${targets.length===1?'':'s'} caught in the field.`;

  let targetIndex=0;

  function runJutsuTarget(){
   if(targetIndex>=jutsuQueue.length)return finishAction();

   let item=jutsuQueue[targetIndex++];
   let enemy=item.enemy;
   let helpers=item.helpers;

   // First: the active fighter performs the Jutsu hit.
   let dx=enemy.x-p.x,dy=enemy.y-p.y,len=Math.max(1,Math.hypot(dx,dy));
   let to={x:enemy.x-dx/len*((enemy.r||19)+27),y:enemy.y-dy/len*((enemy.r||19)+27)};
   let from={x:p.x,y:p.y};

   const runActiveJutsu=(u.name==='Sub-Zero')?animateFreezeBlast:animateLunge;
   const activeFrom=(u.name==='Sub-Zero')?from:from;
   const activeTarget=(u.name==='Sub-Zero')?enemy:to;
   runActiveJutsu(
    u.name,activeFrom,activeTarget,
    ()=>{
     enemy.hp=Math.max(0,enemy.hp-u.jutsuDamage);
     addImpactFlash(enemy.x,enemy.y-8,u.name==='Crimson'?'#ff405c':'#8cecff');
     addFloat(enemy.x,enemy.y-34,'-'+u.jutsuDamage,'#57e7ff');
     let jDir=Math.sign(enemy.x-p.x)||1;
     if(enemy.hp<=0)handleEnemyKO(enemy,jDir); else if(u.name!=='Sub-Zero')recoil(enemy,()=>{},jDir,true);
     if(u.name==='Sub-Zero')enemy.gauge=Math.max(0,(enemy.gauge||0)-35);
     checkVictoryKillshot();
    },
    ()=>{
     let helperIndex=0;

     function runHelper(){
      if(enemy.hp<=0 || helperIndex>=helpers.length)
       return setTimeout(runJutsuTarget,110);

      let hp=helpers[helperIndex++];
      let hu=front(hp);
      let hdx=enemy.x-hp.x,hdy=enemy.y-hp.y,hlen=Math.max(1,Math.hypot(hdx,hdy));
      let hto={
       x:enemy.x-hdx/hlen*((enemy.r||19)+27),
       y:enemy.y-hdy/hlen*((enemy.r||19)+27)
      };
      let hfrom={x:hp.x,y:hp.y};

      animateLunge(
       hu.name,hfrom,hto,
       ()=>{
        let hResult=buffedNormalDamage(hp),hDamage=hResult.damage;
        enemy.hp=Math.max(0,enemy.hp-hDamage);
        addImpactFlash(enemy.x,enemy.y-8,hu.name==='Crimson'?'#ff405c':'#ffffff');
        addFloat(enemy.x,enemy.y-34,'-'+hDamage,hResult.buff.bonus>0?'#65ff9e':'#ffbd4a');
        let hDir=Math.sign(enemy.x-hp.x)||1;
        if(enemy.hp<=0)handleEnemyKO(enemy,hDir); else recoil(enemy,()=>{},hDir,false);
        checkVictoryKillshot();
       },
       ()=>setTimeout(runHelper,85),
       helperIndex%2===1?'punch':'kick'
      )
     }

     runHelper()
    },
    'special'
   )
  }

  return runJutsuTarget()
 }

 let queue=[],anyCombo=false;
 targets.forEach(enemy=>{
  let members=comboMembers(enemy,p),attackers=[p,...members.filter(x=>x!==p)];
  if(attackers.length>1)anyCombo=true;
  queue.push({enemy,attackers})
 });
 let chakraGain=1+(anyCombo?1:0);
 u.chakra=Math.min(u.maxChakra,u.chakra+chakraGain);

 let targetIndex=0;
 function runTarget(){
  if(targetIndex>=queue.length){
   S.log=`${u.name} hit ${targets.length} target${targets.length===1?'':'s'} and gained ${chakraGain} chakra${anyCombo?' from combo positioning':''}.`;
   return finishAction()
  }
  let item=queue[targetIndex++],enemy=item.enemy,attackers=item.attackers,attackIndex=0;
  function runAttacker(){
   if(ene

--- match 13 @ 43595916 ---
where their ranges overlap.`
    : `${u.name} activated ${u.jutsuName||'Jutsu'} — ${targets.length} target${targets.length===1?'':'s'} caught in the field.`;

  let targetIndex=0;

  function runJutsuTarget(){
   if(targetIndex>=jutsuQueue.length)return finishAction();

   let item=jutsuQueue[targetIndex++];
   let enemy=item.enemy;
   let helpers=item.helpers;

   // First: the active fighter performs the Jutsu hit.
   let dx=enemy.x-p.x,dy=enemy.y-p.y,len=Math.max(1,Math.hypot(dx,dy));
   let to={x:enemy.x-dx/len*((enemy.r||19)+27),y:enemy.y-dy/len*((enemy.r||19)+27)};
   let from={x:p.x,y:p.y};

   const runActiveJutsu=(u.name==='Sub-Zero')?animateFreezeBlast:animateLunge;
   const activeFrom=(u.name==='Sub-Zero')?from:from;
   const activeTarget=(u.name==='Sub-Zero')?enemy:to;
   runActiveJutsu(
    u.name,activeFrom,activeTarget,
    ()=>{
     enemy.hp=Math.max(0,enemy.hp-u.jutsuDamage);
     addImpactFlash(enemy.x,enemy.y-8,u.name==='Crimson'?'#ff405c':'#8cecff');
     addFloat(enemy.x,enemy.y-34,'-'+u.jutsuDamage,'#57e7ff');
     let jDir=Math.sign(enemy.x-p.x)||1;
     if(enemy.hp<=0)handleEnemyKO(enemy,jDir); else if(u.name!=='Sub-Zero')recoil(enemy,()=>{},jDir,true);
     if(u.name==='Sub-Zero')enemy.gauge=Math.max(0,(enemy.gauge||0)-35);
     checkVictoryKillshot();
    },
    ()=>{
     let helperIndex=0;

     function runHelper(){
      if(enemy.hp<=0 || helperIndex>=helpers.length)
       return setTimeout(runJutsuTarget,110);

      let hp=helpers[helperIndex++];
      let hu=front(hp);
      let hdx=enemy.x-hp.x,hdy=enemy.y-hp.y,hlen=Math.max(1,Math.hypot(hdx,hdy));
      let hto={
       x:enemy.x-hdx/hlen*((enemy.r||19)+27),
       y:enemy.y-hdy/hlen*((enemy.r||19)+27)
      };
      let hfrom={x:hp.x,y:hp.y};

      animateLunge(
       hu.name,hfrom,hto,
       ()=>{
        let hResult=buffedNormalDamage(hp),hDamage=hResult.damage;
        enemy.hp=Math.max(0,enemy.hp-hDamage);
        addImpactFlash(enemy.x,enemy.y-8,hu.name==='Crimson'?'#ff405c':'#ffffff');
        addFloat(enemy.x,enemy.y-34,'-'+hDamage,hResult.buff.bonus>0?'#65ff9e':'#ffbd4a');
        let hDir=Math.sign(enemy.x-hp.x)||1;
        if(enemy.hp<=0)handleEnemyKO(enemy,hDir); else recoil(enemy,()=>{},hDir,false);
        checkVictoryKillshot();
       },
       ()=>setTimeout(runHelper,85),
       helperIndex%2===1?'punch':'kick'
      )
     }

     runHelper()
    },
    'special'
   )
  }

  return runJutsuTarget()
 }

 let queue=[],anyCombo=false;
 targets.forEach(enemy=>{
  let members=comboMembers(enemy,p),attackers=[p,...members.filter(x=>x!==p)];
  if(attackers.length>1)anyCombo=true;
  queue.push({enemy,attackers})
 });
 let chakraGain=1+(anyCombo?1:0);
 u.chakra=Math.min(u.maxChakra,u.chakra+chakraGain);

 let targetIndex=0;
 function runTarget(){
  if(targetIndex>=queue.length){
   S.log=`${u.name} hit ${targets.length} target${targets.length===1?'':'s'} and gained ${chakraGain} chakra${anyCombo?' from combo positioning':''}.`;
   return finishAction()
  }
  let item=queue[targetIndex++],enemy=item.enemy,attackers=item.attackers,attackIndex=0;
  function runAttacker(){
   if(enemy.hp<=0||attackIndex>=attackers.length)return setTimeout(runTarget,110);

--- match 14 @ 43595989 ---
utsu'} — ${targets.length} target${targets.length===1?'':'s'} caught in the field.`;

  let targetIndex=0;

  function runJutsuTarget(){
   if(targetIndex>=jutsuQueue.length)return finishAction();

   let item=jutsuQueue[targetIndex++];
   let enemy=item.enemy;
   let helpers=item.helpers;

   // First: the active fighter performs the Jutsu hit.
   let dx=enemy.x-p.x,dy=enemy.y-p.y,len=Math.max(1,Math.hypot(dx,dy));
   let to={x:enemy.x-dx/len*((enemy.r||19)+27),y:enemy.y-dy/len*((enemy.r||19)+27)};
   let from={x:p.x,y:p.y};

   const runActiveJutsu=(u.name==='Sub-Zero')?animateFreezeBlast:animateLunge;
   const activeFrom=(u.name==='Sub-Zero')?from:from;
   const activeTarget=(u.name==='Sub-Zero')?enemy:to;
   runActiveJutsu(
    u.name,activeFrom,activeTarget,
    ()=>{
     enemy.hp=Math.max(0,enemy.hp-u.jutsuDamage);
     addImpactFlash(enemy.x,enemy.y-8,u.name==='Crimson'?'#ff405c':'#8cecff');
     addFloat(enemy.x,enemy.y-34,'-'+u.jutsuDamage,'#57e7ff');
     let jDir=Math.sign(enemy.x-p.x)||1;
     if(enemy.hp<=0)handleEnemyKO(enemy,jDir); else if(u.name!=='Sub-Zero')recoil(enemy,()=>{},jDir,true);
     if(u.name==='Sub-Zero')enemy.gauge=Math.max(0,(enemy.gauge||0)-35);
     checkVictoryKillshot();
    },
    ()=>{
     let helperIndex=0;

     function runHelper(){
      if(enemy.hp<=0 || helperIndex>=helpers.length)
       return setTimeout(runJutsuTarget,110);

      let hp=helpers[helperIndex++];
      let hu=front(hp);
      let hdx=enemy.x-hp.x,hdy=enemy.y-hp.y,hlen=Math.max(1,Math.hypot(hdx,hdy));
      let hto={
       x:enemy.x-hdx/hlen*((enemy.r||19)+27),
       y:enemy.y-hdy/hlen*((enemy.r||19)+27)
      };
      let hfrom={x:hp.x,y:hp.y};

      animateLunge(
       hu.name,hfrom,hto,
       ()=>{
        let hResult=buffedNormalDamage(hp),hDamage=hResult.damage;
        enemy.hp=Math.max(0,enemy.hp-hDamage);
        addImpactFlash(enemy.x,enemy.y-8,hu.name==='Crimson'?'#ff405c':'#ffffff');
        addFloat(enemy.x,enemy.y-34,'-'+hDamage,hResult.buff.bonus>0?'#65ff9e':'#ffbd4a');
        let hDir=Math.sign(enemy.x-hp.x)||1;
        if(enemy.hp<=0)handleEnemyKO(enemy,hDir); else recoil(enemy,()=>{},hDir,false);
        checkVictoryKillshot();
       },
       ()=>setTimeout(runHelper,85),
       helperIndex%2===1?'punch':'kick'
      )
     }

     runHelper()
    },
    'special'
   )
  }

  return runJutsuTarget()
 }

 let queue=[],anyCombo=false;
 targets.forEach(enemy=>{
  let members=comboMembers(enemy,p),attackers=[p,...members.filter(x=>x!==p)];
  if(attackers.length>1)anyCombo=true;
  queue.push({enemy,attackers})
 });
 let chakraGain=1+(anyCombo?1:0);
 u.chakra=Math.min(u.maxChakra,u.chakra+chakraGain);

 let targetIndex=0;
 function runTarget(){
  if(targetIndex>=queue.length){
   S.log=`${u.name} hit ${targets.length} target${targets.length===1?'':'s'} and gained ${chakraGain} chakra${anyCombo?' from combo positioning':''}.`;
   return finishAction()
  }
  let item=queue[targetIndex++],enemy=item.enemy,attackers=item.attackers,attackIndex=0;
  function runAttacker(){
   if(enemy.hp<=0||attackIndex>=attackers.length)return setTimeout(runTarget,110);
   let ap=attackers[attackIndex++],au=front(ap),dx=enemy.x-ap.x,dy=enemy

--- match 15 @ 43596459 ---
nemy.y-dy/len*((enemy.r||19)+27)};
   let from={x:p.x,y:p.y};

   const runActiveJutsu=(u.name==='Sub-Zero')?animateFreezeBlast:animateLunge;
   const activeFrom=(u.name==='Sub-Zero')?from:from;
   const activeTarget=(u.name==='Sub-Zero')?enemy:to;
   runActiveJutsu(
    u.name,activeFrom,activeTarget,
    ()=>{
     enemy.hp=Math.max(0,enemy.hp-u.jutsuDamage);
     addImpactFlash(enemy.x,enemy.y-8,u.name==='Crimson'?'#ff405c':'#8cecff');
     addFloat(enemy.x,enemy.y-34,'-'+u.jutsuDamage,'#57e7ff');
     let jDir=Math.sign(enemy.x-p.x)||1;
     if(enemy.hp<=0)handleEnemyKO(enemy,jDir); else if(u.name!=='Sub-Zero')recoil(enemy,()=>{},jDir,true);
     if(u.name==='Sub-Zero')enemy.gauge=Math.max(0,(enemy.gauge||0)-35);
     checkVictoryKillshot();
    },
    ()=>{
     let helperIndex=0;

     function runHelper(){
      if(enemy.hp<=0 || helperIndex>=helpers.length)
       return setTimeout(runJutsuTarget,110);

      let hp=helpers[helperIndex++];
      let hu=front(hp);
      let hdx=enemy.x-hp.x,hdy=enemy.y-hp.y,hlen=Math.max(1,Math.hypot(hdx,hdy));
      let hto={
       x:enemy.x-hdx/hlen*((enemy.r||19)+27),
       y:enemy.y-hdy/hlen*((enemy.r||19)+27)
      };
      let hfrom={x:hp.x,y:hp.y};

      animateLunge(
       hu.name,hfrom,hto,
       ()=>{
        let hResult=buffedNormalDamage(hp),hDamage=hResult.damage;
        enemy.hp=Math.max(0,enemy.hp-hDamage);
        addImpactFlash(enemy.x,enemy.y-8,hu.name==='Crimson'?'#ff405c':'#ffffff');
        addFloat(enemy.x,enemy.y-34,'-'+hDamage,hResult.buff.bonus>0?'#65ff9e':'#ffbd4a');
        let hDir=Math.sign(enemy.x-hp.x)||1;
        if(enemy.hp<=0)handleEnemyKO(enemy,hDir); else recoil(enemy,()=>{},hDir,false);
        checkVictoryKillshot();
       },
       ()=>setTimeout(runHelper,85),
       helperIndex%2===1?'punch':'kick'
      )
     }

     runHelper()
    },
    'special'
   )
  }

  return runJutsuTarget()
 }

 let queue=[],anyCombo=false;
 targets.forEach(enemy=>{
  let members=comboMembers(enemy,p),attackers=[p,...members.filter(x=>x!==p)];
  if(attackers.length>1)anyCombo=true;
  queue.push({enemy,attackers})
 });
 let chakraGain=1+(anyCombo?1:0);
 u.chakra=Math.min(u.maxChakra,u.chakra+chakraGain);

 let targetIndex=0;
 function runTarget(){
  if(targetIndex>=queue.length){
   S.log=`${u.name} hit ${targets.length} target${targets.length===1?'':'s'} and gained ${chakraGain} chakra${anyCombo?' from combo positioning':''}.`;
   return finishAction()
  }
  let item=queue[targetIndex++],enemy=item.enemy,attackers=item.attackers,attackIndex=0;
  function runAttacker(){
   if(enemy.hp<=0||attackIndex>=attackers.length)return setTimeout(runTarget,110);
   let ap=attackers[attackIndex++],au=front(ap),dx=enemy.x-ap.x,dy=enemy.y-ap.y,len=Math.max(1,Math.hypot(dx,dy));
   let stop=(enemy.r||19)+27,to={x:enemy.x-dx/len*stop,y:enemy.y-dy/len*stop},from={x:ap.x,y:ap.y};
   const runBasicAttack=(au.name==='Lebee')?animateLebeeStarBlast:(au.name==='Senku'?animateSenkuBomb:animateLunge);
   const basicTarget=(au.name==='Lebee'||au.name==='Senku')?enemy:to;
   runBasicAttack(au.name,from,basicTarget,()=>{
    let result=buffedNormalDamage(ap),dmg=result.damage;
    enemy.hp=Math.max(0,enemy.hp-d

--- match 16 @ 43596552 ---
e==='Sub-Zero')?animateFreezeBlast:animateLunge;
   const activeFrom=(u.name==='Sub-Zero')?from:from;
   const activeTarget=(u.name==='Sub-Zero')?enemy:to;
   runActiveJutsu(
    u.name,activeFrom,activeTarget,
    ()=>{
     enemy.hp=Math.max(0,enemy.hp-u.jutsuDamage);
     addImpactFlash(enemy.x,enemy.y-8,u.name==='Crimson'?'#ff405c':'#8cecff');
     addFloat(enemy.x,enemy.y-34,'-'+u.jutsuDamage,'#57e7ff');
     let jDir=Math.sign(enemy.x-p.x)||1;
     if(enemy.hp<=0)handleEnemyKO(enemy,jDir); else if(u.name!=='Sub-Zero')recoil(enemy,()=>{},jDir,true);
     if(u.name==='Sub-Zero')enemy.gauge=Math.max(0,(enemy.gauge||0)-35);
     checkVictoryKillshot();
    },
    ()=>{
     let helperIndex=0;

     function runHelper(){
      if(enemy.hp<=0 || helperIndex>=helpers.length)
       return setTimeout(runJutsuTarget,110);

      let hp=helpers[helperIndex++];
      let hu=front(hp);
      let hdx=enemy.x-hp.x,hdy=enemy.y-hp.y,hlen=Math.max(1,Math.hypot(hdx,hdy));
      let hto={
       x:enemy.x-hdx/hlen*((enemy.r||19)+27),
       y:enemy.y-hdy/hlen*((enemy.r||19)+27)
      };
      let hfrom={x:hp.x,y:hp.y};

      animateLunge(
       hu.name,hfrom,hto,
       ()=>{
        let hResult=buffedNormalDamage(hp),hDamage=hResult.damage;
        enemy.hp=Math.max(0,enemy.hp-hDamage);
        addImpactFlash(enemy.x,enemy.y-8,hu.name==='Crimson'?'#ff405c':'#ffffff');
        addFloat(enemy.x,enemy.y-34,'-'+hDamage,hResult.buff.bonus>0?'#65ff9e':'#ffbd4a');
        let hDir=Math.sign(enemy.x-hp.x)||1;
        if(enemy.hp<=0)handleEnemyKO(enemy,hDir); else recoil(enemy,()=>{},hDir,false);
        checkVictoryKillshot();
       },
       ()=>setTimeout(runHelper,85),
       helperIndex%2===1?'punch':'kick'
      )
     }

     runHelper()
    },
    'special'
   )
  }

  return runJutsuTarget()
 }

 let queue=[],anyCombo=false;
 targets.forEach(enemy=>{
  let members=comboMembers(enemy,p),attackers=[p,...members.filter(x=>x!==p)];
  if(attackers.length>1)anyCombo=true;
  queue.push({enemy,attackers})
 });
 let chakraGain=1+(anyCombo?1:0);
 u.chakra=Math.min(u.maxChakra,u.chakra+chakraGain);

 let targetIndex=0;
 function runTarget(){
  if(targetIndex>=queue.length){
   S.log=`${u.name} hit ${targets.length} target${targets.length===1?'':'s'} and gained ${chakraGain} chakra${anyCombo?' from combo positioning':''}.`;
   return finishAction()
  }
  let item=queue[targetIndex++],enemy=item.enemy,attackers=item.attackers,attackIndex=0;
  function runAttacker(){
   if(enemy.hp<=0||attackIndex>=attackers.length)return setTimeout(runTarget,110);
   let ap=attackers[attackIndex++],au=front(ap),dx=enemy.x-ap.x,dy=enemy.y-ap.y,len=Math.max(1,Math.hypot(dx,dy));
   let stop=(enemy.r||19)+27,to={x:enemy.x-dx/len*stop,y:enemy.y-dy/len*stop},from={x:ap.x,y:ap.y};
   const runBasicAttack=(au.name==='Lebee')?animateLebeeStarBlast:(au.name==='Senku'?animateSenkuBomb:animateLunge);
   const basicTarget=(au.name==='Lebee'||au.name==='Senku')?enemy:to;
   runBasicAttack(au.name,from,basicTarget,()=>{
    let result=buffedNormalDamage(ap),dmg=result.damage;
    enemy.hp=Math.max(0,enemy.hp-dmg);
    addImpactFlash(enemy.x,enemy.y-8,au.name==='Crimson'?'#ff405c':attackIndex>1?'#ffbd4

--- match 17 @ 43596873 ---
imson'?'#ff405c':'#8cecff');
     addFloat(enemy.x,enemy.y-34,'-'+u.jutsuDamage,'#57e7ff');
     let jDir=Math.sign(enemy.x-p.x)||1;
     if(enemy.hp<=0)handleEnemyKO(enemy,jDir); else if(u.name!=='Sub-Zero')recoil(enemy,()=>{},jDir,true);
     if(u.name==='Sub-Zero')enemy.gauge=Math.max(0,(enemy.gauge||0)-35);
     checkVictoryKillshot();
    },
    ()=>{
     let helperIndex=0;

     function runHelper(){
      if(enemy.hp<=0 || helperIndex>=helpers.length)
       return setTimeout(runJutsuTarget,110);

      let hp=helpers[helperIndex++];
      let hu=front(hp);
      let hdx=enemy.x-hp.x,hdy=enemy.y-hp.y,hlen=Math.max(1,Math.hypot(hdx,hdy));
      let hto={
       x:enemy.x-hdx/hlen*((enemy.r||19)+27),
       y:enemy.y-hdy/hlen*((enemy.r||19)+27)
      };
      let hfrom={x:hp.x,y:hp.y};

      animateLunge(
       hu.name,hfrom,hto,
       ()=>{
        let hResult=buffedNormalDamage(hp),hDamage=hResult.damage;
        enemy.hp=Math.max(0,enemy.hp-hDamage);
        addImpactFlash(enemy.x,enemy.y-8,hu.name==='Crimson'?'#ff405c':'#ffffff');
        addFloat(enemy.x,enemy.y-34,'-'+hDamage,hResult.buff.bonus>0?'#65ff9e':'#ffbd4a');
        let hDir=Math.sign(enemy.x-hp.x)||1;
        if(enemy.hp<=0)handleEnemyKO(enemy,hDir); else recoil(enemy,()=>{},hDir,false);
        checkVictoryKillshot();
       },
       ()=>setTimeout(runHelper,85),
       helperIndex%2===1?'punch':'kick'
      )
     }

     runHelper()
    },
    'special'
   )
  }

  return runJutsuTarget()
 }

 let queue=[],anyCombo=false;
 targets.forEach(enemy=>{
  let members=comboMembers(enemy,p),attackers=[p,...members.filter(x=>x!==p)];
  if(attackers.length>1)anyCombo=true;
  queue.push({enemy,attackers})
 });
 let chakraGain=1+(anyCombo?1:0);
 u.chakra=Math.min(u.maxChakra,u.chakra+chakraGain);

 let targetIndex=0;
 function runTarget(){
  if(targetIndex>=queue.length){
   S.log=`${u.name} hit ${targets.length} target${targets.length===1?'':'s'} and gained ${chakraGain} chakra${anyCombo?' from combo positioning':''}.`;
   return finishAction()
  }
  let item=queue[targetIndex++],enemy=item.enemy,attackers=item.attackers,attackIndex=0;
  function runAttacker(){
   if(enemy.hp<=0||attackIndex>=attackers.length)return setTimeout(runTarget,110);
   let ap=attackers[attackIndex++],au=front(ap),dx=enemy.x-ap.x,dy=enemy.y-ap.y,len=Math.max(1,Math.hypot(dx,dy));
   let stop=(enemy.r||19)+27,to={x:enemy.x-dx/len*stop,y:enemy.y-dy/len*stop},from={x:ap.x,y:ap.y};
   const runBasicAttack=(au.name==='Lebee')?animateLebeeStarBlast:(au.name==='Senku'?animateSenkuBomb:animateLunge);
   const basicTarget=(au.name==='Lebee'||au.name==='Senku')?enemy:to;
   runBasicAttack(au.name,from,basicTarget,()=>{
    let result=buffedNormalDamage(ap),dmg=result.damage;
    enemy.hp=Math.max(0,enemy.hp-dmg);
    addImpactFlash(enemy.x,enemy.y-8,au.name==='Crimson'?'#ff405c':attackIndex>1?'#ffbd4a':'#ffffff');
    addFloat(enemy.x,enemy.y-34,'-'+dmg,result.buff.bonus>0?'#65ff9e':attackIndex>1?'#ffbd4a':'#fff');
    let nDir=Math.sign(enemy.x-ap.x)||1;
    if(enemy.hp<=0)handleEnemyKO(enemy,nDir); else recoil(enemy,()=>{},nDir,attackIndex>1);
    checkVictoryKillshot();
   },()=>setTimeout(runAttacker,85),attack

--- match 18 @ 43596950 ---
ge,'#57e7ff');
     let jDir=Math.sign(enemy.x-p.x)||1;
     if(enemy.hp<=0)handleEnemyKO(enemy,jDir); else if(u.name!=='Sub-Zero')recoil(enemy,()=>{},jDir,true);
     if(u.name==='Sub-Zero')enemy.gauge=Math.max(0,(enemy.gauge||0)-35);
     checkVictoryKillshot();
    },
    ()=>{
     let helperIndex=0;

     function runHelper(){
      if(enemy.hp<=0 || helperIndex>=helpers.length)
       return setTimeout(runJutsuTarget,110);

      let hp=helpers[helperIndex++];
      let hu=front(hp);
      let hdx=enemy.x-hp.x,hdy=enemy.y-hp.y,hlen=Math.max(1,Math.hypot(hdx,hdy));
      let hto={
       x:enemy.x-hdx/hlen*((enemy.r||19)+27),
       y:enemy.y-hdy/hlen*((enemy.r||19)+27)
      };
      let hfrom={x:hp.x,y:hp.y};

      animateLunge(
       hu.name,hfrom,hto,
       ()=>{
        let hResult=buffedNormalDamage(hp),hDamage=hResult.damage;
        enemy.hp=Math.max(0,enemy.hp-hDamage);
        addImpactFlash(enemy.x,enemy.y-8,hu.name==='Crimson'?'#ff405c':'#ffffff');
        addFloat(enemy.x,enemy.y-34,'-'+hDamage,hResult.buff.bonus>0?'#65ff9e':'#ffbd4a');
        let hDir=Math.sign(enemy.x-hp.x)||1;
        if(enemy.hp<=0)handleEnemyKO(enemy,hDir); else recoil(enemy,()=>{},hDir,false);
        checkVictoryKillshot();
       },
       ()=>setTimeout(runHelper,85),
       helperIndex%2===1?'punch':'kick'
      )
     }

     runHelper()
    },
    'special'
   )
  }

  return runJutsuTarget()
 }

 let queue=[],anyCombo=false;
 targets.forEach(enemy=>{
  let members=comboMembers(enemy,p),attackers=[p,...members.filter(x=>x!==p)];
  if(attackers.length>1)anyCombo=true;
  queue.push({enemy,attackers})
 });
 let chakraGain=1+(anyCombo?1:0);
 u.chakra=Math.min(u.maxChakra,u.chakra+chakraGain);

 let targetIndex=0;
 function runTarget(){
  if(targetIndex>=queue.length){
   S.log=`${u.name} hit ${targets.length} target${targets.length===1?'':'s'} and gained ${chakraGain} chakra${anyCombo?' from combo positioning':''}.`;
   return finishAction()
  }
  let item=queue[targetIndex++],enemy=item.enemy,attackers=item.attackers,attackIndex=0;
  function runAttacker(){
   if(enemy.hp<=0||attackIndex>=attackers.length)return setTimeout(runTarget,110);
   let ap=attackers[attackIndex++],au=front(ap),dx=enemy.x-ap.x,dy=enemy.y-ap.y,len=Math.max(1,Math.hypot(dx,dy));
   let stop=(enemy.r||19)+27,to={x:enemy.x-dx/len*stop,y:enemy.y-dy/len*stop},from={x:ap.x,y:ap.y};
   const runBasicAttack=(au.name==='Lebee')?animateLebeeStarBlast:(au.name==='Senku'?animateSenkuBomb:animateLunge);
   const basicTarget=(au.name==='Lebee'||au.name==='Senku')?enemy:to;
   runBasicAttack(au.name,from,basicTarget,()=>{
    let result=buffedNormalDamage(ap),dmg=result.damage;
    enemy.hp=Math.max(0,enemy.hp-dmg);
    addImpactFlash(enemy.x,enemy.y-8,au.name==='Crimson'?'#ff405c':attackIndex>1?'#ffbd4a':'#ffffff');
    addFloat(enemy.x,enemy.y-34,'-'+dmg,result.buff.bonus>0?'#65ff9e':attackIndex>1?'#ffbd4a':'#fff');
    let nDir=Math.sign(enemy.x-ap.x)||1;
    if(enemy.hp<=0)handleEnemyKO(enemy,nDir); else recoil(enemy,()=>{},nDir,attackIndex>1);
    checkVictoryKillshot();
   },()=>setTimeout(runAttacker,85),attackIndex%2===1?'punch':'kick')
  }
  runAttacker()
 }
 runTarget()
}

function f

--- match 19 @ 43597051 ---
; else if(u.name!=='Sub-Zero')recoil(enemy,()=>{},jDir,true);
     if(u.name==='Sub-Zero')enemy.gauge=Math.max(0,(enemy.gauge||0)-35);
     checkVictoryKillshot();
    },
    ()=>{
     let helperIndex=0;

     function runHelper(){
      if(enemy.hp<=0 || helperIndex>=helpers.length)
       return setTimeout(runJutsuTarget,110);

      let hp=helpers[helperIndex++];
      let hu=front(hp);
      let hdx=enemy.x-hp.x,hdy=enemy.y-hp.y,hlen=Math.max(1,Math.hypot(hdx,hdy));
      let hto={
       x:enemy.x-hdx/hlen*((enemy.r||19)+27),
       y:enemy.y-hdy/hlen*((enemy.r||19)+27)
      };
      let hfrom={x:hp.x,y:hp.y};

      animateLunge(
       hu.name,hfrom,hto,
       ()=>{
        let hResult=buffedNormalDamage(hp),hDamage=hResult.damage;
        enemy.hp=Math.max(0,enemy.hp-hDamage);
        addImpactFlash(enemy.x,enemy.y-8,hu.name==='Crimson'?'#ff405c':'#ffffff');
        addFloat(enemy.x,enemy.y-34,'-'+hDamage,hResult.buff.bonus>0?'#65ff9e':'#ffbd4a');
        let hDir=Math.sign(enemy.x-hp.x)||1;
        if(enemy.hp<=0)handleEnemyKO(enemy,hDir); else recoil(enemy,()=>{},hDir,false);
        checkVictoryKillshot();
       },
       ()=>setTimeout(runHelper,85),
       helperIndex%2===1?'punch':'kick'
      )
     }

     runHelper()
    },
    'special'
   )
  }

  return runJutsuTarget()
 }

 let queue=[],anyCombo=false;
 targets.forEach(enemy=>{
  let members=comboMembers(enemy,p),attackers=[p,...members.filter(x=>x!==p)];
  if(attackers.length>1)anyCombo=true;
  queue.push({enemy,attackers})
 });
 let chakraGain=1+(anyCombo?1:0);
 u.chakra=Math.min(u.maxChakra,u.chakra+chakraGain);

 let targetIndex=0;
 function runTarget(){
  if(targetIndex>=queue.length){
   S.log=`${u.name} hit ${targets.length} target${targets.length===1?'':'s'} and gained ${chakraGain} chakra${anyCombo?' from combo positioning':''}.`;
   return finishAction()
  }
  let item=queue[targetIndex++],enemy=item.enemy,attackers=item.attackers,attackIndex=0;
  function runAttacker(){
   if(enemy.hp<=0||attackIndex>=attackers.length)return setTimeout(runTarget,110);
   let ap=attackers[attackIndex++],au=front(ap),dx=enemy.x-ap.x,dy=enemy.y-ap.y,len=Math.max(1,Math.hypot(dx,dy));
   let stop=(enemy.r||19)+27,to={x:enemy.x-dx/len*stop,y:enemy.y-dy/len*stop},from={x:ap.x,y:ap.y};
   const runBasicAttack=(au.name==='Lebee')?animateLebeeStarBlast:(au.name==='Senku'?animateSenkuBomb:animateLunge);
   const basicTarget=(au.name==='Lebee'||au.name==='Senku')?enemy:to;
   runBasicAttack(au.name,from,basicTarget,()=>{
    let result=buffedNormalDamage(ap),dmg=result.damage;
    enemy.hp=Math.max(0,enemy.hp-dmg);
    addImpactFlash(enemy.x,enemy.y-8,au.name==='Crimson'?'#ff405c':attackIndex>1?'#ffbd4a':'#ffffff');
    addFloat(enemy.x,enemy.y-34,'-'+dmg,result.buff.bonus>0?'#65ff9e':attackIndex>1?'#ffbd4a':'#fff');
    let nDir=Math.sign(enemy.x-ap.x)||1;
    if(enemy.hp<=0)handleEnemyKO(enemy,nDir); else recoil(enemy,()=>{},nDir,attackIndex>1);
    checkVictoryKillshot();
   },()=>setTimeout(runAttacker,85),attackIndex%2===1?'punch':'kick')
  }
  runAttacker()
 }
 runTarget()
}

function finishAction(){
 const token=ACTIVE_ACTION_TOKEN;
 clearActionWatchdog();
 setTimeout(()=>{
  if(!acti

--- match 20 @ 43598175 ---
ryKillshot();
       },
       ()=>setTimeout(runHelper,85),
       helperIndex%2===1?'punch':'kick'
      )
     }

     runHelper()
    },
    'special'
   )
  }

  return runJutsuTarget()
 }

 let queue=[],anyCombo=false;
 targets.forEach(enemy=>{
  let members=comboMembers(enemy,p),attackers=[p,...members.filter(x=>x!==p)];
  if(attackers.length>1)anyCombo=true;
  queue.push({enemy,attackers})
 });
 let chakraGain=1+(anyCombo?1:0);
 u.chakra=Math.min(u.maxChakra,u.chakra+chakraGain);

 let targetIndex=0;
 function runTarget(){
  if(targetIndex>=queue.length){
   S.log=`${u.name} hit ${targets.length} target${targets.length===1?'':'s'} and gained ${chakraGain} chakra${anyCombo?' from combo positioning':''}.`;
   return finishAction()
  }
  let item=queue[targetIndex++],enemy=item.enemy,attackers=item.attackers,attackIndex=0;
  function runAttacker(){
   if(enemy.hp<=0||attackIndex>=attackers.length)return setTimeout(runTarget,110);
   let ap=attackers[attackIndex++],au=front(ap),dx=enemy.x-ap.x,dy=enemy.y-ap.y,len=Math.max(1,Math.hypot(dx,dy));
   let stop=(enemy.r||19)+27,to={x:enemy.x-dx/len*stop,y:enemy.y-dy/len*stop},from={x:ap.x,y:ap.y};
   const runBasicAttack=(au.name==='Lebee')?animateLebeeStarBlast:(au.name==='Senku'?animateSenkuBomb:animateLunge);
   const basicTarget=(au.name==='Lebee'||au.name==='Senku')?enemy:to;
   runBasicAttack(au.name,from,basicTarget,()=>{
    let result=buffedNormalDamage(ap),dmg=result.damage;
    enemy.hp=Math.max(0,enemy.hp-dmg);
    addImpactFlash(enemy.x,enemy.y-8,au.name==='Crimson'?'#ff405c':attackIndex>1?'#ffbd4a':'#ffffff');
    addFloat(enemy.x,enemy.y-34,'-'+dmg,result.buff.bonus>0?'#65ff9e':attackIndex>1?'#ffbd4a':'#fff');
    let nDir=Math.sign(enemy.x-ap.x)||1;
    if(enemy.hp<=0)handleEnemyKO(enemy,nDir); else recoil(enemy,()=>{},nDir,attackIndex>1);
    checkVictoryKillshot();
   },()=>setTimeout(runAttacker,85),attackIndex%2===1?'punch':'kick')
  }
  runAttacker()
 }
 runTarget()
}

function finishAction(){
 const token=ACTIVE_ACTION_TOKEN;
 clearActionWatchdog();
 setTimeout(()=>{
  if(!actionTokenAlive(token))return;
  // v0.5.10: retire the completed action token BEFORE clearing S.anim.
  // This prevents late recoil/requestAnimationFrame callbacks from recreating
  // S.anim after the turn has returned to charge, which caused the hard freeze.
  invalidateActions();
  S.anim=null;S.ready=null;S.action='normal';
  if(S.victoryFX||!aliveEnemies().length){
   if(!S.victoryFX)startVictorySequence();
   S.phase='resolve';
   return;
  }
  if(!alivePairs().length){
   S.phase='resolve';
   S.log='Defeat — all three fighter pairs have been knocked out.';
   return;
  }
  S.phase='charge';S._chargeSince=performance.now();
 },220)
}

function enemiesMutuallyLinked(a,b){
 if(a===b || a.hp<=0 || b.hp<=0)return false;

 let bodyA={x:a.x,y:a.y,r:a.r||16};
 let bodyB={x:b.x,y:b.y,r:b.r||16};

 let aCoversB=hits(a,{shape:normalShape(),rotation:0},bodyB);
 let bCoversA=hits(b,{shape:normalShape(),rotation:0},bodyA);

 return aCoversB && bCoversA
}

function enemyComboMembers(targetPair,initiator){
 // Target is no longer part of combo eligibility.
 // Once linked, the helper may join the initiator's valid a

--- match 21 @ 43598274 ---
'
      )
     }

     runHelper()
    },
    'special'
   )
  }

  return runJutsuTarget()
 }

 let queue=[],anyCombo=false;
 targets.forEach(enemy=>{
  let members=comboMembers(enemy,p),attackers=[p,...members.filter(x=>x!==p)];
  if(attackers.length>1)anyCombo=true;
  queue.push({enemy,attackers})
 });
 let chakraGain=1+(anyCombo?1:0);
 u.chakra=Math.min(u.maxChakra,u.chakra+chakraGain);

 let targetIndex=0;
 function runTarget(){
  if(targetIndex>=queue.length){
   S.log=`${u.name} hit ${targets.length} target${targets.length===1?'':'s'} and gained ${chakraGain} chakra${anyCombo?' from combo positioning':''}.`;
   return finishAction()
  }
  let item=queue[targetIndex++],enemy=item.enemy,attackers=item.attackers,attackIndex=0;
  function runAttacker(){
   if(enemy.hp<=0||attackIndex>=attackers.length)return setTimeout(runTarget,110);
   let ap=attackers[attackIndex++],au=front(ap),dx=enemy.x-ap.x,dy=enemy.y-ap.y,len=Math.max(1,Math.hypot(dx,dy));
   let stop=(enemy.r||19)+27,to={x:enemy.x-dx/len*stop,y:enemy.y-dy/len*stop},from={x:ap.x,y:ap.y};
   const runBasicAttack=(au.name==='Lebee')?animateLebeeStarBlast:(au.name==='Senku'?animateSenkuBomb:animateLunge);
   const basicTarget=(au.name==='Lebee'||au.name==='Senku')?enemy:to;
   runBasicAttack(au.name,from,basicTarget,()=>{
    let result=buffedNormalDamage(ap),dmg=result.damage;
    enemy.hp=Math.max(0,enemy.hp-dmg);
    addImpactFlash(enemy.x,enemy.y-8,au.name==='Crimson'?'#ff405c':attackIndex>1?'#ffbd4a':'#ffffff');
    addFloat(enemy.x,enemy.y-34,'-'+dmg,result.buff.bonus>0?'#65ff9e':attackIndex>1?'#ffbd4a':'#fff');
    let nDir=Math.sign(enemy.x-ap.x)||1;
    if(enemy.hp<=0)handleEnemyKO(enemy,nDir); else recoil(enemy,()=>{},nDir,attackIndex>1);
    checkVictoryKillshot();
   },()=>setTimeout(runAttacker,85),attackIndex%2===1?'punch':'kick')
  }
  runAttacker()
 }
 runTarget()
}

function finishAction(){
 const token=ACTIVE_ACTION_TOKEN;
 clearActionWatchdog();
 setTimeout(()=>{
  if(!actionTokenAlive(token))return;
  // v0.5.10: retire the completed action token BEFORE clearing S.anim.
  // This prevents late recoil/requestAnimationFrame callbacks from recreating
  // S.anim after the turn has returned to charge, which caused the hard freeze.
  invalidateActions();
  S.anim=null;S.ready=null;S.action='normal';
  if(S.victoryFX||!aliveEnemies().length){
   if(!S.victoryFX)startVictorySequence();
   S.phase='resolve';
   return;
  }
  if(!alivePairs().length){
   S.phase='resolve';
   S.log='Defeat — all three fighter pairs have been knocked out.';
   return;
  }
  S.phase='charge';S._chargeSince=performance.now();
 },220)
}

function enemiesMutuallyLinked(a,b){
 if(a===b || a.hp<=0 || b.hp<=0)return false;

 let bodyA={x:a.x,y:a.y,r:a.r||16};
 let bodyB={x:b.x,y:b.y,r:b.r||16};

 let aCoversB=hits(a,{shape:normalShape(),rotation:0},bodyB);
 let bCoversA=hits(b,{shape:normalShape(),rotation:0},bodyA);

 return aCoversB && bCoversA
}

function enemyComboMembers(targetPair,initiator){
 // Target is no longer part of combo eligibility.
 // Once linked, the helper may join the initiator's valid attack target.
 return S.enemies.filter(enemy=>{
   if(enemy.hp<=0)return false;
   return enemy===i

--- match 22 @ 43598690 ---
function runTarget(){
  if(targetIndex>=queue.length){
   S.log=`${u.name} hit ${targets.length} target${targets.length===1?'':'s'} and gained ${chakraGain} chakra${anyCombo?' from combo positioning':''}.`;
   return finishAction()
  }
  let item=queue[targetIndex++],enemy=item.enemy,attackers=item.attackers,attackIndex=0;
  function runAttacker(){
   if(enemy.hp<=0||attackIndex>=attackers.length)return setTimeout(runTarget,110);
   let ap=attackers[attackIndex++],au=front(ap),dx=enemy.x-ap.x,dy=enemy.y-ap.y,len=Math.max(1,Math.hypot(dx,dy));
   let stop=(enemy.r||19)+27,to={x:enemy.x-dx/len*stop,y:enemy.y-dy/len*stop},from={x:ap.x,y:ap.y};
   const runBasicAttack=(au.name==='Lebee')?animateLebeeStarBlast:(au.name==='Senku'?animateSenkuBomb:animateLunge);
   const basicTarget=(au.name==='Lebee'||au.name==='Senku')?enemy:to;
   runBasicAttack(au.name,from,basicTarget,()=>{
    let result=buffedNormalDamage(ap),dmg=result.damage;
    enemy.hp=Math.max(0,enemy.hp-dmg);
    addImpactFlash(enemy.x,enemy.y-8,au.name==='Crimson'?'#ff405c':attackIndex>1?'#ffbd4a':'#ffffff');
    addFloat(enemy.x,enemy.y-34,'-'+dmg,result.buff.bonus>0?'#65ff9e':attackIndex>1?'#ffbd4a':'#fff');
    let nDir=Math.sign(enemy.x-ap.x)||1;
    if(enemy.hp<=0)handleEnemyKO(enemy,nDir); else recoil(enemy,()=>{},nDir,attackIndex>1);
    checkVictoryKillshot();
   },()=>setTimeout(runAttacker,85),attackIndex%2===1?'punch':'kick')
  }
  runAttacker()
 }
 runTarget()
}

function finishAction(){
 const token=ACTIVE_ACTION_TOKEN;
 clearActionWatchdog();
 setTimeout(()=>{
  if(!actionTokenAlive(token))return;
  // v0.5.10: retire the completed action token BEFORE clearing S.anim.
  // This prevents late recoil/requestAnimationFrame callbacks from recreating
  // S.anim after the turn has returned to charge, which caused the hard freeze.
  invalidateActions();
  S.anim=null;S.ready=null;S.action='normal';
  if(S.victoryFX||!aliveEnemies().length){
   if(!S.victoryFX)startVictorySequence();
   S.phase='resolve';
   return;
  }
  if(!alivePairs().length){
   S.phase='resolve';
   S.log='Defeat — all three fighter pairs have been knocked out.';
   return;
  }
  S.phase='charge';S._chargeSince=performance.now();
 },220)
}

function enemiesMutuallyLinked(a,b){
 if(a===b || a.hp<=0 || b.hp<=0)return false;

 let bodyA={x:a.x,y:a.y,r:a.r||16};
 let bodyB={x:b.x,y:b.y,r:b.r||16};

 let aCoversB=hits(a,{shape:normalShape(),rotation:0},bodyB);
 let bCoversA=hits(b,{shape:normalShape(),rotation:0},bodyA);

 return aCoversB && bCoversA
}

function enemyComboMembers(targetPair,initiator){
 // Target is no longer part of combo eligibility.
 // Once linked, the helper may join the initiator's valid attack target.
 return S.enemies.filter(enemy=>{
   if(enemy.hp<=0)return false;
   return enemy===initiator || enemiesMutuallyLinked(initiator,enemy)
 })
}

function resolveEnemyOverlap(enemy){
 const minDist=47;

 // Resolve only the enemy that just moved. This intentionally does not
 // mutate player positions or other enemy positions.
 for(let pass=0;pass<8;pass++){
   let blocker=null;
   let blockerDist=Infinity;

   S.pairs.forEach(pair=>{
     if(!pairAlive(pair))return;
     let dist=d(enemy,pair);
   

--- match 23 @ 43598787 ---
target${targets.length===1?'':'s'} and gained ${chakraGain} chakra${anyCombo?' from combo positioning':''}.`;
   return finishAction()
  }
  let item=queue[targetIndex++],enemy=item.enemy,attackers=item.attackers,attackIndex=0;
  function runAttacker(){
   if(enemy.hp<=0||attackIndex>=attackers.length)return setTimeout(runTarget,110);
   let ap=attackers[attackIndex++],au=front(ap),dx=enemy.x-ap.x,dy=enemy.y-ap.y,len=Math.max(1,Math.hypot(dx,dy));
   let stop=(enemy.r||19)+27,to={x:enemy.x-dx/len*stop,y:enemy.y-dy/len*stop},from={x:ap.x,y:ap.y};
   const runBasicAttack=(au.name==='Lebee')?animateLebeeStarBlast:(au.name==='Senku'?animateSenkuBomb:animateLunge);
   const basicTarget=(au.name==='Lebee'||au.name==='Senku')?enemy:to;
   runBasicAttack(au.name,from,basicTarget,()=>{
    let result=buffedNormalDamage(ap),dmg=result.damage;
    enemy.hp=Math.max(0,enemy.hp-dmg);
    addImpactFlash(enemy.x,enemy.y-8,au.name==='Crimson'?'#ff405c':attackIndex>1?'#ffbd4a':'#ffffff');
    addFloat(enemy.x,enemy.y-34,'-'+dmg,result.buff.bonus>0?'#65ff9e':attackIndex>1?'#ffbd4a':'#fff');
    let nDir=Math.sign(enemy.x-ap.x)||1;
    if(enemy.hp<=0)handleEnemyKO(enemy,nDir); else recoil(enemy,()=>{},nDir,attackIndex>1);
    checkVictoryKillshot();
   },()=>setTimeout(runAttacker,85),attackIndex%2===1?'punch':'kick')
  }
  runAttacker()
 }
 runTarget()
}

function finishAction(){
 const token=ACTIVE_ACTION_TOKEN;
 clearActionWatchdog();
 setTimeout(()=>{
  if(!actionTokenAlive(token))return;
  // v0.5.10: retire the completed action token BEFORE clearing S.anim.
  // This prevents late recoil/requestAnimationFrame callbacks from recreating
  // S.anim after the turn has returned to charge, which caused the hard freeze.
  invalidateActions();
  S.anim=null;S.ready=null;S.action='normal';
  if(S.victoryFX||!aliveEnemies().length){
   if(!S.victoryFX)startVictorySequence();
   S.phase='resolve';
   return;
  }
  if(!alivePairs().length){
   S.phase='resolve';
   S.log='Defeat — all three fighter pairs have been knocked out.';
   return;
  }
  S.phase='charge';S._chargeSince=performance.now();
 },220)
}

function enemiesMutuallyLinked(a,b){
 if(a===b || a.hp<=0 || b.hp<=0)return false;

 let bodyA={x:a.x,y:a.y,r:a.r||16};
 let bodyB={x:b.x,y:b.y,r:b.r||16};

 let aCoversB=hits(a,{shape:normalShape(),rotation:0},bodyB);
 let bCoversA=hits(b,{shape:normalShape(),rotation:0},bodyA);

 return aCoversB && bCoversA
}

function enemyComboMembers(targetPair,initiator){
 // Target is no longer part of combo eligibility.
 // Once linked, the helper may join the initiator's valid attack target.
 return S.enemies.filter(enemy=>{
   if(enemy.hp<=0)return false;
   return enemy===initiator || enemiesMutuallyLinked(initiator,enemy)
 })
}

function resolveEnemyOverlap(enemy){
 const minDist=47;

 // Resolve only the enemy that just moved. This intentionally does not
 // mutate player positions or other enemy positions.
 for(let pass=0;pass<8;pass++){
   let blocker=null;
   let blockerDist=Infinity;

   S.pairs.forEach(pair=>{
     if(!pairAlive(pair))return;
     let dist=d(enemy,pair);
     if(dist<minDist && dist<blockerDist){
       blocker=pair;
       blockerDist=dist;
     }
   }

--- match 24 @ 43598900 ---
return finishAction()
  }
  let item=queue[targetIndex++],enemy=item.enemy,attackers=item.attackers,attackIndex=0;
  function runAttacker(){
   if(enemy.hp<=0||attackIndex>=attackers.length)return setTimeout(runTarget,110);
   let ap=attackers[attackIndex++],au=front(ap),dx=enemy.x-ap.x,dy=enemy.y-ap.y,len=Math.max(1,Math.hypot(dx,dy));
   let stop=(enemy.r||19)+27,to={x:enemy.x-dx/len*stop,y:enemy.y-dy/len*stop},from={x:ap.x,y:ap.y};
   const runBasicAttack=(au.name==='Lebee')?animateLebeeStarBlast:(au.name==='Senku'?animateSenkuBomb:animateLunge);
   const basicTarget=(au.name==='Lebee'||au.name==='Senku')?enemy:to;
   runBasicAttack(au.name,from,basicTarget,()=>{
    let result=buffedNormalDamage(ap),dmg=result.damage;
    enemy.hp=Math.max(0,enemy.hp-dmg);
    addImpactFlash(enemy.x,enemy.y-8,au.name==='Crimson'?'#ff405c':attackIndex>1?'#ffbd4a':'#ffffff');
    addFloat(enemy.x,enemy.y-34,'-'+dmg,result.buff.bonus>0?'#65ff9e':attackIndex>1?'#ffbd4a':'#fff');
    let nDir=Math.sign(enemy.x-ap.x)||1;
    if(enemy.hp<=0)handleEnemyKO(enemy,nDir); else recoil(enemy,()=>{},nDir,attackIndex>1);
    checkVictoryKillshot();
   },()=>setTimeout(runAttacker,85),attackIndex%2===1?'punch':'kick')
  }
  runAttacker()
 }
 runTarget()
}

function finishAction(){
 const token=ACTIVE_ACTION_TOKEN;
 clearActionWatchdog();
 setTimeout(()=>{
  if(!actionTokenAlive(token))return;
  // v0.5.10: retire the completed action token BEFORE clearing S.anim.
  // This prevents late recoil/requestAnimationFrame callbacks from recreating
  // S.anim after the turn has returned to charge, which caused the hard freeze.
  invalidateActions();
  S.anim=null;S.ready=null;S.action='normal';
  if(S.victoryFX||!aliveEnemies().length){
   if(!S.victoryFX)startVictorySequence();
   S.phase='resolve';
   return;
  }
  if(!alivePairs().length){
   S.phase='resolve';
   S.log='Defeat — all three fighter pairs have been knocked out.';
   return;
  }
  S.phase='charge';S._chargeSince=performance.now();
 },220)
}

function enemiesMutuallyLinked(a,b){
 if(a===b || a.hp<=0 || b.hp<=0)return false;

 let bodyA={x:a.x,y:a.y,r:a.r||16};
 let bodyB={x:b.x,y:b.y,r:b.r||16};

 let aCoversB=hits(a,{shape:normalShape(),rotation:0},bodyB);
 let bCoversA=hits(b,{shape:normalShape(),rotation:0},bodyA);

 return aCoversB && bCoversA
}

function enemyComboMembers(targetPair,initiator){
 // Target is no longer part of combo eligibility.
 // Once linked, the helper may join the initiator's valid attack target.
 return S.enemies.filter(enemy=>{
   if(enemy.hp<=0)return false;
   return enemy===initiator || enemiesMutuallyLinked(initiator,enemy)
 })
}

function resolveEnemyOverlap(enemy){
 const minDist=47;

 // Resolve only the enemy that just moved. This intentionally does not
 // mutate player positions or other enemy positions.
 for(let pass=0;pass<8;pass++){
   let blocker=null;
   let blockerDist=Infinity;

   S.pairs.forEach(pair=>{
     if(!pairAlive(pair))return;
     let dist=d(enemy,pair);
     if(dist<minDist && dist<blockerDist){
       blocker=pair;
       blockerDist=dist;
     }
   });

   S.enemies.forEach(other=>{
     if(other===enemy||other.hp<=0)return;
     let dist=d(enemy,other);
     i

--- match 25 @ 43601239 ---
oMembers(targetPair,initiator){
 // Target is no longer part of combo eligibility.
 // Once linked, the helper may join the initiator's valid attack target.
 return S.enemies.filter(enemy=>{
   if(enemy.hp<=0)return false;
   return enemy===initiator || enemiesMutuallyLinked(initiator,enemy)
 })
}

function resolveEnemyOverlap(enemy){
 const minDist=47;

 // Resolve only the enemy that just moved. This intentionally does not
 // mutate player positions or other enemy positions.
 for(let pass=0;pass<8;pass++){
   let blocker=null;
   let blockerDist=Infinity;

   S.pairs.forEach(pair=>{
     if(!pairAlive(pair))return;
     let dist=d(enemy,pair);
     if(dist<minDist && dist<blockerDist){
       blocker=pair;
       blockerDist=dist;
     }
   });

   S.enemies.forEach(other=>{
     if(other===enemy||other.hp<=0)return;
     let dist=d(enemy,other);
     if(dist<minDist && dist<blockerDist){
       blocker=other;
       blockerDist=dist;
     }
   });

   if(!blocker)break;

   let dx=enemy.x-blocker.x;
   let dy=enemy.y-blocker.y;
   let len=Math.hypot(dx,dy);

   // Exact same-center fallback gets a deterministic escape direction.
   if(len<.01){
     let angle=(pass+1)*1.913;
     dx=Math.cos(angle);
     dy=Math.sin(angle);
     len=1;
   }

   let push=(minDist-len)+.75;
   enemy.x+=(dx/len)*push;
   enemy.y+=(dy/len)*push;

   let legal=clampToBattlefield(enemy);
   enemy.x=legal.x;
   enemy.y=legal.y;
 }
}

function cpuTurn(){
 if(S.phase!=='cpu')return;
 const cpuActionToken=beginActionToken();
 armActionWatchdog('Anubis / enemy turn',cpuActionToken);
 let e=S.ready.ref;
 if(e.hp<=0){e.gauge=0;return finishAction()}

 let candidates=S.pairs.filter(pairAlive);candidates.forEach(ensureFront);
 if(!candidates.length)return;

 // Enemy personality changes each turn so the CPU does not dogpile one unit.
 // Weighted considerations:
 // - nearest target
 // - lowest-health target
 // - clustered target opportunity
 // - mild randomness so equal situations do not look scripted
 const modes=['nearest','weakest','cluster','balanced'];
 const mode=modes[Math.floor(Math.random()*modes.length)];

 function nearbyCount(p){
   return candidates.filter(other=>other!==p && d(p,other)<145).length;
 }

 function targetScore(p){
   let u=front(p);
   let dist=d(e,p);
   let hpRatio=u.hp/u.maxHp;
   let cluster=nearbyCount(p);
   let jitter=Math.random()*12;

   if(mode==='nearest')
     return dist*1.35 + hpRatio*30 - cluster*8 + jitter;
   if(mode==='weakest')
     return hpRatio*150 + dist*.35 - cluster*6 + jitter;
   if(mode==='cluster')
     return dist*.45 + hpRatio*35 - cluster*55 + jitter;

   // balanced
   return dist*.7 + hpRatio*75 - cluster*24 + jitter;
 }

 let target=[...candidates].sort((a,b)=>targetScore(a)-targetScore(b))[0];

 // Evaluate several approach angles instead of always taking the same straight-line slot.
 let angles=[];
 for(let i=0;i<10;i++)angles.push((Math.PI*2/10)*i);

 let best=null;
 angles.forEach(a=>{
   let radius=58+Math.random()*35;
   let pos=clampToBattlefield({
     x:target.x+Math.cos(a)*radius,
     y:target.y+Math.sin(a)*radius
   });

   // Score position by direct hits + mutual proximity links with enemy allies.


===== enemy.y =====

--- match 1 @ 43580792 ---
{x,y,text:'✦',color,life:1,impact:true};
 S.floaters.push(f);
 let id=setInterval(()=>{
   f.y-=.25;
   f.life-=.07;
   if(f.life<=0){
     clearInterval(id);
     S.floaters=S.floaters.filter(item=>item!==f);
   }
 },16);
}
function animateFreezeBlast(unitName,from,enemy,onImpact,onDone){
 const token=ACTIVE_ACTION_TOKEN;
 const castDuration=340;
 const flightDuration=420;
 const freezeHold=520;
 const totalDuration=castDuration+flightDuration+freezeHold;
 const castStart=performance.now();
 const state=ensureAnimState();
 if(!state.attackPose)state.attackPose={};
 // The four user-supplied cast frames are played by drawUnit via
 // ATTACK_SPRITES['Sub-Zero'].freeze while he remains planted in place.
 state.attackPose[unitName]={kind:'freeze',start:castStart,duration:castDuration+flightDuration};

 setTimeout(()=>{
   if(!actionTokenAlive(token))return;
   const projectileStart=performance.now();
   const projectile={kind:'iceProjectile',from:{x:from.x+10,y:from.y-22},to:{x:enemy.x,y:enemy.y-18},start:projectileStart,duration:flightDuration,life:1};
   S.floaters.push(projectile);
   function fly(now){
     if(!actionTokenAlive(token))return;
     const t=Math.min(1,(now-projectileStart)/flightDuration);
     if(t<1)return requestAnimationFrame(fly);
     S.floaters=S.floaters.filter(x=>x!==projectile);
     try{onImpact&&onImpact()}catch(err){console.error('Freeze Blast impact failed:',err);return recoverAction('freeze blast impact')}
     const ice={kind:'frozenTarget',x:enemy.x,y:enemy.y,start:performance.now(),life:1};
     S.floaters.push(ice);
     setTimeout(()=>{
       if(!actionTokenAlive(token))return;
       S.floaters=S.floaters.filter(x=>x!==ice);
       const st=ensureAnimState();
       if(st.attackPose)delete st.attackPose[unitName];
       try{onDone&&onDone()}catch(err){console.error('Freeze Blast completion failed:',err);recoverAction('freeze blast completion')}
     },freezeHold);
   }
   requestAnimationFrame(fly);
 },castDuration);
}


function animateLebeeStarBlast(unitName,from,enemy,onImpact,onDone){
 const token=ACTIVE_ACTION_TOKEN;
 const castDuration=320,flightDuration=300;
 const state=ensureAnimState();
 if(!state.attackPose)state.attackPose={};
 state.attackPose[unitName]={kind:'punch',start:performance.now(),duration:castDuration+flightDuration};
 setTimeout(()=>{
   if(!actionTokenAlive(token))return;
   const projectile={kind:'lebeeStarProjectile',from:{x:from.x,y:from.y-20},to:{x:enemy.x,y:enemy.y-12},start:performance.now(),duration:flightDuration,life:1};
   S.floaters.push(projectile);
   setTimeout(()=>{
     if(!actionTokenAlive(token))return;
     S.floaters=S.floaters.filter(x=>x!==projectile);
     try{onImpact&&onImpact()}catch(err){console.error('Lebee star blast impact failed:',err);return recoverAction('Lebee basic impact')}
     const st=ensureAnimState();if(st.attackPose)delete st.attackPose[unitName];
     setTimeout(()=>{if(actionTokenAlive(token)){try{onDone&&onDone()}catch(err){recoverAction('Lebee basic completion')}}},90);
   },flightDuration);
 },castDuration*.48);
}


function animateSenkuChemicalReaction(unitName,from,target,onImpact,onDone){
 const token=ACTIVE_ACTION_TOKEN;
 const meta=ca

--- match 2 @ 43581301 ---
=ensureAnimState();
 if(!state.attackPose)state.attackPose={};
 // The four user-supplied cast frames are played by drawUnit via
 // ATTACK_SPRITES['Sub-Zero'].freeze while he remains planted in place.
 state.attackPose[unitName]={kind:'freeze',start:castStart,duration:castDuration+flightDuration};

 setTimeout(()=>{
   if(!actionTokenAlive(token))return;
   const projectileStart=performance.now();
   const projectile={kind:'iceProjectile',from:{x:from.x+10,y:from.y-22},to:{x:enemy.x,y:enemy.y-18},start:projectileStart,duration:flightDuration,life:1};
   S.floaters.push(projectile);
   function fly(now){
     if(!actionTokenAlive(token))return;
     const t=Math.min(1,(now-projectileStart)/flightDuration);
     if(t<1)return requestAnimationFrame(fly);
     S.floaters=S.floaters.filter(x=>x!==projectile);
     try{onImpact&&onImpact()}catch(err){console.error('Freeze Blast impact failed:',err);return recoverAction('freeze blast impact')}
     const ice={kind:'frozenTarget',x:enemy.x,y:enemy.y,start:performance.now(),life:1};
     S.floaters.push(ice);
     setTimeout(()=>{
       if(!actionTokenAlive(token))return;
       S.floaters=S.floaters.filter(x=>x!==ice);
       const st=ensureAnimState();
       if(st.attackPose)delete st.attackPose[unitName];
       try{onDone&&onDone()}catch(err){console.error('Freeze Blast completion failed:',err);recoverAction('freeze blast completion')}
     },freezeHold);
   }
   requestAnimationFrame(fly);
 },castDuration);
}


function animateLebeeStarBlast(unitName,from,enemy,onImpact,onDone){
 const token=ACTIVE_ACTION_TOKEN;
 const castDuration=320,flightDuration=300;
 const state=ensureAnimState();
 if(!state.attackPose)state.attackPose={};
 state.attackPose[unitName]={kind:'punch',start:performance.now(),duration:castDuration+flightDuration};
 setTimeout(()=>{
   if(!actionTokenAlive(token))return;
   const projectile={kind:'lebeeStarProjectile',from:{x:from.x,y:from.y-20},to:{x:enemy.x,y:enemy.y-12},start:performance.now(),duration:flightDuration,life:1};
   S.floaters.push(projectile);
   setTimeout(()=>{
     if(!actionTokenAlive(token))return;
     S.floaters=S.floaters.filter(x=>x!==projectile);
     try{onImpact&&onImpact()}catch(err){console.error('Lebee star blast impact failed:',err);return recoverAction('Lebee basic impact')}
     const st=ensureAnimState();if(st.attackPose)delete st.attackPose[unitName];
     setTimeout(()=>{if(actionTokenAlive(token)){try{onDone&&onDone()}catch(err){recoverAction('Lebee basic completion')}}},90);
   },flightDuration);
 },castDuration*.48);
}


function animateSenkuChemicalReaction(unitName,from,target,onImpact,onDone){
 const token=ACTIVE_ACTION_TOKEN;
 const meta=canonicalUnit('senku').abilities.jutsu.presentation||{};
 const castDuration=meta.cast_duration_ms||680;
 const flightDuration=meta.flight_duration_ms||420;
 const impactDuration=meta.impact_duration_ms||620;

 const state=ensureAnimState();
 if(!state.attackPose)state.attackPose={};
 state.attackPose[unitName]={kind:'special',start:performance.now(),duration:castDuration};

 if(meta.screen_dim_alpha){
   S.jutsuDim={start:performance.now(),alpha:meta.screen_dim_alpha,end:null};
 }

 setTimeout(()=>{
   if

--- match 3 @ 43582262 ---
t ice={kind:'frozenTarget',x:enemy.x,y:enemy.y,start:performance.now(),life:1};
     S.floaters.push(ice);
     setTimeout(()=>{
       if(!actionTokenAlive(token))return;
       S.floaters=S.floaters.filter(x=>x!==ice);
       const st=ensureAnimState();
       if(st.attackPose)delete st.attackPose[unitName];
       try{onDone&&onDone()}catch(err){console.error('Freeze Blast completion failed:',err);recoverAction('freeze blast completion')}
     },freezeHold);
   }
   requestAnimationFrame(fly);
 },castDuration);
}


function animateLebeeStarBlast(unitName,from,enemy,onImpact,onDone){
 const token=ACTIVE_ACTION_TOKEN;
 const castDuration=320,flightDuration=300;
 const state=ensureAnimState();
 if(!state.attackPose)state.attackPose={};
 state.attackPose[unitName]={kind:'punch',start:performance.now(),duration:castDuration+flightDuration};
 setTimeout(()=>{
   if(!actionTokenAlive(token))return;
   const projectile={kind:'lebeeStarProjectile',from:{x:from.x,y:from.y-20},to:{x:enemy.x,y:enemy.y-12},start:performance.now(),duration:flightDuration,life:1};
   S.floaters.push(projectile);
   setTimeout(()=>{
     if(!actionTokenAlive(token))return;
     S.floaters=S.floaters.filter(x=>x!==projectile);
     try{onImpact&&onImpact()}catch(err){console.error('Lebee star blast impact failed:',err);return recoverAction('Lebee basic impact')}
     const st=ensureAnimState();if(st.attackPose)delete st.attackPose[unitName];
     setTimeout(()=>{if(actionTokenAlive(token)){try{onDone&&onDone()}catch(err){recoverAction('Lebee basic completion')}}},90);
   },flightDuration);
 },castDuration*.48);
}


function animateSenkuChemicalReaction(unitName,from,target,onImpact,onDone){
 const token=ACTIVE_ACTION_TOKEN;
 const meta=canonicalUnit('senku').abilities.jutsu.presentation||{};
 const castDuration=meta.cast_duration_ms||680;
 const flightDuration=meta.flight_duration_ms||420;
 const impactDuration=meta.impact_duration_ms||620;

 const state=ensureAnimState();
 if(!state.attackPose)state.attackPose={};
 state.attackPose[unitName]={kind:'special',start:performance.now(),duration:castDuration};

 if(meta.screen_dim_alpha){
   S.jutsuDim={start:performance.now(),alpha:meta.screen_dim_alpha,end:null};
 }

 setTimeout(()=>{
   if(!actionTokenAlive(token))return;

   const projectile={
     kind:'senkuChemProjectile',
     from:{x:from.x,y:from.y-30},
     to:{x:target.x,y:target.y-10},
     start:performance.now(),
     duration:flightDuration,
     life:1
   };
   S.floaters.push(projectile);

   setTimeout(()=>{
     if(!actionTokenAlive(token))return;

     S.floaters=S.floaters.filter(x=>x!==projectile);
     const impact={
       kind:'senkuChemImpact',
       x:target.x,
       y:target.y,
       start:performance.now(),
       duration:impactDuration,
       life:1
     };
     S.floaters.push(impact);

     try{onImpact&&onImpact()}
     catch(err){
       console.error('Senku Chemical Reaction impact failed:',err);
       return recoverAction('Senku Chemical Reaction impact');
     }

     setTimeout(()=>{
       S.floaters=S.floaters.filter(x=>x!==impact);
       if(S.jutsuDim)S.jutsuDim.end=performance.now();
       const st=ensureAnimState();
       if(st.atta

--- match 4 @ 43585541 ---
)){
         try{onDone&&onDone()}
         catch(err){recoverAction('Senku Chemical Reaction completion')}
       }
     },impactDuration);
   },flightDuration);
 },castDuration*.70);
}

function animateSenkuBomb(unitName,from,enemy,onImpact,onDone,attackKind='punch'){
 const token=ACTIVE_ACTION_TOKEN;
 const meta=canonicalUnit('senku')?.abilities?.basic?.presentation||{};
 const castDuration=meta.cast_duration_ms||560;
 const flightDuration=meta.flight_duration_ms||620;
 const blastDuration=meta.impact_duration_ms||430;
 const arcHeight=meta.arc_height_px||86;

 const state=ensureAnimState();
 if(!state.attackPose)state.attackPose={};
 state.attackPose[unitName]={
   kind:attackKind,
   start:performance.now(),
   duration:castDuration
 };

 // Release near the final throwing pose rather than halfway through the cast.
 setTimeout(()=>{
   if(!actionTokenAlive(token))return;

   const projectile={
     kind:'senkuBombProjectile',
     from:{x:from.x,y:from.y-31},
     to:{x:enemy.x,y:enemy.y-12},
     start:performance.now(),
     duration:flightDuration,
     arcHeight,
     life:1
   };
   S.floaters.push(projectile);

   setTimeout(()=>{
     if(!actionTokenAlive(token))return;

     S.floaters=S.floaters.filter(x=>x!==projectile);
     const targetGroundY=(enemy.feetY??enemy.groundY??enemy.y);
     const blast={
       kind:'senkuExplosion',
       x:enemy.x,
       y:targetGroundY,
       layer:'ground',
       start:performance.now(),
       duration:360,
       life:1
     };
     S.floaters.push(blast);

     // Damage and target recoil happen only when the bomb lands.
     try{
       onImpact&&onImpact();
     }catch(err){
       console.error('Senku bomb impact failed:',err);
       return recoverAction('Senku bomb impact');
     }

     setTimeout(()=>{
       S.floaters=S.floaters.filter(x=>x!==blast);
       const st=ensureAnimState();
       if(st.attackPose)delete st.attackPose[unitName];

       if(actionTokenAlive(token)){
         try{
           onDone&&onDone();
         }catch(err){
           recoverAction('Senku bomb completion');
         }
       }
     },blastDuration);
   },flightDuration);
 },castDuration*.76);
}

function setMeteorFullscreenFlash(color,opacity=1,transition='none'){
 const el=getMeteorScreenFlash();
 if(!el)return;
 el.classList.add('active');
 el.style.transition=transition;
 el.style.background=color;
 el.style.opacity=String(opacity);
}

function clearMeteorFullscreenFlash(){
 const el=getMeteorScreenFlash();
 if(!el)return;
 el.style.transition='none';
 el.style.opacity='0';
 el.style.background='transparent';
 el.classList.remove('active');
}

function playLebeeMeteorImpactFlash(done){
 const meta=canonicalUnit('lebee').abilities.jutsu.presentation||{};
 const whiteHold=meta.final_flash_white_hold_ms||170;
 const whiteFade=meta.final_flash_white_fade_ms||620;

 // Final meteor: snap to full white.
 setMeteorFullscreenFlash('#ffffff',1,'none');

 setTimeout(()=>{
   const el=getMeteorScreenFlash();
   if(!el){
     done&&done();
     return;
   }
   el.style.transition=`opacity ${whiteFade}ms ease-out`;
   el.style.opacity='0';

   setTimeout(()=>{
     clearMeteorFullscreenFlash();
     done&&done(

--- match 5 @ 43585851 ---
t meta=canonicalUnit('senku')?.abilities?.basic?.presentation||{};
 const castDuration=meta.cast_duration_ms||560;
 const flightDuration=meta.flight_duration_ms||620;
 const blastDuration=meta.impact_duration_ms||430;
 const arcHeight=meta.arc_height_px||86;

 const state=ensureAnimState();
 if(!state.attackPose)state.attackPose={};
 state.attackPose[unitName]={
   kind:attackKind,
   start:performance.now(),
   duration:castDuration
 };

 // Release near the final throwing pose rather than halfway through the cast.
 setTimeout(()=>{
   if(!actionTokenAlive(token))return;

   const projectile={
     kind:'senkuBombProjectile',
     from:{x:from.x,y:from.y-31},
     to:{x:enemy.x,y:enemy.y-12},
     start:performance.now(),
     duration:flightDuration,
     arcHeight,
     life:1
   };
   S.floaters.push(projectile);

   setTimeout(()=>{
     if(!actionTokenAlive(token))return;

     S.floaters=S.floaters.filter(x=>x!==projectile);
     const targetGroundY=(enemy.feetY??enemy.groundY??enemy.y);
     const blast={
       kind:'senkuExplosion',
       x:enemy.x,
       y:targetGroundY,
       layer:'ground',
       start:performance.now(),
       duration:360,
       life:1
     };
     S.floaters.push(blast);

     // Damage and target recoil happen only when the bomb lands.
     try{
       onImpact&&onImpact();
     }catch(err){
       console.error('Senku bomb impact failed:',err);
       return recoverAction('Senku bomb impact');
     }

     setTimeout(()=>{
       S.floaters=S.floaters.filter(x=>x!==blast);
       const st=ensureAnimState();
       if(st.attackPose)delete st.attackPose[unitName];

       if(actionTokenAlive(token)){
         try{
           onDone&&onDone();
         }catch(err){
           recoverAction('Senku bomb completion');
         }
       }
     },blastDuration);
   },flightDuration);
 },castDuration*.76);
}

function setMeteorFullscreenFlash(color,opacity=1,transition='none'){
 const el=getMeteorScreenFlash();
 if(!el)return;
 el.classList.add('active');
 el.style.transition=transition;
 el.style.background=color;
 el.style.opacity=String(opacity);
}

function clearMeteorFullscreenFlash(){
 const el=getMeteorScreenFlash();
 if(!el)return;
 el.style.transition='none';
 el.style.opacity='0';
 el.style.background='transparent';
 el.classList.remove('active');
}

function playLebeeMeteorImpactFlash(done){
 const meta=canonicalUnit('lebee').abilities.jutsu.presentation||{};
 const whiteHold=meta.final_flash_white_hold_ms||170;
 const whiteFade=meta.final_flash_white_fade_ms||620;

 // Final meteor: snap to full white.
 setMeteorFullscreenFlash('#ffffff',1,'none');

 setTimeout(()=>{
   const el=getMeteorScreenFlash();
   if(!el){
     done&&done();
     return;
   }
   el.style.transition=`opacity ${whiteFade}ms ease-out`;
   el.style.opacity='0';

   setTimeout(()=>{
     clearMeteorFullscreenFlash();
     done&&done();
   },whiteFade+20);
 },whiteHold);
}

function animateLebeeMeteorJutsu(unitName,from,targets,onImpact,onDone){
 const token=ACTIVE_ACTION_TOKEN;
 const meta=canonicalUnit('lebee').abilities.jutsu.presentation||{};
 const castDuration=meta.cast_duration_ms||900;
 const fallDuration=meta.meteor_fall_duration

--- match 6 @ 43591688 ---
ellow snaps into white.
   setTimeout(()=>{
     if(!actionTokenAlive(token))return;

     try{
       onImpact&&onImpact();
     }catch(err){
       console.error('Lebee Meteor Jutsu impact failed:',err);
       S.floaters=S.floaters.filter(x=>!meteors.includes(x));
       if(S.jutsuDim)S.jutsuDim.end=performance.now();
       clearMeteorFullscreenFlash();
       return recoverAction('Lebee Meteor impact');
     }

     // Meteors immediately begin disappearing once the final hit lands.
     S.floaters=S.floaters.filter(x=>!meteors.includes(x));
     if(S.jutsuDim)S.jutsuDim.end=performance.now();

     playLebeeMeteorImpactFlash(()=>{
       if(!actionTokenAlive(token))return;
       try{
         onDone&&onDone();
       }catch(err){
         recoverAction('Lebee Meteor completion');
       }
     });
   },finalImpact);

 },castDuration);
}

function recoil(enemy,done,dir=1,strong=false){
 const token=ACTIVE_ACTION_TOKEN;
 startHitReaction(enemy.name,dir,strong);
 let ox=enemy.x,oy=enemy.y,dx=18*(dir||1),start=performance.now();
 function f(n){
  if(!actionTokenAlive(token))return;
  let state=ensureAnimState(),t=Math.min(1,(n-start)/130),off=Math.sin(t*Math.PI)*dx;
  state.positions[enemy.name]={x:ox+off,y:oy};
  if(t<1)return requestAnimationFrame(f);
  delete state.positions[enemy.name];
  try{done&&done()}catch(err){console.error('Recoil callback failed:',err);recoverAction('recoil callback')}
 }
 requestAnimationFrame(f)
}
function resolvePlayer(){

 let p=S.ready.ref,u=front(p),useJutsu=S.action==='jutsu',proxy=attackProxy(u);
 const supportJutsu=useJutsu&&isAllySupportJutsu(u);
 let targets=supportJutsu?[]:aliveEnemies().filter(e=>hits(p,proxy,e));
 p.gauge=0;S.phase='resolve';S.anim={positions:{}};const playerActionToken=beginActionToken();armActionWatchdog('player attack',playerActionToken);

 if(useJutsu&&u.chakra<u.jutsuCost){
  S.log=`${u.name} does not have enough chakra.`;S.action='normal';return finishAction()
 }

 if(supportJutsu){
  u.chakra-=u.jutsuCost;

  const allies=alivePairs().filter(pair=>front(pair).hp>0);
  if(!allies.length){
   S.log=`${u.name} activated ${u.jutsuName||'Jutsu'}, but no allies can be healed.`;
   return finishAction();
  }

  const teamCenter={
   x:allies.reduce((sum,pair)=>sum+pair.x,0)/allies.length,
   y:allies.reduce((sum,pair)=>sum+pair.y,0)/allies.length-18
  };

  const from={x:p.x,y:p.y};
  S.log=`${u.name} activated ${u.jutsuName||'Revival Formula'} — restoring all active allies to full HP.`;

  return animateSenkuChemicalReaction(
   u.name,from,teamCenter,
   ()=>{
    allies.forEach(pair=>{
     const ally=front(pair);
     const healed=Math.max(0,ally.maxHp-ally.hp);
     ally.hp=ally.maxHp;

     addImpactFlash(pair.x,pair.y-18,'#64ff7d');
     if(healed>0){
      addFloat(pair.x,pair.y-40,`+${healed}`,'#74ff91');
     }else{
      addFloat(pair.x,pair.y-40,'FULL','#74ff91');
     }
    });
   },
   ()=>finishAction()
  );
 }

 if(!targets.length){
  S.log=`${u.name} committed the move but caught no target.`;return finishAction()
 }

 if(useJutsu){
  u.chakra-=u.jutsuCost;

  if(u.name==='Lebee'){
   S.log=`Lebee activated Meteor Jutsu — ${targets.length} target${targets.length===1?'':'s

--- match 7 @ 43594172 ---
ull HP.`;

  return animateSenkuChemicalReaction(
   u.name,from,teamCenter,
   ()=>{
    allies.forEach(pair=>{
     const ally=front(pair);
     const healed=Math.max(0,ally.maxHp-ally.hp);
     ally.hp=ally.maxHp;

     addImpactFlash(pair.x,pair.y-18,'#64ff7d');
     if(healed>0){
      addFloat(pair.x,pair.y-40,`+${healed}`,'#74ff91');
     }else{
      addFloat(pair.x,pair.y-40,'FULL','#74ff91');
     }
    });
   },
   ()=>finishAction()
  );
 }

 if(!targets.length){
  S.log=`${u.name} committed the move but caught no target.`;return finishAction()
 }

 if(useJutsu){
  u.chakra-=u.jutsuCost;

  if(u.name==='Lebee'){
   S.log=`Lebee activated Meteor Jutsu — ${targets.length} target${targets.length===1?'':'s'} caught in the cosmic impact zone.`;
   const from={x:p.x,y:p.y};
   return animateLebeeMeteorJutsu(
    u.name,from,targets,
    ()=>{
     targets.forEach(enemy=>{
      if(enemy.hp<=0)return;
      enemy.hp=Math.max(0,enemy.hp-u.jutsuDamage);
      addImpactFlash(enemy.x,enemy.y-8,'#ff7ae6');
      addFloat(enemy.x,enemy.y-34,'-'+u.jutsuDamage,'#ff8be8');
      const dir=Math.sign(enemy.x-p.x)||1;
      if(enemy.hp<=0)handleEnemyKO(enemy,dir); else recoil(enemy,()=>{},dir,true);
     });
     checkVictoryKillshot();
    },
    ()=>finishAction()
   );
  }

  // Every Jutsu target gets:
  //   active fighter's Jutsu hit
  //   + normal attacks from OTHER allies whose normal range covers that target.
  let jutsuQueue=targets.map(enemy=>{
    let helpers=comboMembers(enemy,p).filter(helper=>helper!==p);
    return {enemy,helpers};
  });

  let anyHelpers=jutsuQueue.some(item=>item.helpers.length>0);
  S.log=anyHelpers
    ? `${u.name} activated ${u.jutsuName||'Jutsu'} — normal combo partners are joining where their ranges overlap.`
    : `${u.name} activated ${u.jutsuName||'Jutsu'} — ${targets.length} target${targets.length===1?'':'s'} caught in the field.`;

  let targetIndex=0;

  function runJutsuTarget(){
   if(targetIndex>=jutsuQueue.length)return finishAction();

   let item=jutsuQueue[targetIndex++];
   let enemy=item.enemy;
   let helpers=item.helpers;

   // First: the active fighter performs the Jutsu hit.
   let dx=enemy.x-p.x,dy=enemy.y-p.y,len=Math.max(1,Math.hypot(dx,dy));
   let to={x:enemy.x-dx/len*((enemy.r||19)+27),y:enemy.y-dy/len*((enemy.r||19)+27)};
   let from={x:p.x,y:p.y};

   const runActiveJutsu=(u.name==='Sub-Zero')?animateFreezeBlast:animateLunge;
   const activeFrom=(u.name==='Sub-Zero')?from:from;
   const activeTarget=(u.name==='Sub-Zero')?enemy:to;
   runActiveJutsu(
    u.name,activeFrom,activeTarget,
    ()=>{
     enemy.hp=Math.max(0,enemy.hp-u.jutsuDamage);
     addImpactFlash(enemy.x,enemy.y-8,u.name==='Crimson'?'#ff405c':'#8cecff');
     addFloat(enemy.x,enemy.y-34,'-'+u.jutsuDamage,'#57e7ff');
     let jDir=Math.sign(enemy.x-p.x)||1;
     if(enemy.hp<=0)handleEnemyKO(enemy,jDir); else if(u.name!=='Sub-Zero')recoil(enemy,()=>{},jDir,true);
     if(u.name==='Sub-Zero')enemy.gauge=Math.max(0,(enemy.gauge||0)-35);
     checkVictoryKillshot();
    },
    ()=>{
     let helperIndex=0;

     function runHelper(){
      if(enemy.hp<=0 || helperIndex>=helpers.length)
       return setTimeout(runJutsuTarget,11

--- match 8 @ 43594217 ---
ion(
   u.name,from,teamCenter,
   ()=>{
    allies.forEach(pair=>{
     const ally=front(pair);
     const healed=Math.max(0,ally.maxHp-ally.hp);
     ally.hp=ally.maxHp;

     addImpactFlash(pair.x,pair.y-18,'#64ff7d');
     if(healed>0){
      addFloat(pair.x,pair.y-40,`+${healed}`,'#74ff91');
     }else{
      addFloat(pair.x,pair.y-40,'FULL','#74ff91');
     }
    });
   },
   ()=>finishAction()
  );
 }

 if(!targets.length){
  S.log=`${u.name} committed the move but caught no target.`;return finishAction()
 }

 if(useJutsu){
  u.chakra-=u.jutsuCost;

  if(u.name==='Lebee'){
   S.log=`Lebee activated Meteor Jutsu — ${targets.length} target${targets.length===1?'':'s'} caught in the cosmic impact zone.`;
   const from={x:p.x,y:p.y};
   return animateLebeeMeteorJutsu(
    u.name,from,targets,
    ()=>{
     targets.forEach(enemy=>{
      if(enemy.hp<=0)return;
      enemy.hp=Math.max(0,enemy.hp-u.jutsuDamage);
      addImpactFlash(enemy.x,enemy.y-8,'#ff7ae6');
      addFloat(enemy.x,enemy.y-34,'-'+u.jutsuDamage,'#ff8be8');
      const dir=Math.sign(enemy.x-p.x)||1;
      if(enemy.hp<=0)handleEnemyKO(enemy,dir); else recoil(enemy,()=>{},dir,true);
     });
     checkVictoryKillshot();
    },
    ()=>finishAction()
   );
  }

  // Every Jutsu target gets:
  //   active fighter's Jutsu hit
  //   + normal attacks from OTHER allies whose normal range covers that target.
  let jutsuQueue=targets.map(enemy=>{
    let helpers=comboMembers(enemy,p).filter(helper=>helper!==p);
    return {enemy,helpers};
  });

  let anyHelpers=jutsuQueue.some(item=>item.helpers.length>0);
  S.log=anyHelpers
    ? `${u.name} activated ${u.jutsuName||'Jutsu'} — normal combo partners are joining where their ranges overlap.`
    : `${u.name} activated ${u.jutsuName||'Jutsu'} — ${targets.length} target${targets.length===1?'':'s'} caught in the field.`;

  let targetIndex=0;

  function runJutsuTarget(){
   if(targetIndex>=jutsuQueue.length)return finishAction();

   let item=jutsuQueue[targetIndex++];
   let enemy=item.enemy;
   let helpers=item.helpers;

   // First: the active fighter performs the Jutsu hit.
   let dx=enemy.x-p.x,dy=enemy.y-p.y,len=Math.max(1,Math.hypot(dx,dy));
   let to={x:enemy.x-dx/len*((enemy.r||19)+27),y:enemy.y-dy/len*((enemy.r||19)+27)};
   let from={x:p.x,y:p.y};

   const runActiveJutsu=(u.name==='Sub-Zero')?animateFreezeBlast:animateLunge;
   const activeFrom=(u.name==='Sub-Zero')?from:from;
   const activeTarget=(u.name==='Sub-Zero')?enemy:to;
   runActiveJutsu(
    u.name,activeFrom,activeTarget,
    ()=>{
     enemy.hp=Math.max(0,enemy.hp-u.jutsuDamage);
     addImpactFlash(enemy.x,enemy.y-8,u.name==='Crimson'?'#ff405c':'#8cecff');
     addFloat(enemy.x,enemy.y-34,'-'+u.jutsuDamage,'#57e7ff');
     let jDir=Math.sign(enemy.x-p.x)||1;
     if(enemy.hp<=0)handleEnemyKO(enemy,jDir); else if(u.name!=='Sub-Zero')recoil(enemy,()=>{},jDir,true);
     if(u.name==='Sub-Zero')enemy.gauge=Math.max(0,(enemy.gauge||0)-35);
     checkVictoryKillshot();
    },
    ()=>{
     let helperIndex=0;

     function runHelper(){
      if(enemy.hp<=0 || helperIndex>=helpers.length)
       return setTimeout(runJutsuTarget,110);

      let hp=helpers[helperIndex++];
   

--- match 9 @ 43595362 ---
emy,()=>{},dir,true);
     });
     checkVictoryKillshot();
    },
    ()=>finishAction()
   );
  }

  // Every Jutsu target gets:
  //   active fighter's Jutsu hit
  //   + normal attacks from OTHER allies whose normal range covers that target.
  let jutsuQueue=targets.map(enemy=>{
    let helpers=comboMembers(enemy,p).filter(helper=>helper!==p);
    return {enemy,helpers};
  });

  let anyHelpers=jutsuQueue.some(item=>item.helpers.length>0);
  S.log=anyHelpers
    ? `${u.name} activated ${u.jutsuName||'Jutsu'} — normal combo partners are joining where their ranges overlap.`
    : `${u.name} activated ${u.jutsuName||'Jutsu'} — ${targets.length} target${targets.length===1?'':'s'} caught in the field.`;

  let targetIndex=0;

  function runJutsuTarget(){
   if(targetIndex>=jutsuQueue.length)return finishAction();

   let item=jutsuQueue[targetIndex++];
   let enemy=item.enemy;
   let helpers=item.helpers;

   // First: the active fighter performs the Jutsu hit.
   let dx=enemy.x-p.x,dy=enemy.y-p.y,len=Math.max(1,Math.hypot(dx,dy));
   let to={x:enemy.x-dx/len*((enemy.r||19)+27),y:enemy.y-dy/len*((enemy.r||19)+27)};
   let from={x:p.x,y:p.y};

   const runActiveJutsu=(u.name==='Sub-Zero')?animateFreezeBlast:animateLunge;
   const activeFrom=(u.name==='Sub-Zero')?from:from;
   const activeTarget=(u.name==='Sub-Zero')?enemy:to;
   runActiveJutsu(
    u.name,activeFrom,activeTarget,
    ()=>{
     enemy.hp=Math.max(0,enemy.hp-u.jutsuDamage);
     addImpactFlash(enemy.x,enemy.y-8,u.name==='Crimson'?'#ff405c':'#8cecff');
     addFloat(enemy.x,enemy.y-34,'-'+u.jutsuDamage,'#57e7ff');
     let jDir=Math.sign(enemy.x-p.x)||1;
     if(enemy.hp<=0)handleEnemyKO(enemy,jDir); else if(u.name!=='Sub-Zero')recoil(enemy,()=>{},jDir,true);
     if(u.name==='Sub-Zero')enemy.gauge=Math.max(0,(enemy.gauge||0)-35);
     checkVictoryKillshot();
    },
    ()=>{
     let helperIndex=0;

     function runHelper(){
      if(enemy.hp<=0 || helperIndex>=helpers.length)
       return setTimeout(runJutsuTarget,110);

      let hp=helpers[helperIndex++];
      let hu=front(hp);
      let hdx=enemy.x-hp.x,hdy=enemy.y-hp.y,hlen=Math.max(1,Math.hypot(hdx,hdy));
      let hto={
       x:enemy.x-hdx/hlen*((enemy.r||19)+27),
       y:enemy.y-hdy/hlen*((enemy.r||19)+27)
      };
      let hfrom={x:hp.x,y:hp.y};

      animateLunge(
       hu.name,hfrom,hto,
       ()=>{
        let hResult=buffedNormalDamage(hp),hDamage=hResult.damage;
        enemy.hp=Math.max(0,enemy.hp-hDamage);
        addImpactFlash(enemy.x,enemy.y-8,hu.name==='Crimson'?'#ff405c':'#ffffff');
        addFloat(enemy.x,enemy.y-34,'-'+hDamage,hResult.buff.bonus>0?'#65ff9e':'#ffbd4a');
        let hDir=Math.sign(enemy.x-hp.x)||1;
        if(enemy.hp<=0)handleEnemyKO(enemy,hDir); else recoil(enemy,()=>{},hDir,false);
        checkVictoryKillshot();
       },
       ()=>setTimeout(runHelper,85),
       helperIndex%2===1?'punch':'kick'
      )
     }

     runHelper()
    },
    'special'
   )
  }

  return runJutsuTarget()
 }

 let queue=[],anyCombo=false;
 targets.forEach(enemy=>{
  let members=comboMembers(enemy,p),attackers=[p,...members.filter(x=>x!==p)];
  if(attackers.length>1)anyCombo=true;
  queue.push({enemy,attac

--- match 10 @ 43595458 ---
  }

  // Every Jutsu target gets:
  //   active fighter's Jutsu hit
  //   + normal attacks from OTHER allies whose normal range covers that target.
  let jutsuQueue=targets.map(enemy=>{
    let helpers=comboMembers(enemy,p).filter(helper=>helper!==p);
    return {enemy,helpers};
  });

  let anyHelpers=jutsuQueue.some(item=>item.helpers.length>0);
  S.log=anyHelpers
    ? `${u.name} activated ${u.jutsuName||'Jutsu'} — normal combo partners are joining where their ranges overlap.`
    : `${u.name} activated ${u.jutsuName||'Jutsu'} — ${targets.length} target${targets.length===1?'':'s'} caught in the field.`;

  let targetIndex=0;

  function runJutsuTarget(){
   if(targetIndex>=jutsuQueue.length)return finishAction();

   let item=jutsuQueue[targetIndex++];
   let enemy=item.enemy;
   let helpers=item.helpers;

   // First: the active fighter performs the Jutsu hit.
   let dx=enemy.x-p.x,dy=enemy.y-p.y,len=Math.max(1,Math.hypot(dx,dy));
   let to={x:enemy.x-dx/len*((enemy.r||19)+27),y:enemy.y-dy/len*((enemy.r||19)+27)};
   let from={x:p.x,y:p.y};

   const runActiveJutsu=(u.name==='Sub-Zero')?animateFreezeBlast:animateLunge;
   const activeFrom=(u.name==='Sub-Zero')?from:from;
   const activeTarget=(u.name==='Sub-Zero')?enemy:to;
   runActiveJutsu(
    u.name,activeFrom,activeTarget,
    ()=>{
     enemy.hp=Math.max(0,enemy.hp-u.jutsuDamage);
     addImpactFlash(enemy.x,enemy.y-8,u.name==='Crimson'?'#ff405c':'#8cecff');
     addFloat(enemy.x,enemy.y-34,'-'+u.jutsuDamage,'#57e7ff');
     let jDir=Math.sign(enemy.x-p.x)||1;
     if(enemy.hp<=0)handleEnemyKO(enemy,jDir); else if(u.name!=='Sub-Zero')recoil(enemy,()=>{},jDir,true);
     if(u.name==='Sub-Zero')enemy.gauge=Math.max(0,(enemy.gauge||0)-35);
     checkVictoryKillshot();
    },
    ()=>{
     let helperIndex=0;

     function runHelper(){
      if(enemy.hp<=0 || helperIndex>=helpers.length)
       return setTimeout(runJutsuTarget,110);

      let hp=helpers[helperIndex++];
      let hu=front(hp);
      let hdx=enemy.x-hp.x,hdy=enemy.y-hp.y,hlen=Math.max(1,Math.hypot(hdx,hdy));
      let hto={
       x:enemy.x-hdx/hlen*((enemy.r||19)+27),
       y:enemy.y-hdy/hlen*((enemy.r||19)+27)
      };
      let hfrom={x:hp.x,y:hp.y};

      animateLunge(
       hu.name,hfrom,hto,
       ()=>{
        let hResult=buffedNormalDamage(hp),hDamage=hResult.damage;
        enemy.hp=Math.max(0,enemy.hp-hDamage);
        addImpactFlash(enemy.x,enemy.y-8,hu.name==='Crimson'?'#ff405c':'#ffffff');
        addFloat(enemy.x,enemy.y-34,'-'+hDamage,hResult.buff.bonus>0?'#65ff9e':'#ffbd4a');
        let hDir=Math.sign(enemy.x-hp.x)||1;
        if(enemy.hp<=0)handleEnemyKO(enemy,hDir); else recoil(enemy,()=>{},hDir,false);
        checkVictoryKillshot();
       },
       ()=>setTimeout(runHelper,85),
       helperIndex%2===1?'punch':'kick'
      )
     }

     runHelper()
    },
    'special'
   )
  }

  return runJutsuTarget()
 }

 let queue=[],anyCombo=false;
 targets.forEach(enemy=>{
  let members=comboMembers(enemy,p),attackers=[p,...members.filter(x=>x!==p)];
  if(attackers.length>1)anyCombo=true;
  queue.push({enemy,attackers})
 });
 let chakraGain=1+(anyCombo?1:0);
 u.chakra=Math.min(u.maxChakra,u.chakra+chakraGain

--- match 11 @ 43595851 ---
ated ${u.jutsuName||'Jutsu'} — normal combo partners are joining where their ranges overlap.`
    : `${u.name} activated ${u.jutsuName||'Jutsu'} — ${targets.length} target${targets.length===1?'':'s'} caught in the field.`;

  let targetIndex=0;

  function runJutsuTarget(){
   if(targetIndex>=jutsuQueue.length)return finishAction();

   let item=jutsuQueue[targetIndex++];
   let enemy=item.enemy;
   let helpers=item.helpers;

   // First: the active fighter performs the Jutsu hit.
   let dx=enemy.x-p.x,dy=enemy.y-p.y,len=Math.max(1,Math.hypot(dx,dy));
   let to={x:enemy.x-dx/len*((enemy.r||19)+27),y:enemy.y-dy/len*((enemy.r||19)+27)};
   let from={x:p.x,y:p.y};

   const runActiveJutsu=(u.name==='Sub-Zero')?animateFreezeBlast:animateLunge;
   const activeFrom=(u.name==='Sub-Zero')?from:from;
   const activeTarget=(u.name==='Sub-Zero')?enemy:to;
   runActiveJutsu(
    u.name,activeFrom,activeTarget,
    ()=>{
     enemy.hp=Math.max(0,enemy.hp-u.jutsuDamage);
     addImpactFlash(enemy.x,enemy.y-8,u.name==='Crimson'?'#ff405c':'#8cecff');
     addFloat(enemy.x,enemy.y-34,'-'+u.jutsuDamage,'#57e7ff');
     let jDir=Math.sign(enemy.x-p.x)||1;
     if(enemy.hp<=0)handleEnemyKO(enemy,jDir); else if(u.name!=='Sub-Zero')recoil(enemy,()=>{},jDir,true);
     if(u.name==='Sub-Zero')enemy.gauge=Math.max(0,(enemy.gauge||0)-35);
     checkVictoryKillshot();
    },
    ()=>{
     let helperIndex=0;

     function runHelper(){
      if(enemy.hp<=0 || helperIndex>=helpers.length)
       return setTimeout(runJutsuTarget,110);

      let hp=helpers[helperIndex++];
      let hu=front(hp);
      let hdx=enemy.x-hp.x,hdy=enemy.y-hp.y,hlen=Math.max(1,Math.hypot(hdx,hdy));
      let hto={
       x:enemy.x-hdx/hlen*((enemy.r||19)+27),
       y:enemy.y-hdy/hlen*((enemy.r||19)+27)
      };
      let hfrom={x:hp.x,y:hp.y};

      animateLunge(
       hu.name,hfrom,hto,
       ()=>{
        let hResult=buffedNormalDamage(hp),hDamage=hResult.damage;
        enemy.hp=Math.max(0,enemy.hp-hDamage);
        addImpactFlash(enemy.x,enemy.y-8,hu.name==='Crimson'?'#ff405c':'#ffffff');
        addFloat(enemy.x,enemy.y-34,'-'+hDamage,hResult.buff.bonus>0?'#65ff9e':'#ffbd4a');
        let hDir=Math.sign(enemy.x-hp.x)||1;
        if(enemy.hp<=0)handleEnemyKO(enemy,hDir); else recoil(enemy,()=>{},hDir,false);
        checkVictoryKillshot();
       },
       ()=>setTimeout(runHelper,85),
       helperIndex%2===1?'punch':'kick'
      )
     }

     runHelper()
    },
    'special'
   )
  }

  return runJutsuTarget()
 }

 let queue=[],anyCombo=false;
 targets.forEach(enemy=>{
  let members=comboMembers(enemy,p),attackers=[p,...members.filter(x=>x!==p)];
  if(attackers.length>1)anyCombo=true;
  queue.push({enemy,attackers})
 });
 let chakraGain=1+(anyCombo?1:0);
 u.chakra=Math.min(u.maxChakra,u.chakra+chakraGain);

 let targetIndex=0;
 function runTarget(){
  if(targetIndex>=queue.length){
   S.log=`${u.name} hit ${targets.length} target${targets.length===1?'':'s'} and gained ${chakraGain} chakra${anyCombo?' from combo positioning':''}.`;
   return finishAction()
  }
  let item=queue[targetIndex++],enemy=item.enemy,attackers=item.attackers,attackIndex=0;
  function runAttacker(){
   if(enemy.hp<=0

--- match 12 @ 43595924 ---
eir ranges overlap.`
    : `${u.name} activated ${u.jutsuName||'Jutsu'} — ${targets.length} target${targets.length===1?'':'s'} caught in the field.`;

  let targetIndex=0;

  function runJutsuTarget(){
   if(targetIndex>=jutsuQueue.length)return finishAction();

   let item=jutsuQueue[targetIndex++];
   let enemy=item.enemy;
   let helpers=item.helpers;

   // First: the active fighter performs the Jutsu hit.
   let dx=enemy.x-p.x,dy=enemy.y-p.y,len=Math.max(1,Math.hypot(dx,dy));
   let to={x:enemy.x-dx/len*((enemy.r||19)+27),y:enemy.y-dy/len*((enemy.r||19)+27)};
   let from={x:p.x,y:p.y};

   const runActiveJutsu=(u.name==='Sub-Zero')?animateFreezeBlast:animateLunge;
   const activeFrom=(u.name==='Sub-Zero')?from:from;
   const activeTarget=(u.name==='Sub-Zero')?enemy:to;
   runActiveJutsu(
    u.name,activeFrom,activeTarget,
    ()=>{
     enemy.hp=Math.max(0,enemy.hp-u.jutsuDamage);
     addImpactFlash(enemy.x,enemy.y-8,u.name==='Crimson'?'#ff405c':'#8cecff');
     addFloat(enemy.x,enemy.y-34,'-'+u.jutsuDamage,'#57e7ff');
     let jDir=Math.sign(enemy.x-p.x)||1;
     if(enemy.hp<=0)handleEnemyKO(enemy,jDir); else if(u.name!=='Sub-Zero')recoil(enemy,()=>{},jDir,true);
     if(u.name==='Sub-Zero')enemy.gauge=Math.max(0,(enemy.gauge||0)-35);
     checkVictoryKillshot();
    },
    ()=>{
     let helperIndex=0;

     function runHelper(){
      if(enemy.hp<=0 || helperIndex>=helpers.length)
       return setTimeout(runJutsuTarget,110);

      let hp=helpers[helperIndex++];
      let hu=front(hp);
      let hdx=enemy.x-hp.x,hdy=enemy.y-hp.y,hlen=Math.max(1,Math.hypot(hdx,hdy));
      let hto={
       x:enemy.x-hdx/hlen*((enemy.r||19)+27),
       y:enemy.y-hdy/hlen*((enemy.r||19)+27)
      };
      let hfrom={x:hp.x,y:hp.y};

      animateLunge(
       hu.name,hfrom,hto,
       ()=>{
        let hResult=buffedNormalDamage(hp),hDamage=hResult.damage;
        enemy.hp=Math.max(0,enemy.hp-hDamage);
        addImpactFlash(enemy.x,enemy.y-8,hu.name==='Crimson'?'#ff405c':'#ffffff');
        addFloat(enemy.x,enemy.y-34,'-'+hDamage,hResult.buff.bonus>0?'#65ff9e':'#ffbd4a');
        let hDir=Math.sign(enemy.x-hp.x)||1;
        if(enemy.hp<=0)handleEnemyKO(enemy,hDir); else recoil(enemy,()=>{},hDir,false);
        checkVictoryKillshot();
       },
       ()=>setTimeout(runHelper,85),
       helperIndex%2===1?'punch':'kick'
      )
     }

     runHelper()
    },
    'special'
   )
  }

  return runJutsuTarget()
 }

 let queue=[],anyCombo=false;
 targets.forEach(enemy=>{
  let members=comboMembers(enemy,p),attackers=[p,...members.filter(x=>x!==p)];
  if(attackers.length>1)anyCombo=true;
  queue.push({enemy,attackers})
 });
 let chakraGain=1+(anyCombo?1:0);
 u.chakra=Math.min(u.maxChakra,u.chakra+chakraGain);

 let targetIndex=0;
 function runTarget(){
  if(targetIndex>=queue.length){
   S.log=`${u.name} hit ${targets.length} target${targets.length===1?'':'s'} and gained ${chakraGain} chakra${anyCombo?' from combo positioning':''}.`;
   return finishAction()
  }
  let item=queue[targetIndex++],enemy=item.enemy,attackers=item.attackers,attackIndex=0;
  function runAttacker(){
   if(enemy.hp<=0||attackIndex>=attackers.length)return setTimeout(runTarget,110);
   let 

--- match 13 @ 43596476 ---
nemy.r||19)+27)};
   let from={x:p.x,y:p.y};

   const runActiveJutsu=(u.name==='Sub-Zero')?animateFreezeBlast:animateLunge;
   const activeFrom=(u.name==='Sub-Zero')?from:from;
   const activeTarget=(u.name==='Sub-Zero')?enemy:to;
   runActiveJutsu(
    u.name,activeFrom,activeTarget,
    ()=>{
     enemy.hp=Math.max(0,enemy.hp-u.jutsuDamage);
     addImpactFlash(enemy.x,enemy.y-8,u.name==='Crimson'?'#ff405c':'#8cecff');
     addFloat(enemy.x,enemy.y-34,'-'+u.jutsuDamage,'#57e7ff');
     let jDir=Math.sign(enemy.x-p.x)||1;
     if(enemy.hp<=0)handleEnemyKO(enemy,jDir); else if(u.name!=='Sub-Zero')recoil(enemy,()=>{},jDir,true);
     if(u.name==='Sub-Zero')enemy.gauge=Math.max(0,(enemy.gauge||0)-35);
     checkVictoryKillshot();
    },
    ()=>{
     let helperIndex=0;

     function runHelper(){
      if(enemy.hp<=0 || helperIndex>=helpers.length)
       return setTimeout(runJutsuTarget,110);

      let hp=helpers[helperIndex++];
      let hu=front(hp);
      let hdx=enemy.x-hp.x,hdy=enemy.y-hp.y,hlen=Math.max(1,Math.hypot(hdx,hdy));
      let hto={
       x:enemy.x-hdx/hlen*((enemy.r||19)+27),
       y:enemy.y-hdy/hlen*((enemy.r||19)+27)
      };
      let hfrom={x:hp.x,y:hp.y};

      animateLunge(
       hu.name,hfrom,hto,
       ()=>{
        let hResult=buffedNormalDamage(hp),hDamage=hResult.damage;
        enemy.hp=Math.max(0,enemy.hp-hDamage);
        addImpactFlash(enemy.x,enemy.y-8,hu.name==='Crimson'?'#ff405c':'#ffffff');
        addFloat(enemy.x,enemy.y-34,'-'+hDamage,hResult.buff.bonus>0?'#65ff9e':'#ffbd4a');
        let hDir=Math.sign(enemy.x-hp.x)||1;
        if(enemy.hp<=0)handleEnemyKO(enemy,hDir); else recoil(enemy,()=>{},hDir,false);
        checkVictoryKillshot();
       },
       ()=>setTimeout(runHelper,85),
       helperIndex%2===1?'punch':'kick'
      )
     }

     runHelper()
    },
    'special'
   )
  }

  return runJutsuTarget()
 }

 let queue=[],anyCombo=false;
 targets.forEach(enemy=>{
  let members=comboMembers(enemy,p),attackers=[p,...members.filter(x=>x!==p)];
  if(attackers.length>1)anyCombo=true;
  queue.push({enemy,attackers})
 });
 let chakraGain=1+(anyCombo?1:0);
 u.chakra=Math.min(u.maxChakra,u.chakra+chakraGain);

 let targetIndex=0;
 function runTarget(){
  if(targetIndex>=queue.length){
   S.log=`${u.name} hit ${targets.length} target${targets.length===1?'':'s'} and gained ${chakraGain} chakra${anyCombo?' from combo positioning':''}.`;
   return finishAction()
  }
  let item=queue[targetIndex++],enemy=item.enemy,attackers=item.attackers,attackIndex=0;
  function runAttacker(){
   if(enemy.hp<=0||attackIndex>=attackers.length)return setTimeout(runTarget,110);
   let ap=attackers[attackIndex++],au=front(ap),dx=enemy.x-ap.x,dy=enemy.y-ap.y,len=Math.max(1,Math.hypot(dx,dy));
   let stop=(enemy.r||19)+27,to={x:enemy.x-dx/len*stop,y:enemy.y-dy/len*stop},from={x:ap.x,y:ap.y};
   const runBasicAttack=(au.name==='Lebee')?animateLebeeStarBlast:(au.name==='Senku'?animateSenkuBomb:animateLunge);
   const basicTarget=(au.name==='Lebee'||au.name==='Senku')?enemy:to;
   runBasicAttack(au.name,from,basicTarget,()=>{
    let result=buffedNormalDamage(ap),dmg=result.damage;
    enemy.hp=Math.max(0,enemy.hp-dmg);
    addImpac

--- match 14 @ 43596598 ---
e;
   const activeFrom=(u.name==='Sub-Zero')?from:from;
   const activeTarget=(u.name==='Sub-Zero')?enemy:to;
   runActiveJutsu(
    u.name,activeFrom,activeTarget,
    ()=>{
     enemy.hp=Math.max(0,enemy.hp-u.jutsuDamage);
     addImpactFlash(enemy.x,enemy.y-8,u.name==='Crimson'?'#ff405c':'#8cecff');
     addFloat(enemy.x,enemy.y-34,'-'+u.jutsuDamage,'#57e7ff');
     let jDir=Math.sign(enemy.x-p.x)||1;
     if(enemy.hp<=0)handleEnemyKO(enemy,jDir); else if(u.name!=='Sub-Zero')recoil(enemy,()=>{},jDir,true);
     if(u.name==='Sub-Zero')enemy.gauge=Math.max(0,(enemy.gauge||0)-35);
     checkVictoryKillshot();
    },
    ()=>{
     let helperIndex=0;

     function runHelper(){
      if(enemy.hp<=0 || helperIndex>=helpers.length)
       return setTimeout(runJutsuTarget,110);

      let hp=helpers[helperIndex++];
      let hu=front(hp);
      let hdx=enemy.x-hp.x,hdy=enemy.y-hp.y,hlen=Math.max(1,Math.hypot(hdx,hdy));
      let hto={
       x:enemy.x-hdx/hlen*((enemy.r||19)+27),
       y:enemy.y-hdy/hlen*((enemy.r||19)+27)
      };
      let hfrom={x:hp.x,y:hp.y};

      animateLunge(
       hu.name,hfrom,hto,
       ()=>{
        let hResult=buffedNormalDamage(hp),hDamage=hResult.damage;
        enemy.hp=Math.max(0,enemy.hp-hDamage);
        addImpactFlash(enemy.x,enemy.y-8,hu.name==='Crimson'?'#ff405c':'#ffffff');
        addFloat(enemy.x,enemy.y-34,'-'+hDamage,hResult.buff.bonus>0?'#65ff9e':'#ffbd4a');
        let hDir=Math.sign(enemy.x-hp.x)||1;
        if(enemy.hp<=0)handleEnemyKO(enemy,hDir); else recoil(enemy,()=>{},hDir,false);
        checkVictoryKillshot();
       },
       ()=>setTimeout(runHelper,85),
       helperIndex%2===1?'punch':'kick'
      )
     }

     runHelper()
    },
    'special'
   )
  }

  return runJutsuTarget()
 }

 let queue=[],anyCombo=false;
 targets.forEach(enemy=>{
  let members=comboMembers(enemy,p),attackers=[p,...members.filter(x=>x!==p)];
  if(attackers.length>1)anyCombo=true;
  queue.push({enemy,attackers})
 });
 let chakraGain=1+(anyCombo?1:0);
 u.chakra=Math.min(u.maxChakra,u.chakra+chakraGain);

 let targetIndex=0;
 function runTarget(){
  if(targetIndex>=queue.length){
   S.log=`${u.name} hit ${targets.length} target${targets.length===1?'':'s'} and gained ${chakraGain} chakra${anyCombo?' from combo positioning':''}.`;
   return finishAction()
  }
  let item=queue[targetIndex++],enemy=item.enemy,attackers=item.attackers,attackIndex=0;
  function runAttacker(){
   if(enemy.hp<=0||attackIndex>=attackers.length)return setTimeout(runTarget,110);
   let ap=attackers[attackIndex++],au=front(ap),dx=enemy.x-ap.x,dy=enemy.y-ap.y,len=Math.max(1,Math.hypot(dx,dy));
   let stop=(enemy.r||19)+27,to={x:enemy.x-dx/len*stop,y:enemy.y-dy/len*stop},from={x:ap.x,y:ap.y};
   const runBasicAttack=(au.name==='Lebee')?animateLebeeStarBlast:(au.name==='Senku'?animateSenkuBomb:animateLunge);
   const basicTarget=(au.name==='Lebee'||au.name==='Senku')?enemy:to;
   runBasicAttack(au.name,from,basicTarget,()=>{
    let result=buffedNormalDamage(ap),dmg=result.damage;
    enemy.hp=Math.max(0,enemy.hp-dmg);
    addImpactFlash(enemy.x,enemy.y-8,au.name==='Crimson'?'#ff405c':attackIndex>1?'#ffbd4a':'#ffffff');
    addFloat(enemy.x,enemy.y-34

--- match 15 @ 43596881 ---
#ff405c':'#8cecff');
     addFloat(enemy.x,enemy.y-34,'-'+u.jutsuDamage,'#57e7ff');
     let jDir=Math.sign(enemy.x-p.x)||1;
     if(enemy.hp<=0)handleEnemyKO(enemy,jDir); else if(u.name!=='Sub-Zero')recoil(enemy,()=>{},jDir,true);
     if(u.name==='Sub-Zero')enemy.gauge=Math.max(0,(enemy.gauge||0)-35);
     checkVictoryKillshot();
    },
    ()=>{
     let helperIndex=0;

     function runHelper(){
      if(enemy.hp<=0 || helperIndex>=helpers.length)
       return setTimeout(runJutsuTarget,110);

      let hp=helpers[helperIndex++];
      let hu=front(hp);
      let hdx=enemy.x-hp.x,hdy=enemy.y-hp.y,hlen=Math.max(1,Math.hypot(hdx,hdy));
      let hto={
       x:enemy.x-hdx/hlen*((enemy.r||19)+27),
       y:enemy.y-hdy/hlen*((enemy.r||19)+27)
      };
      let hfrom={x:hp.x,y:hp.y};

      animateLunge(
       hu.name,hfrom,hto,
       ()=>{
        let hResult=buffedNormalDamage(hp),hDamage=hResult.damage;
        enemy.hp=Math.max(0,enemy.hp-hDamage);
        addImpactFlash(enemy.x,enemy.y-8,hu.name==='Crimson'?'#ff405c':'#ffffff');
        addFloat(enemy.x,enemy.y-34,'-'+hDamage,hResult.buff.bonus>0?'#65ff9e':'#ffbd4a');
        let hDir=Math.sign(enemy.x-hp.x)||1;
        if(enemy.hp<=0)handleEnemyKO(enemy,hDir); else recoil(enemy,()=>{},hDir,false);
        checkVictoryKillshot();
       },
       ()=>setTimeout(runHelper,85),
       helperIndex%2===1?'punch':'kick'
      )
     }

     runHelper()
    },
    'special'
   )
  }

  return runJutsuTarget()
 }

 let queue=[],anyCombo=false;
 targets.forEach(enemy=>{
  let members=comboMembers(enemy,p),attackers=[p,...members.filter(x=>x!==p)];
  if(attackers.length>1)anyCombo=true;
  queue.push({enemy,attackers})
 });
 let chakraGain=1+(anyCombo?1:0);
 u.chakra=Math.min(u.maxChakra,u.chakra+chakraGain);

 let targetIndex=0;
 function runTarget(){
  if(targetIndex>=queue.length){
   S.log=`${u.name} hit ${targets.length} target${targets.length===1?'':'s'} and gained ${chakraGain} chakra${anyCombo?' from combo positioning':''}.`;
   return finishAction()
  }
  let item=queue[targetIndex++],enemy=item.enemy,attackers=item.attackers,attackIndex=0;
  function runAttacker(){
   if(enemy.hp<=0||attackIndex>=attackers.length)return setTimeout(runTarget,110);
   let ap=attackers[attackIndex++],au=front(ap),dx=enemy.x-ap.x,dy=enemy.y-ap.y,len=Math.max(1,Math.hypot(dx,dy));
   let stop=(enemy.r||19)+27,to={x:enemy.x-dx/len*stop,y:enemy.y-dy/len*stop},from={x:ap.x,y:ap.y};
   const runBasicAttack=(au.name==='Lebee')?animateLebeeStarBlast:(au.name==='Senku'?animateSenkuBomb:animateLunge);
   const basicTarget=(au.name==='Lebee'||au.name==='Senku')?enemy:to;
   runBasicAttack(au.name,from,basicTarget,()=>{
    let result=buffedNormalDamage(ap),dmg=result.damage;
    enemy.hp=Math.max(0,enemy.hp-dmg);
    addImpactFlash(enemy.x,enemy.y-8,au.name==='Crimson'?'#ff405c':attackIndex>1?'#ffbd4a':'#ffffff');
    addFloat(enemy.x,enemy.y-34,'-'+dmg,result.buff.bonus>0?'#65ff9e':attackIndex>1?'#ffbd4a':'#fff');
    let nDir=Math.sign(enemy.x-ap.x)||1;
    if(enemy.hp<=0)handleEnemyKO(enemy,nDir); else recoil(enemy,()=>{},nDir,attackIndex>1);
    checkVictoryKillshot();
   },()=>setTimeout(runAttacker,85),attackIndex%2=

--- match 16 @ 43596958 ---
7ff');
     let jDir=Math.sign(enemy.x-p.x)||1;
     if(enemy.hp<=0)handleEnemyKO(enemy,jDir); else if(u.name!=='Sub-Zero')recoil(enemy,()=>{},jDir,true);
     if(u.name==='Sub-Zero')enemy.gauge=Math.max(0,(enemy.gauge||0)-35);
     checkVictoryKillshot();
    },
    ()=>{
     let helperIndex=0;

     function runHelper(){
      if(enemy.hp<=0 || helperIndex>=helpers.length)
       return setTimeout(runJutsuTarget,110);

      let hp=helpers[helperIndex++];
      let hu=front(hp);
      let hdx=enemy.x-hp.x,hdy=enemy.y-hp.y,hlen=Math.max(1,Math.hypot(hdx,hdy));
      let hto={
       x:enemy.x-hdx/hlen*((enemy.r||19)+27),
       y:enemy.y-hdy/hlen*((enemy.r||19)+27)
      };
      let hfrom={x:hp.x,y:hp.y};

      animateLunge(
       hu.name,hfrom,hto,
       ()=>{
        let hResult=buffedNormalDamage(hp),hDamage=hResult.damage;
        enemy.hp=Math.max(0,enemy.hp-hDamage);
        addImpactFlash(enemy.x,enemy.y-8,hu.name==='Crimson'?'#ff405c':'#ffffff');
        addFloat(enemy.x,enemy.y-34,'-'+hDamage,hResult.buff.bonus>0?'#65ff9e':'#ffbd4a');
        let hDir=Math.sign(enemy.x-hp.x)||1;
        if(enemy.hp<=0)handleEnemyKO(enemy,hDir); else recoil(enemy,()=>{},hDir,false);
        checkVictoryKillshot();
       },
       ()=>setTimeout(runHelper,85),
       helperIndex%2===1?'punch':'kick'
      )
     }

     runHelper()
    },
    'special'
   )
  }

  return runJutsuTarget()
 }

 let queue=[],anyCombo=false;
 targets.forEach(enemy=>{
  let members=comboMembers(enemy,p),attackers=[p,...members.filter(x=>x!==p)];
  if(attackers.length>1)anyCombo=true;
  queue.push({enemy,attackers})
 });
 let chakraGain=1+(anyCombo?1:0);
 u.chakra=Math.min(u.maxChakra,u.chakra+chakraGain);

 let targetIndex=0;
 function runTarget(){
  if(targetIndex>=queue.length){
   S.log=`${u.name} hit ${targets.length} target${targets.length===1?'':'s'} and gained ${chakraGain} chakra${anyCombo?' from combo positioning':''}.`;
   return finishAction()
  }
  let item=queue[targetIndex++],enemy=item.enemy,attackers=item.attackers,attackIndex=0;
  function runAttacker(){
   if(enemy.hp<=0||attackIndex>=attackers.length)return setTimeout(runTarget,110);
   let ap=attackers[attackIndex++],au=front(ap),dx=enemy.x-ap.x,dy=enemy.y-ap.y,len=Math.max(1,Math.hypot(dx,dy));
   let stop=(enemy.r||19)+27,to={x:enemy.x-dx/len*stop,y:enemy.y-dy/len*stop},from={x:ap.x,y:ap.y};
   const runBasicAttack=(au.name==='Lebee')?animateLebeeStarBlast:(au.name==='Senku'?animateSenkuBomb:animateLunge);
   const basicTarget=(au.name==='Lebee'||au.name==='Senku')?enemy:to;
   runBasicAttack(au.name,from,basicTarget,()=>{
    let result=buffedNormalDamage(ap),dmg=result.damage;
    enemy.hp=Math.max(0,enemy.hp-dmg);
    addImpactFlash(enemy.x,enemy.y-8,au.name==='Crimson'?'#ff405c':attackIndex>1?'#ffbd4a':'#ffffff');
    addFloat(enemy.x,enemy.y-34,'-'+dmg,result.buff.bonus>0?'#65ff9e':attackIndex>1?'#ffbd4a':'#fff');
    let nDir=Math.sign(enemy.x-ap.x)||1;
    if(enemy.hp<=0)handleEnemyKO(enemy,nDir); else recoil(enemy,()=>{},nDir,attackIndex>1);
    checkVictoryKillshot();
   },()=>setTimeout(runAttacker,85),attackIndex%2===1?'punch':'kick')
  }
  runAttacker()
 }
 runTarget()
}

function finishAct

--- match 17 @ 43598191 ---
     },
       ()=>setTimeout(runHelper,85),
       helperIndex%2===1?'punch':'kick'
      )
     }

     runHelper()
    },
    'special'
   )
  }

  return runJutsuTarget()
 }

 let queue=[],anyCombo=false;
 targets.forEach(enemy=>{
  let members=comboMembers(enemy,p),attackers=[p,...members.filter(x=>x!==p)];
  if(attackers.length>1)anyCombo=true;
  queue.push({enemy,attackers})
 });
 let chakraGain=1+(anyCombo?1:0);
 u.chakra=Math.min(u.maxChakra,u.chakra+chakraGain);

 let targetIndex=0;
 function runTarget(){
  if(targetIndex>=queue.length){
   S.log=`${u.name} hit ${targets.length} target${targets.length===1?'':'s'} and gained ${chakraGain} chakra${anyCombo?' from combo positioning':''}.`;
   return finishAction()
  }
  let item=queue[targetIndex++],enemy=item.enemy,attackers=item.attackers,attackIndex=0;
  function runAttacker(){
   if(enemy.hp<=0||attackIndex>=attackers.length)return setTimeout(runTarget,110);
   let ap=attackers[attackIndex++],au=front(ap),dx=enemy.x-ap.x,dy=enemy.y-ap.y,len=Math.max(1,Math.hypot(dx,dy));
   let stop=(enemy.r||19)+27,to={x:enemy.x-dx/len*stop,y:enemy.y-dy/len*stop},from={x:ap.x,y:ap.y};
   const runBasicAttack=(au.name==='Lebee')?animateLebeeStarBlast:(au.name==='Senku'?animateSenkuBomb:animateLunge);
   const basicTarget=(au.name==='Lebee'||au.name==='Senku')?enemy:to;
   runBasicAttack(au.name,from,basicTarget,()=>{
    let result=buffedNormalDamage(ap),dmg=result.damage;
    enemy.hp=Math.max(0,enemy.hp-dmg);
    addImpactFlash(enemy.x,enemy.y-8,au.name==='Crimson'?'#ff405c':attackIndex>1?'#ffbd4a':'#ffffff');
    addFloat(enemy.x,enemy.y-34,'-'+dmg,result.buff.bonus>0?'#65ff9e':attackIndex>1?'#ffbd4a':'#fff');
    let nDir=Math.sign(enemy.x-ap.x)||1;
    if(enemy.hp<=0)handleEnemyKO(enemy,nDir); else recoil(enemy,()=>{},nDir,attackIndex>1);
    checkVictoryKillshot();
   },()=>setTimeout(runAttacker,85),attackIndex%2===1?'punch':'kick')
  }
  runAttacker()
 }
 runTarget()
}

function finishAction(){
 const token=ACTIVE_ACTION_TOKEN;
 clearActionWatchdog();
 setTimeout(()=>{
  if(!actionTokenAlive(token))return;
  // v0.5.10: retire the completed action token BEFORE clearing S.anim.
  // This prevents late recoil/requestAnimationFrame callbacks from recreating
  // S.anim after the turn has returned to charge, which caused the hard freeze.
  invalidateActions();
  S.anim=null;S.ready=null;S.action='normal';
  if(S.victoryFX||!aliveEnemies().length){
   if(!S.victoryFX)startVictorySequence();
   S.phase='resolve';
   return;
  }
  if(!alivePairs().length){
   S.phase='resolve';
   S.log='Defeat — all three fighter pairs have been knocked out.';
   return;
  }
  S.phase='charge';S._chargeSince=performance.now();
 },220)
}

function enemiesMutuallyLinked(a,b){
 if(a===b || a.hp<=0 || b.hp<=0)return false;

 let bodyA={x:a.x,y:a.y,r:a.r||16};
 let bodyB={x:b.x,y:b.y,r:b.r||16};

 let aCoversB=hits(a,{shape:normalShape(),rotation:0},bodyB);
 let bCoversA=hits(b,{shape:normalShape(),rotation:0},bodyA);

 return aCoversB && bCoversA
}

function enemyComboMembers(targetPair,initiator){
 // Target is no longer part of combo eligibility.
 // Once linked, the helper may join the initiator's valid attack target.
 r

--- match 18 @ 43598296 ---
 runHelper()
    },
    'special'
   )
  }

  return runJutsuTarget()
 }

 let queue=[],anyCombo=false;
 targets.forEach(enemy=>{
  let members=comboMembers(enemy,p),attackers=[p,...members.filter(x=>x!==p)];
  if(attackers.length>1)anyCombo=true;
  queue.push({enemy,attackers})
 });
 let chakraGain=1+(anyCombo?1:0);
 u.chakra=Math.min(u.maxChakra,u.chakra+chakraGain);

 let targetIndex=0;
 function runTarget(){
  if(targetIndex>=queue.length){
   S.log=`${u.name} hit ${targets.length} target${targets.length===1?'':'s'} and gained ${chakraGain} chakra${anyCombo?' from combo positioning':''}.`;
   return finishAction()
  }
  let item=queue[targetIndex++],enemy=item.enemy,attackers=item.attackers,attackIndex=0;
  function runAttacker(){
   if(enemy.hp<=0||attackIndex>=attackers.length)return setTimeout(runTarget,110);
   let ap=attackers[attackIndex++],au=front(ap),dx=enemy.x-ap.x,dy=enemy.y-ap.y,len=Math.max(1,Math.hypot(dx,dy));
   let stop=(enemy.r||19)+27,to={x:enemy.x-dx/len*stop,y:enemy.y-dy/len*stop},from={x:ap.x,y:ap.y};
   const runBasicAttack=(au.name==='Lebee')?animateLebeeStarBlast:(au.name==='Senku'?animateSenkuBomb:animateLunge);
   const basicTarget=(au.name==='Lebee'||au.name==='Senku')?enemy:to;
   runBasicAttack(au.name,from,basicTarget,()=>{
    let result=buffedNormalDamage(ap),dmg=result.damage;
    enemy.hp=Math.max(0,enemy.hp-dmg);
    addImpactFlash(enemy.x,enemy.y-8,au.name==='Crimson'?'#ff405c':attackIndex>1?'#ffbd4a':'#ffffff');
    addFloat(enemy.x,enemy.y-34,'-'+dmg,result.buff.bonus>0?'#65ff9e':attackIndex>1?'#ffbd4a':'#fff');
    let nDir=Math.sign(enemy.x-ap.x)||1;
    if(enemy.hp<=0)handleEnemyKO(enemy,nDir); else recoil(enemy,()=>{},nDir,attackIndex>1);
    checkVictoryKillshot();
   },()=>setTimeout(runAttacker,85),attackIndex%2===1?'punch':'kick')
  }
  runAttacker()
 }
 runTarget()
}

function finishAction(){
 const token=ACTIVE_ACTION_TOKEN;
 clearActionWatchdog();
 setTimeout(()=>{
  if(!actionTokenAlive(token))return;
  // v0.5.10: retire the completed action token BEFORE clearing S.anim.
  // This prevents late recoil/requestAnimationFrame callbacks from recreating
  // S.anim after the turn has returned to charge, which caused the hard freeze.
  invalidateActions();
  S.anim=null;S.ready=null;S.action='normal';
  if(S.victoryFX||!aliveEnemies().length){
   if(!S.victoryFX)startVictorySequence();
   S.phase='resolve';
   return;
  }
  if(!alivePairs().length){
   S.phase='resolve';
   S.log='Defeat — all three fighter pairs have been knocked out.';
   return;
  }
  S.phase='charge';S._chargeSince=performance.now();
 },220)
}

function enemiesMutuallyLinked(a,b){
 if(a===b || a.hp<=0 || b.hp<=0)return false;

 let bodyA={x:a.x,y:a.y,r:a.r||16};
 let bodyB={x:b.x,y:b.y,r:b.r||16};

 let aCoversB=hits(a,{shape:normalShape(),rotation:0},bodyB);
 let bCoversA=hits(b,{shape:normalShape(),rotation:0},bodyA);

 return aCoversB && bCoversA
}

function enemyComboMembers(targetPair,initiator){
 // Target is no longer part of combo eligibility.
 // Once linked, the helper may join the initiator's valid attack target.
 return S.enemies.filter(enemy=>{
   if(enemy.hp<=0)return false;
   return enemy===initiator || enemiesMut

--- match 19 @ 43598698 ---
 runTarget(){
  if(targetIndex>=queue.length){
   S.log=`${u.name} hit ${targets.length} target${targets.length===1?'':'s'} and gained ${chakraGain} chakra${anyCombo?' from combo positioning':''}.`;
   return finishAction()
  }
  let item=queue[targetIndex++],enemy=item.enemy,attackers=item.attackers,attackIndex=0;
  function runAttacker(){
   if(enemy.hp<=0||attackIndex>=attackers.length)return setTimeout(runTarget,110);
   let ap=attackers[attackIndex++],au=front(ap),dx=enemy.x-ap.x,dy=enemy.y-ap.y,len=Math.max(1,Math.hypot(dx,dy));
   let stop=(enemy.r||19)+27,to={x:enemy.x-dx/len*stop,y:enemy.y-dy/len*stop},from={x:ap.x,y:ap.y};
   const runBasicAttack=(au.name==='Lebee')?animateLebeeStarBlast:(au.name==='Senku'?animateSenkuBomb:animateLunge);
   const basicTarget=(au.name==='Lebee'||au.name==='Senku')?enemy:to;
   runBasicAttack(au.name,from,basicTarget,()=>{
    let result=buffedNormalDamage(ap),dmg=result.damage;
    enemy.hp=Math.max(0,enemy.hp-dmg);
    addImpactFlash(enemy.x,enemy.y-8,au.name==='Crimson'?'#ff405c':attackIndex>1?'#ffbd4a':'#ffffff');
    addFloat(enemy.x,enemy.y-34,'-'+dmg,result.buff.bonus>0?'#65ff9e':attackIndex>1?'#ffbd4a':'#fff');
    let nDir=Math.sign(enemy.x-ap.x)||1;
    if(enemy.hp<=0)handleEnemyKO(enemy,nDir); else recoil(enemy,()=>{},nDir,attackIndex>1);
    checkVictoryKillshot();
   },()=>setTimeout(runAttacker,85),attackIndex%2===1?'punch':'kick')
  }
  runAttacker()
 }
 runTarget()
}

function finishAction(){
 const token=ACTIVE_ACTION_TOKEN;
 clearActionWatchdog();
 setTimeout(()=>{
  if(!actionTokenAlive(token))return;
  // v0.5.10: retire the completed action token BEFORE clearing S.anim.
  // This prevents late recoil/requestAnimationFrame callbacks from recreating
  // S.anim after the turn has returned to charge, which caused the hard freeze.
  invalidateActions();
  S.anim=null;S.ready=null;S.action='normal';
  if(S.victoryFX||!aliveEnemies().length){
   if(!S.victoryFX)startVictorySequence();
   S.phase='resolve';
   return;
  }
  if(!alivePairs().length){
   S.phase='resolve';
   S.log='Defeat — all three fighter pairs have been knocked out.';
   return;
  }
  S.phase='charge';S._chargeSince=performance.now();
 },220)
}

function enemiesMutuallyLinked(a,b){
 if(a===b || a.hp<=0 || b.hp<=0)return false;

 let bodyA={x:a.x,y:a.y,r:a.r||16};
 let bodyB={x:b.x,y:b.y,r:b.r||16};

 let aCoversB=hits(a,{shape:normalShape(),rotation:0},bodyB);
 let bCoversA=hits(b,{shape:normalShape(),rotation:0},bodyA);

 return aCoversB && bCoversA
}

function enemyComboMembers(targetPair,initiator){
 // Target is no longer part of combo eligibility.
 // Once linked, the helper may join the initiator's valid attack target.
 return S.enemies.filter(enemy=>{
   if(enemy.hp<=0)return false;
   return enemy===initiator || enemiesMutuallyLinked(initiator,enemy)
 })
}

function resolveEnemyOverlap(enemy){
 const minDist=47;

 // Resolve only the enemy that just moved. This intentionally does not
 // mutate player positions or other enemy positions.
 for(let pass=0;pass<8;pass++){
   let blocker=null;
   let blockerDist=Infinity;

   S.pairs.forEach(pair=>{
     if(!pairAlive(pair))return;
     let dist=d(enemy,pair);
     if(dis

--- match 20 @ 43598795 ---
targets.length===1?'':'s'} and gained ${chakraGain} chakra${anyCombo?' from combo positioning':''}.`;
   return finishAction()
  }
  let item=queue[targetIndex++],enemy=item.enemy,attackers=item.attackers,attackIndex=0;
  function runAttacker(){
   if(enemy.hp<=0||attackIndex>=attackers.length)return setTimeout(runTarget,110);
   let ap=attackers[attackIndex++],au=front(ap),dx=enemy.x-ap.x,dy=enemy.y-ap.y,len=Math.max(1,Math.hypot(dx,dy));
   let stop=(enemy.r||19)+27,to={x:enemy.x-dx/len*stop,y:enemy.y-dy/len*stop},from={x:ap.x,y:ap.y};
   const runBasicAttack=(au.name==='Lebee')?animateLebeeStarBlast:(au.name==='Senku'?animateSenkuBomb:animateLunge);
   const basicTarget=(au.name==='Lebee'||au.name==='Senku')?enemy:to;
   runBasicAttack(au.name,from,basicTarget,()=>{
    let result=buffedNormalDamage(ap),dmg=result.damage;
    enemy.hp=Math.max(0,enemy.hp-dmg);
    addImpactFlash(enemy.x,enemy.y-8,au.name==='Crimson'?'#ff405c':attackIndex>1?'#ffbd4a':'#ffffff');
    addFloat(enemy.x,enemy.y-34,'-'+dmg,result.buff.bonus>0?'#65ff9e':attackIndex>1?'#ffbd4a':'#fff');
    let nDir=Math.sign(enemy.x-ap.x)||1;
    if(enemy.hp<=0)handleEnemyKO(enemy,nDir); else recoil(enemy,()=>{},nDir,attackIndex>1);
    checkVictoryKillshot();
   },()=>setTimeout(runAttacker,85),attackIndex%2===1?'punch':'kick')
  }
  runAttacker()
 }
 runTarget()
}

function finishAction(){
 const token=ACTIVE_ACTION_TOKEN;
 clearActionWatchdog();
 setTimeout(()=>{
  if(!actionTokenAlive(token))return;
  // v0.5.10: retire the completed action token BEFORE clearing S.anim.
  // This prevents late recoil/requestAnimationFrame callbacks from recreating
  // S.anim after the turn has returned to charge, which caused the hard freeze.
  invalidateActions();
  S.anim=null;S.ready=null;S.action='normal';
  if(S.victoryFX||!aliveEnemies().length){
   if(!S.victoryFX)startVictorySequence();
   S.phase='resolve';
   return;
  }
  if(!alivePairs().length){
   S.phase='resolve';
   S.log='Defeat — all three fighter pairs have been knocked out.';
   return;
  }
  S.phase='charge';S._chargeSince=performance.now();
 },220)
}

function enemiesMutuallyLinked(a,b){
 if(a===b || a.hp<=0 || b.hp<=0)return false;

 let bodyA={x:a.x,y:a.y,r:a.r||16};
 let bodyB={x:b.x,y:b.y,r:b.r||16};

 let aCoversB=hits(a,{shape:normalShape(),rotation:0},bodyB);
 let bCoversA=hits(b,{shape:normalShape(),rotation:0},bodyA);

 return aCoversB && bCoversA
}

function enemyComboMembers(targetPair,initiator){
 // Target is no longer part of combo eligibility.
 // Once linked, the helper may join the initiator's valid attack target.
 return S.enemies.filter(enemy=>{
   if(enemy.hp<=0)return false;
   return enemy===initiator || enemiesMutuallyLinked(initiator,enemy)
 })
}

function resolveEnemyOverlap(enemy){
 const minDist=47;

 // Resolve only the enemy that just moved. This intentionally does not
 // mutate player positions or other enemy positions.
 for(let pass=0;pass<8;pass++){
   let blocker=null;
   let blockerDist=Infinity;

   S.pairs.forEach(pair=>{
     if(!pairAlive(pair))return;
     let dist=d(enemy,pair);
     if(dist<minDist && dist<blockerDist){
       blocker=pair;
       blockerDist=dist;
     }
   });

   S

--- match 21 @ 43601268 ---
){
 // Target is no longer part of combo eligibility.
 // Once linked, the helper may join the initiator's valid attack target.
 return S.enemies.filter(enemy=>{
   if(enemy.hp<=0)return false;
   return enemy===initiator || enemiesMutuallyLinked(initiator,enemy)
 })
}

function resolveEnemyOverlap(enemy){
 const minDist=47;

 // Resolve only the enemy that just moved. This intentionally does not
 // mutate player positions or other enemy positions.
 for(let pass=0;pass<8;pass++){
   let blocker=null;
   let blockerDist=Infinity;

   S.pairs.forEach(pair=>{
     if(!pairAlive(pair))return;
     let dist=d(enemy,pair);
     if(dist<minDist && dist<blockerDist){
       blocker=pair;
       blockerDist=dist;
     }
   });

   S.enemies.forEach(other=>{
     if(other===enemy||other.hp<=0)return;
     let dist=d(enemy,other);
     if(dist<minDist && dist<blockerDist){
       blocker=other;
       blockerDist=dist;
     }
   });

   if(!blocker)break;

   let dx=enemy.x-blocker.x;
   let dy=enemy.y-blocker.y;
   let len=Math.hypot(dx,dy);

   // Exact same-center fallback gets a deterministic escape direction.
   if(len<.01){
     let angle=(pass+1)*1.913;
     dx=Math.cos(angle);
     dy=Math.sin(angle);
     len=1;
   }

   let push=(minDist-len)+.75;
   enemy.x+=(dx/len)*push;
   enemy.y+=(dy/len)*push;

   let legal=clampToBattlefield(enemy);
   enemy.x=legal.x;
   enemy.y=legal.y;
 }
}

function cpuTurn(){
 if(S.phase!=='cpu')return;
 const cpuActionToken=beginActionToken();
 armActionWatchdog('Anubis / enemy turn',cpuActionToken);
 let e=S.ready.ref;
 if(e.hp<=0){e.gauge=0;return finishAction()}

 let candidates=S.pairs.filter(pairAlive);candidates.forEach(ensureFront);
 if(!candidates.length)return;

 // Enemy personality changes each turn so the CPU does not dogpile one unit.
 // Weighted considerations:
 // - nearest target
 // - lowest-health target
 // - clustered target opportunity
 // - mild randomness so equal situations do not look scripted
 const modes=['nearest','weakest','cluster','balanced'];
 const mode=modes[Math.floor(Math.random()*modes.length)];

 function nearbyCount(p){
   return candidates.filter(other=>other!==p && d(p,other)<145).length;
 }

 function targetScore(p){
   let u=front(p);
   let dist=d(e,p);
   let hpRatio=u.hp/u.maxHp;
   let cluster=nearbyCount(p);
   let jitter=Math.random()*12;

   if(mode==='nearest')
     return dist*1.35 + hpRatio*30 - cluster*8 + jitter;
   if(mode==='weakest')
     return hpRatio*150 + dist*.35 - cluster*6 + jitter;
   if(mode==='cluster')
     return dist*.45 + hpRatio*35 - cluster*55 + jitter;

   // balanced
   return dist*.7 + hpRatio*75 - cluster*24 + jitter;
 }

 let target=[...candidates].sort((a,b)=>targetScore(a)-targetScore(b))[0];

 // Evaluate several approach angles instead of always taking the same straight-line slot.
 let angles=[];
 for(let i=0;i<10;i++)angles.push((Math.PI*2/10)*i);

 let best=null;
 angles.forEach(a=>{
   let radius=58+Math.random()*35;
   let pos=clampToBattlefield({
     x:target.x+Math.cos(a)*radius,
     y:target.y+Math.sin(a)*radius
   });

   // Score position by direct hits + mutual proximity links with enemy allies.
   let testEnemy={...e,x:pos.

--- match 22 @ 43601566 ---
p(enemy){
 const minDist=47;

 // Resolve only the enemy that just moved. This intentionally does not
 // mutate player positions or other enemy positions.
 for(let pass=0;pass<8;pass++){
   let blocker=null;
   let blockerDist=Infinity;

   S.pairs.forEach(pair=>{
     if(!pairAlive(pair))return;
     let dist=d(enemy,pair);
     if(dist<minDist && dist<blockerDist){
       blocker=pair;
       blockerDist=dist;
     }
   });

   S.enemies.forEach(other=>{
     if(other===enemy||other.hp<=0)return;
     let dist=d(enemy,other);
     if(dist<minDist && dist<blockerDist){
       blocker=other;
       blockerDist=dist;
     }
   });

   if(!blocker)break;

   let dx=enemy.x-blocker.x;
   let dy=enemy.y-blocker.y;
   let len=Math.hypot(dx,dy);

   // Exact same-center fallback gets a deterministic escape direction.
   if(len<.01){
     let angle=(pass+1)*1.913;
     dx=Math.cos(angle);
     dy=Math.sin(angle);
     len=1;
   }

   let push=(minDist-len)+.75;
   enemy.x+=(dx/len)*push;
   enemy.y+=(dy/len)*push;

   let legal=clampToBattlefield(enemy);
   enemy.x=legal.x;
   enemy.y=legal.y;
 }
}

function cpuTurn(){
 if(S.phase!=='cpu')return;
 const cpuActionToken=beginActionToken();
 armActionWatchdog('Anubis / enemy turn',cpuActionToken);
 let e=S.ready.ref;
 if(e.hp<=0){e.gauge=0;return finishAction()}

 let candidates=S.pairs.filter(pairAlive);candidates.forEach(ensureFront);
 if(!candidates.length)return;

 // Enemy personality changes each turn so the CPU does not dogpile one unit.
 // Weighted considerations:
 // - nearest target
 // - lowest-health target
 // - clustered target opportunity
 // - mild randomness so equal situations do not look scripted
 const modes=['nearest','weakest','cluster','balanced'];
 const mode=modes[Math.floor(Math.random()*modes.length)];

 function nearbyCount(p){
   return candidates.filter(other=>other!==p && d(p,other)<145).length;
 }

 function targetScore(p){
   let u=front(p);
   let dist=d(e,p);
   let hpRatio=u.hp/u.maxHp;
   let cluster=nearbyCount(p);
   let jitter=Math.random()*12;

   if(mode==='nearest')
     return dist*1.35 + hpRatio*30 - cluster*8 + jitter;
   if(mode==='weakest')
     return hpRatio*150 + dist*.35 - cluster*6 + jitter;
   if(mode==='cluster')
     return dist*.45 + hpRatio*35 - cluster*55 + jitter;

   // balanced
   return dist*.7 + hpRatio*75 - cluster*24 + jitter;
 }

 let target=[...candidates].sort((a,b)=>targetScore(a)-targetScore(b))[0];

 // Evaluate several approach angles instead of always taking the same straight-line slot.
 let angles=[];
 for(let i=0;i<10;i++)angles.push((Math.PI*2/10)*i);

 let best=null;
 angles.forEach(a=>{
   let radius=58+Math.random()*35;
   let pos=clampToBattlefield({
     x:target.x+Math.cos(a)*radius,
     y:target.y+Math.sin(a)*radius
   });

   // Score position by direct hits + mutual proximity links with enemy allies.
   let testEnemy={...e,x:pos.x,y:pos.y};
   testEnemy.rotation=Math.atan2(target.y-pos.y,target.x-pos.x);
   let hitCount=0;
   let comboSupport=0;

   candidates.forEach(p=>{
     let body={x:p.x,y:p.y,r:18};
     let fakeUnit={shape:normalShape(),rotation:testEnemy.rotation||0};
     if(hits(testEnemy,fakeUnit,body))hitCoun

--- match 23 @ 43601654 ---
ally does not
 // mutate player positions or other enemy positions.
 for(let pass=0;pass<8;pass++){
   let blocker=null;
   let blockerDist=Infinity;

   S.pairs.forEach(pair=>{
     if(!pairAlive(pair))return;
     let dist=d(enemy,pair);
     if(dist<minDist && dist<blockerDist){
       blocker=pair;
       blockerDist=dist;
     }
   });

   S.enemies.forEach(other=>{
     if(other===enemy||other.hp<=0)return;
     let dist=d(enemy,other);
     if(dist<minDist && dist<blockerDist){
       blocker=other;
       blockerDist=dist;
     }
   });

   if(!blocker)break;

   let dx=enemy.x-blocker.x;
   let dy=enemy.y-blocker.y;
   let len=Math.hypot(dx,dy);

   // Exact same-center fallback gets a deterministic escape direction.
   if(len<.01){
     let angle=(pass+1)*1.913;
     dx=Math.cos(angle);
     dy=Math.sin(angle);
     len=1;
   }

   let push=(minDist-len)+.75;
   enemy.x+=(dx/len)*push;
   enemy.y+=(dy/len)*push;

   let legal=clampToBattlefield(enemy);
   enemy.x=legal.x;
   enemy.y=legal.y;
 }
}

function cpuTurn(){
 if(S.phase!=='cpu')return;
 const cpuActionToken=beginActionToken();
 armActionWatchdog('Anubis / enemy turn',cpuActionToken);
 let e=S.ready.ref;
 if(e.hp<=0){e.gauge=0;return finishAction()}

 let candidates=S.pairs.filter(pairAlive);candidates.forEach(ensureFront);
 if(!candidates.length)return;

 // Enemy personality changes each turn so the CPU does not dogpile one unit.
 // Weighted considerations:
 // - nearest target
 // - lowest-health target
 // - clustered target opportunity
 // - mild randomness so equal situations do not look scripted
 const modes=['nearest','weakest','cluster','balanced'];
 const mode=modes[Math.floor(Math.random()*modes.length)];

 function nearbyCount(p){
   return candidates.filter(other=>other!==p && d(p,other)<145).length;
 }

 function targetScore(p){
   let u=front(p);
   let dist=d(e,p);
   let hpRatio=u.hp/u.maxHp;
   let cluster=nearbyCount(p);
   let jitter=Math.random()*12;

   if(mode==='nearest')
     return dist*1.35 + hpRatio*30 - cluster*8 + jitter;
   if(mode==='weakest')
     return hpRatio*150 + dist*.35 - cluster*6 + jitter;
   if(mode==='cluster')
     return dist*.45 + hpRatio*35 - cluster*55 + jitter;

   // balanced
   return dist*.7 + hpRatio*75 - cluster*24 + jitter;
 }

 let target=[...candidates].sort((a,b)=>targetScore(a)-targetScore(b))[0];

 // Evaluate several approach angles instead of always taking the same straight-line slot.
 let angles=[];
 for(let i=0;i<10;i++)angles.push((Math.PI*2/10)*i);

 let best=null;
 angles.forEach(a=>{
   let radius=58+Math.random()*35;
   let pos=clampToBattlefield({
     x:target.x+Math.cos(a)*radius,
     y:target.y+Math.sin(a)*radius
   });

   // Score position by direct hits + mutual proximity links with enemy allies.
   let testEnemy={...e,x:pos.x,y:pos.y};
   testEnemy.rotation=Math.atan2(target.y-pos.y,target.x-pos.x);
   let hitCount=0;
   let comboSupport=0;

   candidates.forEach(p=>{
     let body={x:p.x,y:p.y,r:18};
     let fakeUnit={shape:normalShape(),rotation:testEnemy.rotation||0};
     if(hits(testEnemy,fakeUnit,body))hitCount++;
   });

   S.enemies.forEach(ally=>{
     if(ally===e||ally.hp<=0)return;

     let

===== dragAim =====

===== aimAngle =====
