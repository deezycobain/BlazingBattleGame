{
  "name": "blazing-battle-game",
  "private": true,
  "scripts": {
    "validate": "node scripts/validate-runtime.mjs",
    "build": "npm run validate && node scripts/build-cloudflare.mjs",
    "deploy": "wrangler deploy",
    "preview": "wrangler versions upload"
  },
  "devDependencies": {
    "wrangler": "4.114.0"
  }
}