(()=>{
'use strict';
const esc=value=>String(value??'').replace(/[&<>"']/g,ch=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[ch]));
const pct=value=>`${Math.round(Number(value||0)*100)}%`;
const stat=(label,value)=>`<div class="bbud-stat"><span>${esc(label)}</span><strong>${esc(value)}</strong></div>`;
const ability=item=>item?`<article class="bbud-ability"><div><span class="bbud-kicker">${esc(item.kind==='jutsu'?'JUTSU':'BASIC ATTACK')}</span><h3>${esc(item.name)}</h3></div><div class="bbud-ability-meta">${item.cost?`<span>${item.cost} CHAKRA</span>`:''}${item.chakraGain?`<span>+${item.chakraGain} CHAKRA</span>`:''}${item.damageMultiplier?`<span>${item.damageMultiplier}× POWER</span>`:''}${item.healPercent?`<span>${pct(item.healPercent)} HEAL</span>`:''}${item.delivery?`<span>${esc(window.BlazingUnitDetails.titleCase(item.delivery))}</span>`:''}</div></article>`:'';
function ensure(){let root=document.getElementById('bbUnitDetails');if(root)return root;root=document.createElement('section');root.id='bbUnitDetails';root.className='bbud';root.hidden=true;root.tabIndex=-1;root.setAttribute('aria-label','Character details');root.innerHTML='<div class="bbud-shell"><header class="bbud-head"><button class="bbud-back" type="button" aria-label="Back">‹</button><div><span class="bbud-eyebrow">CHARACTER DETAILS</span><h2 class="bbud-name"></h2><p class="bbud-title"></p></div></header><div class="bbud-hero"><div class="bbud-art"><img alt=""></div><div class="bbud-badges"></div></div><nav class="bbud-tabs" aria-label="Character details sections"><button type="button" data-tab="overview" class="is-active">OVERVIEW</button><button type="button" data-tab="stats">STATS</button><button type="button" data-tab="abilities">JUTSU</button></nav><div class="bbud-panels"></div></div>';document.body.appendChild(root);root.querySelector('.bbud-back').addEventListener('click',close);root.addEventListener('click',event=>{const btn=event.target.closest('[data-tab]');if(!btn||!root.contains(btn))return;event.preventDefault();event.stopPropagation();selectTab(btn.dataset.tab);});return root;}
let current=null,returnScreen=null,legacyScreen=null;
const normalized=value=>String(value||'').trim().toLowerCase().replace(/[^a-z0-9]+/g,'');
function selectTab(tab){const root=ensure();root.querySelectorAll('[data-tab]').forEach(b=>{const active=b.dataset.tab===tab;b.classList.toggle('is-active',active);b.setAttribute('aria-selected',String(active));});root.querySelectorAll('[data-panel]').forEach(p=>{p.hidden=p.dataset.panel!==tab;});const panels=root.querySelector('.bbud-panels');if(panels&&root.scrollTop>panels.offsetTop)root.scrollTo({top:Math.max(0,panels.offsetTop-118),behavior:'smooth'});}
function render(vm){const root=ensure();current=vm;root.dataset.element=String(vm.element||'neutral').toLowerCase();root.querySelector('.bbud-name').textContent=vm.name;root.querySelector('.bbud-title').textContent=vm.title;root.querySelector('.bbud-badges').innerHTML=[vm.rarity,vm.element,window.BlazingUnitDetails.titleCase(vm.archetype||vm.role)].filter(Boolean).map(x=>`<span>${esc(x)}</span>`).join('');const img=root.querySelector('.bbud-art img');img.alt=vm.name;img.src=vm.art.full||vm.art.card||'';root.querySelector('.bbud-panels').innerHTML=`<section data-panel="overview"><div class="bbud-statgrid">${stat('LEVEL',vm.stats.level)}${stat('HP',vm.stats.hp)}${stat('ATK',vm.stats.attack)}${stat('DEF',vm.stats.defense)}</div>${ability(vm.abilities.basic)}${ability(vm.abilities.jutsu)}</section><section data-panel="stats" hidden><div class="bbud-section-title">CHARACTER STATS</div><div class="bbud-statgrid bbud-statgrid-full">${stat('LEVEL',vm.stats.level)}${stat('HP',vm.stats.hp)}${stat('ATTACK',vm.stats.attack)}${stat('DEFENSE',vm.stats.defense)}${stat('SPEED',vm.stats.speed)}${stat('MAX CHAKRA',vm.resources.chakraMax)}</div></section><section data-panel="abilities" hidden><div class="bbud-section-title">ABILITIES & JUTSU</div>${ability(vm.abilities.basic)}${ability(vm.abilities.jutsu)}</section>`;selectTab('overview');}
function resolveUnit(value){if(typeof value!=='string')return value;const direct=window.BLAZING_UNIT_DATA?.[value];if(direct)return direct;const key=normalized(value);return Object.values(window.BLAZING_UNIT_DATA||{}).find(unit=>normalized(unit?.id)===key||normalized(unit?.display_name)===key)||null;}
function open(unitOrId,options={}){const unit=resolveUnit(unitOrId);if(!unit)return false;const root=ensure();render(window.BlazingUnitDetails.fromUnit(unit));returnScreen=options.returnScreen||document.querySelector('.screen:not([hidden])');legacyScreen=options.legacyScreen||null;if(returnScreen&&returnScreen!==root)returnScreen.hidden=true;if(legacyScreen&&legacyScreen!==returnScreen)legacyScreen.hidden=true;root.hidden=false;document.body.classList.add('bbud-open');root.scrollTop=0;requestAnimationFrame(()=>root.focus({preventScroll:true}));return true;}
function close(){const root=ensure();root.hidden=true;document.body.classList.remove('bbud-open');if(returnScreen)returnScreen.hidden=false;else if(legacyScreen)legacyScreen.hidden=false;returnScreen=null;legacyScreen=null;}
function unitFromElement(el){if(!el)return null;const direct=el.dataset?.unitId||el.dataset?.characterId||el.dataset?.unit||el.dataset?.character;const resolved=resolveUnit(direct);if(resolved)return resolved;const hay=[direct,el.getAttribute?.('aria-label'),el.getAttribute?.('title'),el.querySelector?.('[data-unit-name]')?.textContent,el.querySelector?.('.name,.unitName,.characterName,.rosterName,.teamName')?.textContent,el.textContent].filter(Boolean).join(' ');for(const unit of Object.values(window.BLAZING_UNIT_DATA||{})){if(unit?.collection?.inventory_visible===false)continue;const names=[unit.id,unit.display_name].filter(Boolean).map(normalized);const text=normalized(hay);if(names.some(name=>name&&text.includes(name)))return unit;}return null;}
function isRosterSurface(el){return !!el.closest?.('#teamScreen,.teamScreen,#rosterScreen,.rosterScreen,#inventoryScreen,.inventoryScreen,[data-screen="team"],[data-screen="roster"],[data-screen="inventory"],.teamBody,.rosterGrid,.characterGrid,.inventoryGrid');}
function isCard(el){return el.closest?.('[data-unit-id],[data-character-id],[data-unit],[data-character],.rosterCard,.characterCard,.unitCard,.teamCard,.inventoryCard,.charCard,.rosterItem,.characterItem');}
function bindRosterNavigation(){document.addEventListener('click',event=>{if(event.defaultPrevented||event.button>0)return;const card=isCard(event.target);if(!card||!isRosterSurface(card))return;if(event.target.closest('button,input,select,textarea,a,[data-team-slot],[data-action="select"],[data-action="remove"]'))return;const unit=unitFromElement(card);if(!unit)return;event.preventDefault();event.stopImmediatePropagation();open(unit,{returnScreen:card.closest('.screen,#teamScreen,#rosterScreen,#inventoryScreen')||undefined});},true);document.addEventListener('keydown',event=>{if(event.key!=='Enter'&&event.key!==' ')return;const card=isCard(event.target);if(!card||!isRosterSurface(card))return;const unit=unitFromElement(card);if(!unit)return;event.preventDefault();open(unit,{returnScreen:card.closest('.screen,#teamScreen,#rosterScreen,#inventoryScreen')||undefined});});}
function visible(el){if(!el||el===ensure()||el.hidden)return false;const s=getComputedStyle(el);return s.display!=='none'&&s.visibility!=='hidden'&&s.opacity!=='0';}
function findInventoryReturn(exclude){const candidates=[...document.querySelectorAll('#inventoryScreen,.inventoryScreen,[data-screen="inventory"],.screen')];return candidates.find(el=>el!==exclude&&!el.hidden&&/inventory|collection|roster/i.test(`${el.id} ${el.className} ${el.getAttribute?.('data-screen')||''}`))||candidates.find(el=>el!==exclude&&/inventory|collection|roster/i.test(`${el.id} ${el.className} ${el.getAttribute?.('data-screen')||''}`))||null;}
function takeoverLegacyDetails(){if(!ensure().hidden)return;for(const el of document.querySelectorAll('section,main,.screen,[role="main"],div')){if(!visible(el))continue;const text=(el.innerText||'').slice(0,5000);if(!/FIGHTER\s+DETAILS/i.test(text)||!/JUTSU/i.test(text)||!/STATS/i.test(text))continue;const unit=Object.values(window.BLAZING_UNIT_DATA||{}).find(u=>u?.display_name&&new RegExp(`\\b${String(u.display_name).replace(/[.*+?^${}()|[\]\\]/g,'\\$&')}\\b`,'i').test(text));if(!unit)continue;const parent=[...document.querySelectorAll('.screen,section,main,[role="main"]')].find(candidate=>candidate.contains(el)&&visible(candidate))||el;const inventory=findInventoryReturn(parent);open(unit,{returnScreen:inventory||undefined,legacyScreen:parent});break;}}
function bindLegacyTakeover(){let queued=false;const scan=()=>{if(queued)return;queued=true;requestAnimationFrame(()=>{queued=false;takeoverLegacyDetails();});};new MutationObserver(scan).observe(document.body,{subtree:true,childList:true,attributes:true,attributeFilter:['hidden','class','style']});document.addEventListener('click',()=>setTimeout(scan,0),true);setTimeout(scan,0);}
bindRosterNavigation();bindLegacyTakeover();
window.BlazingUnitDetailsScreen=Object.freeze({open,close,render,selectTab,resolveUnit,bindRosterNavigation,takeoverLegacyDetails,get current(){return current;}});
})();
