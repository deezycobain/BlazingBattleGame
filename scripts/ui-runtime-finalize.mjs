import fs from 'node:fs/promises';
import path from 'node:path';

const ROOT=process.cwd();
const file=path.join(ROOT,'dist','index.html');
let html=await fs.readFile(file,'utf8');
const read=rel=>fs.readFile(path.join(ROOT,rel),'utf8');
function removeTagById(tag,id){const rx=new RegExp(`<${tag}\\b[^>]*\\bid=["']${id}["'][^>]*>[\\s\\S]*?<\\/${tag}>`,'gi');html=html.replace(rx,'');}
function insertBeforeLast(tag,text,label){const at=html.toLowerCase().lastIndexOf(tag.toLowerCase());if(at<0)throw new Error(`UI finalize: missing ${label}`);html=html.slice(0,at)+text+html.slice(at);}
function normalizeDocumentHead(){const hm=html.match(/<head\b[^>]*>/i),bodyAt=html.search(/<body\b/i);if(!hm||bodyAt<0)throw new Error('UI finalize: document head/body missing');const ho=hm.index??-1,hc=ho+hm[0].length;if(ho<0||hc>=bodyAt)throw new Error('UI finalize: invalid document head boundary');html=html.slice(0,hc)+html.slice(hc,bodyAt).replace(/<\/head\s*>/gi,'')+'</head>'+html.slice(bodyAt);}
function stripLeakedDetailsRuntime(){let removed=0;const markers=['window.BlazingUnitDetailsScreen=Object.freeze','function bindLegacyTakeover(){','findInventoryReturn(el);open(unit'];for(const needle of markers){let anchor=html.indexOf(needle);while(anchor>=0){const priorClose=html.lastIndexOf('</script>',anchor),nextOpen=html.lastIndexOf('<script',anchor);if(nextOpen>priorClose){anchor=html.indexOf(needle,anchor+needle.length);continue;}let start=priorClose>=0?priorClose+9:html.search(/<body\b[^>]*>/i);if(start<0)start=0;const tagBetween=html.lastIndexOf('>',anchor);if(tagBetween>start)start=tagBetween+1;let end=html.indexOf('<',anchor);const iifeEnd=html.indexOf('})();',anchor);if(iifeEnd>=0&&(end<0||iifeEnd+5<end))end=iifeEnd+5;if(end<0)end=anchor+needle.length;const segment=html.slice(start,end);if(/bindLegacyTakeover|findInventoryReturn|BlazingUnitDetailsScreen=Object\.freeze|^\s*\\n\s*$/.test(segment)){html=html.slice(0,start)+html.slice(end);removed++;anchor=html.indexOf(needle);continue;}anchor=html.indexOf(needle,anchor+needle.length);}}
html=html.replace(/(<body\b[^>]*>)\s*\\n\s*(?=<)/i,'$1');return removed;}
const safeScript=source=>source.replace(/<\/script/gi,'<\\/script');
for(const id of ['bb-unit-details-style','bb-ui-polish-style','bb-home-wallpaper-style'])removeTagById('style',id);
for(const id of ['bb-unit-details-model','bb-unit-details-screen','bb-inventory-skin','bb-inventory-screen','bb-legacy-details-suppress','bb-reserved-details-tab','bb-home-wallpaper-runtime'])removeTagById('script',id);
normalizeDocumentHead();stripLeakedDetailsRuntime();
if(!/<meta\b[^>]*charset/i.test(html))html=html.replace(/<head\b([^>]*)>/i,'<head$1><meta charset="utf-8">');
const fontCss=await read('runtime/ui/theme/animeace-font.css');const themeCss=await read('runtime/ui/unit-details/theme.css');const detailFonts=await read('runtime/ui/unit-details/theme/fonts.css');let backdropCss=await read('runtime/ui/unit-details/theme/backdrop.css');const buttonsCss=await read('runtime/ui/unit-details/components/buttons.css');const framesCss=await read('runtime/ui/unit-details/components/frames.css');const scrollsCss=await read('runtime/ui/unit-details/components/scrolls.css');const artViewerCss=await read('runtime/ui/unit-details/components/art-viewer.css');let detailShellCss=await read('runtime/ui/unit-details.css');let inventoryCss=await read('runtime/ui/inventory/inventory.css');const cardsCss=await read('runtime/ui/inventory/cards.css');
detailShellCss=detailShellCss.replace(/^@import[^;]+;\s*/gm,'');inventoryCss=inventoryCss.replace(/^@import[^;]+;\s*/gm,'');backdropCss=backdropCss.replace(/^@import[^;]+;\s*/gm,'');
const cloudUrl='url("runtime/ui/shared/cloud-backdrop-hq.png")';inventoryCss=inventoryCss.replace(/var\(--bb-cloud-backdrop\)/g,cloudUrl);backdropCss=backdropCss.replace(/var\(--bb-cloud-backdrop\)/g,cloudUrl);
const homeUrl='runtime/ui/home/home-wallpaper-hq.png';const homeCss=`.bb-home-theme{position:relative!important;isolation:isolate!important;background:#10131a!important;background-image:none!important;overflow:hidden!important}.bb-home-theme:before{content:""!important;position:absolute!important;inset:0!important;z-index:0!important;pointer-events:none!important;background-image:url("${homeUrl}")!important;background-repeat:no-repeat!important;background-position:center center!important;background-size:cover!important;opacity:1!important}.bb-home-theme>*{position:relative;z-index:1}.bb-home-theme .bb-home-old-wallpaper{display:none!important}.bb-legacy-inventory-suppressed,.bb-legacy-details-suppressed{display:none!important;visibility:hidden!important;pointer-events:none!important}`;
const css=[fontCss,themeCss,detailFonts,backdropCss,buttonsCss,framesCss,scrollsCss,artViewerCss,detailShellCss,inventoryCss,cardsCss,homeCss].join('\n');insertBeforeLast('</head>',`<style id="bb-ui-polish-style">${css}</style>`,'closing head');
const detailsVm=safeScript(await read('runtime/ui/unit-details.js')),detailsScreen=safeScript(await read('runtime/ui/unit-details-screen.js')),inventoryScreen=safeScript(await read('runtime/ui/inventory/inventory-screen.js')),legacySuppress=safeScript(await read('runtime/ui/unit-details/legacy-suppress.js')),homeRuntime=safeScript(await read('runtime/ui/home/home-skin.js'));const scripts=`<script id="bb-unit-details-model">${detailsVm}</script><script id="bb-unit-details-screen">${detailsScreen}</script><script id="bb-inventory-screen">${inventoryScreen}</script><script id="bb-legacy-details-suppress">${legacySuppress}</script><script id="bb-home-wallpaper-runtime">${homeRuntime}</script>`;insertBeforeLast('</body>',scripts,'closing body');normalizeDocumentHead();
const headOpen=html.search(/<head\b/i),headClose=html.search(/<\/head>/i),bodyOpen=html.search(/<body\b/i),bodyClose=html.toLowerCase().lastIndexOf('</body>');if(headOpen<0||headClose<0||bodyOpen<0||bodyClose<0||!(headOpen<headClose&&headClose<bodyOpen&&bodyOpen<bodyClose))throw new Error('UI finalize: invalid final document boundaries');for(const id of ['bb-ui-polish-style','bb-unit-details-model','bb-unit-details-screen','bb-inventory-screen','bb-legacy-details-suppress','bb-home-wallpaper-runtime']){const count=(html.match(new RegExp(`id=["']${id}["']`,'g'))||[]).length;if(count!==1)throw new Error(`UI finalize: expected one ${id}, found ${count}`);}if(/id=["']bb-inventory-skin["']/.test(html))throw new Error('UI finalize: legacy inventory decorator survived finalization');if(!html.includes('grid-template-columns:repeat(4,minmax(0,1fr))'))throw new Error('UI finalize: four-column inventory contract missing');if(!html.includes('assets/characters/${unit.id}/${asset}'))throw new Error('UI finalize: canonical clean-art path resolver missing');if(!html.includes("img.src=assetAvailable(vm.art.full)?vm.art.full:''"))throw new Error('UI finalize: details clean-art-only contract missing');if(!html.includes('runtime/ui/shared/cloud-backdrop-hq.png')||!html.includes('runtime/ui/home/home-wallpaper-hq.png'))throw new Error('UI finalize: external HQ wallpaper paths missing');if(/data:image\/png;base64,iVBOR/.test(css))throw new Error('UI finalize: HQ PNG wallpaper was embedded in runtime shell');if(!html.includes('bb-home-theme'))throw new Error('UI finalize: HQ PNG home wallpaper missing');const visibleBody=html.slice(bodyOpen,bodyClose).replace(/<script\b[^>]*>[\s\S]*?<\/script>/gi,'').replace(/<style\b[^>]*>[\s\S]*?<\/style>/gi,'');if(/bindLegacyTakeover|findInventoryReturn|BlazingUnitDetailsScreen=Object\.freeze/.test(visibleBody))throw new Error('UI finalize: leaked details runtime text survived');await fs.writeFile(file,html);console.log('UI finalizer PASS: external HQ Cloud/Home wallpapers, no duplicate base64 payload, no visible runtime source.');
