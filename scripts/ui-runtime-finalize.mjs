import fs from 'node:fs/promises';
import path from 'node:path';
const ROOT=process.cwd();
const file=path.join(ROOT,'dist','index.html');
let html=await fs.readFile(file,'utf8');
const read=rel=>fs.readFile(path.join(ROOT,rel),'utf8');
function removeTagById(tag,id){const rx=new RegExp(`<${tag}\\b[^>]*\\bid=["']${id}["'][^>]*>[\\s\\S]*?<\\/${tag}>`,'gi');html=html.replace(rx,'');}
function insertBeforeLast(tag,text,label){const at=html.toLowerCase().lastIndexOf(tag.toLowerCase());if(at<0)throw new Error(`UI finalize: missing ${label}`);html=html.slice(0,at)+text+html.slice(at);}
function normalizeDocumentHead(){const m=html.match(/<head\b[^>]*>/i),bodyAt=html.search(/<body\b/i);if(!m||bodyAt<0)throw new Error('UI finalize: document head/body missing');const at=(m.index??-1)+m[0].length;if(at>=bodyAt)throw new Error('UI finalize: invalid document head boundary');html=html.slice(0,at)+html.slice(at,bodyAt).replace(/<\/head\s*>/gi,'')+'</head>'+html.slice(bodyAt);}
for(const id of ['bb-unit-details-style','bb-ui-polish-style','bb-home-wallpaper-style'])removeTagById('style',id);
for(const id of ['bb-unit-details-model','bb-unit-details-screen','bb-inventory-skin','bb-inventory-screen','bb-legacy-details-suppress','bb-reserved-details-tab','bb-home-wallpaper-runtime'])removeTagById('script',id);
normalizeDocumentHead();
if(!/<meta\b[^>]*charset/i.test(html))html=html.replace(/<head\b([^>]*)>/i,'<head$1><meta charset="utf-8">');
const fontCss=await read('runtime/ui/theme/animeace-font.css');
const themeCss=await read('runtime/ui/unit-details/theme.css');
const detailFonts=await read('runtime/ui/unit-details/theme/fonts.css');
let backdropCss=await read('runtime/ui/unit-details/theme/backdrop.css');
const buttonsCss=await read('runtime/ui/unit-details/components/buttons.css');
const framesCss=await read('runtime/ui/unit-details/components/frames.css');
const scrollsCss=await read('runtime/ui/unit-details/components/scrolls.css');
const artViewerCss=await read('runtime/ui/unit-details/components/art-viewer.css');
let detailShellCss=await read('runtime/ui/unit-details.css');
let inventoryCss=await read('runtime/ui/inventory/inventory.css');
const cardsCss=await read('runtime/ui/inventory/cards.css');
const cloudCss=await read('runtime/ui/shared/cloud-backdrop-v2.css');
detailShellCss=detailShellCss.replace(/^@import[^;]+;\s*/gm,'');
backdropCss=backdropCss.replace(/^@import[^;]+;\s*/gm,'');
inventoryCss=inventoryCss.replace(/^@import[^;]+;\s*/gm,'');
const homeDir=path.join(ROOT,'runtime/ui/home');
const replacement=(await fs.readdir(homeDir)).filter(n=>/^wallpaper-\d+\.txt$/.test(n)).sort();
const legacy=['home-bg-00.txt','home-bg-01.txt','home-bg-02.txt','home-bg-03.txt'];
const chosen=replacement.length?replacement:legacy;
const homeBase64=(await Promise.all(chosen.map(n=>fs.readFile(path.join(homeDir,n),'utf8')))).join('').replace(/\s+/g,'');
if(homeBase64.length<12000||!homeBase64.startsWith('/9j/'))throw new Error('UI finalize: home wallpaper payload invalid');
const homeCss=`.bb-home-theme{position:relative!important;isolation:isolate!important;background:#10131a!important;background-image:none!important}.bb-home-theme:before{content:""!important;position:absolute!important;inset:0!important;z-index:0!important;pointer-events:none!important;background-image:linear-gradient(rgba(6,8,13,.03),rgba(6,8,13,.10)),url("data:image/jpeg;base64,${homeBase64}")!important;background-repeat:no-repeat!important;background-position:center!important;background-size:cover!important;opacity:1!important}.bb-home-theme>*{position:relative;z-index:1}.bb-home-theme .bb-home-old-wallpaper{display:none!important;visibility:hidden!important;opacity:0!important}.bb-legacy-inventory-suppressed,.bb-legacy-details-suppressed{display:none!important;visibility:hidden!important;pointer-events:none!important}`;
const css=[cloudCss,fontCss,themeCss,detailFonts,backdropCss,buttonsCss,framesCss,scrollsCss,artViewerCss,detailShellCss,inventoryCss,cardsCss,homeCss].join('\n');
insertBeforeLast('</head>',`<style id="bb-ui-polish-style">${css}</style>`,'closing head');
const detailsVm=await read('runtime/ui/unit-details.js');
const detailsScreen=await read('runtime/ui/unit-details-screen.js');
const inventoryScreen=await read('runtime/ui/inventory/inventory-screen.js');
const legacySuppress=await read('runtime/ui/unit-details/legacy-suppress.js');
const homeRuntime=await read('runtime/ui/home/home-skin.js');
insertBeforeLast('</body>',`<script id="bb-unit-details-model">${detailsVm}</script><script id="bb-unit-details-screen">${detailsScreen}</script><script id="bb-inventory-screen">${inventoryScreen}</script><script id="bb-legacy-details-suppress">${legacySuppress}</script><script id="bb-home-wallpaper-runtime">${homeRuntime}</script>`,'closing body');
normalizeDocumentHead();
const end=html.toLowerCase().lastIndexOf('</html>');if(end>=0)html=html.slice(0,end+7);
const ho=html.search(/<head\b/i),hc=html.search(/<\/head>/i),bo=html.search(/<body\b/i),bc=html.toLowerCase().lastIndexOf('</body>');if(ho<0||hc<0||bo<0||bc<0||!(ho<hc&&hc<bo&&bo<bc))throw new Error('UI finalize: invalid final document boundaries');
for(const id of ['bb-ui-polish-style','bb-unit-details-model','bb-unit-details-screen','bb-inventory-screen','bb-legacy-details-suppress','bb-home-wallpaper-runtime']){const count=(html.match(new RegExp(`id=["']${id}["']`,'g'))||[]).length;if(count!==1)throw new Error(`UI finalize: expected one ${id}, found ${count}`);}
if(/id=["']bb-inventory-skin["']/.test(html))throw new Error('UI finalize: legacy inventory decorator survived');
if(!html.includes('--bb-cloud-backdrop:url("data:image/webp;base64,'))throw new Error('UI finalize: cloud backdrop not inlined');
if(!html.includes('grid-template-columns:repeat(4,minmax(0,1fr))'))throw new Error('UI finalize: four-column inventory contract missing');
if(!html.includes('assets/characters/${unit.id}/${asset}'))throw new Error('UI finalize: canonical clean-art resolver missing');
if(!html.includes("img.src=assetAvailable(vm.art.full)?vm.art.full:''"))throw new Error('UI finalize: details clean-art-only contract missing');
if(!html.includes('bb-home-theme')||!html.includes('data:image/jpeg;base64,/9j/'))throw new Error('UI finalize: home wallpaper missing');
await fs.writeFile(file,html);
console.log(`UI finalizer PASS: replacement UI authoritative; cloud inlined; home=${chosen.join(',')}`);
