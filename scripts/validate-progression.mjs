import fs from 'node:fs/promises';
import path from 'node:path';
import vm from 'node:vm';
const root=process.cwd(),read=rel=>fs.readFile(path.join(root,rel),'utf8'),readBinary=rel=>fs.readFile(path.join(root,rel)),fail=msg=>{throw new Error(`Progression validation failed: ${msg}`)};
const js=await read('runtime/ui/progression/progression.js'),css=await read('runtime/ui/progression/progression.css'),pkg=JSON.parse(await read('package.json'));
try{new vm.Script(js)}catch(error){fail(`runtime syntax error: ${error.message}`)}
for(const marker of ["KEY='blazing.progression.v1'",'MAX_RESONANCE=5','STAT_BUDGET=12',"FIGHTERS=['Crimson','Sub-Zero','Lebee','Senku','Tyler']",'u.resonance<MAX_RESONANCE','u.shards++','u.shiny=true','candidate=rollStats(u.locks,u.roll)','KEEP CURRENT','ACCEPT NEW','summonEmbers=999999','spendEmbers=function()','localStorage.setItem','function applyCombatBonuses()','target.jutsuDamage=Math.round','RESET DEV PROGRESSION',"multiSummonBtn.addEventListener('click'",'Array.from({length:10}','CORE RESONANCE BANNER','SKIP TO RESULTS','showSummonResultsNow','bb-shiny-awakening','data-open-forge','cardForSummon=function(name){return CARD_ART[name]','const FORGE_ART=','const SHINY_CUTOUT=','const SHINY_POPOUT_PROFILE=',"'Sub-Zero':'ice-hand'","'Tyler':'head-hand'","'Lebee':'top-left'","'Senku':'top-left'",'syncRevealArt','syncResultArt','dataset.popoutProfile','assets/characters/crimson/art/current_collection_art.jpg','assets/characters/subzero/art/full_art_absolute_zero_v2.jpeg','assets/characters/lebee/art/full_art_cosmic_wish.jpeg','assets/characters/senku/cards/senku_card.jpeg','assets/characters/tyler/cards/current_collection_card.png','assets/characters/subzero/art/shiny_foreground_cutout_v2.webp','assets/characters/tyler/art/shiny_foreground_cutout_v1.webp','forgeArtDepth','forgePopout','hasPopout','forgeHoloTexture','summonPullScreen.scrollTop=0','PULL${pulls.length===1','function fitForgeArtwork(image)','image.naturalWidth/image.naturalHeight','--forge-art-max'])if(!js.includes(marker))fail(`runtime missing ${marker}`);
for(const marker of ['#coreSummonNotice{display:none!important}','.forgeNode','.forgeCard.shiny','.forgeCandidate.active','.bb-resonance-badge','SUMMON OVERHAUL','.bb-summon-lobby','.bb-skip-reveal','object-fit:contain!important','.bb-shiny-awakening .pullCardWrap::after','animation:bbShinySweep 1.05s','grid-template-columns:repeat(2,minmax(0,1fr))','.bb-shiny-awakening .pullHeroArea::after','@keyframes bbGoldFlow','.forgeArtDepth','.forgePopout','[data-popout-profile="head-hand"]','[data-popout-profile="ice-hand"]','[data-popout-profile="top-left"]','radial-gradient(ellipse 24% 11.5% at 40% 0%','radial-gradient(ellipse 24% 10% at 30% 96%','radial-gradient(ellipse 14% 23% at 0% 34%','radial-gradient(ellipse 27% 12% at 48% 0%','radial-gradient(ellipse 16% 24% at 0% 45%','transform:translate(.39%,2.68%) scale(.997)','transform:translate(-3.23%,2.15%) scale(1.058)','.forgeCard.shiny.hasPopout .forgeArtStage{inset:10%','width:125%;height:125%','.forgeCard.shiny.hasPopout[data-fighter="subzero"] .forgeArtStage{inset:12%','width:131.58%;height:131.58%','.forgeCard.shiny .forgeHoloTexture','repeating-conic-gradient','@keyframes forgeHoloDrift','overflow-y:auto!important','width:min(100%,var(--forge-art-max,323px))','aspect-ratio:var(--forge-art-ratio,.75)'])if(!css.includes(marker))fail(`style missing ${marker}`);
for(const obsolete of ['installTylerPopoutFraming','bbTylerSelectivePopoutV3','bbTylerPopLayer','bbTylerPopFx','bbTylerPopHand','bbTylerPopHair','MutationObserver'])if(js.includes(obsolete)||css.includes(obsolete))fail(`stacked pop-out implementation survived: ${obsolete}`);
const popoutMasks=[...css.matchAll(/\.forgeCard\[data-popout-profile="(?:head-hand|ice-hand|top-left)"\] \.forgePopout\{[^}]+\}/g)].map(match=>match[0]);
if(popoutMasks.length!==3)fail(`expected 3 profile-driven pop-out masks, found ${popoutMasks.length}`);
if(popoutMasks.some(mask=>mask.includes('linear-gradient(')||mask.includes('transparent 19%')))fail('broad overlapping pop-out mask survived');
for(const rel of ['assets/characters/subzero/art/shiny_foreground_cutout_v2.webp','assets/characters/tyler/art/shiny_foreground_cutout_v1.webp']){const webp=await readBinary(rel);if(webp.length<50000)fail(`Shiny cutout too small: ${rel}`);if(webp.toString('ascii',0,4)!=='RIFF'||webp.toString('ascii',8,12)!=='WEBP'||webp.toString('ascii',12,16)!=='VP8X')fail(`Shiny cutout is not extended WebP: ${rel}`);if(!(webp[20]&16))fail(`Shiny cutout lost alpha transparency: ${rel}`);if(webp.readUIntLE(24,3)+1!==1086||webp.readUIntLE(27,3)+1!==1448)fail(`Shiny cutout dimensions changed: ${rel}`)}
if(!pkg.scripts?.build?.includes('progression-postprocess.mjs'))fail('build chain missing progression postprocess');
console.log('Progression PASS: canonical summon art, single-layer edge-only Shiny pop-outs, dormant Lebee/Senku profiles, and persistent Resonance rerolls are enforced.');
